# خطة بناء تجربة البندول البسيط
## (انطلاقاً من تجربة النابض)

---

## 1. الفيزياء

### المعادلات الأساسية
```
T = 2π√(L/g)        →    g = 4π²L/T²
ω = 2π/T = √(g/L)
f = 1/T
```

### الفرق عن النابض
| | النابض | البندول |
|---|---|---|
| المتغير المستقل | الكتلة m | طول الخيط L |
| ثابت النظام | k (ثابت النابض) | g (تسارع الجاذبية) |
| العلاقة | T ∝ √m | T ∝ √L |
| جزء استاتيكي | ✅ قانون هوك | ❌ لا يوجد |
| جزء ديناميكي | ✅ قياس T | ✅ قياس T |

---

## 2. الملفات الجديدة

### محاكاة (Physics)
| الملف | الوظيفة |
|---|---|
| `usePendulumPhysics.ts` | معادلة الحركة θ'' = -(g/L)·sin(θ) |
| `PendulumCanvas.vue` | رسم البندول المتحرك |

### بيانات (Data)
| الملف | الوظيفة |
|---|---|
| `usePendulumTrials.ts` | تسجيل قراءات (L, t₁, t₂, t₃, T, T²) |
| `usePendulumReport.ts` | بناء تقرير البندول الكامل |

### لوحات (Panels)
| الملف | الوظيفة |
|---|---|
| `PendulumParamPanel.vue` | تعديل L, m, θ₀, g |
| `PendulumDataPanel.vue` | جدول القراءات + Scatter |
| `PendulumCalcPanel.vue` | حساب g من القياسات |
| `PendulumReport.vue` | تقرير داخلي (overlay) |

### تجربة رئيسية
| الملف | الوظيفة |
|---|---|
| `PendulumExperiment.vue` | تركيب كل المكونات |

---

## 3. بنية التجربة

```
┌─────────────────────────────────────────────────────────────┐
│  شريط القائمة (بدء/توقف/تسجيل/تقرير)                       │
├──────────────┬──────────────────────────────┬───────────────┤
│  اللوحة      │  المحاكاة                    │  المعاملات    │
│  البيانات    │  (PendulumCanvas)            │  (L, m, θ₀)   │
│  ├─ جدول     │                              │               │
│  └─ Scatter  │                              │               │
├──────────────┴──────────────────────────────┴───────────────┤
│  شريط التحكم (سرعة/إعادة/تسجيل/حساب g)                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. الجدول الوحيد (الديناميكي)

| # | L (m) | t₁ (s) | t₂ (s) | t₃ (s) | T (s) | T² (s²) | g (m/s²) |
|---|---|---|---|---|---|---|---|
| 1 | 0.25 | 10.00 | 10.05 | 9.98 | 2.001 | 4.004 | 9.87 |
| 2 | 0.50 | 14.18 | 14.20 | 14.15 | 2.836 | 8.043 | 9.81 |
| 3 | 1.00 | 20.05 | 20.10 | 20.02 | 4.003 | 16.024 | 9.79 |

### الانحدار: L vs T²
```
slope = Δ(T²)/Δ(L) = 4π²/g
→ g = 4π² / slope
```

---

## 5. التقرير الكامل

### القوانين (3 أسطر)
```
T = 2π√(L/g)     →     g = 4π²L/T²
ω = √(g/L)       (التردد الزاوي)
E = mgL(1-cosθ₀) (الطاقة الميكانيكية)
```

### النتائج
- g المحسوب من القياسات
- g النظري (9.81 m/s²)
- نسبة الخطأ

### الجداول
- جدول واحد فقط (الديناميكي)

### مصادر الأخطاء
- احتكاك الهواء
- دقة ساعة الإيقاف
- خطأ زاوية النظر
- حركة اليد عند الإطلاق
- كتلة الخيط غير مهملة

---

## 6. ملاحظات التنفيذ

- **لا يوجد** `usePendulumStatic.ts` (البندول لا يملك جزء استاتيكي)
- `usePendulumTrials.ts` مشابه لـ `useSpringTrials.ts` لكن المتغير هو L بدلاً من m
- `PendulumCanvas.vue` يرسم: نقطة تثبيت + خيط + كرة متحركة
- `PendulumExperiment.vue` يُبنى بنسخ/لصق من `SpringExperiment.vue` مع تعديل أسماء الملفات
- كل ملف < 300 سطر

---

## 7. خطوات البناء

1. `usePendulumPhysics.ts` — المعادلات
2. `PendulumCanvas.vue` — الرسم
3. `usePendulumTrials.ts` — البيانات
4. `PendulumParamPanel.vue` — المعاملات
5. `PendulumDataPanel.vue` — الجدول + Scatter
6. `PendulumCalcPanel.vue` — الحسابات
7. `usePendulumReport.ts` — التقرير
8. `PendulumReport.vue` — overlay
9. `PendulumExperiment.vue` — التجميع
10. `physicsUtils.test.ts` — اختبارات g

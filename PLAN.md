# خطة إعادة هيكلة لوحات التحكم
## تقرير شامل + خطة تنفيذ

---

## الوضع الحالي

### الأدوار الأربعة

| الدور | الجدول | صفحة الدخول | التسجيل |
|---|---|---|---|
| طالب | `users` | `/home` | ذاتي |
| مدرس | `users` | `/home` | ذاتي |
| أدمن | `users` | `/admin` | يدوي (seed) |
| مدرسة | `schools` | `/school` | ذاتي |

### العلاقات

```
المدرسة
  ├── كود (SCH-XXXX) → يُعطى للطلاب والمدرسين
  ├── سعة (max_students / max_teachers)
  ├── مستخدمون (users.school_id)
  │   ├── طلاب
  │   └── مدرسون → فصول → طلاب → تقارير
  └── تقارير (عبر users.school_id)

الأدمن → يتحكم بكل ما سبق
```

---

## الثغرات الحرجة (يجب إصلاحها أولاً)

### 1. JWT `sub` غير متسق للمدرسة
- `/api/auth/login` يوقّع بـ `sub: "school:5"`
- `/api/school/login` يوقّع بـ `sub: "5"`
- `schoolAuthMiddleware` يستخدم `Number(payload.sub)` → `NaN` للحالة الأولى
- **النتيجة**: المدرسة التي تدخل عبر الـ landing لا تستطيع الوصول لـ endpoints المدرسة

### 2. تخزين refresh_token للمدرسة
- `school/services.ts` يخزن `user_id = school.id` (موجب) في جدول له FK على `users(id)` → سيفشل
- `auth/services.ts` يعطل FK ويخزن سالب → حل هش
- **النتيجة**: refresh يفشل للمدرسة → تفقد جلستها بعد 15 دقيقة

### 3. endpoints بدون تحقق (zValidator)
- `admin/handlers.ts`: ban, warnings, notes, feedback status, classes update, reports grade, settings — كلها تاخذ `c.req.json()` مباشرة

---

## مقارنة: الموجود vs المطلوب

| الميزة | موجودة | مطلوبة |
|---|---|---|
| الطالب يسجل بمفرده | ✅ | ✅ |
| المدرس يسجل بمفرده | ✅ | ✅ |
| المدرس يشترك مع طلابه (فصول) | ✅ | ✅ |
| المدرسة تسجل وتأخذ كود | ✅ | ✅ |
| المدرسة تعطي كود لطلابها ومدرسيها | ✅ | ✅ |
| البريد ثابت للمدرسة | ✅ | ✅ |
| تغيير البريد يحتاج طلب من الأدمن | ❌ | ✅ |
| الطلب يظهر في لوحة المدرسة | ❌ | ✅ |
| الأدمن يتحكم بكل شيء | ✅ | ✅ |
| المدرسة تحظر مستخدميها | ❌ | ✅ |
| المدرسة تغير كلمة مرورها | ❌ | ✅ |
| المدرسة تغير اسمها (لا البريد) | ❌ | ✅ |
| المدرسة ترى تقارير مدرستها | ❌ | ✅ |
| المدرسة ترى محادثات فصولها | ❌ | ✅ |
| نظام صارم ومفصل لكل حساب | ⚠️ جزئي | ✅ |

---

## الصلاحيات المطلوبة لكل حساب (النظام الصارم)

### الطالب
- دخول التجارب (فيزياء/كيمياء/رياضيات/أحياء)
- إنشاء تقارير
- الانضمام لفصل بكود
- الانضمام لمدرسة بكود (عند التسجيل)
- المحادثة في الفصل
- تغيير كلمة مروره
- طلب تغيير اسمه (يحتاج موافقة أدمن)
- طلب تغيير بريده (يحتاج موافقة أدمن)
- حذف حسابه

### المدرس
- كل صلاحيات الطالب +
- إنشاء فصول
- دعوة طلاب بكود الفصل
- تصحيح تقارير طلابه
- رؤية إحصائيات فصوله
- تغيير اسمه مباشرة
- تغيير كلمة مروره
- طلب تغيير بريده (يحتاج موافقة أدمن)
- الانضمام لمدرسة بكود (عند التسجيل)

### المدرسة
- رؤية إحصائيات (طلاب/مدرسون/فصول/تقارير)
- رؤية مستخدميها (طلاب + مدرسين)
- رؤية فصولها
- رؤية تقارير مدرستها
- رؤية محادثات فصولها
- إزالة مستخدم من المدرسة
- حظر/إلغاء حظر مستخدم
- رؤية كود المدرسة والسعة
- تغيير اسمها (مباشرة)
- تغيير كلمة مرورها
- طلب تغيير بريدها (يحتاج موافقة أدمن)
- تسجيل خروج

### الأدمن
- كل صلاحيات المدرسة + المدرس + الطالب (عبر لوحة الأدمن)
- إدارة كاملة للمستخدمين (إنشاء/حذف/حظر/تغيير دور/إعادة كلمة مرور/تعديل)
- إدارة كاملة للفصول (إنشاء/حذف/تعديل/نقل مدرس)
- إدارة كاملة للتقارير (تصحيح/حذف)
- إدارة كاملة للمدارس (إنشاء/حذف/تعديل/تفعيل/إيقاف)
- إدارة مستخدمي/فصول/تقارير كل مدرسة
- مراجعة طلبات تغيير البريد (موافقة/رفض)
- مراجعة طلبات تغيير الاسم (موافقة/رفض)
- سجل النشاط + سجل التدقيق + الجلسات
- صحة النظام + إعدادات + تصدير
- مراقبة المحادثات
- انتحال هوية مستخدم

---

## خطة التنفيذ

### المرحلة 1: إصلاحات حرجة
1. توحيد JWT `sub` للمدرسة → `String(school.id)` في كل مكان
2. إنشاء جدول `school_refresh_tokens` منفصل
3. إصلاح `refreshAccessToken` ليتعامل مع الجدولين
4. إضافة `zValidator` لكل endpoints الإدارية

### المرحلة 2: صلاحيات المدرسة
5. `PATCH /api/school/me` — تعديل الاسم
6. `POST /api/school/password` — تغيير كلمة المرور
7. `PATCH /api/school/users/:userId/block` + `unblock` — حظر مستخدم
8. `GET /api/school/reports` — رؤية تقارير المدرسة
9. `GET /api/school/chat/:classId` — رؤية محادثات فصولها

### المرحلة 3: نظام طلب تغيير البريد
10. جدول `email_change_requests`
11. endpoints: طلب (مدرسة/مستخدم) + مراجعة (أدمن) + تنفيذ
12. UI في لوحة المدرسة + لوحة الأدمن

### المرحلة 4: إعادة هيكلة الواجهة
13. تبسيط لوحة الأدمن
14. تبسيط لوحة المدرسة
15. تبسيط لوحة المدرس/الطالب

---

## إعادة هيكلة الواجهة: البساطة والقوة

### المبادئ
- **كل صفحة تحت 250 سطر** (باستثناء الضروري)
- **مكونات صغيرة قابلة لإعادة الاستخدام** (كل مكون تحت 100 سطر)
- **CSS مشترك** بدلاً من تكرار الأنماط في كل مكون
- **تبويبات واضحة** — لا أكثر من 5 تبويبات في أي لوحة
- **أزرار واضحة** — كل إجراء له زمر مرئي ومباشر
- **لا نوافذ منبثقة مدمجة** — كل modal في مكون مستقل

### لوحة الأدمن المقترحة (مبسطة)

```
الأدمن
├── 📊 نظرة عامة (إحصائيات + رسوم)
├── 👥 المستخدمون (جدول + بحث + إجراءات)
├── 🏫 المدارس (قائمة → تفاصيل → مستخدمين/فصول/تقارير)
├── 📋 الطلبات (تغيير بريد + تغيير اسم + قيد المراجعة)
└── ⚙️ النظام (إعدادات + سجلات + صحة + تصدير)
```

**ما يُحذف من التبويبات الحالية (يُدمج):**
- "الفصول" → يُدمج في "المدارس" (تحت كل مدرسة) + في "المستخدمون" (تفاصيل المدرس)
- "التقارير" → يُدمج في "المدارس" (تحت كل مدرسة)
- "ذكية" → يُدمج في "نظرة عامة"
- "الملاحظات" → يُدمج في "النظام"
- "شات" → يُدمج في "المدارس" (تحت كل فصل)
- "سجل التدقيق" → يُدمج في "النظام"

**النتيجة**: 5 تبويبات بدلاً من 14.

### لوحة المدرسة المقترحة (مبسطة)

```
المدرسة
├── 📊 نظرة عامة (إحصائيات + كود + سعة)
├── 👥 المستخدمون (طلاب + مدرسين + حظر/إزالة)
├── 📚 الفصول (قائمة + محادثات)
├── 📄 التقارير (تقارير المدرسة)
└── ⚙️ الإعدادات (اسم + كلمة مرور + طلب تغيير بريد)
```

### لوحة المدرس/الطالب (مبسطة)

```
المدرس
├── 🏠 الرئيسية (فصولي + إحصائيات)
├── 📝 التصحيح (تقارير الطلاب)
└── ⚙️ الإعدادات (اسم + كلمة مرور + طلب تغيير بريد)

الطالب
├── 🏠 الرئيسية (فصولي + تجاربي)
├── 📄 تقاريري
└── ⚙️ الإعدادات (كلمة مرور + طلب تغيير اسم/بريد)
```

### التصميم البصري
- **ألوان**: خلفية داكنة (`#0a0f1c`) + أزرق سماوي (`#06b6d4`) كلون أساسي
- **بطاقات**: خلفية شفافة (`rgba(255,255,255,0.03)`) + حدود رفيعة
- **جداول**: صفوف متناوبة + hover خفيف + حدود سفلية فقط
- **أزرار**: متدرجة لونية للأفعال الرئيسية + شفافة للأفعال الثانوية
- **حالات**: شارات ملونة (نشط/موقوف/مسودة/مُرسل/مُصحح)
- **أيقونات**: emoji بسيطة بدلاً من مكتبة أيقونات
- **خطوط**: صغيرة وواضحة (0.8-0.9rem للنصوص، 1.2rem للعناوين)

---

## قاعدة بيانات جديدة مطلوبة

### جدول `email_change_requests`
```sql
CREATE TABLE IF NOT EXISTS email_change_requests (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  requester_type TEXT NOT NULL CHECK(requester_type IN ('user', 'school')),
  requester_id INTEGER NOT NULL,
  current_email TEXT NOT NULL,
  requested_email TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'pending' CHECK(status IN ('pending', 'approved', 'rejected')),
  reviewed_by INTEGER REFERENCES users(id),
  reviewed_at DATETIME,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### جدول `school_refresh_tokens`
```sql
CREATE TABLE IF NOT EXISTS school_refresh_tokens (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  token_hash TEXT UNIQUE NOT NULL,
  school_id INTEGER NOT NULL REFERENCES schools(id) ON DELETE CASCADE,
  expires_at DATETIME NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

## endpoints الجديدة المطلوبة

### المدرسة
| Method | Path | الوصف |
|---|---|---|
| `PATCH` | `/api/school/me` | تعديل الاسم |
| `POST` | `/api/school/password` | تغيير كلمة المرور |
| `PATCH` | `/api/school/users/:userId/block` | حظر مستخدم |
| `PATCH` | `/api/school/users/:userId/unblock` | إلغاء حظر |
| `GET` | `/api/school/reports` | تقارير المدرسة |
| `POST` | `/api/school/email-change-request` | طلب تغيير بريد |

### الأدمن
| Method | Path | الوصف |
|---|---|---|
| `GET` | `/api/admin/email-requests` | رؤية طلبات تغيير البريد |
| `PATCH` | `/api/admin/email-requests/:id` | موافقة/رفض |

### المستخدمون (طالب/مدرس)
| Method | Path | الوصف |
|---|---|---|
| `POST` | `/api/auth/email-change-request` | طلب تغيير بريد |

---

## ترتيب التنفيذ

1. **إصلاحات حرجة** (JWT + refresh token + zValidator)
2. **جداول جديدة** (email_change_requests + school_refresh_tokens)
3. **endpoints المدرسة** (me + password + block + reports)
4. **نظام طلب تغيير البريد** (backend + frontend)
5. **إعادة هيكلة لوحة الأدمن** (5 تبويبات)
6. **إعادة هيكلة لوحة المدرسة** (5 تبويبات)
7. **إعادة هيكلة لوحة المدرس/الطالب** (3 تبويبات)
8. **بناء + اختبار**

{
  "resolvedId": "C:/Users/feras/Desktop/feras/apps/web/src/composables/spring/useSpringTrials.ts",
  "transforms": [
    {
      "name": "__load__",
      "result": "import { ref, computed, type Ref } from 'vue'\nimport { useI18n } from '../../composables/useI18n'\nimport { downloadCsv } from '../../components/experiment/spring/downloadCsv'\nimport { linearRegression } from '../../components/experiment/spring/linearRegression'\nimport type { SpringParams } from '../../modules/physics/experiments/spring/useSpringPhysics'\n\nexport interface SpringTrial {\n  id: number; mass: number; k: number; amplitude: number\n  T: number; f: number; omega: number; kCalc: number; err: number\n}\n\nexport interface SpringMeasured {\n  T: number | null; f: number | null; omega: number | null; kCalc: number | null; kCalcEff: number | null\n}\n\nconst SAVE_KEY = 'spring:trials:v1'\n\nexport function useSpringTrials(params: SpringParams, measured: Ref<SpringMeasured>) {\n  const { t } = useI18n()\n  const trials = ref<SpringTrial[]>([])\n  let nextTrialId = 1\n\n  // Undo/Redo history\n  const history = ref<SpringTrial[][]>([])\n  const historyIndex = ref(-1)\n\n  function pushHistory() {\n    // Remove any future history if we're not at the end\n    if (historyIndex.value < history.value.length - 1) {\n      history.value = history.value.slice(0, historyIndex.value + 1)\n    }\n    history.value.push([...trials.value])\n    historyIndex.value++\n    // Limit history to 20 steps\n    if (history.value.length > 20) {\n      history.value.shift()\n      historyIndex.value--\n    }\n  }\n\n  function undo() {\n    if (historyIndex.value > 0) {\n      historyIndex.value--\n      trials.value = [...history.value[historyIndex.value]]\n      nextTrialId = trials.value.length > 0 ? Math.max(...trials.value.map(t => t.id)) + 1 : 1\n    }\n  }\n\n  function redo() {\n    if (historyIndex.value < history.value.length - 1) {\n      historyIndex.value++\n      trials.value = [...history.value[historyIndex.value]]\n      nextTrialId = trials.value.length > 0 ? Math.max(...trials.value.map(t => t.id)) + 1 : 1\n    }\n  }\n\n  function canUndo() { return historyIndex.value > 0 }\n  function canRedo() { return historyIndex.value < history.value.length - 1 }\n\n  const trialStats = computed(() => {\n    if (trials.value.length === 0) return { T_mean: 0, T_std: 0, k_mean: 0, k_std: 0 }\n    const Ts = trials.value.map(t => t.T)\n    const ks = trials.value.map(t => t.kCalc)\n    const mean = (arr: number[]) => arr.reduce((a, b) => a + b, 0) / arr.length\n    const std = (arr: number[]) => { const m = mean(arr); return Math.sqrt(arr.reduce((sum, v) => sum + (v - m) ** 2, 0) / arr.length) }\n    return { T_mean: mean(Ts), T_std: std(Ts), k_mean: mean(ks), k_std: std(ks) }\n  })\n\n  // Gaussian noise simulation (~2% error, mimics human reading error)\n  function gaussianNoise(mean: number, stdDev: number) {\n    let u = 0, v = 0\n    while (u === 0) u = Math.random()\n    while (v === 0) v = Math.random()\n    const z = Math.sqrt(-2.0 * Math.log(u)) * Math.cos(2.0 * Math.PI * v)\n    return mean + z * stdDev\n  }\n\n  function recordTrial() {\n    if (!measured.value.T) return\n    if (params.mass < 0.01 || params.mass > 20) {\n      alert(t('experiments.massMustBeBetween', { min: '0.01', max: '20' }))\n      return\n    }\n    pushHistory()\n    const noiseLevel = 0.02  // ~2% reading error (human-like)\n    const noisyT = gaussianNoise(measured.value.T, measured.value.T * noiseLevel)\n    const noisyK = (4 * Math.PI * Math.PI * params.mass) / (noisyT * noisyT)\n    const err = Math.abs((noisyK - params.k) / params.k) * 100\n    trials.value = [...trials.value, {\n      id: nextTrialId++, mass: params.mass, k: params.k, amplitude: params.amplitude,\n      T: noisyT, f: 1 / noisyT, omega: (2 * Math.PI) / noisyT, kCalc: noisyK, err,\n    }]\n    autoSave()\n  }\n\n  function removeTrial(id: number) {\n    pushHistory()\n    trials.value = trials.value.filter(t => t.id !== id)\n    autoSave()\n  }\n  function clearTrials() {\n    pushHistory()\n    trials.value = []\n    autoSave()\n  }\n\n  // Auto-save to localStorage\n  function autoSave() {\n    try {\n      localStorage.setItem(SAVE_KEY, JSON.stringify({\n        trials: trials.value,\n        nextId: nextTrialId,\n        calcResult: calcResult.value,\n      }))\n    } catch { /* ignore */ }\n  }\n\n  function autoLoad() {\n    try {\n      const raw = localStorage.getItem(SAVE_KEY)\n      if (!raw) return\n      const parsed = JSON.parse(raw)\n      if (Array.isArray(parsed.trials)) {\n        const valid = parsed.trials.filter((t: Record<string, unknown>) => {\n          if (!t || typeof t.mass !== 'number' || t.mass < 0.01 || t.mass > 20) return false\n          if (typeof t.T !== 'number' || t.T <= 0 || t.T > 20) return false\n          // Sanity check: T should be roughly consistent with mass and k (within 3x of theoretical)\n          const theoreticalT = 2 * Math.PI * Math.sqrt(Number(t.mass) / Math.max(Number(t.k), 1))\n          if (t.T < theoreticalT / 3 || t.T > theoreticalT * 3) return false\n          return true\n        })\n        trials.value = valid\n        nextTrialId = parsed.nextId ?? (valid.length > 0 ? Math.max(...valid.map((t: Record<string, unknown>) => Number(t.id))) + 1 : 1)\n        if (parsed.calcResult) calcResult.value = parsed.calcResult\n        history.value = [[...valid]]\n        historyIndex.value = 0\n      }\n    } catch { /* ignore */ }\n  }\n\n  function exportCsv() {\n    if (!trials.value.length) return\n    downloadCsv('spring_mass_data.csv', [\n      ['#', 'm', 'k', 'A', 'T', 'f', 'ω', 'kcalc', 'error'],\n      ...trials.value.map((t, i) => [\n        i + 1, t.mass.toFixed(3), t.k.toFixed(1), t.amplitude.toFixed(3),\n        t.T.toFixed(4), t.f.toFixed(3), t.omega.toFixed(3),\n        t.kCalc.toFixed(2), t.err.toFixed(2) + '%',\n      ]),\n    ])\n  }\n\n  const calcResult = ref(t('experiments.clickBtnShowCalc'))\n\n  function calcK() {\n    if (!measured.value.T) { calcResult.value = t('experiments.measurePeriodFirst'); return }\n    const k = (4 * Math.PI * Math.PI * params.mass) / (measured.value.T * measured.value.T)\n    const err = Math.abs((k - params.k) / params.k) * 100\n    calcResult.value = `<b>${t('experiments.equationLabel')}:</b> k = 4π² × m / T²<br><b>${t('experiments.substitutionLabel')}:</b> k = 4π² × ${params.mass.toFixed(3)} / (${measured.value.T.toFixed(4)})²<br><b>${t('experiments.resultLabel')}:</b> k = <b>${k.toFixed(2)} N/m</b><br><b>${t('experiments.errorPercent')}:</b> ${err.toFixed(2)}%`\n  }\n\n  function calcT() {\n    const T = 2 * Math.PI * Math.sqrt(params.mass / params.k)\n    calcResult.value = `<b>${t('experiments.equationLabel')}:</b> T = 2π √(m/k)<br><b>${t('experiments.substitutionLabel')}:</b> T = 2π √(${params.mass.toFixed(3)} / ${params.k.toFixed(1)})<br><b>${t('experiments.resultLabel')}:</b> T = <b>${T.toFixed(4)} s</b>`\n  }\n\n  function calcM() {\n    if (!measured.value.T) { calcResult.value = t('experiments.measurePeriodFirst'); return }\n    const mass = (measured.value.T * measured.value.T * params.k) / (4 * Math.PI * Math.PI)\n    calcResult.value = `<b>${t('experiments.equationLabel')}:</b> m = (T² × k) / (4π²)<br><b>${t('experiments.substitutionLabel')}:</b> m = (${measured.value.T.toFixed(4)}² × ${params.k.toFixed(1)}) / (4π²)<br><b>${t('experiments.resultLabel')}:</b> m = <b>${mass.toFixed(3)} kg</b>`\n  }\n\n  function calcFitK() {\n    if (trials.value.length < 2) { calcResult.value = t('experiments.atLeastTwoTrials'); return }\n    const xs = trials.value.map(t => t.mass)\n    const ys = trials.value.map(t => t.T * t.T)\n    const fit = linearRegression(xs, ys)\n    if (!fit || Math.abs(fit.slope) < 1e-12) { calcResult.value = t('experiments.insufficientData'); return }\n    const k = (4 * Math.PI * Math.PI) / fit.slope\n    const err = Math.abs((k - params.k) / params.k) * 100\n    const quality = fit.r2 > 0.98 ? '✅' : fit.r2 > 0.9 ? '🟡' : '⚠️'\n    calcResult.value = `T² = ${fit.slope.toFixed(5)}·m ${fit.intercept >= 0 ? '+' : ''} ${fit.intercept.toFixed(5)}<br>R² = ${fit.r2.toFixed(4)} ${quality}<br>k = 4π² / slope = <b>${k.toFixed(2)} N/m</b><br>${t('experiments.errorPercent')} = <b>${err.toFixed(2)}%</b>`\n  }\n\n  return {\n    trials, trialStats,\n    recordTrial, removeTrial, clearTrials, exportCsv,\n    undo, redo, canUndo, canRedo,\n    autoLoad,\n    calcResult, calcK, calcT, calcM, calcFitK,\n  }\n}\n",
      "start": 1783635314781,
      "end": 1783635314781
    },
    {
      "name": "vite-plugin-vue-inspector",
      "start": 1783635314781,
      "end": 1783635314781,
      "order": "pre"
    },
    {
      "name": "vite-plugin-vue-devtools",
      "result": "import { ref, computed, type Ref } from 'vue'\nimport { useI18n } from '../../composables/useI18n'\nimport { downloadCsv } from '../../components/experiment/spring/downloadCsv'\nimport { linearRegression } from '../../components/experiment/spring/linearRegression'\nimport type { SpringParams } from '../../modules/physics/experiments/spring/useSpringPhysics'\n\nexport interface SpringTrial {\n  id: number; mass: number; k: number; amplitude: number\n  T: number; f: number; omega: number; kCalc: number; err: number\n}\n\nexport interface SpringMeasured {\n  T: number | null; f: number | null; omega: number | null; kCalc: number | null; kCalcEff: number | null\n}\n\nconst SAVE_KEY = 'spring:trials:v1'\n\nexport function useSpringTrials(params: SpringParams, measured: Ref<SpringMeasured>) {\n  const { t } = useI18n()\n  const trials = ref<SpringTrial[]>([])\n  let nextTrialId = 1\n\n  // Undo/Redo history\n  const history = ref<SpringTrial[][]>([])\n  const historyIndex = ref(-1)\n\n  function pushHistory() {\n    // Remove any future history if we're not at the end\n    if (historyIndex.value < history.value.length - 1) {\n      history.value = history.value.slice(0, historyIndex.value + 1)\n    }\n    history.value.push([...trials.value])\n    historyIndex.value++\n    // Limit history to 20 steps\n    if (history.value.length > 20) {\n      history.value.shift()\n      historyIndex.value--\n    }\n  }\n\n  function undo() {\n    if (historyIndex.value > 0) {\n      historyIndex.value--\n      trials.value = [...history.value[historyIndex.value]]\n      nextTrialId = trials.value.length > 0 ? Math.max(...trials.value.map(t => t.id)) + 1 : 1\n    }\n  }\n\n  function redo() {\n    if (historyIndex.value < history.value.length - 1) {\n      historyIndex.value++\n      trials.value = [...history.value[historyIndex.value]]\n      nextTrialId = trials.value.length > 0 ? Math.max(...trials.value.map(t => t.id)) + 1 : 1\n    }\n  }\n\n  function canUndo() { return historyIndex.value > 0 }\n  function canRedo() { return historyIndex.value < history.value.length - 1 }\n\n  const trialStats = computed(() => {\n    if (trials.value.length === 0) return { T_mean: 0, T_std: 0, k_mean: 0, k_std: 0 }\n    const Ts = trials.value.map(t => t.T)\n    const ks = trials.value.map(t => t.kCalc)\n    const mean = (arr: number[]) => arr.reduce((a, b) => a + b, 0) / arr.length\n    const std = (arr: number[]) => { const m = mean(arr); return Math.sqrt(arr.reduce((sum, v) => sum + (v - m) ** 2, 0) / arr.length) }\n    return { T_mean: mean(Ts), T_std: std(Ts), k_mean: mean(ks), k_std: std(ks) }\n  })\n\n  // Gaussian noise simulation (~2% error, mimics human reading error)\n  function gaussianNoise(mean: number, stdDev: number) {\n    let u = 0, v = 0\n    while (u === 0) u = Math.random()\n    while (v === 0) v = Math.random()\n    const z = Math.sqrt(-2.0 * Math.log(u)) * Math.cos(2.0 * Math.PI * v)\n    return mean + z * stdDev\n  }\n\n  function recordTrial() {\n    if (!measured.value.T) return\n    if (params.mass < 0.01 || params.mass > 20) {\n      alert(t('experiments.massMustBeBetween', { min: '0.01', max: '20' }))\n      return\n    }\n    pushHistory()\n    const noiseLevel = 0.02  // ~2% reading error (human-like)\n    const noisyT = gaussianNoise(measured.value.T, measured.value.T * noiseLevel)\n    const noisyK = (4 * Math.PI * Math.PI * params.mass) / (noisyT * noisyT)\n    const err = Math.abs((noisyK - params.k) / params.k) * 100\n    trials.value = [...trials.value, {\n      id: nextTrialId++, mass: params.mass, k: params.k, amplitude: params.amplitude,\n      T: noisyT, f: 1 / noisyT, omega: (2 * Math.PI) / noisyT, kCalc: noisyK, err,\n    }]\n    autoSave()\n  }\n\n  function removeTrial(id: number) {\n    pushHistory()\n    trials.value = trials.value.filter(t => t.id !== id)\n    autoSave()\n  }\n  function clearTrials() {\n    pushHistory()\n    trials.value = []\n    autoSave()\n  }\n\n  // Auto-save to localStorage\n  function autoSave() {\n    try {\n      localStorage.setItem(SAVE_KEY, JSON.stringify({\n        trials: trials.value,\n        nextId: nextTrialId,\n        calcResult: calcResult.value,\n      }))\n    } catch { /* ignore */ }\n  }\n\n  function autoLoad() {\n    try {\n      const raw = localStorage.getItem(SAVE_KEY)\n      if (!raw) return\n      const parsed = JSON.parse(raw)\n      if (Array.isArray(parsed.trials)) {\n        const valid = parsed.trials.filter((t: Record<string, unknown>) => {\n          if (!t || typeof t.mass !== 'number' || t.mass < 0.01 || t.mass > 20) return false\n          if (typeof t.T !== 'number' || t.T <= 0 || t.T > 20) return false\n          // Sanity check: T should be roughly consistent with mass and k (within 3x of theoretical)\n          const theoreticalT = 2 * Math.PI * Math.sqrt(Number(t.mass) / Math.max(Number(t.k), 1))\n          if (t.T < theoreticalT / 3 || t.T > theoreticalT * 3) return false\n          return true\n        })\n        trials.value = valid\n        nextTrialId = parsed.nextId ?? (valid.length > 0 ? Math.max(...valid.map((t: Record<string, unknown>) => Number(t.id))) + 1 : 1)\n        if (parsed.calcResult) calcResult.value = parsed.calcResult\n        history.value = [[...valid]]\n        historyIndex.value = 0\n      }\n    } catch { /* ignore */ }\n  }\n\n  function exportCsv() {\n    if (!trials.value.length) return\n    downloadCsv('spring_mass_data.csv', [\n      ['#', 'm', 'k', 'A', 'T', 'f', 'ω', 'kcalc', 'error'],\n      ...trials.value.map((t, i) => [\n        i + 1, t.mass.toFixed(3), t.k.toFixed(1), t.amplitude.toFixed(3),\n        t.T.toFixed(4), t.f.toFixed(3), t.omega.toFixed(3),\n        t.kCalc.toFixed(2), t.err.toFixed(2) + '%',\n      ]),\n    ])\n  }\n\n  const calcResult = ref(t('experiments.clickBtnShowCalc'))\n\n  function calcK() {\n    if (!measured.value.T) { calcResult.value = t('experiments.measurePeriodFirst'); return }\n    const k = (4 * Math.PI * Math.PI * params.mass) / (measured.value.T * measured.value.T)\n    const err = Math.abs((k - params.k) / params.k) * 100\n    calcResult.value = `<b>${t('experiments.equationLabel')}:</b> k = 4π² × m / T²<br><b>${t('experiments.substitutionLabel')}:</b> k = 4π² × ${params.mass.toFixed(3)} / (${measured.value.T.toFixed(4)})²<br><b>${t('experiments.resultLabel')}:</b> k = <b>${k.toFixed(2)} N/m</b><br><b>${t('experiments.errorPercent')}:</b> ${err.toFixed(2)}%`\n  }\n\n  function calcT() {\n    const T = 2 * Math.PI * Math.sqrt(params.mass / params.k)\n    calcResult.value = `<b>${t('experiments.equationLabel')}:</b> T = 2π √(m/k)<br><b>${t('experiments.substitutionLabel')}:</b> T = 2π √(${params.mass.toFixed(3)} / ${params.k.toFixed(1)})<br><b>${t('experiments.resultLabel')}:</b> T = <b>${T.toFixed(4)} s</b>`\n  }\n\n  function calcM() {\n    if (!measured.value.T) { calcResult.value = t('experiments.measurePeriodFirst'); return }\n    const mass = (measured.value.T * measured.value.T * params.k) / (4 * Math.PI * Math.PI)\n    calcResult.value = `<b>${t('experiments.equationLabel')}:</b> m = (T² × k) / (4π²)<br><b>${t('experiments.substitutionLabel')}:</b> m = (${measured.value.T.toFixed(4)}² × ${params.k.toFixed(1)}) / (4π²)<br><b>${t('experiments.resultLabel')}:</b> m = <b>${mass.toFixed(3)} kg</b>`\n  }\n\n  function calcFitK() {\n    if (trials.value.length < 2) { calcResult.value = t('experiments.atLeastTwoTrials'); return }\n    const xs = trials.value.map(t => t.mass)\n    const ys = trials.value.map(t => t.T * t.T)\n    const fit = linearRegression(xs, ys)\n    if (!fit || Math.abs(fit.slope) < 1e-12) { calcResult.value = t('experiments.insufficientData'); return }\n    const k = (4 * Math.PI * Math.PI) / fit.slope\n    const err = Math.abs((k - params.k) / params.k) * 100\n    const quality = fit.r2 > 0.98 ? '✅' : fit.r2 > 0.9 ? '🟡' : '⚠️'\n    calcResult.value = `T² = ${fit.slope.toFixed(5)}·m ${fit.intercept >= 0 ? '+' : ''} ${fit.intercept.toFixed(5)}<br>R² = ${fit.r2.toFixed(4)} ${quality}<br>k = 4π² / slope = <b>${k.toFixed(2)} N/m</b><br>${t('experiments.errorPercent')} = <b>${err.toFixed(2)}%</b>`\n  }\n\n  return {\n    trials, trialStats,\n    recordTrial, removeTrial, clearTrials, exportCsv,\n    undo, redo, canUndo, canRedo,\n    autoLoad,\n    calcResult, calcK, calcT, calcM, calcFitK,\n  }\n}\n",
      "start": 1783635314781,
      "end": 1783635314781,
      "order": "pre",
      "sourcemaps": null
    },
    {
      "name": "vite:css",
      "start": 1783635314781,
      "end": 1783635314781,
      "order": "normal"
    },
    {
      "name": "vite:esbuild",
      "result": "import { ref, computed } from \"vue\";\nimport { useI18n } from \"../../composables/useI18n\";\nimport { downloadCsv } from \"../../components/experiment/spring/downloadCsv\";\nimport { linearRegression } from \"../../components/experiment/spring/linearRegression\";\nconst SAVE_KEY = \"spring:trials:v1\";\nexport function useSpringTrials(params, measured) {\n  const { t } = useI18n();\n  const trials = ref([]);\n  let nextTrialId = 1;\n  const history = ref([]);\n  const historyIndex = ref(-1);\n  function pushHistory() {\n    if (historyIndex.value < history.value.length - 1) {\n      history.value = history.value.slice(0, historyIndex.value + 1);\n    }\n    history.value.push([...trials.value]);\n    historyIndex.value++;\n    if (history.value.length > 20) {\n      history.value.shift();\n      historyIndex.value--;\n    }\n  }\n  function undo() {\n    if (historyIndex.value > 0) {\n      historyIndex.value--;\n      trials.value = [...history.value[historyIndex.value]];\n      nextTrialId = trials.value.length > 0 ? Math.max(...trials.value.map((t2) => t2.id)) + 1 : 1;\n    }\n  }\n  function redo() {\n    if (historyIndex.value < history.value.length - 1) {\n      historyIndex.value++;\n      trials.value = [...history.value[historyIndex.value]];\n      nextTrialId = trials.value.length > 0 ? Math.max(...trials.value.map((t2) => t2.id)) + 1 : 1;\n    }\n  }\n  function canUndo() {\n    return historyIndex.value > 0;\n  }\n  function canRedo() {\n    return historyIndex.value < history.value.length - 1;\n  }\n  const trialStats = computed(() => {\n    if (trials.value.length === 0) return { T_mean: 0, T_std: 0, k_mean: 0, k_std: 0 };\n    const Ts = trials.value.map((t2) => t2.T);\n    const ks = trials.value.map((t2) => t2.kCalc);\n    const mean = (arr) => arr.reduce((a, b) => a + b, 0) / arr.length;\n    const std = (arr) => {\n      const m = mean(arr);\n      return Math.sqrt(arr.reduce((sum, v) => sum + (v - m) ** 2, 0) / arr.length);\n    };\n    return { T_mean: mean(Ts), T_std: std(Ts), k_mean: mean(ks), k_std: std(ks) };\n  });\n  function gaussianNoise(mean, stdDev) {\n    let u = 0, v = 0;\n    while (u === 0) u = Math.random();\n    while (v === 0) v = Math.random();\n    const z = Math.sqrt(-2 * Math.log(u)) * Math.cos(2 * Math.PI * v);\n    return mean + z * stdDev;\n  }\n  function recordTrial() {\n    if (!measured.value.T) return;\n    if (params.mass < 0.01 || params.mass > 20) {\n      alert(t(\"experiments.massMustBeBetween\", { min: \"0.01\", max: \"20\" }));\n      return;\n    }\n    pushHistory();\n    const noiseLevel = 0.02;\n    const noisyT = gaussianNoise(measured.value.T, measured.value.T * noiseLevel);\n    const noisyK = 4 * Math.PI * Math.PI * params.mass / (noisyT * noisyT);\n    const err = Math.abs((noisyK - params.k) / params.k) * 100;\n    trials.value = [...trials.value, {\n      id: nextTrialId++,\n      mass: params.mass,\n      k: params.k,\n      amplitude: params.amplitude,\n      T: noisyT,\n      f: 1 / noisyT,\n      omega: 2 * Math.PI / noisyT,\n      kCalc: noisyK,\n      err\n    }];\n    autoSave();\n  }\n  function removeTrial(id) {\n    pushHistory();\n    trials.value = trials.value.filter((t2) => t2.id !== id);\n    autoSave();\n  }\n  function clearTrials() {\n    pushHistory();\n    trials.value = [];\n    autoSave();\n  }\n  function autoSave() {\n    try {\n      localStorage.setItem(SAVE_KEY, JSON.stringify({\n        trials: trials.value,\n        nextId: nextTrialId,\n        calcResult: calcResult.value\n      }));\n    } catch {\n    }\n  }\n  function autoLoad() {\n    try {\n      const raw = localStorage.getItem(SAVE_KEY);\n      if (!raw) return;\n      const parsed = JSON.parse(raw);\n      if (Array.isArray(parsed.trials)) {\n        const valid = parsed.trials.filter((t2) => {\n          if (!t2 || typeof t2.mass !== \"number\" || t2.mass < 0.01 || t2.mass > 20) return false;\n          if (typeof t2.T !== \"number\" || t2.T <= 0 || t2.T > 20) return false;\n          const theoreticalT = 2 * Math.PI * Math.sqrt(Number(t2.mass) / Math.max(Number(t2.k), 1));\n          if (t2.T < theoreticalT / 3 || t2.T > theoreticalT * 3) return false;\n          return true;\n        });\n        trials.value = valid;\n        nextTrialId = parsed.nextId ?? (valid.length > 0 ? Math.max(...valid.map((t2) => Number(t2.id))) + 1 : 1);\n        if (parsed.calcResult) calcResult.value = parsed.calcResult;\n        history.value = [[...valid]];\n        historyIndex.value = 0;\n      }\n    } catch {\n    }\n  }\n  function exportCsv() {\n    if (!trials.value.length) return;\n    downloadCsv(\"spring_mass_data.csv\", [\n      [\"#\", \"m\", \"k\", \"A\", \"T\", \"f\", \"ω\", \"kcalc\", \"error\"],\n      ...trials.value.map((t2, i) => [\n        i + 1,\n        t2.mass.toFixed(3),\n        t2.k.toFixed(1),\n        t2.amplitude.toFixed(3),\n        t2.T.toFixed(4),\n        t2.f.toFixed(3),\n        t2.omega.toFixed(3),\n        t2.kCalc.toFixed(2),\n        t2.err.toFixed(2) + \"%\"\n      ])\n    ]);\n  }\n  const calcResult = ref(t(\"experiments.clickBtnShowCalc\"));\n  function calcK() {\n    if (!measured.value.T) {\n      calcResult.value = t(\"experiments.measurePeriodFirst\");\n      return;\n    }\n    const k = 4 * Math.PI * Math.PI * params.mass / (measured.value.T * measured.value.T);\n    const err = Math.abs((k - params.k) / params.k) * 100;\n    calcResult.value = `<b>${t(\"experiments.equationLabel\")}:</b> k = 4π² × m / T²<br><b>${t(\"experiments.substitutionLabel\")}:</b> k = 4π² × ${params.mass.toFixed(3)} / (${measured.value.T.toFixed(4)})²<br><b>${t(\"experiments.resultLabel\")}:</b> k = <b>${k.toFixed(2)} N/m</b><br><b>${t(\"experiments.errorPercent\")}:</b> ${err.toFixed(2)}%`;\n  }\n  function calcT() {\n    const T = 2 * Math.PI * Math.sqrt(params.mass / params.k);\n    calcResult.value = `<b>${t(\"experiments.equationLabel\")}:</b> T = 2π √(m/k)<br><b>${t(\"experiments.substitutionLabel\")}:</b> T = 2π √(${params.mass.toFixed(3)} / ${params.k.toFixed(1)})<br><b>${t(\"experiments.resultLabel\")}:</b> T = <b>${T.toFixed(4)} s</b>`;\n  }\n  function calcM() {\n    if (!measured.value.T) {\n      calcResult.value = t(\"experiments.measurePeriodFirst\");\n      return;\n    }\n    const mass = measured.value.T * measured.value.T * params.k / (4 * Math.PI * Math.PI);\n    calcResult.value = `<b>${t(\"experiments.equationLabel\")}:</b> m = (T² × k) / (4π²)<br><b>${t(\"experiments.substitutionLabel\")}:</b> m = (${measured.value.T.toFixed(4)}² × ${params.k.toFixed(1)}) / (4π²)<br><b>${t(\"experiments.resultLabel\")}:</b> m = <b>${mass.toFixed(3)} kg</b>`;\n  }\n  function calcFitK() {\n    if (trials.value.length < 2) {\n      calcResult.value = t(\"experiments.atLeastTwoTrials\");\n      return;\n    }\n    const xs = trials.value.map((t2) => t2.mass);\n    const ys = trials.value.map((t2) => t2.T * t2.T);\n    const fit = linearRegression(xs, ys);\n    if (!fit || Math.abs(fit.slope) < 1e-12) {\n      calcResult.value = t(\"experiments.insufficientData\");\n      return;\n    }\n    const k = 4 * Math.PI * Math.PI / fit.slope;\n    const err = Math.abs((k - params.k) / params.k) * 100;\n    const quality = fit.r2 > 0.98 ? \"✅\" : fit.r2 > 0.9 ? \"🟡\" : \"⚠️\";\n    calcResult.value = `T² = ${fit.slope.toFixed(5)}·m ${fit.intercept >= 0 ? \"+\" : \"\"} ${fit.intercept.toFixed(5)}<br>R² = ${fit.r2.toFixed(4)} ${quality}<br>k = 4π² / slope = <b>${k.toFixed(2)} N/m</b><br>${t(\"experiments.errorPercent\")} = <b>${err.toFixed(2)}%</b>`;\n  }\n  return {\n    trials,\n    trialStats,\n    recordTrial,\n    removeTrial,\n    clearTrials,\n    exportCsv,\n    undo,\n    redo,\n    canUndo,\n    canRedo,\n    autoLoad,\n    calcResult,\n    calcK,\n    calcT,\n    calcM,\n    calcFitK\n  };\n}\n",
      "start": 1783635314781,
      "end": 1783635315627,
      "order": "normal",
      "sourcemaps": {
        "version": 3,
        "sources": [
          "useSpringTrials.ts"
        ],
        "sourcesContent": [
          "import { ref, computed, type Ref } from 'vue'\nimport { useI18n } from '../../composables/useI18n'\nimport { downloadCsv } from '../../components/experiment/spring/downloadCsv'\nimport { linearRegression } from '../../components/experiment/spring/linearRegression'\nimport type { SpringParams } from '../../modules/physics/experiments/spring/useSpringPhysics'\n\nexport interface SpringTrial {\n  id: number; mass: number; k: number; amplitude: number\n  T: number; f: number; omega: number; kCalc: number; err: number\n}\n\nexport interface SpringMeasured {\n  T: number | null; f: number | null; omega: number | null; kCalc: number | null; kCalcEff: number | null\n}\n\nconst SAVE_KEY = 'spring:trials:v1'\n\nexport function useSpringTrials(params: SpringParams, measured: Ref<SpringMeasured>) {\n  const { t } = useI18n()\n  const trials = ref<SpringTrial[]>([])\n  let nextTrialId = 1\n\n  // Undo/Redo history\n  const history = ref<SpringTrial[][]>([])\n  const historyIndex = ref(-1)\n\n  function pushHistory() {\n    // Remove any future history if we're not at the end\n    if (historyIndex.value < history.value.length - 1) {\n      history.value = history.value.slice(0, historyIndex.value + 1)\n    }\n    history.value.push([...trials.value])\n    historyIndex.value++\n    // Limit history to 20 steps\n    if (history.value.length > 20) {\n      history.value.shift()\n      historyIndex.value--\n    }\n  }\n\n  function undo() {\n    if (historyIndex.value > 0) {\n      historyIndex.value--\n      trials.value = [...history.value[historyIndex.value]]\n      nextTrialId = trials.value.length > 0 ? Math.max(...trials.value.map(t => t.id)) + 1 : 1\n    }\n  }\n\n  function redo() {\n    if (historyIndex.value < history.value.length - 1) {\n      historyIndex.value++\n      trials.value = [...history.value[historyIndex.value]]\n      nextTrialId = trials.value.length > 0 ? Math.max(...trials.value.map(t => t.id)) + 1 : 1\n    }\n  }\n\n  function canUndo() { return historyIndex.value > 0 }\n  function canRedo() { return historyIndex.value < history.value.length - 1 }\n\n  const trialStats = computed(() => {\n    if (trials.value.length === 0) return { T_mean: 0, T_std: 0, k_mean: 0, k_std: 0 }\n    const Ts = trials.value.map(t => t.T)\n    const ks = trials.value.map(t => t.kCalc)\n    const mean = (arr: number[]) => arr.reduce((a, b) => a + b, 0) / arr.length\n    const std = (arr: number[]) => { const m = mean(arr); return Math.sqrt(arr.reduce((sum, v) => sum + (v - m) ** 2, 0) / arr.length) }\n    return { T_mean: mean(Ts), T_std: std(Ts), k_mean: mean(ks), k_std: std(ks) }\n  })\n\n  // Gaussian noise simulation (~2% error, mimics human reading error)\n  function gaussianNoise(mean: number, stdDev: number) {\n    let u = 0, v = 0\n    while (u === 0) u = Math.random()\n    while (v === 0) v = Math.random()\n    const z = Math.sqrt(-2.0 * Math.log(u)) * Math.cos(2.0 * Math.PI * v)\n    return mean + z * stdDev\n  }\n\n  function recordTrial() {\n    if (!measured.value.T) return\n    if (params.mass < 0.01 || params.mass > 20) {\n      alert(t('experiments.massMustBeBetween', { min: '0.01', max: '20' }))\n      return\n    }\n    pushHistory()\n    const noiseLevel = 0.02  // ~2% reading error (human-like)\n    const noisyT = gaussianNoise(measured.value.T, measured.value.T * noiseLevel)\n    const noisyK = (4 * Math.PI * Math.PI * params.mass) / (noisyT * noisyT)\n    const err = Math.abs((noisyK - params.k) / params.k) * 100\n    trials.value = [...trials.value, {\n      id: nextTrialId++, mass: params.mass, k: params.k, amplitude: params.amplitude,\n      T: noisyT, f: 1 / noisyT, omega: (2 * Math.PI) / noisyT, kCalc: noisyK, err,\n    }]\n    autoSave()\n  }\n\n  function removeTrial(id: number) {\n    pushHistory()\n    trials.value = trials.value.filter(t => t.id !== id)\n    autoSave()\n  }\n  function clearTrials() {\n    pushHistory()\n    trials.value = []\n    autoSave()\n  }\n\n  // Auto-save to localStorage\n  function autoSave() {\n    try {\n      localStorage.setItem(SAVE_KEY, JSON.stringify({\n        trials: trials.value,\n        nextId: nextTrialId,\n        calcResult: calcResult.value,\n      }))\n    } catch { /* ignore */ }\n  }\n\n  function autoLoad() {\n    try {\n      const raw = localStorage.getItem(SAVE_KEY)\n      if (!raw) return\n      const parsed = JSON.parse(raw)\n      if (Array.isArray(parsed.trials)) {\n        const valid = parsed.trials.filter((t: Record<string, unknown>) => {\n          if (!t || typeof t.mass !== 'number' || t.mass < 0.01 || t.mass > 20) return false\n          if (typeof t.T !== 'number' || t.T <= 0 || t.T > 20) return false\n          // Sanity check: T should be roughly consistent with mass and k (within 3x of theoretical)\n          const theoreticalT = 2 * Math.PI * Math.sqrt(Number(t.mass) / Math.max(Number(t.k), 1))\n          if (t.T < theoreticalT / 3 || t.T > theoreticalT * 3) return false\n          return true\n        })\n        trials.value = valid\n        nextTrialId = parsed.nextId ?? (valid.length > 0 ? Math.max(...valid.map((t: Record<string, unknown>) => Number(t.id))) + 1 : 1)\n        if (parsed.calcResult) calcResult.value = parsed.calcResult\n        history.value = [[...valid]]\n        historyIndex.value = 0\n      }\n    } catch { /* ignore */ }\n  }\n\n  function exportCsv() {\n    if (!trials.value.length) return\n    downloadCsv('spring_mass_data.csv', [\n      ['#', 'm', 'k', 'A', 'T', 'f', 'ω', 'kcalc', 'error'],\n      ...trials.value.map((t, i) => [\n        i + 1, t.mass.toFixed(3), t.k.toFixed(1), t.amplitude.toFixed(3),\n        t.T.toFixed(4), t.f.toFixed(3), t.omega.toFixed(3),\n        t.kCalc.toFixed(2), t.err.toFixed(2) + '%',\n      ]),\n    ])\n  }\n\n  const calcResult = ref(t('experiments.clickBtnShowCalc'))\n\n  function calcK() {\n    if (!measured.value.T) { calcResult.value = t('experiments.measurePeriodFirst'); return }\n    const k = (4 * Math.PI * Math.PI * params.mass) / (measured.value.T * measured.value.T)\n    const err = Math.abs((k - params.k) / params.k) * 100\n    calcResult.value = `<b>${t('experiments.equationLabel')}:</b> k = 4π² × m / T²<br><b>${t('experiments.substitutionLabel')}:</b> k = 4π² × ${params.mass.toFixed(3)} / (${measured.value.T.toFixed(4)})²<br><b>${t('experiments.resultLabel')}:</b> k = <b>${k.toFixed(2)} N/m</b><br><b>${t('experiments.errorPercent')}:</b> ${err.toFixed(2)}%`\n  }\n\n  function calcT() {\n    const T = 2 * Math.PI * Math.sqrt(params.mass / params.k)\n    calcResult.value = `<b>${t('experiments.equationLabel')}:</b> T = 2π √(m/k)<br><b>${t('experiments.substitutionLabel')}:</b> T = 2π √(${params.mass.toFixed(3)} / ${params.k.toFixed(1)})<br><b>${t('experiments.resultLabel')}:</b> T = <b>${T.toFixed(4)} s</b>`\n  }\n\n  function calcM() {\n    if (!measured.value.T) { calcResult.value = t('experiments.measurePeriodFirst'); return }\n    const mass = (measured.value.T * measured.value.T * params.k) / (4 * Math.PI * Math.PI)\n    calcResult.value = `<b>${t('experiments.equationLabel')}:</b> m = (T² × k) / (4π²)<br><b>${t('experiments.substitutionLabel')}:</b> m = (${measured.value.T.toFixed(4)}² × ${params.k.toFixed(1)}) / (4π²)<br><b>${t('experiments.resultLabel')}:</b> m = <b>${mass.toFixed(3)} kg</b>`\n  }\n\n  function calcFitK() {\n    if (trials.value.length < 2) { calcResult.value = t('experiments.atLeastTwoTrials'); return }\n    const xs = trials.value.map(t => t.mass)\n    const ys = trials.value.map(t => t.T * t.T)\n    const fit = linearRegression(xs, ys)\n    if (!fit || Math.abs(fit.slope) < 1e-12) { calcResult.value = t('experiments.insufficientData'); return }\n    const k = (4 * Math.PI * Math.PI) / fit.slope\n    const err = Math.abs((k - params.k) / params.k) * 100\n    const quality = fit.r2 > 0.98 ? '✅' : fit.r2 > 0.9 ? '🟡' : '⚠️'\n    calcResult.value = `T² = ${fit.slope.toFixed(5)}·m ${fit.intercept >= 0 ? '+' : ''} ${fit.intercept.toFixed(5)}<br>R² = ${fit.r2.toFixed(4)} ${quality}<br>k = 4π² / slope = <b>${k.toFixed(2)} N/m</b><br>${t('experiments.errorPercent')} = <b>${err.toFixed(2)}%</b>`\n  }\n\n  return {\n    trials, trialStats,\n    recordTrial, removeTrial, clearTrials, exportCsv,\n    undo, redo, canUndo, canRedo,\n    autoLoad,\n    calcResult, calcK, calcT, calcM, calcFitK,\n  }\n}\n"
        ],
        "mappings": "AAAA,SAAS,KAAK,gBAA0B;AACxC,SAAS,eAAe;AACxB,SAAS,mBAAmB;AAC5B,SAAS,wBAAwB;AAYjC,MAAM,WAAW;AAEV,gBAAS,gBAAgB,QAAsB,UAA+B;AACnF,QAAM,EAAE,EAAE,IAAI,QAAQ;AACtB,QAAM,SAAS,IAAmB,CAAC,CAAC;AACpC,MAAI,cAAc;AAGlB,QAAM,UAAU,IAAqB,CAAC,CAAC;AACvC,QAAM,eAAe,IAAI,EAAE;AAE3B,WAAS,cAAc;AAErB,QAAI,aAAa,QAAQ,QAAQ,MAAM,SAAS,GAAG;AACjD,cAAQ,QAAQ,QAAQ,MAAM,MAAM,GAAG,aAAa,QAAQ,CAAC;AAAA,IAC/D;AACA,YAAQ,MAAM,KAAK,CAAC,GAAG,OAAO,KAAK,CAAC;AACpC,iBAAa;AAEb,QAAI,QAAQ,MAAM,SAAS,IAAI;AAC7B,cAAQ,MAAM,MAAM;AACpB,mBAAa;AAAA,IACf;AAAA,EACF;AAEA,WAAS,OAAO;AACd,QAAI,aAAa,QAAQ,GAAG;AAC1B,mBAAa;AACb,aAAO,QAAQ,CAAC,GAAG,QAAQ,MAAM,aAAa,KAAK,CAAC;AACpD,oBAAc,OAAO,MAAM,SAAS,IAAI,KAAK,IAAI,GAAG,OAAO,MAAM,IAAI,CAAAA,OAAKA,GAAE,EAAE,CAAC,IAAI,IAAI;AAAA,IACzF;AAAA,EACF;AAEA,WAAS,OAAO;AACd,QAAI,aAAa,QAAQ,QAAQ,MAAM,SAAS,GAAG;AACjD,mBAAa;AACb,aAAO,QAAQ,CAAC,GAAG,QAAQ,MAAM,aAAa,KAAK,CAAC;AACpD,oBAAc,OAAO,MAAM,SAAS,IAAI,KAAK,IAAI,GAAG,OAAO,MAAM,IAAI,CAAAA,OAAKA,GAAE,EAAE,CAAC,IAAI,IAAI;AAAA,IACzF;AAAA,EACF;AAEA,WAAS,UAAU;AAAE,WAAO,aAAa,QAAQ;AAAA,EAAE;AACnD,WAAS,UAAU;AAAE,WAAO,aAAa,QAAQ,QAAQ,MAAM,SAAS;AAAA,EAAE;AAE1E,QAAM,aAAa,SAAS,MAAM;AAChC,QAAI,OAAO,MAAM,WAAW,EAAG,QAAO,EAAE,QAAQ,GAAG,OAAO,GAAG,QAAQ,GAAG,OAAO,EAAE;AACjF,UAAM,KAAK,OAAO,MAAM,IAAI,CAAAA,OAAKA,GAAE,CAAC;AACpC,UAAM,KAAK,OAAO,MAAM,IAAI,CAAAA,OAAKA,GAAE,KAAK;AACxC,UAAM,OAAO,CAAC,QAAkB,IAAI,OAAO,CAAC,GAAG,MAAM,IAAI,GAAG,CAAC,IAAI,IAAI;AACrE,UAAM,MAAM,CAAC,QAAkB;AAAE,YAAM,IAAI,KAAK,GAAG;AAAG,aAAO,KAAK,KAAK,IAAI,OAAO,CAAC,KAAK,MAAM,OAAO,IAAI,MAAM,GAAG,CAAC,IAAI,IAAI,MAAM;AAAA,IAAE;AACnI,WAAO,EAAE,QAAQ,KAAK,EAAE,GAAG,OAAO,IAAI,EAAE,GAAG,QAAQ,KAAK,EAAE,GAAG,OAAO,IAAI,EAAE,EAAE;AAAA,EAC9E,CAAC;AAGD,WAAS,cAAc,MAAc,QAAgB;AACnD,QAAI,IAAI,GAAG,IAAI;AACf,WAAO,MAAM,EAAG,KAAI,KAAK,OAAO;AAChC,WAAO,MAAM,EAAG,KAAI,KAAK,OAAO;AAChC,UAAM,IAAI,KAAK,KAAK,KAAO,KAAK,IAAI,CAAC,CAAC,IAAI,KAAK,IAAI,IAAM,KAAK,KAAK,CAAC;AACpE,WAAO,OAAO,IAAI;AAAA,EACpB;AAEA,WAAS,cAAc;AACrB,QAAI,CAAC,SAAS,MAAM,EAAG;AACvB,QAAI,OAAO,OAAO,QAAQ,OAAO,OAAO,IAAI;AAC1C,YAAM,EAAE,iCAAiC,EAAE,KAAK,QAAQ,KAAK,KAAK,CAAC,CAAC;AACpE;AAAA,IACF;AACA,gBAAY;AACZ,UAAM,aAAa;AACnB,UAAM,SAAS,cAAc,SAAS,MAAM,GAAG,SAAS,MAAM,IAAI,UAAU;AAC5E,UAAM,SAAU,IAAI,KAAK,KAAK,KAAK,KAAK,OAAO,QAAS,SAAS;AACjE,UAAM,MAAM,KAAK,KAAK,SAAS,OAAO,KAAK,OAAO,CAAC,IAAI;AACvD,WAAO,QAAQ,CAAC,GAAG,OAAO,OAAO;AAAA,MAC/B,IAAI;AAAA,MAAe,MAAM,OAAO;AAAA,MAAM,GAAG,OAAO;AAAA,MAAG,WAAW,OAAO;AAAA,MACrE,GAAG;AAAA,MAAQ,GAAG,IAAI;AAAA,MAAQ,OAAQ,IAAI,KAAK,KAAM;AAAA,MAAQ,OAAO;AAAA,MAAQ;AAAA,IAC1E,CAAC;AACD,aAAS;AAAA,EACX;AAEA,WAAS,YAAY,IAAY;AAC/B,gBAAY;AACZ,WAAO,QAAQ,OAAO,MAAM,OAAO,CAAAA,OAAKA,GAAE,OAAO,EAAE;AACnD,aAAS;AAAA,EACX;AACA,WAAS,cAAc;AACrB,gBAAY;AACZ,WAAO,QAAQ,CAAC;AAChB,aAAS;AAAA,EACX;AAGA,WAAS,WAAW;AAClB,QAAI;AACF,mBAAa,QAAQ,UAAU,KAAK,UAAU;AAAA,QAC5C,QAAQ,OAAO;AAAA,QACf,QAAQ;AAAA,QACR,YAAY,WAAW;AAAA,MACzB,CAAC,CAAC;AAAA,IACJ,QAAQ;AAAA,IAAe;AAAA,EACzB;AAEA,WAAS,WAAW;AAClB,QAAI;AACF,YAAM,MAAM,aAAa,QAAQ,QAAQ;AACzC,UAAI,CAAC,IAAK;AACV,YAAM,SAAS,KAAK,MAAM,GAAG;AAC7B,UAAI,MAAM,QAAQ,OAAO,MAAM,GAAG;AAChC,cAAM,QAAQ,OAAO,OAAO,OAAO,CAACA,OAA+B;AACjE,cAAI,CAACA,MAAK,OAAOA,GAAE,SAAS,YAAYA,GAAE,OAAO,QAAQA,GAAE,OAAO,GAAI,QAAO;AAC7E,cAAI,OAAOA,GAAE,MAAM,YAAYA,GAAE,KAAK,KAAKA,GAAE,IAAI,GAAI,QAAO;AAE5D,gBAAM,eAAe,IAAI,KAAK,KAAK,KAAK,KAAK,OAAOA,GAAE,IAAI,IAAI,KAAK,IAAI,OAAOA,GAAE,CAAC,GAAG,CAAC,CAAC;AACtF,cAAIA,GAAE,IAAI,eAAe,KAAKA,GAAE,IAAI,eAAe,EAAG,QAAO;AAC7D,iBAAO;AAAA,QACT,CAAC;AACD,eAAO,QAAQ;AACf,sBAAc,OAAO,WAAW,MAAM,SAAS,IAAI,KAAK,IAAI,GAAG,MAAM,IAAI,CAACA,OAA+B,OAAOA,GAAE,EAAE,CAAC,CAAC,IAAI,IAAI;AAC9H,YAAI,OAAO,WAAY,YAAW,QAAQ,OAAO;AACjD,gBAAQ,QAAQ,CAAC,CAAC,GAAG,KAAK,CAAC;AAC3B,qBAAa,QAAQ;AAAA,MACvB;AAAA,IACF,QAAQ;AAAA,IAAe;AAAA,EACzB;AAEA,WAAS,YAAY;AACnB,QAAI,CAAC,OAAO,MAAM,OAAQ;AAC1B,gBAAY,wBAAwB;AAAA,MAClC,CAAC,KAAK,KAAK,KAAK,KAAK,KAAK,KAAK,KAAK,SAAS,OAAO;AAAA,MACpD,GAAG,OAAO,MAAM,IAAI,CAACA,IAAG,MAAM;AAAA,QAC5B,IAAI;AAAA,QAAGA,GAAE,KAAK,QAAQ,CAAC;AAAA,QAAGA,GAAE,EAAE,QAAQ,CAAC;AAAA,QAAGA,GAAE,UAAU,QAAQ,CAAC;AAAA,QAC/DA,GAAE,EAAE,QAAQ,CAAC;AAAA,QAAGA,GAAE,EAAE,QAAQ,CAAC;AAAA,QAAGA,GAAE,MAAM,QAAQ,CAAC;AAAA,QACjDA,GAAE,MAAM,QAAQ,CAAC;AAAA,QAAGA,GAAE,IAAI,QAAQ,CAAC,IAAI;AAAA,MACzC,CAAC;AAAA,IACH,CAAC;AAAA,EACH;AAEA,QAAM,aAAa,IAAI,EAAE,8BAA8B,CAAC;AAExD,WAAS,QAAQ;AACf,QAAI,CAAC,SAAS,MAAM,GAAG;AAAE,iBAAW,QAAQ,EAAE,gCAAgC;AAAG;AAAA,IAAO;AACxF,UAAM,IAAK,IAAI,KAAK,KAAK,KAAK,KAAK,OAAO,QAAS,SAAS,MAAM,IAAI,SAAS,MAAM;AACrF,UAAM,MAAM,KAAK,KAAK,IAAI,OAAO,KAAK,OAAO,CAAC,IAAI;AAClD,eAAW,QAAQ,MAAM,EAAE,2BAA2B,CAAC,gCAAgC,EAAE,+BAA+B,CAAC,mBAAmB,OAAO,KAAK,QAAQ,CAAC,CAAC,OAAO,SAAS,MAAM,EAAE,QAAQ,CAAC,CAAC,YAAY,EAAE,yBAAyB,CAAC,gBAAgB,EAAE,QAAQ,CAAC,CAAC,kBAAkB,EAAE,0BAA0B,CAAC,SAAS,IAAI,QAAQ,CAAC,CAAC;AAAA,EAChV;AAEA,WAAS,QAAQ;AACf,UAAM,IAAI,IAAI,KAAK,KAAK,KAAK,KAAK,OAAO,OAAO,OAAO,CAAC;AACxD,eAAW,QAAQ,MAAM,EAAE,2BAA2B,CAAC,6BAA6B,EAAE,+BAA+B,CAAC,kBAAkB,OAAO,KAAK,QAAQ,CAAC,CAAC,MAAM,OAAO,EAAE,QAAQ,CAAC,CAAC,WAAW,EAAE,yBAAyB,CAAC,gBAAgB,EAAE,QAAQ,CAAC,CAAC;AAAA,EAC5P;AAEA,WAAS,QAAQ;AACf,QAAI,CAAC,SAAS,MAAM,GAAG;AAAE,iBAAW,QAAQ,EAAE,gCAAgC;AAAG;AAAA,IAAO;AACxF,UAAM,OAAQ,SAAS,MAAM,IAAI,SAAS,MAAM,IAAI,OAAO,KAAM,IAAI,KAAK,KAAK,KAAK;AACpF,eAAW,QAAQ,MAAM,EAAE,2BAA2B,CAAC,oCAAoC,EAAE,+BAA+B,CAAC,cAAc,SAAS,MAAM,EAAE,QAAQ,CAAC,CAAC,OAAO,OAAO,EAAE,QAAQ,CAAC,CAAC,mBAAmB,EAAE,yBAAyB,CAAC,gBAAgB,KAAK,QAAQ,CAAC,CAAC;AAAA,EAChR;AAEA,WAAS,WAAW;AAClB,QAAI,OAAO,MAAM,SAAS,GAAG;AAAE,iBAAW,QAAQ,EAAE,8BAA8B;AAAG;AAAA,IAAO;AAC5F,UAAM,KAAK,OAAO,MAAM,IAAI,CAAAA,OAAKA,GAAE,IAAI;AACvC,UAAM,KAAK,OAAO,MAAM,IAAI,CAAAA,OAAKA,GAAE,IAAIA,GAAE,CAAC;AAC1C,UAAM,MAAM,iBAAiB,IAAI,EAAE;AACnC,QAAI,CAAC,OAAO,KAAK,IAAI,IAAI,KAAK,IAAI,OAAO;AAAE,iBAAW,QAAQ,EAAE,8BAA8B;AAAG;AAAA,IAAO;AACxG,UAAM,IAAK,IAAI,KAAK,KAAK,KAAK,KAAM,IAAI;AACxC,UAAM,MAAM,KAAK,KAAK,IAAI,OAAO,KAAK,OAAO,CAAC,IAAI;AAClD,UAAM,UAAU,IAAI,KAAK,OAAO,MAAM,IAAI,KAAK,MAAM,OAAO;AAC5D,eAAW,QAAQ,QAAQ,IAAI,MAAM,QAAQ,CAAC,CAAC,MAAM,IAAI,aAAa,IAAI,MAAM,EAAE,IAAI,IAAI,UAAU,QAAQ,CAAC,CAAC,YAAY,IAAI,GAAG,QAAQ,CAAC,CAAC,IAAI,OAAO,4BAA4B,EAAE,QAAQ,CAAC,CAAC,eAAe,EAAE,0BAA0B,CAAC,SAAS,IAAI,QAAQ,CAAC,CAAC;AAAA,EACnQ;AAEA,SAAO;AAAA,IACL;AAAA,IAAQ;AAAA,IACR;AAAA,IAAa;AAAA,IAAa;AAAA,IAAa;AAAA,IACvC;AAAA,IAAM;AAAA,IAAM;AAAA,IAAS;AAAA,IACrB;AAAA,IACA;AAAA,IAAY;AAAA,IAAO;AAAA,IAAO;AAAA,IAAO;AAAA,EACnC;AACF;",
        "names": [
          "t"
        ]
      }
    },
    {
      "name": "vite:json",
      "start": 1783635315627,
      "end": 1783635315627,
      "order": "normal"
    },
    {
      "name": "vite:worker",
      "start": 1783635315627,
      "end": 1783635315627,
      "order": "normal"
    },
    {
      "name": "vite:vue",
      "start": 1783635315627,
      "end": 1783635315627,
      "order": "normal-normal"
    },
    {
      "name": "vite:define",
      "start": 1783635315627,
      "end": 1783635315627,
      "order": "normal"
    },
    {
      "name": "vite:css-post",
      "start": 1783635315627,
      "end": 1783635315627,
      "order": "normal"
    },
    {
      "name": "vite:worker-import-meta-url",
      "start": 1783635315627,
      "end": 1783635315627,
      "order": "normal"
    },
    {
      "name": "vite:asset-import-meta-url",
      "start": 1783635315627,
      "end": 1783635315627,
      "order": "normal"
    },
    {
      "name": "vite:dynamic-import-vars",
      "start": 1783635315627,
      "end": 1783635315627,
      "order": "normal"
    },
    {
      "name": "vite:import-glob",
      "start": 1783635315627,
      "end": 1783635315627,
      "order": "normal"
    },
    {
      "name": "vite-plugin-vue-inspector:post",
      "start": 1783635315627,
      "end": 1783635315627,
      "order": "post"
    },
    {
      "name": "vite:client-inject",
      "start": 1783635315627,
      "end": 1783635315627,
      "order": "normal"
    },
    {
      "name": "vite:css-analysis",
      "start": 1783635315627,
      "end": 1783635315627,
      "order": "normal"
    },
    {
      "name": "vite:import-analysis",
      "result": "import { ref, computed } from \"/node_modules/.vite/deps/vue.js?v=09a3ad4c\";\nimport { useI18n } from \"/src/composables/useI18n.ts\";\nimport { downloadCsv } from \"/src/components/experiment/spring/downloadCsv.ts\";\nimport { linearRegression } from \"/src/components/experiment/spring/linearRegression.ts\";\nconst SAVE_KEY = \"spring:trials:v1\";\nexport function useSpringTrials(params, measured) {\n  const { t } = useI18n();\n  const trials = ref([]);\n  let nextTrialId = 1;\n  const history = ref([]);\n  const historyIndex = ref(-1);\n  function pushHistory() {\n    if (historyIndex.value < history.value.length - 1) {\n      history.value = history.value.slice(0, historyIndex.value + 1);\n    }\n    history.value.push([...trials.value]);\n    historyIndex.value++;\n    if (history.value.length > 20) {\n      history.value.shift();\n      historyIndex.value--;\n    }\n  }\n  function undo() {\n    if (historyIndex.value > 0) {\n      historyIndex.value--;\n      trials.value = [...history.value[historyIndex.value]];\n      nextTrialId = trials.value.length > 0 ? Math.max(...trials.value.map((t2) => t2.id)) + 1 : 1;\n    }\n  }\n  function redo() {\n    if (historyIndex.value < history.value.length - 1) {\n      historyIndex.value++;\n      trials.value = [...history.value[historyIndex.value]];\n      nextTrialId = trials.value.length > 0 ? Math.max(...trials.value.map((t2) => t2.id)) + 1 : 1;\n    }\n  }\n  function canUndo() {\n    return historyIndex.value > 0;\n  }\n  function canRedo() {\n    return historyIndex.value < history.value.length - 1;\n  }\n  const trialStats = computed(() => {\n    if (trials.value.length === 0) return { T_mean: 0, T_std: 0, k_mean: 0, k_std: 0 };\n    const Ts = trials.value.map((t2) => t2.T);\n    const ks = trials.value.map((t2) => t2.kCalc);\n    const mean = (arr) => arr.reduce((a, b) => a + b, 0) / arr.length;\n    const std = (arr) => {\n      const m = mean(arr);\n      return Math.sqrt(arr.reduce((sum, v) => sum + (v - m) ** 2, 0) / arr.length);\n    };\n    return { T_mean: mean(Ts), T_std: std(Ts), k_mean: mean(ks), k_std: std(ks) };\n  });\n  function gaussianNoise(mean, stdDev) {\n    let u = 0, v = 0;\n    while (u === 0) u = Math.random();\n    while (v === 0) v = Math.random();\n    const z = Math.sqrt(-2 * Math.log(u)) * Math.cos(2 * Math.PI * v);\n    return mean + z * stdDev;\n  }\n  function recordTrial() {\n    if (!measured.value.T) return;\n    if (params.mass < 0.01 || params.mass > 20) {\n      alert(t(\"experiments.massMustBeBetween\", { min: \"0.01\", max: \"20\" }));\n      return;\n    }\n    pushHistory();\n    const noiseLevel = 0.02;\n    const noisyT = gaussianNoise(measured.value.T, measured.value.T * noiseLevel);\n    const noisyK = 4 * Math.PI * Math.PI * params.mass / (noisyT * noisyT);\n    const err = Math.abs((noisyK - params.k) / params.k) * 100;\n    trials.value = [...trials.value, {\n      id: nextTrialId++,\n      mass: params.mass,\n      k: params.k,\n      amplitude: params.amplitude,\n      T: noisyT,\n      f: 1 / noisyT,\n      omega: 2 * Math.PI / noisyT,\n      kCalc: noisyK,\n      err\n    }];\n    autoSave();\n  }\n  function removeTrial(id) {\n    pushHistory();\n    trials.value = trials.value.filter((t2) => t2.id !== id);\n    autoSave();\n  }\n  function clearTrials() {\n    pushHistory();\n    trials.value = [];\n    autoSave();\n  }\n  function autoSave() {\n    try {\n      localStorage.setItem(SAVE_KEY, JSON.stringify({\n        trials: trials.value,\n        nextId: nextTrialId,\n        calcResult: calcResult.value\n      }));\n    } catch {\n    }\n  }\n  function autoLoad() {\n    try {\n      const raw = localStorage.getItem(SAVE_KEY);\n      if (!raw) return;\n      const parsed = JSON.parse(raw);\n      if (Array.isArray(parsed.trials)) {\n        const valid = parsed.trials.filter((t2) => {\n          if (!t2 || typeof t2.mass !== \"number\" || t2.mass < 0.01 || t2.mass > 20) return false;\n          if (typeof t2.T !== \"number\" || t2.T <= 0 || t2.T > 20) return false;\n          const theoreticalT = 2 * Math.PI * Math.sqrt(Number(t2.mass) / Math.max(Number(t2.k), 1));\n          if (t2.T < theoreticalT / 3 || t2.T > theoreticalT * 3) return false;\n          return true;\n        });\n        trials.value = valid;\n        nextTrialId = parsed.nextId ?? (valid.length > 0 ? Math.max(...valid.map((t2) => Number(t2.id))) + 1 : 1);\n        if (parsed.calcResult) calcResult.value = parsed.calcResult;\n        history.value = [[...valid]];\n        historyIndex.value = 0;\n      }\n    } catch {\n    }\n  }\n  function exportCsv() {\n    if (!trials.value.length) return;\n    downloadCsv(\"spring_mass_data.csv\", [\n      [\"#\", \"m\", \"k\", \"A\", \"T\", \"f\", \"ω\", \"kcalc\", \"error\"],\n      ...trials.value.map((t2, i) => [\n        i + 1,\n        t2.mass.toFixed(3),\n        t2.k.toFixed(1),\n        t2.amplitude.toFixed(3),\n        t2.T.toFixed(4),\n        t2.f.toFixed(3),\n        t2.omega.toFixed(3),\n        t2.kCalc.toFixed(2),\n        t2.err.toFixed(2) + \"%\"\n      ])\n    ]);\n  }\n  const calcResult = ref(t(\"experiments.clickBtnShowCalc\"));\n  function calcK() {\n    if (!measured.value.T) {\n      calcResult.value = t(\"experiments.measurePeriodFirst\");\n      return;\n    }\n    const k = 4 * Math.PI * Math.PI * params.mass / (measured.value.T * measured.value.T);\n    const err = Math.abs((k - params.k) / params.k) * 100;\n    calcResult.value = `<b>${t(\"experiments.equationLabel\")}:</b> k = 4π² × m / T²<br><b>${t(\"experiments.substitutionLabel\")}:</b> k = 4π² × ${params.mass.toFixed(3)} / (${measured.value.T.toFixed(4)})²<br><b>${t(\"experiments.resultLabel\")}:</b> k = <b>${k.toFixed(2)} N/m</b><br><b>${t(\"experiments.errorPercent\")}:</b> ${err.toFixed(2)}%`;\n  }\n  function calcT() {\n    const T = 2 * Math.PI * Math.sqrt(params.mass / params.k);\n    calcResult.value = `<b>${t(\"experiments.equationLabel\")}:</b> T = 2π √(m/k)<br><b>${t(\"experiments.substitutionLabel\")}:</b> T = 2π √(${params.mass.toFixed(3)} / ${params.k.toFixed(1)})<br><b>${t(\"experiments.resultLabel\")}:</b> T = <b>${T.toFixed(4)} s</b>`;\n  }\n  function calcM() {\n    if (!measured.value.T) {\n      calcResult.value = t(\"experiments.measurePeriodFirst\");\n      return;\n    }\n    const mass = measured.value.T * measured.value.T * params.k / (4 * Math.PI * Math.PI);\n    calcResult.value = `<b>${t(\"experiments.equationLabel\")}:</b> m = (T² × k) / (4π²)<br><b>${t(\"experiments.substitutionLabel\")}:</b> m = (${measured.value.T.toFixed(4)}² × ${params.k.toFixed(1)}) / (4π²)<br><b>${t(\"experiments.resultLabel\")}:</b> m = <b>${mass.toFixed(3)} kg</b>`;\n  }\n  function calcFitK() {\n    if (trials.value.length < 2) {\n      calcResult.value = t(\"experiments.atLeastTwoTrials\");\n      return;\n    }\n    const xs = trials.value.map((t2) => t2.mass);\n    const ys = trials.value.map((t2) => t2.T * t2.T);\n    const fit = linearRegression(xs, ys);\n    if (!fit || Math.abs(fit.slope) < 1e-12) {\n      calcResult.value = t(\"experiments.insufficientData\");\n      return;\n    }\n    const k = 4 * Math.PI * Math.PI / fit.slope;\n    const err = Math.abs((k - params.k) / params.k) * 100;\n    const quality = fit.r2 > 0.98 ? \"✅\" : fit.r2 > 0.9 ? \"🟡\" : \"⚠️\";\n    calcResult.value = `T² = ${fit.slope.toFixed(5)}·m ${fit.intercept >= 0 ? \"+\" : \"\"} ${fit.intercept.toFixed(5)}<br>R² = ${fit.r2.toFixed(4)} ${quality}<br>k = 4π² / slope = <b>${k.toFixed(2)} N/m</b><br>${t(\"experiments.errorPercent\")} = <b>${err.toFixed(2)}%</b>`;\n  }\n  return {\n    trials,\n    trialStats,\n    recordTrial,\n    removeTrial,\n    clearTrials,\n    exportCsv,\n    undo,\n    redo,\n    canUndo,\n    canRedo,\n    autoLoad,\n    calcResult,\n    calcK,\n    calcT,\n    calcM,\n    calcFitK\n  };\n}\n",
      "start": 1783635315627,
      "end": 1783635315634,
      "order": "normal",
      "sourcemaps": null
    }
  ]
}

{
  "resolvedId": "C:/Users/feras/Desktop/feras/apps/web/src/composables/freefall/useFreeFallTrials.ts",
  "transforms": [
    {
      "name": "vite:load-fallback",
      "result": "import { ref, computed, type Ref } from 'vue'\nimport { useI18n } from '../../composables/useI18n'\nimport { downloadCsv } from '../../components/experiment/spring/downloadCsv'\nimport { linearRegression } from '../../components/experiment/spring/linearRegression'\nimport type { FreeFallParams } from '../../modules/physics/experiments/freefall/useFreeFallPhysics'\n\nexport interface FreeFallTrial {\n  id: number\n  heightMeters: number\n  timeSec: number\n  timeSquaredSec2: number\n  impactVelocityMs: number\n  gCalc: number\n  err: number\n}\n\nexport interface FreeFallMeasured {\n  flightTime: number | null\n  impactVelocity: number | null\n}\n\nconst SAVE_KEY = 'freefall:trials:v1'\n\nexport function useFreeFallTrials(params: FreeFallParams, measured: Ref<FreeFallMeasured>, enableNoise: Ref<boolean> = ref(true)) {\n  const { t } = useI18n()\n  const trials = ref<FreeFallTrial[]>([])\n  let nextTrialId = 1\n  const history = ref<FreeFallTrial[][]>([])\n  const historyIndex = ref(-1)\n\n  function pushHistory() {\n    if (historyIndex.value < history.value.length - 1) history.value = history.value.slice(0, historyIndex.value + 1)\n    history.value.push([...trials.value])\n    historyIndex.value++\n    if (history.value.length > 20) { history.value.shift(); historyIndex.value-- }\n  }\n  function undo() { if (historyIndex.value > 0) { historyIndex.value--; trials.value = [...history.value[historyIndex.value]]; nextTrialId = trials.value.length > 0 ? Math.max(...trials.value.map(t => t.id)) + 1 : 1 } }\n  function redo() { if (historyIndex.value < history.value.length - 1) { historyIndex.value++; trials.value = [...history.value[historyIndex.value]]; nextTrialId = trials.value.length > 0 ? Math.max(...trials.value.map(t => t.id)) + 1 : 1 } }\n  function canUndo() { return historyIndex.value > 0 }\n  function canRedo() { return historyIndex.value < history.value.length - 1 }\n\n  const trialStats = computed(() => {\n    if (trials.value.length === 0) return { time_mean: 0, time_std: 0, g_mean: 0, g_std: 0 }\n    const mean = (arr: number[]) => arr.reduce((a, b) => a + b, 0) / arr.length\n    const std = (arr: number[]) => { const m = mean(arr); return Math.sqrt(arr.reduce((sum, v) => sum + (v - m) ** 2, 0) / arr.length) }\n    return { time_mean: mean(trials.value.map(t => t.timeSec)), time_std: std(trials.value.map(t => t.timeSec)), g_mean: mean(trials.value.map(t => t.gCalc)), g_std: std(trials.value.map(t => t.gCalc)) }\n  })\n\n  function gaussianNoise(mean: number, stdDev: number) {\n    let u = 0, v = 0\n    while (u === 0) u = Math.random(); while (v === 0) v = Math.random()\n    return mean + Math.sqrt(-2.0 * Math.log(u)) * Math.cos(2.0 * Math.PI * v) * stdDev\n  }\n\n  function recordTrial() {\n    if (!measured.value.flightTime) return\n    pushHistory()\n    const noiseLevel = 0.02\n    const timeToRecord = enableNoise.value\n      ? gaussianNoise(measured.value.flightTime, measured.value.flightTime * noiseLevel)\n      : measured.value.flightTime\n    const gCalc = (2 * params.h) / (timeToRecord * timeToRecord)\n    const err = Math.abs((gCalc - params.g) / params.g) * 100\n    trials.value = [...trials.value, {\n      id: nextTrialId++, heightMeters: params.h, timeSec: timeToRecord,\n      timeSquaredSec2: timeToRecord * timeToRecord,\n      impactVelocityMs: params.g * timeToRecord,\n      gCalc, err,\n    }]\n    autoSave()\n  }\n\n  function removeTrial(id: number) { pushHistory(); trials.value = trials.value.filter(t => t.id !== id); autoSave() }\n  function clearTrials() { pushHistory(); trials.value = []; autoSave() }\n\n  function autoSave() { try { localStorage.setItem(SAVE_KEY, JSON.stringify({ trials: trials.value, nextId: nextTrialId, calcResult: calcResult.value })) } catch { /* ignore */ } }\n  function autoLoad() {\n    try {\n      const raw = localStorage.getItem(SAVE_KEY)\n      if (!raw) return\n      const parsed = JSON.parse(raw)\n      if (Array.isArray(parsed.trials)) {\n        const valid = parsed.trials.filter((t: Record<string, unknown>) => t && typeof t.heightMeters === 'number' && t.heightMeters > 0)\n        trials.value = valid\n        nextTrialId = parsed.nextId ?? (valid.length > 0 ? Math.max(...valid.map((t: Record<string, unknown>) => Number(t.id))) + 1 : 1)\n        if (parsed.calcResult) calcResult.value = parsed.calcResult\n        history.value = [[...valid]]; historyIndex.value = 0\n      }\n    } catch { /* ignore */ }\n  }\n\n  function exportCsv() {\n    if (!trials.value.length) return\n    downloadCsv('freefall_data.csv', [\n      ['#', 'h (m)', 't (s)', 't² (s²)', 'v_impact (m/s)', 'g_calc (m/s²)', 'error'],\n      ...trials.value.map((t, i) => [i + 1, t.heightMeters.toFixed(2), t.timeSec.toFixed(3), t.timeSquaredSec2.toFixed(4), t.impactVelocityMs.toFixed(2), t.gCalc.toFixed(2), t.err.toFixed(2) + '%']),\n    ])\n  }\n\n  const calcResult = ref(t('experiments.clickBtnShowCalc'))\n  function calcG() {\n    if (!measured.value.flightTime) { calcResult.value = t('experiments.measureTimeFirst'); return }\n    const g = (2 * params.h) / (measured.value.flightTime * measured.value.flightTime)\n    const err = Math.abs((g - params.g) / params.g) * 100\n    calcResult.value = `<b>${t('experiments.equationLabel')}:</b> g = 2h/t²<br><b>${t('experiments.substitutionLabel')}:</b> g = 2×${params.h.toFixed(2)} / (${measured.value.flightTime.toFixed(4)})²<br><b>${t('experiments.resultLabel')}:</b> g = <b>${g.toFixed(2)} m/s²</b><br><b>${t('experiments.errorPercent')}:</b> ${err.toFixed(2)}%`\n  }\n  function calcT() {\n    const tVal = Math.sqrt((2 * params.h) / params.g)\n    calcResult.value = `<b>${t('experiments.equationLabel')}:</b> t = √(2h/g)<br><b>${t('experiments.substitutionLabel')}:</b> t = √(2×${params.h.toFixed(2)} / ${params.g.toFixed(2)})<br><b>${t('experiments.resultLabel')}:</b> t = <b>${tVal.toFixed(4)} s</b>`\n  }\n  function calcV() {\n    const v = Math.sqrt(2 * params.g * params.h)\n    calcResult.value = `<b>${t('experiments.equationLabel')}:</b> v = √(2gh)<br><b>${t('experiments.substitutionLabel')}:</b> v = √(2×${params.g.toFixed(2)}×${params.h.toFixed(2)})<br><b>${t('experiments.resultLabel')}:</b> v = <b>${v.toFixed(2)} m/s</b>`\n  }\n  function calcFitG() {\n    if (trials.value.length < 2) { calcResult.value = t('experiments.atLeastTwoTrials'); return }\n    const xs = trials.value.map(t => t.timeSquaredSec2)\n    const ys = trials.value.map(t => t.heightMeters)\n    const fit = linearRegression(xs, ys)\n    if (!fit || Math.abs(fit.slope) < 1e-12) { calcResult.value = t('experiments.insufficientData'); return }\n    const g = 2 * fit.slope\n    const err = Math.abs((g - params.g) / params.g) * 100\n    const quality = fit.r2 > 0.98 ? '✅' : fit.r2 > 0.9 ? '🟡' : '⚠️'\n    calcResult.value = `h = ${fit.slope.toFixed(5)}·t² ${fit.intercept >= 0 ? '+' : ''} ${fit.intercept.toFixed(5)}<br>R² = ${fit.r2.toFixed(4)} ${quality}<br>g = 2×slope = <b>${g.toFixed(2)} m/s²</b><br>${t('experiments.errorPercent')} = <b>${err.toFixed(2)}%</b>`\n  }\n\n  return {\n    trials, trialStats, recordTrial, removeTrial, clearTrials, exportCsv,\n    undo, redo, canUndo, canRedo, autoLoad,\n    calcResult, calcG, calcT, calcV, calcFitG,\n  }\n}\n",
      "start": 1783196194689,
      "end": 1783196194792,
      "sourcemaps": null
    },
    {
      "name": "vite:css",
      "start": 1783196194792,
      "end": 1783196194792,
      "order": "normal"
    },
    {
      "name": "vite:esbuild",
      "result": "import { ref, computed } from \"vue\";\nimport { useI18n } from \"../../composables/useI18n\";\nimport { downloadCsv } from \"../../components/experiment/spring/downloadCsv\";\nimport { linearRegression } from \"../../components/experiment/spring/linearRegression\";\nconst SAVE_KEY = \"freefall:trials:v1\";\nexport function useFreeFallTrials(params, measured, enableNoise = ref(true)) {\n  const { t } = useI18n();\n  const trials = ref([]);\n  let nextTrialId = 1;\n  const history = ref([]);\n  const historyIndex = ref(-1);\n  function pushHistory() {\n    if (historyIndex.value < history.value.length - 1) history.value = history.value.slice(0, historyIndex.value + 1);\n    history.value.push([...trials.value]);\n    historyIndex.value++;\n    if (history.value.length > 20) {\n      history.value.shift();\n      historyIndex.value--;\n    }\n  }\n  function undo() {\n    if (historyIndex.value > 0) {\n      historyIndex.value--;\n      trials.value = [...history.value[historyIndex.value]];\n      nextTrialId = trials.value.length > 0 ? Math.max(...trials.value.map((t2) => t2.id)) + 1 : 1;\n    }\n  }\n  function redo() {\n    if (historyIndex.value < history.value.length - 1) {\n      historyIndex.value++;\n      trials.value = [...history.value[historyIndex.value]];\n      nextTrialId = trials.value.length > 0 ? Math.max(...trials.value.map((t2) => t2.id)) + 1 : 1;\n    }\n  }\n  function canUndo() {\n    return historyIndex.value > 0;\n  }\n  function canRedo() {\n    return historyIndex.value < history.value.length - 1;\n  }\n  const trialStats = computed(() => {\n    if (trials.value.length === 0) return { time_mean: 0, time_std: 0, g_mean: 0, g_std: 0 };\n    const mean = (arr) => arr.reduce((a, b) => a + b, 0) / arr.length;\n    const std = (arr) => {\n      const m = mean(arr);\n      return Math.sqrt(arr.reduce((sum, v) => sum + (v - m) ** 2, 0) / arr.length);\n    };\n    return { time_mean: mean(trials.value.map((t2) => t2.timeSec)), time_std: std(trials.value.map((t2) => t2.timeSec)), g_mean: mean(trials.value.map((t2) => t2.gCalc)), g_std: std(trials.value.map((t2) => t2.gCalc)) };\n  });\n  function gaussianNoise(mean, stdDev) {\n    let u = 0, v = 0;\n    while (u === 0) u = Math.random();\n    while (v === 0) v = Math.random();\n    return mean + Math.sqrt(-2 * Math.log(u)) * Math.cos(2 * Math.PI * v) * stdDev;\n  }\n  function recordTrial() {\n    if (!measured.value.flightTime) return;\n    pushHistory();\n    const noiseLevel = 0.02;\n    const timeToRecord = enableNoise.value ? gaussianNoise(measured.value.flightTime, measured.value.flightTime * noiseLevel) : measured.value.flightTime;\n    const gCalc = 2 * params.h / (timeToRecord * timeToRecord);\n    const err = Math.abs((gCalc - params.g) / params.g) * 100;\n    trials.value = [...trials.value, {\n      id: nextTrialId++,\n      heightMeters: params.h,\n      timeSec: timeToRecord,\n      timeSquaredSec2: timeToRecord * timeToRecord,\n      impactVelocityMs: params.g * timeToRecord,\n      gCalc,\n      err\n    }];\n    autoSave();\n  }\n  function removeTrial(id) {\n    pushHistory();\n    trials.value = trials.value.filter((t2) => t2.id !== id);\n    autoSave();\n  }\n  function clearTrials() {\n    pushHistory();\n    trials.value = [];\n    autoSave();\n  }\n  function autoSave() {\n    try {\n      localStorage.setItem(SAVE_KEY, JSON.stringify({ trials: trials.value, nextId: nextTrialId, calcResult: calcResult.value }));\n    } catch {\n    }\n  }\n  function autoLoad() {\n    try {\n      const raw = localStorage.getItem(SAVE_KEY);\n      if (!raw) return;\n      const parsed = JSON.parse(raw);\n      if (Array.isArray(parsed.trials)) {\n        const valid = parsed.trials.filter((t2) => t2 && typeof t2.heightMeters === \"number\" && t2.heightMeters > 0);\n        trials.value = valid;\n        nextTrialId = parsed.nextId ?? (valid.length > 0 ? Math.max(...valid.map((t2) => Number(t2.id))) + 1 : 1);\n        if (parsed.calcResult) calcResult.value = parsed.calcResult;\n        history.value = [[...valid]];\n        historyIndex.value = 0;\n      }\n    } catch {\n    }\n  }\n  function exportCsv() {\n    if (!trials.value.length) return;\n    downloadCsv(\"freefall_data.csv\", [\n      [\"#\", \"h (m)\", \"t (s)\", \"t² (s²)\", \"v_impact (m/s)\", \"g_calc (m/s²)\", \"error\"],\n      ...trials.value.map((t2, i) => [i + 1, t2.heightMeters.toFixed(2), t2.timeSec.toFixed(3), t2.timeSquaredSec2.toFixed(4), t2.impactVelocityMs.toFixed(2), t2.gCalc.toFixed(2), t2.err.toFixed(2) + \"%\"])\n    ]);\n  }\n  const calcResult = ref(t(\"experiments.clickBtnShowCalc\"));\n  function calcG() {\n    if (!measured.value.flightTime) {\n      calcResult.value = t(\"experiments.measureTimeFirst\");\n      return;\n    }\n    const g = 2 * params.h / (measured.value.flightTime * measured.value.flightTime);\n    const err = Math.abs((g - params.g) / params.g) * 100;\n    calcResult.value = `<b>${t(\"experiments.equationLabel\")}:</b> g = 2h/t²<br><b>${t(\"experiments.substitutionLabel\")}:</b> g = 2×${params.h.toFixed(2)} / (${measured.value.flightTime.toFixed(4)})²<br><b>${t(\"experiments.resultLabel\")}:</b> g = <b>${g.toFixed(2)} m/s²</b><br><b>${t(\"experiments.errorPercent\")}:</b> ${err.toFixed(2)}%`;\n  }\n  function calcT() {\n    const tVal = Math.sqrt(2 * params.h / params.g);\n    calcResult.value = `<b>${t(\"experiments.equationLabel\")}:</b> t = √(2h/g)<br><b>${t(\"experiments.substitutionLabel\")}:</b> t = √(2×${params.h.toFixed(2)} / ${params.g.toFixed(2)})<br><b>${t(\"experiments.resultLabel\")}:</b> t = <b>${tVal.toFixed(4)} s</b>`;\n  }\n  function calcV() {\n    const v = Math.sqrt(2 * params.g * params.h);\n    calcResult.value = `<b>${t(\"experiments.equationLabel\")}:</b> v = √(2gh)<br><b>${t(\"experiments.substitutionLabel\")}:</b> v = √(2×${params.g.toFixed(2)}×${params.h.toFixed(2)})<br><b>${t(\"experiments.resultLabel\")}:</b> v = <b>${v.toFixed(2)} m/s</b>`;\n  }\n  function calcFitG() {\n    if (trials.value.length < 2) {\n      calcResult.value = t(\"experiments.atLeastTwoTrials\");\n      return;\n    }\n    const xs = trials.value.map((t2) => t2.timeSquaredSec2);\n    const ys = trials.value.map((t2) => t2.heightMeters);\n    const fit = linearRegression(xs, ys);\n    if (!fit || Math.abs(fit.slope) < 1e-12) {\n      calcResult.value = t(\"experiments.insufficientData\");\n      return;\n    }\n    const g = 2 * fit.slope;\n    const err = Math.abs((g - params.g) / params.g) * 100;\n    const quality = fit.r2 > 0.98 ? \"✅\" : fit.r2 > 0.9 ? \"🟡\" : \"⚠️\";\n    calcResult.value = `h = ${fit.slope.toFixed(5)}·t² ${fit.intercept >= 0 ? \"+\" : \"\"} ${fit.intercept.toFixed(5)}<br>R² = ${fit.r2.toFixed(4)} ${quality}<br>g = 2×slope = <b>${g.toFixed(2)} m/s²</b><br>${t(\"experiments.errorPercent\")} = <b>${err.toFixed(2)}%</b>`;\n  }\n  return {\n    trials,\n    trialStats,\n    recordTrial,\n    removeTrial,\n    clearTrials,\n    exportCsv,\n    undo,\n    redo,\n    canUndo,\n    canRedo,\n    autoLoad,\n    calcResult,\n    calcG,\n    calcT,\n    calcV,\n    calcFitG\n  };\n}\n",
      "start": 1783196194792,
      "end": 1783196194968,
      "order": "normal",
      "sourcemaps": {
        "version": 3,
        "sources": [
          "C:/Users/feras/Desktop/feras/apps/web/src/composables/freefall/useFreeFallTrials.ts"
        ],
        "sourcesContent": [
          "import { ref, computed, type Ref } from 'vue'\nimport { useI18n } from '../../composables/useI18n'\nimport { downloadCsv } from '../../components/experiment/spring/downloadCsv'\nimport { linearRegression } from '../../components/experiment/spring/linearRegression'\nimport type { FreeFallParams } from '../../modules/physics/experiments/freefall/useFreeFallPhysics'\n\nexport interface FreeFallTrial {\n  id: number\n  heightMeters: number\n  timeSec: number\n  timeSquaredSec2: number\n  impactVelocityMs: number\n  gCalc: number\n  err: number\n}\n\nexport interface FreeFallMeasured {\n  flightTime: number | null\n  impactVelocity: number | null\n}\n\nconst SAVE_KEY = 'freefall:trials:v1'\n\nexport function useFreeFallTrials(params: FreeFallParams, measured: Ref<FreeFallMeasured>, enableNoise: Ref<boolean> = ref(true)) {\n  const { t } = useI18n()\n  const trials = ref<FreeFallTrial[]>([])\n  let nextTrialId = 1\n  const history = ref<FreeFallTrial[][]>([])\n  const historyIndex = ref(-1)\n\n  function pushHistory() {\n    if (historyIndex.value < history.value.length - 1) history.value = history.value.slice(0, historyIndex.value + 1)\n    history.value.push([...trials.value])\n    historyIndex.value++\n    if (history.value.length > 20) { history.value.shift(); historyIndex.value-- }\n  }\n  function undo() { if (historyIndex.value > 0) { historyIndex.value--; trials.value = [...history.value[historyIndex.value]]; nextTrialId = trials.value.length > 0 ? Math.max(...trials.value.map(t => t.id)) + 1 : 1 } }\n  function redo() { if (historyIndex.value < history.value.length - 1) { historyIndex.value++; trials.value = [...history.value[historyIndex.value]]; nextTrialId = trials.value.length > 0 ? Math.max(...trials.value.map(t => t.id)) + 1 : 1 } }\n  function canUndo() { return historyIndex.value > 0 }\n  function canRedo() { return historyIndex.value < history.value.length - 1 }\n\n  const trialStats = computed(() => {\n    if (trials.value.length === 0) return { time_mean: 0, time_std: 0, g_mean: 0, g_std: 0 }\n    const mean = (arr: number[]) => arr.reduce((a, b) => a + b, 0) / arr.length\n    const std = (arr: number[]) => { const m = mean(arr); return Math.sqrt(arr.reduce((sum, v) => sum + (v - m) ** 2, 0) / arr.length) }\n    return { time_mean: mean(trials.value.map(t => t.timeSec)), time_std: std(trials.value.map(t => t.timeSec)), g_mean: mean(trials.value.map(t => t.gCalc)), g_std: std(trials.value.map(t => t.gCalc)) }\n  })\n\n  function gaussianNoise(mean: number, stdDev: number) {\n    let u = 0, v = 0\n    while (u === 0) u = Math.random(); while (v === 0) v = Math.random()\n    return mean + Math.sqrt(-2.0 * Math.log(u)) * Math.cos(2.0 * Math.PI * v) * stdDev\n  }\n\n  function recordTrial() {\n    if (!measured.value.flightTime) return\n    pushHistory()\n    const noiseLevel = 0.02\n    const timeToRecord = enableNoise.value\n      ? gaussianNoise(measured.value.flightTime, measured.value.flightTime * noiseLevel)\n      : measured.value.flightTime\n    const gCalc = (2 * params.h) / (timeToRecord * timeToRecord)\n    const err = Math.abs((gCalc - params.g) / params.g) * 100\n    trials.value = [...trials.value, {\n      id: nextTrialId++, heightMeters: params.h, timeSec: timeToRecord,\n      timeSquaredSec2: timeToRecord * timeToRecord,\n      impactVelocityMs: params.g * timeToRecord,\n      gCalc, err,\n    }]\n    autoSave()\n  }\n\n  function removeTrial(id: number) { pushHistory(); trials.value = trials.value.filter(t => t.id !== id); autoSave() }\n  function clearTrials() { pushHistory(); trials.value = []; autoSave() }\n\n  function autoSave() { try { localStorage.setItem(SAVE_KEY, JSON.stringify({ trials: trials.value, nextId: nextTrialId, calcResult: calcResult.value })) } catch { /* ignore */ } }\n  function autoLoad() {\n    try {\n      const raw = localStorage.getItem(SAVE_KEY)\n      if (!raw) return\n      const parsed = JSON.parse(raw)\n      if (Array.isArray(parsed.trials)) {\n        const valid = parsed.trials.filter((t: Record<string, unknown>) => t && typeof t.heightMeters === 'number' && t.heightMeters > 0)\n        trials.value = valid\n        nextTrialId = parsed.nextId ?? (valid.length > 0 ? Math.max(...valid.map((t: Record<string, unknown>) => Number(t.id))) + 1 : 1)\n        if (parsed.calcResult) calcResult.value = parsed.calcResult\n        history.value = [[...valid]]; historyIndex.value = 0\n      }\n    } catch { /* ignore */ }\n  }\n\n  function exportCsv() {\n    if (!trials.value.length) return\n    downloadCsv('freefall_data.csv', [\n      ['#', 'h (m)', 't (s)', 't² (s²)', 'v_impact (m/s)', 'g_calc (m/s²)', 'error'],\n      ...trials.value.map((t, i) => [i + 1, t.heightMeters.toFixed(2), t.timeSec.toFixed(3), t.timeSquaredSec2.toFixed(4), t.impactVelocityMs.toFixed(2), t.gCalc.toFixed(2), t.err.toFixed(2) + '%']),\n    ])\n  }\n\n  const calcResult = ref(t('experiments.clickBtnShowCalc'))\n  function calcG() {\n    if (!measured.value.flightTime) { calcResult.value = t('experiments.measureTimeFirst'); return }\n    const g = (2 * params.h) / (measured.value.flightTime * measured.value.flightTime)\n    const err = Math.abs((g - params.g) / params.g) * 100\n    calcResult.value = `<b>${t('experiments.equationLabel')}:</b> g = 2h/t²<br><b>${t('experiments.substitutionLabel')}:</b> g = 2×${params.h.toFixed(2)} / (${measured.value.flightTime.toFixed(4)})²<br><b>${t('experiments.resultLabel')}:</b> g = <b>${g.toFixed(2)} m/s²</b><br><b>${t('experiments.errorPercent')}:</b> ${err.toFixed(2)}%`\n  }\n  function calcT() {\n    const tVal = Math.sqrt((2 * params.h) / params.g)\n    calcResult.value = `<b>${t('experiments.equationLabel')}:</b> t = √(2h/g)<br><b>${t('experiments.substitutionLabel')}:</b> t = √(2×${params.h.toFixed(2)} / ${params.g.toFixed(2)})<br><b>${t('experiments.resultLabel')}:</b> t = <b>${tVal.toFixed(4)} s</b>`\n  }\n  function calcV() {\n    const v = Math.sqrt(2 * params.g * params.h)\n    calcResult.value = `<b>${t('experiments.equationLabel')}:</b> v = √(2gh)<br><b>${t('experiments.substitutionLabel')}:</b> v = √(2×${params.g.toFixed(2)}×${params.h.toFixed(2)})<br><b>${t('experiments.resultLabel')}:</b> v = <b>${v.toFixed(2)} m/s</b>`\n  }\n  function calcFitG() {\n    if (trials.value.length < 2) { calcResult.value = t('experiments.atLeastTwoTrials'); return }\n    const xs = trials.value.map(t => t.timeSquaredSec2)\n    const ys = trials.value.map(t => t.heightMeters)\n    const fit = linearRegression(xs, ys)\n    if (!fit || Math.abs(fit.slope) < 1e-12) { calcResult.value = t('experiments.insufficientData'); return }\n    const g = 2 * fit.slope\n    const err = Math.abs((g - params.g) / params.g) * 100\n    const quality = fit.r2 > 0.98 ? '✅' : fit.r2 > 0.9 ? '🟡' : '⚠️'\n    calcResult.value = `h = ${fit.slope.toFixed(5)}·t² ${fit.intercept >= 0 ? '+' : ''} ${fit.intercept.toFixed(5)}<br>R² = ${fit.r2.toFixed(4)} ${quality}<br>g = 2×slope = <b>${g.toFixed(2)} m/s²</b><br>${t('experiments.errorPercent')} = <b>${err.toFixed(2)}%</b>`\n  }\n\n  return {\n    trials, trialStats, recordTrial, removeTrial, clearTrials, exportCsv,\n    undo, redo, canUndo, canRedo, autoLoad,\n    calcResult, calcG, calcT, calcV, calcFitG,\n  }\n}\n"
        ],
        "mappings": "AAAA,SAAS,KAAK,gBAA0B;AACxC,SAAS,eAAe;AACxB,SAAS,mBAAmB;AAC5B,SAAS,wBAAwB;AAkBjC,MAAM,WAAW;AAEV,gBAAS,kBAAkB,QAAwB,UAAiC,cAA4B,IAAI,IAAI,GAAG;AAChI,QAAM,EAAE,EAAE,IAAI,QAAQ;AACtB,QAAM,SAAS,IAAqB,CAAC,CAAC;AACtC,MAAI,cAAc;AAClB,QAAM,UAAU,IAAuB,CAAC,CAAC;AACzC,QAAM,eAAe,IAAI,EAAE;AAE3B,WAAS,cAAc;AACrB,QAAI,aAAa,QAAQ,QAAQ,MAAM,SAAS,EAAG,SAAQ,QAAQ,QAAQ,MAAM,MAAM,GAAG,aAAa,QAAQ,CAAC;AAChH,YAAQ,MAAM,KAAK,CAAC,GAAG,OAAO,KAAK,CAAC;AACpC,iBAAa;AACb,QAAI,QAAQ,MAAM,SAAS,IAAI;AAAE,cAAQ,MAAM,MAAM;AAAG,mBAAa;AAAA,IAAQ;AAAA,EAC/E;AACA,WAAS,OAAO;AAAE,QAAI,aAAa,QAAQ,GAAG;AAAE,mBAAa;AAAS,aAAO,QAAQ,CAAC,GAAG,QAAQ,MAAM,aAAa,KAAK,CAAC;AAAG,oBAAc,OAAO,MAAM,SAAS,IAAI,KAAK,IAAI,GAAG,OAAO,MAAM,IAAI,CAAAA,OAAKA,GAAE,EAAE,CAAC,IAAI,IAAI;AAAA,IAAE;AAAA,EAAE;AACxN,WAAS,OAAO;AAAE,QAAI,aAAa,QAAQ,QAAQ,MAAM,SAAS,GAAG;AAAE,mBAAa;AAAS,aAAO,QAAQ,CAAC,GAAG,QAAQ,MAAM,aAAa,KAAK,CAAC;AAAG,oBAAc,OAAO,MAAM,SAAS,IAAI,KAAK,IAAI,GAAG,OAAO,MAAM,IAAI,CAAAA,OAAKA,GAAE,EAAE,CAAC,IAAI,IAAI;AAAA,IAAE;AAAA,EAAE;AAC/O,WAAS,UAAU;AAAE,WAAO,aAAa,QAAQ;AAAA,EAAE;AACnD,WAAS,UAAU;AAAE,WAAO,aAAa,QAAQ,QAAQ,MAAM,SAAS;AAAA,EAAE;AAE1E,QAAM,aAAa,SAAS,MAAM;AAChC,QAAI,OAAO,MAAM,WAAW,EAAG,QAAO,EAAE,WAAW,GAAG,UAAU,GAAG,QAAQ,GAAG,OAAO,EAAE;AACvF,UAAM,OAAO,CAAC,QAAkB,IAAI,OAAO,CAAC,GAAG,MAAM,IAAI,GAAG,CAAC,IAAI,IAAI;AACrE,UAAM,MAAM,CAAC,QAAkB;AAAE,YAAM,IAAI,KAAK,GAAG;AAAG,aAAO,KAAK,KAAK,IAAI,OAAO,CAAC,KAAK,MAAM,OAAO,IAAI,MAAM,GAAG,CAAC,IAAI,IAAI,MAAM;AAAA,IAAE;AACnI,WAAO,EAAE,WAAW,KAAK,OAAO,MAAM,IAAI,CAAAA,OAAKA,GAAE,OAAO,CAAC,GAAG,UAAU,IAAI,OAAO,MAAM,IAAI,CAAAA,OAAKA,GAAE,OAAO,CAAC,GAAG,QAAQ,KAAK,OAAO,MAAM,IAAI,CAAAA,OAAKA,GAAE,KAAK,CAAC,GAAG,OAAO,IAAI,OAAO,MAAM,IAAI,CAAAA,OAAKA,GAAE,KAAK,CAAC,EAAE;AAAA,EACxM,CAAC;AAED,WAAS,cAAc,MAAc,QAAgB;AACnD,QAAI,IAAI,GAAG,IAAI;AACf,WAAO,MAAM,EAAG,KAAI,KAAK,OAAO;AAAG,WAAO,MAAM,EAAG,KAAI,KAAK,OAAO;AACnE,WAAO,OAAO,KAAK,KAAK,KAAO,KAAK,IAAI,CAAC,CAAC,IAAI,KAAK,IAAI,IAAM,KAAK,KAAK,CAAC,IAAI;AAAA,EAC9E;AAEA,WAAS,cAAc;AACrB,QAAI,CAAC,SAAS,MAAM,WAAY;AAChC,gBAAY;AACZ,UAAM,aAAa;AACnB,UAAM,eAAe,YAAY,QAC7B,cAAc,SAAS,MAAM,YAAY,SAAS,MAAM,aAAa,UAAU,IAC/E,SAAS,MAAM;AACnB,UAAM,QAAS,IAAI,OAAO,KAAM,eAAe;AAC/C,UAAM,MAAM,KAAK,KAAK,QAAQ,OAAO,KAAK,OAAO,CAAC,IAAI;AACtD,WAAO,QAAQ,CAAC,GAAG,OAAO,OAAO;AAAA,MAC/B,IAAI;AAAA,MAAe,cAAc,OAAO;AAAA,MAAG,SAAS;AAAA,MACpD,iBAAiB,eAAe;AAAA,MAChC,kBAAkB,OAAO,IAAI;AAAA,MAC7B;AAAA,MAAO;AAAA,IACT,CAAC;AACD,aAAS;AAAA,EACX;AAEA,WAAS,YAAY,IAAY;AAAE,gBAAY;AAAG,WAAO,QAAQ,OAAO,MAAM,OAAO,CAAAA,OAAKA,GAAE,OAAO,EAAE;AAAG,aAAS;AAAA,EAAE;AACnH,WAAS,cAAc;AAAE,gBAAY;AAAG,WAAO,QAAQ,CAAC;AAAG,aAAS;AAAA,EAAE;AAEtE,WAAS,WAAW;AAAE,QAAI;AAAE,mBAAa,QAAQ,UAAU,KAAK,UAAU,EAAE,QAAQ,OAAO,OAAO,QAAQ,aAAa,YAAY,WAAW,MAAM,CAAC,CAAC;AAAA,IAAE,QAAQ;AAAA,IAAe;AAAA,EAAE;AACjL,WAAS,WAAW;AAClB,QAAI;AACF,YAAM,MAAM,aAAa,QAAQ,QAAQ;AACzC,UAAI,CAAC,IAAK;AACV,YAAM,SAAS,KAAK,MAAM,GAAG;AAC7B,UAAI,MAAM,QAAQ,OAAO,MAAM,GAAG;AAChC,cAAM,QAAQ,OAAO,OAAO,OAAO,CAACA,OAA+BA,MAAK,OAAOA,GAAE,iBAAiB,YAAYA,GAAE,eAAe,CAAC;AAChI,eAAO,QAAQ;AACf,sBAAc,OAAO,WAAW,MAAM,SAAS,IAAI,KAAK,IAAI,GAAG,MAAM,IAAI,CAACA,OAA+B,OAAOA,GAAE,EAAE,CAAC,CAAC,IAAI,IAAI;AAC9H,YAAI,OAAO,WAAY,YAAW,QAAQ,OAAO;AACjD,gBAAQ,QAAQ,CAAC,CAAC,GAAG,KAAK,CAAC;AAAG,qBAAa,QAAQ;AAAA,MACrD;AAAA,IACF,QAAQ;AAAA,IAAe;AAAA,EACzB;AAEA,WAAS,YAAY;AACnB,QAAI,CAAC,OAAO,MAAM,OAAQ;AAC1B,gBAAY,qBAAqB;AAAA,MAC/B,CAAC,KAAK,SAAS,SAAS,WAAW,kBAAkB,iBAAiB,OAAO;AAAA,MAC7E,GAAG,OAAO,MAAM,IAAI,CAACA,IAAG,MAAM,CAAC,IAAI,GAAGA,GAAE,aAAa,QAAQ,CAAC,GAAGA,GAAE,QAAQ,QAAQ,CAAC,GAAGA,GAAE,gBAAgB,QAAQ,CAAC,GAAGA,GAAE,iBAAiB,QAAQ,CAAC,GAAGA,GAAE,MAAM,QAAQ,CAAC,GAAGA,GAAE,IAAI,QAAQ,CAAC,IAAI,GAAG,CAAC;AAAA,IACjM,CAAC;AAAA,EACH;AAEA,QAAM,aAAa,IAAI,EAAE,8BAA8B,CAAC;AACxD,WAAS,QAAQ;AACf,QAAI,CAAC,SAAS,MAAM,YAAY;AAAE,iBAAW,QAAQ,EAAE,8BAA8B;AAAG;AAAA,IAAO;AAC/F,UAAM,IAAK,IAAI,OAAO,KAAM,SAAS,MAAM,aAAa,SAAS,MAAM;AACvE,UAAM,MAAM,KAAK,KAAK,IAAI,OAAO,KAAK,OAAO,CAAC,IAAI;AAClD,eAAW,QAAQ,MAAM,EAAE,2BAA2B,CAAC,yBAAyB,EAAE,+BAA+B,CAAC,eAAe,OAAO,EAAE,QAAQ,CAAC,CAAC,OAAO,SAAS,MAAM,WAAW,QAAQ,CAAC,CAAC,YAAY,EAAE,yBAAyB,CAAC,gBAAgB,EAAE,QAAQ,CAAC,CAAC,mBAAmB,EAAE,0BAA0B,CAAC,SAAS,IAAI,QAAQ,CAAC,CAAC;AAAA,EAC5U;AACA,WAAS,QAAQ;AACf,UAAM,OAAO,KAAK,KAAM,IAAI,OAAO,IAAK,OAAO,CAAC;AAChD,eAAW,QAAQ,MAAM,EAAE,2BAA2B,CAAC,2BAA2B,EAAE,+BAA+B,CAAC,iBAAiB,OAAO,EAAE,QAAQ,CAAC,CAAC,MAAM,OAAO,EAAE,QAAQ,CAAC,CAAC,WAAW,EAAE,yBAAyB,CAAC,gBAAgB,KAAK,QAAQ,CAAC,CAAC;AAAA,EACzP;AACA,WAAS,QAAQ;AACf,UAAM,IAAI,KAAK,KAAK,IAAI,OAAO,IAAI,OAAO,CAAC;AAC3C,eAAW,QAAQ,MAAM,EAAE,2BAA2B,CAAC,0BAA0B,EAAE,+BAA+B,CAAC,iBAAiB,OAAO,EAAE,QAAQ,CAAC,CAAC,IAAI,OAAO,EAAE,QAAQ,CAAC,CAAC,WAAW,EAAE,yBAAyB,CAAC,gBAAgB,EAAE,QAAQ,CAAC,CAAC;AAAA,EACnP;AACA,WAAS,WAAW;AAClB,QAAI,OAAO,MAAM,SAAS,GAAG;AAAE,iBAAW,QAAQ,EAAE,8BAA8B;AAAG;AAAA,IAAO;AAC5F,UAAM,KAAK,OAAO,MAAM,IAAI,CAAAA,OAAKA,GAAE,eAAe;AAClD,UAAM,KAAK,OAAO,MAAM,IAAI,CAAAA,OAAKA,GAAE,YAAY;AAC/C,UAAM,MAAM,iBAAiB,IAAI,EAAE;AACnC,QAAI,CAAC,OAAO,KAAK,IAAI,IAAI,KAAK,IAAI,OAAO;AAAE,iBAAW,QAAQ,EAAE,8BAA8B;AAAG;AAAA,IAAO;AACxG,UAAM,IAAI,IAAI,IAAI;AAClB,UAAM,MAAM,KAAK,KAAK,IAAI,OAAO,KAAK,OAAO,CAAC,IAAI;AAClD,UAAM,UAAU,IAAI,KAAK,OAAO,MAAM,IAAI,KAAK,MAAM,OAAO;AAC5D,eAAW,QAAQ,OAAO,IAAI,MAAM,QAAQ,CAAC,CAAC,OAAO,IAAI,aAAa,IAAI,MAAM,EAAE,IAAI,IAAI,UAAU,QAAQ,CAAC,CAAC,YAAY,IAAI,GAAG,QAAQ,CAAC,CAAC,IAAI,OAAO,wBAAwB,EAAE,QAAQ,CAAC,CAAC,gBAAgB,EAAE,0BAA0B,CAAC,SAAS,IAAI,QAAQ,CAAC,CAAC;AAAA,EAChQ;AAEA,SAAO;AAAA,IACL;AAAA,IAAQ;AAAA,IAAY;AAAA,IAAa;AAAA,IAAa;AAAA,IAAa;AAAA,IAC3D;AAAA,IAAM;AAAA,IAAM;AAAA,IAAS;AAAA,IAAS;AAAA,IAC9B;AAAA,IAAY;AAAA,IAAO;AAAA,IAAO;AAAA,IAAO;AAAA,EACnC;AACF;",
        "names": [
          "t"
        ]
      }
    },
    {
      "name": "vite:json",
      "start": 1783196194968,
      "end": 1783196194968,
      "order": "normal"
    },
    {
      "name": "vite:worker",
      "start": 1783196194968,
      "end": 1783196194968,
      "order": "normal"
    },
    {
      "name": "vite:define",
      "start": 1783196194968,
      "end": 1783196194968,
      "order": "normal"
    },
    {
      "name": "vite:css-post",
      "start": 1783196194968,
      "end": 1783196194968,
      "order": "normal"
    },
    {
      "name": "vite:build-html",
      "start": 1783196194968,
      "end": 1783196194968,
      "order": "normal"
    },
    {
      "name": "vite:worker-import-meta-url",
      "start": 1783196194968,
      "end": 1783196194968,
      "order": "normal"
    },
    {
      "name": "vite:asset-import-meta-url",
      "start": 1783196194968,
      "end": 1783196194968,
      "order": "normal"
    },
    {
      "name": "commonjs",
      "start": 1783196194968,
      "end": 1783196194968,
      "order": "normal"
    },
    {
      "name": "vite:dynamic-import-vars",
      "start": 1783196194968,
      "end": 1783196194968,
      "order": "normal"
    },
    {
      "name": "vite:import-glob",
      "start": 1783196194968,
      "end": 1783196194968,
      "order": "normal"
    },
    {
      "name": "vite:build-import-analysis",
      "start": 1783196194968,
      "end": 1783196194968,
      "order": "normal"
    },
    {
      "name": "vite:reporter",
      "start": 1783196194968,
      "end": 1783196194968,
      "order": "normal"
    }
  ]
}

{
  "resolvedId": "C:/Users/feras/Desktop/feras/apps/web/src/composables/chemistry/useSpillDrops.ts",
  "transforms": [
    {
      "name": "vite:load-fallback",
      "result": "import { watch, onMounted, onUnmounted } from 'vue';\nimport type { Ref } from 'vue';\n\nexport interface DropPhysicsOptions {\n  canvasRef: Ref<HTMLCanvasElement | null>;\n  tiltAngle: Ref<number> | (() => number);\n  volume: Ref<number> | (() => number);\n  maxVolume: Ref<number> | (() => number);\n  liquidColor: Ref<string> | (() => string);\n  itemX: Ref<number> | (() => number);\n  itemY: Ref<number> | (() => number);\n  mouthPosition: (tilt: number) => { x: number; y: number };\n  canvasW: number;\n  canvasH: number;\n  mouthBounds?: { minX: number; maxX: number; minY: number; maxY: number };\n  exitY?: number;\n  onSpill: (amount: number) => void;\n  onDropExited: (wx: number, wy: number, color: string) => void;\n}\n\nexport function useSpillDrops(opts: DropPhysicsOptions) {\n  let activeDrops: { x: number; y: number; vx: number; vy: number; size: number; born: number }[] = [];\n  let dropTimer = 0;\n  let animId = 0;\n  let retryTimeout = 0;\n  let running = false;\n\n  const getVal = <T>(v: Ref<T> | (() => T)): T => (typeof v === 'function' ? (v as () => T)() : v.value);\n\n  function startDrops() {\n    if (running) return;\n    running = true;\n    const ctx = opts.canvasRef.value?.getContext('2d');\n    if (!ctx) { retryTimeout = window.setTimeout(startDrops, 50); return; }\n\n    const loop = () => {\n      if (!running) return;\n      ctx.clearRect(0, 0, opts.canvasW, opts.canvasH);\n      const tilt = getVal(opts.tiltAngle);\n      const absTilt = Math.abs(tilt);\n      const volume = getVal(opts.volume);\n      const maxVolume = getVal(opts.maxVolume);\n      const fillH = Math.min(volume / maxVolume, 1) * (opts.canvasH * 0.4);\n      const threshold = 55 - (fillH / (opts.canvasH * 0.4)) * 45;\n\n      const mouth = opts.mouthPosition(tilt);\n      const minX = opts.mouthBounds?.minX ?? 10;\n      const maxX = opts.mouthBounds?.maxX ?? opts.canvasW - 10;\n      const minY = opts.mouthBounds?.minY ?? 10;\n      const maxY = opts.mouthBounds?.maxY ?? opts.canvasH - 10;\n      const mouthX = Math.max(minX, Math.min(maxX, mouth.x));\n      const mouthY = Math.max(minY, Math.min(maxY, mouth.y));\n\n      let isSpilling = false;\n      if (absTilt > threshold && volume > 2) isSpilling = true;\n\n      if (isSpilling) {\n        dropTimer++;\n        let interval = 15; if (absTilt > 20) interval = 8; if (absTilt > 40) interval = 4; if (absTilt > 55) interval = 2;\n        if (dropTimer >= interval) {\n          activeDrops.push({\n            x: mouthX + (Math.random() - 0.5) * 3,\n            y: mouthY + 2 + (Math.random() - 0.5) * 2,\n            vx: (tilt > 0 ? 0.6 : -0.6) + (Math.random() - 0.5) * 0.3,\n            vy: 0.3,\n            size: 1.8 + Math.random() * 1.2,\n            born: performance.now(),\n          });\n          dropTimer = 0;\n          opts.onSpill(0.05);\n        }\n      } else { dropTimer = 0; }\n\n      // Draw stream: connect nearby drops with thin lines\n      const drops = activeDrops;\n      for (let i = 0; i < drops.length; i++) {\n        for (let j = i + 1; j < drops.length; j++) {\n          const dx = drops[j].x - drops[i].x;\n          const dy = drops[j].y - drops[i].y;\n          const dist = Math.sqrt(dx * dx + dy * dy);\n          if (dist < 18) {\n            ctx.beginPath();\n            ctx.moveTo(drops[i].x, drops[i].y);\n            ctx.lineTo(drops[j].x, drops[j].y);\n            ctx.strokeStyle = getVal(opts.liquidColor);\n            ctx.globalAlpha = 0.35 * (1 - dist / 18);\n            ctx.lineWidth = 1.2;\n            ctx.stroke();\n            ctx.globalAlpha = 1;\n          }\n        }\n      }\n\n      // Gravity and draw each drop\n      for (let i = activeDrops.length - 1; i >= 0; i--) {\n        const d = activeDrops[i];\n        d.vy += 0.35;\n        d.vx *= 0.99; // slight air resistance\n        d.x += d.vx;\n        d.y += d.vy;\n\n        // Main drop body\n        ctx.beginPath();\n        ctx.arc(d.x, d.y, d.size, 0, Math.PI * 2);\n        ctx.fillStyle = getVal(opts.liquidColor);\n        ctx.globalAlpha = 0.8;\n        ctx.fill();\n        ctx.globalAlpha = 1;\n\n        // Highlight\n        ctx.beginPath();\n        ctx.arc(d.x - d.size * 0.25, d.y - d.size * 0.3, d.size * 0.4, 0, Math.PI * 2);\n        ctx.fillStyle = 'rgba(255,255,255,0.5)';\n        ctx.fill();\n\n        const exitY = opts.exitY ?? opts.canvasH;\n        if (d.y > exitY) {\n          activeDrops.splice(i, 1);\n          opts.onDropExited(getVal(opts.itemX) + d.x, getVal(opts.itemY) + d.y, getVal(opts.liquidColor));\n        }\n      }\n      animId = requestAnimationFrame(loop);\n    };\n    loop();\n  }\n\n  function stopDrops() {\n    running = false;\n    if (animId) cancelAnimationFrame(animId);\n    if (retryTimeout) clearTimeout(retryTimeout);\n    activeDrops = [];\n    dropTimer = 0;\n  }\n\n  const ta = opts.tiltAngle;\n  const vol = opts.volume;\n  watch(() => getVal(ta), () => { if (!running && getVal(vol) > 0) startDrops(); });\n  watch(() => getVal(vol), () => { if (!running && getVal(vol) > 0 && getVal(ta) !== 0) startDrops(); });\n\n  onMounted(() => { if (getVal(vol) > 0 && getVal(ta) !== 0) startDrops(); });\n  onUnmounted(() => { stopDrops(); });\n\n  return { startDrops, stopDrops };\n}\n",
      "start": 1783634642422,
      "end": 1783634642731,
      "sourcemaps": null
    },
    {
      "name": "vite:css",
      "start": 1783634642731,
      "end": 1783634642731,
      "order": "normal"
    },
    {
      "name": "vite:esbuild",
      "result": "import { watch, onMounted, onUnmounted } from \"vue\";\nexport function useSpillDrops(opts) {\n  let activeDrops = [];\n  let dropTimer = 0;\n  let animId = 0;\n  let retryTimeout = 0;\n  let running = false;\n  const getVal = (v) => typeof v === \"function\" ? v() : v.value;\n  function startDrops() {\n    if (running) return;\n    running = true;\n    const ctx = opts.canvasRef.value?.getContext(\"2d\");\n    if (!ctx) {\n      retryTimeout = window.setTimeout(startDrops, 50);\n      return;\n    }\n    const loop = () => {\n      if (!running) return;\n      ctx.clearRect(0, 0, opts.canvasW, opts.canvasH);\n      const tilt = getVal(opts.tiltAngle);\n      const absTilt = Math.abs(tilt);\n      const volume = getVal(opts.volume);\n      const maxVolume = getVal(opts.maxVolume);\n      const fillH = Math.min(volume / maxVolume, 1) * (opts.canvasH * 0.4);\n      const threshold = 55 - fillH / (opts.canvasH * 0.4) * 45;\n      const mouth = opts.mouthPosition(tilt);\n      const minX = opts.mouthBounds?.minX ?? 10;\n      const maxX = opts.mouthBounds?.maxX ?? opts.canvasW - 10;\n      const minY = opts.mouthBounds?.minY ?? 10;\n      const maxY = opts.mouthBounds?.maxY ?? opts.canvasH - 10;\n      const mouthX = Math.max(minX, Math.min(maxX, mouth.x));\n      const mouthY = Math.max(minY, Math.min(maxY, mouth.y));\n      let isSpilling = false;\n      if (absTilt > threshold && volume > 2) isSpilling = true;\n      if (isSpilling) {\n        dropTimer++;\n        let interval = 15;\n        if (absTilt > 20) interval = 8;\n        if (absTilt > 40) interval = 4;\n        if (absTilt > 55) interval = 2;\n        if (dropTimer >= interval) {\n          activeDrops.push({\n            x: mouthX + (Math.random() - 0.5) * 3,\n            y: mouthY + 2 + (Math.random() - 0.5) * 2,\n            vx: (tilt > 0 ? 0.6 : -0.6) + (Math.random() - 0.5) * 0.3,\n            vy: 0.3,\n            size: 1.8 + Math.random() * 1.2,\n            born: performance.now()\n          });\n          dropTimer = 0;\n          opts.onSpill(0.05);\n        }\n      } else {\n        dropTimer = 0;\n      }\n      const drops = activeDrops;\n      for (let i = 0; i < drops.length; i++) {\n        for (let j = i + 1; j < drops.length; j++) {\n          const dx = drops[j].x - drops[i].x;\n          const dy = drops[j].y - drops[i].y;\n          const dist = Math.sqrt(dx * dx + dy * dy);\n          if (dist < 18) {\n            ctx.beginPath();\n            ctx.moveTo(drops[i].x, drops[i].y);\n            ctx.lineTo(drops[j].x, drops[j].y);\n            ctx.strokeStyle = getVal(opts.liquidColor);\n            ctx.globalAlpha = 0.35 * (1 - dist / 18);\n            ctx.lineWidth = 1.2;\n            ctx.stroke();\n            ctx.globalAlpha = 1;\n          }\n        }\n      }\n      for (let i = activeDrops.length - 1; i >= 0; i--) {\n        const d = activeDrops[i];\n        d.vy += 0.35;\n        d.vx *= 0.99;\n        d.x += d.vx;\n        d.y += d.vy;\n        ctx.beginPath();\n        ctx.arc(d.x, d.y, d.size, 0, Math.PI * 2);\n        ctx.fillStyle = getVal(opts.liquidColor);\n        ctx.globalAlpha = 0.8;\n        ctx.fill();\n        ctx.globalAlpha = 1;\n        ctx.beginPath();\n        ctx.arc(d.x - d.size * 0.25, d.y - d.size * 0.3, d.size * 0.4, 0, Math.PI * 2);\n        ctx.fillStyle = \"rgba(255,255,255,0.5)\";\n        ctx.fill();\n        const exitY = opts.exitY ?? opts.canvasH;\n        if (d.y > exitY) {\n          activeDrops.splice(i, 1);\n          opts.onDropExited(getVal(opts.itemX) + d.x, getVal(opts.itemY) + d.y, getVal(opts.liquidColor));\n        }\n      }\n      animId = requestAnimationFrame(loop);\n    };\n    loop();\n  }\n  function stopDrops() {\n    running = false;\n    if (animId) cancelAnimationFrame(animId);\n    if (retryTimeout) clearTimeout(retryTimeout);\n    activeDrops = [];\n    dropTimer = 0;\n  }\n  const ta = opts.tiltAngle;\n  const vol = opts.volume;\n  watch(() => getVal(ta), () => {\n    if (!running && getVal(vol) > 0) startDrops();\n  });\n  watch(() => getVal(vol), () => {\n    if (!running && getVal(vol) > 0 && getVal(ta) !== 0) startDrops();\n  });\n  onMounted(() => {\n    if (getVal(vol) > 0 && getVal(ta) !== 0) startDrops();\n  });\n  onUnmounted(() => {\n    stopDrops();\n  });\n  return { startDrops, stopDrops };\n}\n",
      "start": 1783634642731,
      "end": 1783634642878,
      "order": "normal",
      "sourcemaps": {
        "version": 3,
        "sources": [
          "C:/Users/feras/Desktop/feras/apps/web/src/composables/chemistry/useSpillDrops.ts"
        ],
        "sourcesContent": [
          "import { watch, onMounted, onUnmounted } from 'vue';\nimport type { Ref } from 'vue';\n\nexport interface DropPhysicsOptions {\n  canvasRef: Ref<HTMLCanvasElement | null>;\n  tiltAngle: Ref<number> | (() => number);\n  volume: Ref<number> | (() => number);\n  maxVolume: Ref<number> | (() => number);\n  liquidColor: Ref<string> | (() => string);\n  itemX: Ref<number> | (() => number);\n  itemY: Ref<number> | (() => number);\n  mouthPosition: (tilt: number) => { x: number; y: number };\n  canvasW: number;\n  canvasH: number;\n  mouthBounds?: { minX: number; maxX: number; minY: number; maxY: number };\n  exitY?: number;\n  onSpill: (amount: number) => void;\n  onDropExited: (wx: number, wy: number, color: string) => void;\n}\n\nexport function useSpillDrops(opts: DropPhysicsOptions) {\n  let activeDrops: { x: number; y: number; vx: number; vy: number; size: number; born: number }[] = [];\n  let dropTimer = 0;\n  let animId = 0;\n  let retryTimeout = 0;\n  let running = false;\n\n  const getVal = <T>(v: Ref<T> | (() => T)): T => (typeof v === 'function' ? (v as () => T)() : v.value);\n\n  function startDrops() {\n    if (running) return;\n    running = true;\n    const ctx = opts.canvasRef.value?.getContext('2d');\n    if (!ctx) { retryTimeout = window.setTimeout(startDrops, 50); return; }\n\n    const loop = () => {\n      if (!running) return;\n      ctx.clearRect(0, 0, opts.canvasW, opts.canvasH);\n      const tilt = getVal(opts.tiltAngle);\n      const absTilt = Math.abs(tilt);\n      const volume = getVal(opts.volume);\n      const maxVolume = getVal(opts.maxVolume);\n      const fillH = Math.min(volume / maxVolume, 1) * (opts.canvasH * 0.4);\n      const threshold = 55 - (fillH / (opts.canvasH * 0.4)) * 45;\n\n      const mouth = opts.mouthPosition(tilt);\n      const minX = opts.mouthBounds?.minX ?? 10;\n      const maxX = opts.mouthBounds?.maxX ?? opts.canvasW - 10;\n      const minY = opts.mouthBounds?.minY ?? 10;\n      const maxY = opts.mouthBounds?.maxY ?? opts.canvasH - 10;\n      const mouthX = Math.max(minX, Math.min(maxX, mouth.x));\n      const mouthY = Math.max(minY, Math.min(maxY, mouth.y));\n\n      let isSpilling = false;\n      if (absTilt > threshold && volume > 2) isSpilling = true;\n\n      if (isSpilling) {\n        dropTimer++;\n        let interval = 15; if (absTilt > 20) interval = 8; if (absTilt > 40) interval = 4; if (absTilt > 55) interval = 2;\n        if (dropTimer >= interval) {\n          activeDrops.push({\n            x: mouthX + (Math.random() - 0.5) * 3,\n            y: mouthY + 2 + (Math.random() - 0.5) * 2,\n            vx: (tilt > 0 ? 0.6 : -0.6) + (Math.random() - 0.5) * 0.3,\n            vy: 0.3,\n            size: 1.8 + Math.random() * 1.2,\n            born: performance.now(),\n          });\n          dropTimer = 0;\n          opts.onSpill(0.05);\n        }\n      } else { dropTimer = 0; }\n\n      // Draw stream: connect nearby drops with thin lines\n      const drops = activeDrops;\n      for (let i = 0; i < drops.length; i++) {\n        for (let j = i + 1; j < drops.length; j++) {\n          const dx = drops[j].x - drops[i].x;\n          const dy = drops[j].y - drops[i].y;\n          const dist = Math.sqrt(dx * dx + dy * dy);\n          if (dist < 18) {\n            ctx.beginPath();\n            ctx.moveTo(drops[i].x, drops[i].y);\n            ctx.lineTo(drops[j].x, drops[j].y);\n            ctx.strokeStyle = getVal(opts.liquidColor);\n            ctx.globalAlpha = 0.35 * (1 - dist / 18);\n            ctx.lineWidth = 1.2;\n            ctx.stroke();\n            ctx.globalAlpha = 1;\n          }\n        }\n      }\n\n      // Gravity and draw each drop\n      for (let i = activeDrops.length - 1; i >= 0; i--) {\n        const d = activeDrops[i];\n        d.vy += 0.35;\n        d.vx *= 0.99; // slight air resistance\n        d.x += d.vx;\n        d.y += d.vy;\n\n        // Main drop body\n        ctx.beginPath();\n        ctx.arc(d.x, d.y, d.size, 0, Math.PI * 2);\n        ctx.fillStyle = getVal(opts.liquidColor);\n        ctx.globalAlpha = 0.8;\n        ctx.fill();\n        ctx.globalAlpha = 1;\n\n        // Highlight\n        ctx.beginPath();\n        ctx.arc(d.x - d.size * 0.25, d.y - d.size * 0.3, d.size * 0.4, 0, Math.PI * 2);\n        ctx.fillStyle = 'rgba(255,255,255,0.5)';\n        ctx.fill();\n\n        const exitY = opts.exitY ?? opts.canvasH;\n        if (d.y > exitY) {\n          activeDrops.splice(i, 1);\n          opts.onDropExited(getVal(opts.itemX) + d.x, getVal(opts.itemY) + d.y, getVal(opts.liquidColor));\n        }\n      }\n      animId = requestAnimationFrame(loop);\n    };\n    loop();\n  }\n\n  function stopDrops() {\n    running = false;\n    if (animId) cancelAnimationFrame(animId);\n    if (retryTimeout) clearTimeout(retryTimeout);\n    activeDrops = [];\n    dropTimer = 0;\n  }\n\n  const ta = opts.tiltAngle;\n  const vol = opts.volume;\n  watch(() => getVal(ta), () => { if (!running && getVal(vol) > 0) startDrops(); });\n  watch(() => getVal(vol), () => { if (!running && getVal(vol) > 0 && getVal(ta) !== 0) startDrops(); });\n\n  onMounted(() => { if (getVal(vol) > 0 && getVal(ta) !== 0) startDrops(); });\n  onUnmounted(() => { stopDrops(); });\n\n  return { startDrops, stopDrops };\n}\n"
        ],
        "mappings": "AAAA,SAAS,OAAO,WAAW,mBAAmB;AAoBvC,gBAAS,cAAc,MAA0B;AACtD,MAAI,cAA8F,CAAC;AACnG,MAAI,YAAY;AAChB,MAAI,SAAS;AACb,MAAI,eAAe;AACnB,MAAI,UAAU;AAEd,QAAM,SAAS,CAAI,MAA8B,OAAO,MAAM,aAAc,EAAc,IAAI,EAAE;AAEhG,WAAS,aAAa;AACpB,QAAI,QAAS;AACb,cAAU;AACV,UAAM,MAAM,KAAK,UAAU,OAAO,WAAW,IAAI;AACjD,QAAI,CAAC,KAAK;AAAE,qBAAe,OAAO,WAAW,YAAY,EAAE;AAAG;AAAA,IAAQ;AAEtE,UAAM,OAAO,MAAM;AACjB,UAAI,CAAC,QAAS;AACd,UAAI,UAAU,GAAG,GAAG,KAAK,SAAS,KAAK,OAAO;AAC9C,YAAM,OAAO,OAAO,KAAK,SAAS;AAClC,YAAM,UAAU,KAAK,IAAI,IAAI;AAC7B,YAAM,SAAS,OAAO,KAAK,MAAM;AACjC,YAAM,YAAY,OAAO,KAAK,SAAS;AACvC,YAAM,QAAQ,KAAK,IAAI,SAAS,WAAW,CAAC,KAAK,KAAK,UAAU;AAChE,YAAM,YAAY,KAAM,SAAS,KAAK,UAAU,OAAQ;AAExD,YAAM,QAAQ,KAAK,cAAc,IAAI;AACrC,YAAM,OAAO,KAAK,aAAa,QAAQ;AACvC,YAAM,OAAO,KAAK,aAAa,QAAQ,KAAK,UAAU;AACtD,YAAM,OAAO,KAAK,aAAa,QAAQ;AACvC,YAAM,OAAO,KAAK,aAAa,QAAQ,KAAK,UAAU;AACtD,YAAM,SAAS,KAAK,IAAI,MAAM,KAAK,IAAI,MAAM,MAAM,CAAC,CAAC;AACrD,YAAM,SAAS,KAAK,IAAI,MAAM,KAAK,IAAI,MAAM,MAAM,CAAC,CAAC;AAErD,UAAI,aAAa;AACjB,UAAI,UAAU,aAAa,SAAS,EAAG,cAAa;AAEpD,UAAI,YAAY;AACd;AACA,YAAI,WAAW;AAAI,YAAI,UAAU,GAAI,YAAW;AAAG,YAAI,UAAU,GAAI,YAAW;AAAG,YAAI,UAAU,GAAI,YAAW;AAChH,YAAI,aAAa,UAAU;AACzB,sBAAY,KAAK;AAAA,YACf,GAAG,UAAU,KAAK,OAAO,IAAI,OAAO;AAAA,YACpC,GAAG,SAAS,KAAK,KAAK,OAAO,IAAI,OAAO;AAAA,YACxC,KAAK,OAAO,IAAI,MAAM,SAAS,KAAK,OAAO,IAAI,OAAO;AAAA,YACtD,IAAI;AAAA,YACJ,MAAM,MAAM,KAAK,OAAO,IAAI;AAAA,YAC5B,MAAM,YAAY,IAAI;AAAA,UACxB,CAAC;AACD,sBAAY;AACZ,eAAK,QAAQ,IAAI;AAAA,QACnB;AAAA,MACF,OAAO;AAAE,oBAAY;AAAA,MAAG;AAGxB,YAAM,QAAQ;AACd,eAAS,IAAI,GAAG,IAAI,MAAM,QAAQ,KAAK;AACrC,iBAAS,IAAI,IAAI,GAAG,IAAI,MAAM,QAAQ,KAAK;AACzC,gBAAM,KAAK,MAAM,CAAC,EAAE,IAAI,MAAM,CAAC,EAAE;AACjC,gBAAM,KAAK,MAAM,CAAC,EAAE,IAAI,MAAM,CAAC,EAAE;AACjC,gBAAM,OAAO,KAAK,KAAK,KAAK,KAAK,KAAK,EAAE;AACxC,cAAI,OAAO,IAAI;AACb,gBAAI,UAAU;AACd,gBAAI,OAAO,MAAM,CAAC,EAAE,GAAG,MAAM,CAAC,EAAE,CAAC;AACjC,gBAAI,OAAO,MAAM,CAAC,EAAE,GAAG,MAAM,CAAC,EAAE,CAAC;AACjC,gBAAI,cAAc,OAAO,KAAK,WAAW;AACzC,gBAAI,cAAc,QAAQ,IAAI,OAAO;AACrC,gBAAI,YAAY;AAChB,gBAAI,OAAO;AACX,gBAAI,cAAc;AAAA,UACpB;AAAA,QACF;AAAA,MACF;AAGA,eAAS,IAAI,YAAY,SAAS,GAAG,KAAK,GAAG,KAAK;AAChD,cAAM,IAAI,YAAY,CAAC;AACvB,UAAE,MAAM;AACR,UAAE,MAAM;AACR,UAAE,KAAK,EAAE;AACT,UAAE,KAAK,EAAE;AAGT,YAAI,UAAU;AACd,YAAI,IAAI,EAAE,GAAG,EAAE,GAAG,EAAE,MAAM,GAAG,KAAK,KAAK,CAAC;AACxC,YAAI,YAAY,OAAO,KAAK,WAAW;AACvC,YAAI,cAAc;AAClB,YAAI,KAAK;AACT,YAAI,cAAc;AAGlB,YAAI,UAAU;AACd,YAAI,IAAI,EAAE,IAAI,EAAE,OAAO,MAAM,EAAE,IAAI,EAAE,OAAO,KAAK,EAAE,OAAO,KAAK,GAAG,KAAK,KAAK,CAAC;AAC7E,YAAI,YAAY;AAChB,YAAI,KAAK;AAET,cAAM,QAAQ,KAAK,SAAS,KAAK;AACjC,YAAI,EAAE,IAAI,OAAO;AACf,sBAAY,OAAO,GAAG,CAAC;AACvB,eAAK,aAAa,OAAO,KAAK,KAAK,IAAI,EAAE,GAAG,OAAO,KAAK,KAAK,IAAI,EAAE,GAAG,OAAO,KAAK,WAAW,CAAC;AAAA,QAChG;AAAA,MACF;AACA,eAAS,sBAAsB,IAAI;AAAA,IACrC;AACA,SAAK;AAAA,EACP;AAEA,WAAS,YAAY;AACnB,cAAU;AACV,QAAI,OAAQ,sBAAqB,MAAM;AACvC,QAAI,aAAc,cAAa,YAAY;AAC3C,kBAAc,CAAC;AACf,gBAAY;AAAA,EACd;AAEA,QAAM,KAAK,KAAK;AAChB,QAAM,MAAM,KAAK;AACjB,QAAM,MAAM,OAAO,EAAE,GAAG,MAAM;AAAE,QAAI,CAAC,WAAW,OAAO,GAAG,IAAI,EAAG,YAAW;AAAA,EAAG,CAAC;AAChF,QAAM,MAAM,OAAO,GAAG,GAAG,MAAM;AAAE,QAAI,CAAC,WAAW,OAAO,GAAG,IAAI,KAAK,OAAO,EAAE,MAAM,EAAG,YAAW;AAAA,EAAG,CAAC;AAErG,YAAU,MAAM;AAAE,QAAI,OAAO,GAAG,IAAI,KAAK,OAAO,EAAE,MAAM,EAAG,YAAW;AAAA,EAAG,CAAC;AAC1E,cAAY,MAAM;AAAE,cAAU;AAAA,EAAG,CAAC;AAElC,SAAO,EAAE,YAAY,UAAU;AACjC;",
        "names": []
      }
    },
    {
      "name": "vite:json",
      "start": 1783634642878,
      "end": 1783634642878,
      "order": "normal"
    },
    {
      "name": "vite:worker",
      "start": 1783634642878,
      "end": 1783634642878,
      "order": "normal"
    },
    {
      "name": "vite:define",
      "start": 1783634642878,
      "end": 1783634642878,
      "order": "normal"
    },
    {
      "name": "vite:css-post",
      "start": 1783634642878,
      "end": 1783634642878,
      "order": "normal"
    },
    {
      "name": "vite:build-html",
      "start": 1783634642878,
      "end": 1783634642878,
      "order": "normal"
    },
    {
      "name": "vite:worker-import-meta-url",
      "start": 1783634642878,
      "end": 1783634642878,
      "order": "normal"
    },
    {
      "name": "vite:asset-import-meta-url",
      "start": 1783634642878,
      "end": 1783634642878,
      "order": "normal"
    },
    {
      "name": "commonjs",
      "start": 1783634642878,
      "end": 1783634642878,
      "order": "normal"
    },
    {
      "name": "vite:dynamic-import-vars",
      "start": 1783634642879,
      "end": 1783634642879,
      "order": "normal"
    },
    {
      "name": "vite:import-glob",
      "start": 1783634642879,
      "end": 1783634642879,
      "order": "normal"
    },
    {
      "name": "vite:build-import-analysis",
      "start": 1783634642879,
      "end": 1783634642879,
      "order": "normal"
    },
    {
      "name": "vite:reporter",
      "start": 1783634642879,
      "end": 1783634642879,
      "order": "normal"
    }
  ]
}

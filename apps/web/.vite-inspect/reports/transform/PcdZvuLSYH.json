{
  "resolvedId": "C:/Users/feras/Desktop/feras/apps/web/src/composables/chemistry/useReactionEngine.ts",
  "transforms": [
    {
      "name": "__load__",
      "result": "import { liquidMap } from './useChemistryLab';\nimport type { LiquidState } from '@my-modern-app/chemistry-engine';\nimport { findEquation, isAcid, isBase, isIndicator, mixColor, calculateTitrationPh } from '@my-modern-app/chemistry-engine';\n\n// Re-export the engine's calculateTitrationPh so existing imports keep working\nexport { calculateTitrationPh } from '@my-modern-app/chemistry-engine';\n\n// ================== INDICATOR COLOR SYSTEM ==================\n\nexport function getIndicatorColor(indicatorId: string, ph: number): string {\n  switch (indicatorId) {\n    case 'phenolphthalein':\n      return ph > 8.2 ? '#ec4899' : '#fdf4ff';\n    case 'methyl-orange':\n      return ph < 3.1 ? '#ef4444' : ph > 4.4 ? '#fbbf24' : '#fb923c';\n    case 'bromothymol-blue':\n      return ph > 7.6 ? '#3b82f6' : ph < 6.0 ? '#facc15' : '#22c55e';\n    case 'universal-indicator': {\n      if (ph < 3) return '#ef4444';\n      if (ph < 6) return '#fb923c';\n      if (ph < 7) return '#facc15';\n      if (ph < 8) return '#22c55e';\n      if (ph < 10) return '#3b82f6';\n      return '#7c3aed';\n    }\n    case 'starch':\n      return ph > 6 ? '#1e3a8a' : '#fefce8';\n    default:\n      return '#e0f2fe';\n  }\n}\n\nexport function applyIndicatorsToContainer(liq: LiquidState): void {\n  if (!liq.indicators || liq.indicators.length === 0) return;\n  const currentPh = liq.ph ?? 7;\n  const base = liq.baseColor || liq.color;\n  for (const indId of liq.indicators) {\n    const indColor = getIndicatorColor(indId, currentPh);\n    // Indicators are highly visible even in tiny amounts — give 50% visual weight\n    liq.color = mixColor(base, liq.volume, indColor, liq.volume);\n  }\n}\n\nexport function applyIndicator(indicatorId: string, containerUid: string): void {\n  const liq = liquidMap[containerUid];\n  if (!liq || liq.volume <= 0) return;\n\n  const currentPh = liq.ph ?? 7;\n  const indicatorColor = getIndicatorColor(indicatorId, currentPh);\n  const base = liq.baseColor || liq.color;\n\n  // Indicator visual weight: 50% regardless of actual drop volume\n  liq.color = mixColor(base, liq.volume, indicatorColor, liq.volume);\n  liq.opacity = Math.min(1, liq.opacity + 0.15);\n}\n\n// ================== DROP MIXING ==================\n\nexport interface MixEvent {\n  sourceUid: string;\n  targetUid: string;\n  sourceChemicalId: string;\n  targetChemicalId: string;\n  dropVolume: number;\n}\n\nexport function handleDropMix(event: MixEvent): void {\n  const target = liquidMap[event.targetUid];\n  if (!target) return;\n\n  const src = event.sourceChemicalId;\n  const tgt = target.chemicalId || '';\n\n  // ===== INDICATOR DROPS: don't react chemically, just add to indicators =====\n  if (isIndicator(src)) {\n    if (!target.indicators) target.indicators = [];\n    if (!target.indicators.includes(src)) {\n      target.indicators.push(src);\n    }\n    applyIndicator(src, event.targetUid);\n    return;\n  }\n\n  // ===== TITRATION: acid-base neutralization with proper pH calculation =====\n  if ((isBase(src) && isAcid(tgt)) || (isAcid(src) && isBase(tgt))) {\n    // Track reactants for stoichiometric calculation\n    if (!target.reactants) target.reactants = {};\n    if (!target.reactants[tgt]) {\n      const trackedVol = Object.values(target.reactants).reduce((s, v) => s + v, 0);\n      target.reactants[tgt] = Math.max(0, target.volume - event.dropVolume - trackedVol);\n    }\n    target.reactants[src] = (target.reactants[src] || 0) + event.dropVolume;\n\n    // Calculate pH from excess reagent\n    let acidVol = 0, baseVol = 0;\n    let acidId = '', baseId = '';\n\n    for (const [chemId, vol] of Object.entries(target.reactants)) {\n      if (isAcid(chemId)) { acidVol += vol; acidId = chemId; }\n      if (isBase(chemId)) { baseVol += vol; baseId = chemId; }\n    }\n\n    const newPh = calculateTitrationPh(acidVol, acidId, baseVol, baseId);\n    target.ph = newPh;\n    target.temperature = Math.min(100, target.temperature + 0.5);\n\n    // Find and store the reaction equation\n    const eq = findEquation([acidId, baseId].filter(Boolean));\n    if (eq) {\n      target.equation = eq.equation;\n      target.precipitate = eq.precipitate || false;\n      target.gasEvolution = eq.gasEvolution || false;\n    }\n\n    // Apply indicators based on new pH\n    applyIndicatorsToContainer(target);\n    return;\n  }\n\n  // ===== OTHER CHEMICAL REACTIONS (precipitation, redox, etc.) =====\n  const allReactants = [src, tgt].filter(Boolean);\n  const eq = findEquation(allReactants);\n\n  if (eq) {\n    // Store equation on container\n    target.equation = eq.equation;\n\n    // Update reactants tracking\n    if (!target.reactants) target.reactants = {};\n    target.reactants[src] = (target.reactants[src] || 0) + event.dropVolume;\n    target.reactants[tgt] = (target.reactants[tgt] || 0) + (target.volume - event.dropVolume);\n\n    // Apply reaction results\n    if (eq.resultPh !== undefined) target.ph = eq.resultPh;\n    target.color = eq.color;\n    target.opacity = eq.opacity;\n    target.label = `REACTION:${eq.equation}`;\n    target.temperature = Math.min(100, target.temperature + eq.temperatureRise);\n    target.precipitate = eq.precipitate || false;\n    target.gasEvolution = eq.gasEvolution || false;\n\n    if (eq.precipitate) {\n      target.opacity = Math.min(1, target.opacity + 0.2);\n    }\n\n    // Re-apply indicators after reaction\n    applyIndicatorsToContainer(target);\n    return;\n  }\n\n  // ===== NO REACTION: simple mixing =====\n  // Just add the new chemical to reactants without visual change\n  if (!target.reactants) target.reactants = {};\n  target.reactants[src] = (target.reactants[src] || 0) + event.dropVolume;\n}\n",
      "start": 1783626304177,
      "end": 1783626304177
    },
    {
      "name": "vite-plugin-vue-inspector",
      "start": 1783626304177,
      "end": 1783626304177,
      "order": "pre"
    },
    {
      "name": "vite-plugin-vue-devtools",
      "result": "import { liquidMap } from './useChemistryLab';\nimport type { LiquidState } from '@my-modern-app/chemistry-engine';\nimport { findEquation, isAcid, isBase, isIndicator, mixColor, calculateTitrationPh } from '@my-modern-app/chemistry-engine';\n\n// Re-export the engine's calculateTitrationPh so existing imports keep working\nexport { calculateTitrationPh } from '@my-modern-app/chemistry-engine';\n\n// ================== INDICATOR COLOR SYSTEM ==================\n\nexport function getIndicatorColor(indicatorId: string, ph: number): string {\n  switch (indicatorId) {\n    case 'phenolphthalein':\n      return ph > 8.2 ? '#ec4899' : '#fdf4ff';\n    case 'methyl-orange':\n      return ph < 3.1 ? '#ef4444' : ph > 4.4 ? '#fbbf24' : '#fb923c';\n    case 'bromothymol-blue':\n      return ph > 7.6 ? '#3b82f6' : ph < 6.0 ? '#facc15' : '#22c55e';\n    case 'universal-indicator': {\n      if (ph < 3) return '#ef4444';\n      if (ph < 6) return '#fb923c';\n      if (ph < 7) return '#facc15';\n      if (ph < 8) return '#22c55e';\n      if (ph < 10) return '#3b82f6';\n      return '#7c3aed';\n    }\n    case 'starch':\n      return ph > 6 ? '#1e3a8a' : '#fefce8';\n    default:\n      return '#e0f2fe';\n  }\n}\n\nexport function applyIndicatorsToContainer(liq: LiquidState): void {\n  if (!liq.indicators || liq.indicators.length === 0) return;\n  const currentPh = liq.ph ?? 7;\n  const base = liq.baseColor || liq.color;\n  for (const indId of liq.indicators) {\n    const indColor = getIndicatorColor(indId, currentPh);\n    // Indicators are highly visible even in tiny amounts — give 50% visual weight\n    liq.color = mixColor(base, liq.volume, indColor, liq.volume);\n  }\n}\n\nexport function applyIndicator(indicatorId: string, containerUid: string): void {\n  const liq = liquidMap[containerUid];\n  if (!liq || liq.volume <= 0) return;\n\n  const currentPh = liq.ph ?? 7;\n  const indicatorColor = getIndicatorColor(indicatorId, currentPh);\n  const base = liq.baseColor || liq.color;\n\n  // Indicator visual weight: 50% regardless of actual drop volume\n  liq.color = mixColor(base, liq.volume, indicatorColor, liq.volume);\n  liq.opacity = Math.min(1, liq.opacity + 0.15);\n}\n\n// ================== DROP MIXING ==================\n\nexport interface MixEvent {\n  sourceUid: string;\n  targetUid: string;\n  sourceChemicalId: string;\n  targetChemicalId: string;\n  dropVolume: number;\n}\n\nexport function handleDropMix(event: MixEvent): void {\n  const target = liquidMap[event.targetUid];\n  if (!target) return;\n\n  const src = event.sourceChemicalId;\n  const tgt = target.chemicalId || '';\n\n  // ===== INDICATOR DROPS: don't react chemically, just add to indicators =====\n  if (isIndicator(src)) {\n    if (!target.indicators) target.indicators = [];\n    if (!target.indicators.includes(src)) {\n      target.indicators.push(src);\n    }\n    applyIndicator(src, event.targetUid);\n    return;\n  }\n\n  // ===== TITRATION: acid-base neutralization with proper pH calculation =====\n  if ((isBase(src) && isAcid(tgt)) || (isAcid(src) && isBase(tgt))) {\n    // Track reactants for stoichiometric calculation\n    if (!target.reactants) target.reactants = {};\n    if (!target.reactants[tgt]) {\n      const trackedVol = Object.values(target.reactants).reduce((s, v) => s + v, 0);\n      target.reactants[tgt] = Math.max(0, target.volume - event.dropVolume - trackedVol);\n    }\n    target.reactants[src] = (target.reactants[src] || 0) + event.dropVolume;\n\n    // Calculate pH from excess reagent\n    let acidVol = 0, baseVol = 0;\n    let acidId = '', baseId = '';\n\n    for (const [chemId, vol] of Object.entries(target.reactants)) {\n      if (isAcid(chemId)) { acidVol += vol; acidId = chemId; }\n      if (isBase(chemId)) { baseVol += vol; baseId = chemId; }\n    }\n\n    const newPh = calculateTitrationPh(acidVol, acidId, baseVol, baseId);\n    target.ph = newPh;\n    target.temperature = Math.min(100, target.temperature + 0.5);\n\n    // Find and store the reaction equation\n    const eq = findEquation([acidId, baseId].filter(Boolean));\n    if (eq) {\n      target.equation = eq.equation;\n      target.precipitate = eq.precipitate || false;\n      target.gasEvolution = eq.gasEvolution || false;\n    }\n\n    // Apply indicators based on new pH\n    applyIndicatorsToContainer(target);\n    return;\n  }\n\n  // ===== OTHER CHEMICAL REACTIONS (precipitation, redox, etc.) =====\n  const allReactants = [src, tgt].filter(Boolean);\n  const eq = findEquation(allReactants);\n\n  if (eq) {\n    // Store equation on container\n    target.equation = eq.equation;\n\n    // Update reactants tracking\n    if (!target.reactants) target.reactants = {};\n    target.reactants[src] = (target.reactants[src] || 0) + event.dropVolume;\n    target.reactants[tgt] = (target.reactants[tgt] || 0) + (target.volume - event.dropVolume);\n\n    // Apply reaction results\n    if (eq.resultPh !== undefined) target.ph = eq.resultPh;\n    target.color = eq.color;\n    target.opacity = eq.opacity;\n    target.label = `REACTION:${eq.equation}`;\n    target.temperature = Math.min(100, target.temperature + eq.temperatureRise);\n    target.precipitate = eq.precipitate || false;\n    target.gasEvolution = eq.gasEvolution || false;\n\n    if (eq.precipitate) {\n      target.opacity = Math.min(1, target.opacity + 0.2);\n    }\n\n    // Re-apply indicators after reaction\n    applyIndicatorsToContainer(target);\n    return;\n  }\n\n  // ===== NO REACTION: simple mixing =====\n  // Just add the new chemical to reactants without visual change\n  if (!target.reactants) target.reactants = {};\n  target.reactants[src] = (target.reactants[src] || 0) + event.dropVolume;\n}\n",
      "start": 1783626304177,
      "end": 1783626304177,
      "order": "pre",
      "sourcemaps": null
    },
    {
      "name": "vite:css",
      "start": 1783626304177,
      "end": 1783626304177,
      "order": "normal"
    },
    {
      "name": "vite:esbuild",
      "result": "import { liquidMap } from \"./useChemistryLab\";\nimport { findEquation, isAcid, isBase, isIndicator, mixColor, calculateTitrationPh } from \"@my-modern-app/chemistry-engine\";\nexport { calculateTitrationPh } from \"@my-modern-app/chemistry-engine\";\nexport function getIndicatorColor(indicatorId, ph) {\n  switch (indicatorId) {\n    case \"phenolphthalein\":\n      return ph > 8.2 ? \"#ec4899\" : \"#fdf4ff\";\n    case \"methyl-orange\":\n      return ph < 3.1 ? \"#ef4444\" : ph > 4.4 ? \"#fbbf24\" : \"#fb923c\";\n    case \"bromothymol-blue\":\n      return ph > 7.6 ? \"#3b82f6\" : ph < 6 ? \"#facc15\" : \"#22c55e\";\n    case \"universal-indicator\": {\n      if (ph < 3) return \"#ef4444\";\n      if (ph < 6) return \"#fb923c\";\n      if (ph < 7) return \"#facc15\";\n      if (ph < 8) return \"#22c55e\";\n      if (ph < 10) return \"#3b82f6\";\n      return \"#7c3aed\";\n    }\n    case \"starch\":\n      return ph > 6 ? \"#1e3a8a\" : \"#fefce8\";\n    default:\n      return \"#e0f2fe\";\n  }\n}\nexport function applyIndicatorsToContainer(liq) {\n  if (!liq.indicators || liq.indicators.length === 0) return;\n  const currentPh = liq.ph ?? 7;\n  const base = liq.baseColor || liq.color;\n  for (const indId of liq.indicators) {\n    const indColor = getIndicatorColor(indId, currentPh);\n    liq.color = mixColor(base, liq.volume, indColor, liq.volume);\n  }\n}\nexport function applyIndicator(indicatorId, containerUid) {\n  const liq = liquidMap[containerUid];\n  if (!liq || liq.volume <= 0) return;\n  const currentPh = liq.ph ?? 7;\n  const indicatorColor = getIndicatorColor(indicatorId, currentPh);\n  const base = liq.baseColor || liq.color;\n  liq.color = mixColor(base, liq.volume, indicatorColor, liq.volume);\n  liq.opacity = Math.min(1, liq.opacity + 0.15);\n}\nexport function handleDropMix(event) {\n  const target = liquidMap[event.targetUid];\n  if (!target) return;\n  const src = event.sourceChemicalId;\n  const tgt = target.chemicalId || \"\";\n  if (isIndicator(src)) {\n    if (!target.indicators) target.indicators = [];\n    if (!target.indicators.includes(src)) {\n      target.indicators.push(src);\n    }\n    applyIndicator(src, event.targetUid);\n    return;\n  }\n  if (isBase(src) && isAcid(tgt) || isAcid(src) && isBase(tgt)) {\n    if (!target.reactants) target.reactants = {};\n    if (!target.reactants[tgt]) {\n      const trackedVol = Object.values(target.reactants).reduce((s, v) => s + v, 0);\n      target.reactants[tgt] = Math.max(0, target.volume - event.dropVolume - trackedVol);\n    }\n    target.reactants[src] = (target.reactants[src] || 0) + event.dropVolume;\n    let acidVol = 0, baseVol = 0;\n    let acidId = \"\", baseId = \"\";\n    for (const [chemId, vol] of Object.entries(target.reactants)) {\n      if (isAcid(chemId)) {\n        acidVol += vol;\n        acidId = chemId;\n      }\n      if (isBase(chemId)) {\n        baseVol += vol;\n        baseId = chemId;\n      }\n    }\n    const newPh = calculateTitrationPh(acidVol, acidId, baseVol, baseId);\n    target.ph = newPh;\n    target.temperature = Math.min(100, target.temperature + 0.5);\n    const eq2 = findEquation([acidId, baseId].filter(Boolean));\n    if (eq2) {\n      target.equation = eq2.equation;\n      target.precipitate = eq2.precipitate || false;\n      target.gasEvolution = eq2.gasEvolution || false;\n    }\n    applyIndicatorsToContainer(target);\n    return;\n  }\n  const allReactants = [src, tgt].filter(Boolean);\n  const eq = findEquation(allReactants);\n  if (eq) {\n    target.equation = eq.equation;\n    if (!target.reactants) target.reactants = {};\n    target.reactants[src] = (target.reactants[src] || 0) + event.dropVolume;\n    target.reactants[tgt] = (target.reactants[tgt] || 0) + (target.volume - event.dropVolume);\n    if (eq.resultPh !== void 0) target.ph = eq.resultPh;\n    target.color = eq.color;\n    target.opacity = eq.opacity;\n    target.label = `REACTION:${eq.equation}`;\n    target.temperature = Math.min(100, target.temperature + eq.temperatureRise);\n    target.precipitate = eq.precipitate || false;\n    target.gasEvolution = eq.gasEvolution || false;\n    if (eq.precipitate) {\n      target.opacity = Math.min(1, target.opacity + 0.2);\n    }\n    applyIndicatorsToContainer(target);\n    return;\n  }\n  if (!target.reactants) target.reactants = {};\n  target.reactants[src] = (target.reactants[src] || 0) + event.dropVolume;\n}\n",
      "start": 1783626304177,
      "end": 1783626305080,
      "order": "normal",
      "sourcemaps": {
        "version": 3,
        "sources": [
          "useReactionEngine.ts"
        ],
        "sourcesContent": [
          "import { liquidMap } from './useChemistryLab';\nimport type { LiquidState } from '@my-modern-app/chemistry-engine';\nimport { findEquation, isAcid, isBase, isIndicator, mixColor, calculateTitrationPh } from '@my-modern-app/chemistry-engine';\n\n// Re-export the engine's calculateTitrationPh so existing imports keep working\nexport { calculateTitrationPh } from '@my-modern-app/chemistry-engine';\n\n// ================== INDICATOR COLOR SYSTEM ==================\n\nexport function getIndicatorColor(indicatorId: string, ph: number): string {\n  switch (indicatorId) {\n    case 'phenolphthalein':\n      return ph > 8.2 ? '#ec4899' : '#fdf4ff';\n    case 'methyl-orange':\n      return ph < 3.1 ? '#ef4444' : ph > 4.4 ? '#fbbf24' : '#fb923c';\n    case 'bromothymol-blue':\n      return ph > 7.6 ? '#3b82f6' : ph < 6.0 ? '#facc15' : '#22c55e';\n    case 'universal-indicator': {\n      if (ph < 3) return '#ef4444';\n      if (ph < 6) return '#fb923c';\n      if (ph < 7) return '#facc15';\n      if (ph < 8) return '#22c55e';\n      if (ph < 10) return '#3b82f6';\n      return '#7c3aed';\n    }\n    case 'starch':\n      return ph > 6 ? '#1e3a8a' : '#fefce8';\n    default:\n      return '#e0f2fe';\n  }\n}\n\nexport function applyIndicatorsToContainer(liq: LiquidState): void {\n  if (!liq.indicators || liq.indicators.length === 0) return;\n  const currentPh = liq.ph ?? 7;\n  const base = liq.baseColor || liq.color;\n  for (const indId of liq.indicators) {\n    const indColor = getIndicatorColor(indId, currentPh);\n    // Indicators are highly visible even in tiny amounts — give 50% visual weight\n    liq.color = mixColor(base, liq.volume, indColor, liq.volume);\n  }\n}\n\nexport function applyIndicator(indicatorId: string, containerUid: string): void {\n  const liq = liquidMap[containerUid];\n  if (!liq || liq.volume <= 0) return;\n\n  const currentPh = liq.ph ?? 7;\n  const indicatorColor = getIndicatorColor(indicatorId, currentPh);\n  const base = liq.baseColor || liq.color;\n\n  // Indicator visual weight: 50% regardless of actual drop volume\n  liq.color = mixColor(base, liq.volume, indicatorColor, liq.volume);\n  liq.opacity = Math.min(1, liq.opacity + 0.15);\n}\n\n// ================== DROP MIXING ==================\n\nexport interface MixEvent {\n  sourceUid: string;\n  targetUid: string;\n  sourceChemicalId: string;\n  targetChemicalId: string;\n  dropVolume: number;\n}\n\nexport function handleDropMix(event: MixEvent): void {\n  const target = liquidMap[event.targetUid];\n  if (!target) return;\n\n  const src = event.sourceChemicalId;\n  const tgt = target.chemicalId || '';\n\n  // ===== INDICATOR DROPS: don't react chemically, just add to indicators =====\n  if (isIndicator(src)) {\n    if (!target.indicators) target.indicators = [];\n    if (!target.indicators.includes(src)) {\n      target.indicators.push(src);\n    }\n    applyIndicator(src, event.targetUid);\n    return;\n  }\n\n  // ===== TITRATION: acid-base neutralization with proper pH calculation =====\n  if ((isBase(src) && isAcid(tgt)) || (isAcid(src) && isBase(tgt))) {\n    // Track reactants for stoichiometric calculation\n    if (!target.reactants) target.reactants = {};\n    if (!target.reactants[tgt]) {\n      const trackedVol = Object.values(target.reactants).reduce((s, v) => s + v, 0);\n      target.reactants[tgt] = Math.max(0, target.volume - event.dropVolume - trackedVol);\n    }\n    target.reactants[src] = (target.reactants[src] || 0) + event.dropVolume;\n\n    // Calculate pH from excess reagent\n    let acidVol = 0, baseVol = 0;\n    let acidId = '', baseId = '';\n\n    for (const [chemId, vol] of Object.entries(target.reactants)) {\n      if (isAcid(chemId)) { acidVol += vol; acidId = chemId; }\n      if (isBase(chemId)) { baseVol += vol; baseId = chemId; }\n    }\n\n    const newPh = calculateTitrationPh(acidVol, acidId, baseVol, baseId);\n    target.ph = newPh;\n    target.temperature = Math.min(100, target.temperature + 0.5);\n\n    // Find and store the reaction equation\n    const eq = findEquation([acidId, baseId].filter(Boolean));\n    if (eq) {\n      target.equation = eq.equation;\n      target.precipitate = eq.precipitate || false;\n      target.gasEvolution = eq.gasEvolution || false;\n    }\n\n    // Apply indicators based on new pH\n    applyIndicatorsToContainer(target);\n    return;\n  }\n\n  // ===== OTHER CHEMICAL REACTIONS (precipitation, redox, etc.) =====\n  const allReactants = [src, tgt].filter(Boolean);\n  const eq = findEquation(allReactants);\n\n  if (eq) {\n    // Store equation on container\n    target.equation = eq.equation;\n\n    // Update reactants tracking\n    if (!target.reactants) target.reactants = {};\n    target.reactants[src] = (target.reactants[src] || 0) + event.dropVolume;\n    target.reactants[tgt] = (target.reactants[tgt] || 0) + (target.volume - event.dropVolume);\n\n    // Apply reaction results\n    if (eq.resultPh !== undefined) target.ph = eq.resultPh;\n    target.color = eq.color;\n    target.opacity = eq.opacity;\n    target.label = `REACTION:${eq.equation}`;\n    target.temperature = Math.min(100, target.temperature + eq.temperatureRise);\n    target.precipitate = eq.precipitate || false;\n    target.gasEvolution = eq.gasEvolution || false;\n\n    if (eq.precipitate) {\n      target.opacity = Math.min(1, target.opacity + 0.2);\n    }\n\n    // Re-apply indicators after reaction\n    applyIndicatorsToContainer(target);\n    return;\n  }\n\n  // ===== NO REACTION: simple mixing =====\n  // Just add the new chemical to reactants without visual change\n  if (!target.reactants) target.reactants = {};\n  target.reactants[src] = (target.reactants[src] || 0) + event.dropVolume;\n}\n"
        ],
        "mappings": "AAAA,SAAS,iBAAiB;AAE1B,SAAS,cAAc,QAAQ,QAAQ,aAAa,UAAU,4BAA4B;AAG1F,SAAS,4BAA4B;AAI9B,gBAAS,kBAAkB,aAAqB,IAAoB;AACzE,UAAQ,aAAa;AAAA,IACnB,KAAK;AACH,aAAO,KAAK,MAAM,YAAY;AAAA,IAChC,KAAK;AACH,aAAO,KAAK,MAAM,YAAY,KAAK,MAAM,YAAY;AAAA,IACvD,KAAK;AACH,aAAO,KAAK,MAAM,YAAY,KAAK,IAAM,YAAY;AAAA,IACvD,KAAK,uBAAuB;AAC1B,UAAI,KAAK,EAAG,QAAO;AACnB,UAAI,KAAK,EAAG,QAAO;AACnB,UAAI,KAAK,EAAG,QAAO;AACnB,UAAI,KAAK,EAAG,QAAO;AACnB,UAAI,KAAK,GAAI,QAAO;AACpB,aAAO;AAAA,IACT;AAAA,IACA,KAAK;AACH,aAAO,KAAK,IAAI,YAAY;AAAA,IAC9B;AACE,aAAO;AAAA,EACX;AACF;AAEO,gBAAS,2BAA2B,KAAwB;AACjE,MAAI,CAAC,IAAI,cAAc,IAAI,WAAW,WAAW,EAAG;AACpD,QAAM,YAAY,IAAI,MAAM;AAC5B,QAAM,OAAO,IAAI,aAAa,IAAI;AAClC,aAAW,SAAS,IAAI,YAAY;AAClC,UAAM,WAAW,kBAAkB,OAAO,SAAS;AAEnD,QAAI,QAAQ,SAAS,MAAM,IAAI,QAAQ,UAAU,IAAI,MAAM;AAAA,EAC7D;AACF;AAEO,gBAAS,eAAe,aAAqB,cAA4B;AAC9E,QAAM,MAAM,UAAU,YAAY;AAClC,MAAI,CAAC,OAAO,IAAI,UAAU,EAAG;AAE7B,QAAM,YAAY,IAAI,MAAM;AAC5B,QAAM,iBAAiB,kBAAkB,aAAa,SAAS;AAC/D,QAAM,OAAO,IAAI,aAAa,IAAI;AAGlC,MAAI,QAAQ,SAAS,MAAM,IAAI,QAAQ,gBAAgB,IAAI,MAAM;AACjE,MAAI,UAAU,KAAK,IAAI,GAAG,IAAI,UAAU,IAAI;AAC9C;AAYO,gBAAS,cAAc,OAAuB;AACnD,QAAM,SAAS,UAAU,MAAM,SAAS;AACxC,MAAI,CAAC,OAAQ;AAEb,QAAM,MAAM,MAAM;AAClB,QAAM,MAAM,OAAO,cAAc;AAGjC,MAAI,YAAY,GAAG,GAAG;AACpB,QAAI,CAAC,OAAO,WAAY,QAAO,aAAa,CAAC;AAC7C,QAAI,CAAC,OAAO,WAAW,SAAS,GAAG,GAAG;AACpC,aAAO,WAAW,KAAK,GAAG;AAAA,IAC5B;AACA,mBAAe,KAAK,MAAM,SAAS;AACnC;AAAA,EACF;AAGA,MAAK,OAAO,GAAG,KAAK,OAAO,GAAG,KAAO,OAAO,GAAG,KAAK,OAAO,GAAG,GAAI;AAEhE,QAAI,CAAC,OAAO,UAAW,QAAO,YAAY,CAAC;AAC3C,QAAI,CAAC,OAAO,UAAU,GAAG,GAAG;AAC1B,YAAM,aAAa,OAAO,OAAO,OAAO,SAAS,EAAE,OAAO,CAAC,GAAG,MAAM,IAAI,GAAG,CAAC;AAC5E,aAAO,UAAU,GAAG,IAAI,KAAK,IAAI,GAAG,OAAO,SAAS,MAAM,aAAa,UAAU;AAAA,IACnF;AACA,WAAO,UAAU,GAAG,KAAK,OAAO,UAAU,GAAG,KAAK,KAAK,MAAM;AAG7D,QAAI,UAAU,GAAG,UAAU;AAC3B,QAAI,SAAS,IAAI,SAAS;AAE1B,eAAW,CAAC,QAAQ,GAAG,KAAK,OAAO,QAAQ,OAAO,SAAS,GAAG;AAC5D,UAAI,OAAO,MAAM,GAAG;AAAE,mBAAW;AAAK,iBAAS;AAAA,MAAQ;AACvD,UAAI,OAAO,MAAM,GAAG;AAAE,mBAAW;AAAK,iBAAS;AAAA,MAAQ;AAAA,IACzD;AAEA,UAAM,QAAQ,qBAAqB,SAAS,QAAQ,SAAS,MAAM;AACnE,WAAO,KAAK;AACZ,WAAO,cAAc,KAAK,IAAI,KAAK,OAAO,cAAc,GAAG;AAG3D,UAAMA,MAAK,aAAa,CAAC,QAAQ,MAAM,EAAE,OAAO,OAAO,CAAC;AACxD,QAAIA,KAAI;AACN,aAAO,WAAWA,IAAG;AACrB,aAAO,cAAcA,IAAG,eAAe;AACvC,aAAO,eAAeA,IAAG,gBAAgB;AAAA,IAC3C;AAGA,+BAA2B,MAAM;AACjC;AAAA,EACF;AAGA,QAAM,eAAe,CAAC,KAAK,GAAG,EAAE,OAAO,OAAO;AAC9C,QAAM,KAAK,aAAa,YAAY;AAEpC,MAAI,IAAI;AAEN,WAAO,WAAW,GAAG;AAGrB,QAAI,CAAC,OAAO,UAAW,QAAO,YAAY,CAAC;AAC3C,WAAO,UAAU,GAAG,KAAK,OAAO,UAAU,GAAG,KAAK,KAAK,MAAM;AAC7D,WAAO,UAAU,GAAG,KAAK,OAAO,UAAU,GAAG,KAAK,MAAM,OAAO,SAAS,MAAM;AAG9E,QAAI,GAAG,aAAa,OAAW,QAAO,KAAK,GAAG;AAC9C,WAAO,QAAQ,GAAG;AAClB,WAAO,UAAU,GAAG;AACpB,WAAO,QAAQ,YAAY,GAAG,QAAQ;AACtC,WAAO,cAAc,KAAK,IAAI,KAAK,OAAO,cAAc,GAAG,eAAe;AAC1E,WAAO,cAAc,GAAG,eAAe;AACvC,WAAO,eAAe,GAAG,gBAAgB;AAEzC,QAAI,GAAG,aAAa;AAClB,aAAO,UAAU,KAAK,IAAI,GAAG,OAAO,UAAU,GAAG;AAAA,IACnD;AAGA,+BAA2B,MAAM;AACjC;AAAA,EACF;AAIA,MAAI,CAAC,OAAO,UAAW,QAAO,YAAY,CAAC;AAC3C,SAAO,UAAU,GAAG,KAAK,OAAO,UAAU,GAAG,KAAK,KAAK,MAAM;AAC/D;",
        "names": [
          "eq"
        ]
      }
    },
    {
      "name": "vite:json",
      "start": 1783626305080,
      "end": 1783626305080,
      "order": "normal"
    },
    {
      "name": "vite:worker",
      "start": 1783626305080,
      "end": 1783626305080,
      "order": "normal"
    },
    {
      "name": "vite:vue",
      "start": 1783626305080,
      "end": 1783626305080,
      "order": "normal-normal"
    },
    {
      "name": "vite:define",
      "start": 1783626305080,
      "end": 1783626305080,
      "order": "normal"
    },
    {
      "name": "vite:css-post",
      "start": 1783626305080,
      "end": 1783626305080,
      "order": "normal"
    },
    {
      "name": "vite:worker-import-meta-url",
      "start": 1783626305080,
      "end": 1783626305080,
      "order": "normal"
    },
    {
      "name": "vite:asset-import-meta-url",
      "start": 1783626305081,
      "end": 1783626305083,
      "order": "normal"
    },
    {
      "name": "vite:dynamic-import-vars",
      "start": 1783626305083,
      "end": 1783626305083,
      "order": "normal"
    },
    {
      "name": "vite:import-glob",
      "start": 1783626305083,
      "end": 1783626305083,
      "order": "normal"
    },
    {
      "name": "vite-plugin-vue-inspector:post",
      "start": 1783626305083,
      "end": 1783626305083,
      "order": "post"
    },
    {
      "name": "vite:client-inject",
      "start": 1783626305084,
      "end": 1783626305084,
      "order": "normal"
    },
    {
      "name": "vite:css-analysis",
      "start": 1783626305084,
      "end": 1783626305084,
      "order": "normal"
    },
    {
      "name": "vite:import-analysis",
      "result": "import { liquidMap } from \"/src/composables/chemistry/useChemistryLab.ts\";\nimport { findEquation, isAcid, isBase, isIndicator, mixColor, calculateTitrationPh } from \"/@fs/C:/Users/feras/Desktop/feras/packages/chemistry-engine/src/index.ts\";\nexport { calculateTitrationPh } from \"/@fs/C:/Users/feras/Desktop/feras/packages/chemistry-engine/src/index.ts\";\nexport function getIndicatorColor(indicatorId, ph) {\n  switch (indicatorId) {\n    case \"phenolphthalein\":\n      return ph > 8.2 ? \"#ec4899\" : \"#fdf4ff\";\n    case \"methyl-orange\":\n      return ph < 3.1 ? \"#ef4444\" : ph > 4.4 ? \"#fbbf24\" : \"#fb923c\";\n    case \"bromothymol-blue\":\n      return ph > 7.6 ? \"#3b82f6\" : ph < 6 ? \"#facc15\" : \"#22c55e\";\n    case \"universal-indicator\": {\n      if (ph < 3) return \"#ef4444\";\n      if (ph < 6) return \"#fb923c\";\n      if (ph < 7) return \"#facc15\";\n      if (ph < 8) return \"#22c55e\";\n      if (ph < 10) return \"#3b82f6\";\n      return \"#7c3aed\";\n    }\n    case \"starch\":\n      return ph > 6 ? \"#1e3a8a\" : \"#fefce8\";\n    default:\n      return \"#e0f2fe\";\n  }\n}\nexport function applyIndicatorsToContainer(liq) {\n  if (!liq.indicators || liq.indicators.length === 0) return;\n  const currentPh = liq.ph ?? 7;\n  const base = liq.baseColor || liq.color;\n  for (const indId of liq.indicators) {\n    const indColor = getIndicatorColor(indId, currentPh);\n    liq.color = mixColor(base, liq.volume, indColor, liq.volume);\n  }\n}\nexport function applyIndicator(indicatorId, containerUid) {\n  const liq = liquidMap[containerUid];\n  if (!liq || liq.volume <= 0) return;\n  const currentPh = liq.ph ?? 7;\n  const indicatorColor = getIndicatorColor(indicatorId, currentPh);\n  const base = liq.baseColor || liq.color;\n  liq.color = mixColor(base, liq.volume, indicatorColor, liq.volume);\n  liq.opacity = Math.min(1, liq.opacity + 0.15);\n}\nexport function handleDropMix(event) {\n  const target = liquidMap[event.targetUid];\n  if (!target) return;\n  const src = event.sourceChemicalId;\n  const tgt = target.chemicalId || \"\";\n  if (isIndicator(src)) {\n    if (!target.indicators) target.indicators = [];\n    if (!target.indicators.includes(src)) {\n      target.indicators.push(src);\n    }\n    applyIndicator(src, event.targetUid);\n    return;\n  }\n  if (isBase(src) && isAcid(tgt) || isAcid(src) && isBase(tgt)) {\n    if (!target.reactants) target.reactants = {};\n    if (!target.reactants[tgt]) {\n      const trackedVol = Object.values(target.reactants).reduce((s, v) => s + v, 0);\n      target.reactants[tgt] = Math.max(0, target.volume - event.dropVolume - trackedVol);\n    }\n    target.reactants[src] = (target.reactants[src] || 0) + event.dropVolume;\n    let acidVol = 0, baseVol = 0;\n    let acidId = \"\", baseId = \"\";\n    for (const [chemId, vol] of Object.entries(target.reactants)) {\n      if (isAcid(chemId)) {\n        acidVol += vol;\n        acidId = chemId;\n      }\n      if (isBase(chemId)) {\n        baseVol += vol;\n        baseId = chemId;\n      }\n    }\n    const newPh = calculateTitrationPh(acidVol, acidId, baseVol, baseId);\n    target.ph = newPh;\n    target.temperature = Math.min(100, target.temperature + 0.5);\n    const eq2 = findEquation([acidId, baseId].filter(Boolean));\n    if (eq2) {\n      target.equation = eq2.equation;\n      target.precipitate = eq2.precipitate || false;\n      target.gasEvolution = eq2.gasEvolution || false;\n    }\n    applyIndicatorsToContainer(target);\n    return;\n  }\n  const allReactants = [src, tgt].filter(Boolean);\n  const eq = findEquation(allReactants);\n  if (eq) {\n    target.equation = eq.equation;\n    if (!target.reactants) target.reactants = {};\n    target.reactants[src] = (target.reactants[src] || 0) + event.dropVolume;\n    target.reactants[tgt] = (target.reactants[tgt] || 0) + (target.volume - event.dropVolume);\n    if (eq.resultPh !== void 0) target.ph = eq.resultPh;\n    target.color = eq.color;\n    target.opacity = eq.opacity;\n    target.label = `REACTION:${eq.equation}`;\n    target.temperature = Math.min(100, target.temperature + eq.temperatureRise);\n    target.precipitate = eq.precipitate || false;\n    target.gasEvolution = eq.gasEvolution || false;\n    if (eq.precipitate) {\n      target.opacity = Math.min(1, target.opacity + 0.2);\n    }\n    applyIndicatorsToContainer(target);\n    return;\n  }\n  if (!target.reactants) target.reactants = {};\n  target.reactants[src] = (target.reactants[src] || 0) + event.dropVolume;\n}\n",
      "start": 1783626305084,
      "end": 1783626305098,
      "order": "normal",
      "sourcemaps": null
    }
  ]
}

{
  "resolvedId": "C:/Users/feras/Desktop/feras/apps/web/src/composables/spring/useSpringScene.ts",
  "transforms": [
    {
      "name": "__load__",
      "result": "import type { SpringParams } from '../../modules/physics/experiments/spring/useSpringPhysics'\n\ninterface SimState {\n  x: number; v: number; t: number; running: boolean; paused: boolean\n}\n\nexport function drawSpringScene(\n  ctx: CanvasRenderingContext2D,\n  w: number,\n  h: number,\n  params: SpringParams,\n  simState: SimState,\n) {\n  const pivotX = w / 2\n  const pivotY = 45\n  const hasMass = params.mass > 1e-6\n\n  const springTop = pivotY + 12\n  const natLenPx = 50\n  const natEqY = springTop + natLenPx\n\n  const staticStretchCm = hasMass && params.k > 1e-9\n    ? (params.mass * 9.81 / params.k) * 100\n    : 0\n\n  const basePxPerCm = 8\n  const pxPerCm = (natEqY + staticStretchCm * basePxPerCm > h * 0.75 && staticStretchCm > 0.001)\n    ? Math.max(4, (h * 0.75 - natEqY) / staticStretchCm)\n    : basePxPerCm\n\n  const maxTravel = h * 0.22\n  const peakDisp = Math.max(params.amplitude, Math.abs(simState.x), 0.01)\n  const oscillationScale = Math.min(maxTravel / peakDisp, pxPerCm * 100)\n\n  const staticStretchPx = staticStretchCm * pxPerCm\n  const eqY = natEqY + staticStretchPx\n  const massY = eqY + simState.x * oscillationScale\n  const massRadius = hasMass\n    ? Math.max(14, Math.min(38, 12 + params.mass * 5.2))\n    : 0\n\n  // Background grid\n  ctx.strokeStyle = 'rgba(148,163,184,0.2)'\n  ctx.lineWidth = 1\n  for (let i = 0; i < w; i += 40) { ctx.beginPath(); ctx.moveTo(i, 0); ctx.lineTo(i, h); ctx.stroke() }\n  for (let i = 0; i < h; i += 40) { ctx.beginPath(); ctx.moveTo(0, i); ctx.lineTo(w, i); ctx.stroke() }\n\n  // Pivot\n  ctx.fillStyle = '#1e293b'\n  ctx.fillRect(pivotX - 10, pivotY - 2, 20, 14)\n  ctx.strokeStyle = '#64748b'; ctx.lineWidth = 1\n  ctx.strokeRect(pivotX - 10, pivotY - 2, 20, 14)\n\n  // Spring\n  const springBot = hasMass ? massY - massRadius : massY\n  const kNorm = Math.max(0, Math.min(1, (params.k - 5) / (200 - 5)))\n  const coils = Math.max(10, Math.min(28, Math.round(22 - kNorm * 8 + params.mass * 1.5)))\n  const coilWidth = Math.max(8, Math.min(24, 10 + (1 - kNorm) * 8 + params.mass * 1.5))\n  const segLen = Math.max(1.5, (springBot - springTop - 8) / coils)\n\n  const springGrad = ctx.createLinearGradient(pivotX - coilWidth, 0, pivotX + coilWidth, 0)\n  springGrad.addColorStop(0, `rgb(${50 + kNorm * 15},${55 + kNorm * 15},${70 + kNorm * 10})`)\n  springGrad.addColorStop(0.5, `rgb(${90 + kNorm * 25},${95 + kNorm * 25},${110 + kNorm * 15})`)\n  springGrad.addColorStop(1, `rgb(${50 + kNorm * 15},${55 + kNorm * 15},${70 + kNorm * 10})`)\n\n  ctx.beginPath()\n  ctx.moveTo(pivotX, springTop + 4)\n  for (let i = 1; i <= coils; i++) {\n    const cy = springTop + 4 + i * segLen\n    const cx = pivotX + (i % 2 === 0 ? -coilWidth : coilWidth)\n    ctx.lineTo(cx, cy)\n  }\n  ctx.lineTo(pivotX, springBot)\n  ctx.lineWidth = 1.8 + kNorm * 2.2\n  ctx.strokeStyle = springGrad\n  ctx.lineCap = 'round'\n  ctx.lineJoin = 'round'\n  ctx.stroke()\n  ctx.lineCap = 'butt'\n  ctx.lineJoin = 'miter'\n\n  // Mass\n  if (hasMass) {\n    ctx.fillStyle = 'rgba(15,23,42,0.1)'\n    ctx.beginPath()\n    ctx.ellipse(pivotX + 6, massY + massRadius + 6, massRadius * 1.1, massRadius * 0.35, 0, 0, Math.PI * 2)\n    ctx.fill()\n\n    const massGrad = ctx.createRadialGradient(pivotX - massRadius * 0.3, massY - massRadius * 0.3, 3, pivotX, massY, massRadius * 1.1)\n    massGrad.addColorStop(0, '#fca5a5')\n    massGrad.addColorStop(0.5, '#e11d48')\n    massGrad.addColorStop(1, '#7f1d1d')\n\n    ctx.fillStyle = massGrad\n    ctx.beginPath(); ctx.arc(pivotX, massY, massRadius, 0, 2 * Math.PI); ctx.fill()\n    ctx.strokeStyle = '#7f1d1d'; ctx.lineWidth = 1.5; ctx.stroke()\n\n    ctx.fillStyle = '#fff'\n    ctx.font = `bold ${Math.max(11, Math.min(16, Math.round(massRadius * 0.6)))}px Arial`\n    ctx.textAlign = 'center'\n    ctx.textBaseline = 'middle'\n    ctx.fillText(params.mass.toFixed(2), pivotX, massY - 3)\n    ctx.font = '9px Arial'\n    ctx.fillText('kg', pivotX, massY + 9)\n    ctx.textAlign = 'start'\n    ctx.textBaseline = 'alphabetic'\n\n    const trayW = massRadius * 2.5\n    const trayH = 8\n    ctx.fillStyle = '#64748b'\n    ctx.fillRect(pivotX - trayW / 2, massY + massRadius, trayW, trayH)\n    ctx.strokeStyle = '#475569'; ctx.lineWidth = 1\n    ctx.strokeRect(pivotX - trayW / 2, massY + massRadius, trayW, trayH)\n\n    const numWeights = Math.min(10, Math.max(0, Math.round(params.mass * 2)))\n    for (let i = 0; i < numWeights; i++) {\n      const wy = massY + massRadius - 3 - i * 3\n      ctx.fillStyle = i % 2 === 0 ? '#3b82f6' : '#60a5fa'\n      ctx.beginPath(); ctx.arc(pivotX, wy, 4, 0, 2 * Math.PI); ctx.fill()\n    }\n  }\n\n  // Reference lines\n  ctx.strokeStyle = 'rgba(59,130,246,0.4)'\n  ctx.setLineDash([5, 4])\n  ctx.beginPath(); ctx.moveTo(pivotX - 60, natEqY); ctx.lineTo(pivotX + 60, natEqY); ctx.stroke()\n  ctx.setLineDash([])\n\n  if (hasMass) {\n    ctx.strokeStyle = 'rgba(34,197,94,0.5)'\n    ctx.setLineDash([5, 4])\n    ctx.beginPath(); ctx.moveTo(pivotX - 60, eqY); ctx.lineTo(pivotX + 60, eqY); ctx.stroke()\n    ctx.setLineDash([])\n  }\n\n  // Ruler\n  const rulerLeft = pivotX + 50\n  const rulerTop = pivotY + 10\n  const rulerBot = h - 20\n  const zeroY = natEqY\n\n  ctx.fillStyle = 'rgba(241,245,249,0.3)'\n  ctx.fillRect(rulerLeft - 2, rulerTop, 22, rulerBot - rulerTop)\n  ctx.strokeStyle = 'rgba(148,163,184,0.4)'; ctx.lineWidth = 1\n  ctx.strokeRect(rulerLeft - 2, rulerTop, 22, rulerBot - rulerTop)\n\n  const pxPerMmRuler = pxPerCm / 10\n\n  const maxCmDown = Math.floor((rulerBot - zeroY) / pxPerCm)\n  for (let cm = 0; cm <= maxCmDown; cm++) {\n    const ry = zeroY + cm * pxPerCm\n    ctx.beginPath(); ctx.moveTo(rulerLeft, ry); ctx.lineTo(rulerLeft + 20, ry); ctx.stroke()\n\n    if (cm < maxCmDown) {\n      for (let mm = 1; mm < 10; mm++) {\n        const mmy = ry + mm * pxPerMmRuler\n        const mTickLen = mm === 5 ? 12 : 6\n        ctx.beginPath(); ctx.moveTo(rulerLeft, mmy); ctx.lineTo(rulerLeft + mTickLen, mmy); ctx.stroke()\n      }\n    }\n  }\n\n  const maxCmUp = Math.floor((zeroY - rulerTop) / pxPerCm)\n  for (let cm = 1; cm <= maxCmUp; cm++) {\n    const ry = zeroY - cm * pxPerCm\n    ctx.beginPath(); ctx.moveTo(rulerLeft, ry); ctx.lineTo(rulerLeft + 20, ry); ctx.stroke()\n\n    if (cm < maxCmUp || cm === maxCmUp) {\n      for (let mm = 1; mm < 10; mm++) {\n        const mmy = ry + mm * pxPerMmRuler\n        if (mmy > rulerBot) break\n        const mTickLen = mm === 5 ? 12 : 6\n        ctx.beginPath(); ctx.moveTo(rulerLeft, mmy); ctx.lineTo(rulerLeft + mTickLen, mmy); ctx.stroke()\n      }\n    }\n  }\n\n  // Readouts\n  const readoutX = rulerLeft + 28\n\n  if (hasMass) {\n    ctx.strokeStyle = '#dc2626'; ctx.lineWidth = 1.5\n    ctx.setLineDash([3, 3])\n    ctx.beginPath(); ctx.moveTo(readoutX, massY); ctx.lineTo(pivotX + massRadius + 5, massY); ctx.stroke()\n    ctx.setLineDash([])\n  }\n\n  ctx.fillStyle = 'rgba(59,130,246,0.12)'\n  ctx.fillRect(readoutX, natEqY - 10, 52, 20)\n  ctx.strokeStyle = 'rgba(59,130,246,0.4)'; ctx.lineWidth = 1\n  ctx.strokeRect(readoutX, natEqY - 10, 52, 20)\n  ctx.fillStyle = '#3b82f6'; ctx.font = 'bold 11px \"Courier New\",monospace'\n  ctx.textAlign = 'center'\n  ctx.fillText('0 cm', readoutX + 26, natEqY + 4)\n\n  if (hasMass) {\n    const dyStr = staticStretchCm.toFixed(2)\n    ctx.fillStyle = 'rgba(34,197,94,0.12)'\n    ctx.fillRect(readoutX, eqY - 10, 52, 20)\n    ctx.strokeStyle = 'rgba(34,197,94,0.4)'\n    ctx.strokeRect(readoutX, eqY - 10, 52, 20)\n    ctx.fillStyle = '#22c55e'; ctx.font = 'bold 11px \"Courier New\",monospace'\n    ctx.fillText(`${dyStr} cm`, readoutX + 26, eqY + 4)\n\n    const yCm = (staticStretchCm + simState.x * 100).toFixed(2)\n    ctx.fillStyle = 'rgba(220,38,38,0.12)'\n    ctx.fillRect(readoutX, massY - 10, 52, 20)\n    ctx.strokeStyle = 'rgba(220,38,38,0.4)'\n    ctx.strokeRect(readoutX, massY - 10, 52, 20)\n    ctx.fillStyle = '#dc2626'; ctx.font = 'bold 11px \"Courier New\",monospace'\n    ctx.fillText(`${yCm} cm`, readoutX + 26, massY + 4)\n  }\n\n  ctx.textAlign = 'start'\n\n  // Velocity arrow\n  if (Math.abs(simState.v) > 0.01) {\n    const vScale = 30\n    const vDir = simState.v > 0 ? 1 : -1\n    const vLen = Math.min(60, Math.abs(simState.v) * vScale)\n    const arrowY = massY + vDir * (massRadius + 10)\n    const tipY = arrowY + vDir * vLen\n    ctx.strokeStyle = '#2563eb'; ctx.lineWidth = 2\n    ctx.beginPath(); ctx.moveTo(pivotX + massRadius + 12, arrowY); ctx.lineTo(pivotX + massRadius + 12, tipY); ctx.stroke()\n    ctx.fillStyle = '#2563eb'\n    ctx.beginPath()\n    ctx.moveTo(pivotX + massRadius + 12, tipY)\n    ctx.lineTo(pivotX + massRadius + 12 - 4, tipY - vDir * 8)\n    ctx.lineTo(pivotX + massRadius + 12 + 4, tipY - vDir * 8)\n    ctx.fill()\n  }\n\n  return { massY, natEqY, staticStretchCm, pxPerCm }\n}\n",
      "start": 1783635315868,
      "end": 1783635315868
    },
    {
      "name": "vite-plugin-vue-inspector",
      "start": 1783635315868,
      "end": 1783635315868,
      "order": "pre"
    },
    {
      "name": "vite-plugin-vue-devtools",
      "result": "import type { SpringParams } from '../../modules/physics/experiments/spring/useSpringPhysics'\n\ninterface SimState {\n  x: number; v: number; t: number; running: boolean; paused: boolean\n}\n\nexport function drawSpringScene(\n  ctx: CanvasRenderingContext2D,\n  w: number,\n  h: number,\n  params: SpringParams,\n  simState: SimState,\n) {\n  const pivotX = w / 2\n  const pivotY = 45\n  const hasMass = params.mass > 1e-6\n\n  const springTop = pivotY + 12\n  const natLenPx = 50\n  const natEqY = springTop + natLenPx\n\n  const staticStretchCm = hasMass && params.k > 1e-9\n    ? (params.mass * 9.81 / params.k) * 100\n    : 0\n\n  const basePxPerCm = 8\n  const pxPerCm = (natEqY + staticStretchCm * basePxPerCm > h * 0.75 && staticStretchCm > 0.001)\n    ? Math.max(4, (h * 0.75 - natEqY) / staticStretchCm)\n    : basePxPerCm\n\n  const maxTravel = h * 0.22\n  const peakDisp = Math.max(params.amplitude, Math.abs(simState.x), 0.01)\n  const oscillationScale = Math.min(maxTravel / peakDisp, pxPerCm * 100)\n\n  const staticStretchPx = staticStretchCm * pxPerCm\n  const eqY = natEqY + staticStretchPx\n  const massY = eqY + simState.x * oscillationScale\n  const massRadius = hasMass\n    ? Math.max(14, Math.min(38, 12 + params.mass * 5.2))\n    : 0\n\n  // Background grid\n  ctx.strokeStyle = 'rgba(148,163,184,0.2)'\n  ctx.lineWidth = 1\n  for (let i = 0; i < w; i += 40) { ctx.beginPath(); ctx.moveTo(i, 0); ctx.lineTo(i, h); ctx.stroke() }\n  for (let i = 0; i < h; i += 40) { ctx.beginPath(); ctx.moveTo(0, i); ctx.lineTo(w, i); ctx.stroke() }\n\n  // Pivot\n  ctx.fillStyle = '#1e293b'\n  ctx.fillRect(pivotX - 10, pivotY - 2, 20, 14)\n  ctx.strokeStyle = '#64748b'; ctx.lineWidth = 1\n  ctx.strokeRect(pivotX - 10, pivotY - 2, 20, 14)\n\n  // Spring\n  const springBot = hasMass ? massY - massRadius : massY\n  const kNorm = Math.max(0, Math.min(1, (params.k - 5) / (200 - 5)))\n  const coils = Math.max(10, Math.min(28, Math.round(22 - kNorm * 8 + params.mass * 1.5)))\n  const coilWidth = Math.max(8, Math.min(24, 10 + (1 - kNorm) * 8 + params.mass * 1.5))\n  const segLen = Math.max(1.5, (springBot - springTop - 8) / coils)\n\n  const springGrad = ctx.createLinearGradient(pivotX - coilWidth, 0, pivotX + coilWidth, 0)\n  springGrad.addColorStop(0, `rgb(${50 + kNorm * 15},${55 + kNorm * 15},${70 + kNorm * 10})`)\n  springGrad.addColorStop(0.5, `rgb(${90 + kNorm * 25},${95 + kNorm * 25},${110 + kNorm * 15})`)\n  springGrad.addColorStop(1, `rgb(${50 + kNorm * 15},${55 + kNorm * 15},${70 + kNorm * 10})`)\n\n  ctx.beginPath()\n  ctx.moveTo(pivotX, springTop + 4)\n  for (let i = 1; i <= coils; i++) {\n    const cy = springTop + 4 + i * segLen\n    const cx = pivotX + (i % 2 === 0 ? -coilWidth : coilWidth)\n    ctx.lineTo(cx, cy)\n  }\n  ctx.lineTo(pivotX, springBot)\n  ctx.lineWidth = 1.8 + kNorm * 2.2\n  ctx.strokeStyle = springGrad\n  ctx.lineCap = 'round'\n  ctx.lineJoin = 'round'\n  ctx.stroke()\n  ctx.lineCap = 'butt'\n  ctx.lineJoin = 'miter'\n\n  // Mass\n  if (hasMass) {\n    ctx.fillStyle = 'rgba(15,23,42,0.1)'\n    ctx.beginPath()\n    ctx.ellipse(pivotX + 6, massY + massRadius + 6, massRadius * 1.1, massRadius * 0.35, 0, 0, Math.PI * 2)\n    ctx.fill()\n\n    const massGrad = ctx.createRadialGradient(pivotX - massRadius * 0.3, massY - massRadius * 0.3, 3, pivotX, massY, massRadius * 1.1)\n    massGrad.addColorStop(0, '#fca5a5')\n    massGrad.addColorStop(0.5, '#e11d48')\n    massGrad.addColorStop(1, '#7f1d1d')\n\n    ctx.fillStyle = massGrad\n    ctx.beginPath(); ctx.arc(pivotX, massY, massRadius, 0, 2 * Math.PI); ctx.fill()\n    ctx.strokeStyle = '#7f1d1d'; ctx.lineWidth = 1.5; ctx.stroke()\n\n    ctx.fillStyle = '#fff'\n    ctx.font = `bold ${Math.max(11, Math.min(16, Math.round(massRadius * 0.6)))}px Arial`\n    ctx.textAlign = 'center'\n    ctx.textBaseline = 'middle'\n    ctx.fillText(params.mass.toFixed(2), pivotX, massY - 3)\n    ctx.font = '9px Arial'\n    ctx.fillText('kg', pivotX, massY + 9)\n    ctx.textAlign = 'start'\n    ctx.textBaseline = 'alphabetic'\n\n    const trayW = massRadius * 2.5\n    const trayH = 8\n    ctx.fillStyle = '#64748b'\n    ctx.fillRect(pivotX - trayW / 2, massY + massRadius, trayW, trayH)\n    ctx.strokeStyle = '#475569'; ctx.lineWidth = 1\n    ctx.strokeRect(pivotX - trayW / 2, massY + massRadius, trayW, trayH)\n\n    const numWeights = Math.min(10, Math.max(0, Math.round(params.mass * 2)))\n    for (let i = 0; i < numWeights; i++) {\n      const wy = massY + massRadius - 3 - i * 3\n      ctx.fillStyle = i % 2 === 0 ? '#3b82f6' : '#60a5fa'\n      ctx.beginPath(); ctx.arc(pivotX, wy, 4, 0, 2 * Math.PI); ctx.fill()\n    }\n  }\n\n  // Reference lines\n  ctx.strokeStyle = 'rgba(59,130,246,0.4)'\n  ctx.setLineDash([5, 4])\n  ctx.beginPath(); ctx.moveTo(pivotX - 60, natEqY); ctx.lineTo(pivotX + 60, natEqY); ctx.stroke()\n  ctx.setLineDash([])\n\n  if (hasMass) {\n    ctx.strokeStyle = 'rgba(34,197,94,0.5)'\n    ctx.setLineDash([5, 4])\n    ctx.beginPath(); ctx.moveTo(pivotX - 60, eqY); ctx.lineTo(pivotX + 60, eqY); ctx.stroke()\n    ctx.setLineDash([])\n  }\n\n  // Ruler\n  const rulerLeft = pivotX + 50\n  const rulerTop = pivotY + 10\n  const rulerBot = h - 20\n  const zeroY = natEqY\n\n  ctx.fillStyle = 'rgba(241,245,249,0.3)'\n  ctx.fillRect(rulerLeft - 2, rulerTop, 22, rulerBot - rulerTop)\n  ctx.strokeStyle = 'rgba(148,163,184,0.4)'; ctx.lineWidth = 1\n  ctx.strokeRect(rulerLeft - 2, rulerTop, 22, rulerBot - rulerTop)\n\n  const pxPerMmRuler = pxPerCm / 10\n\n  const maxCmDown = Math.floor((rulerBot - zeroY) / pxPerCm)\n  for (let cm = 0; cm <= maxCmDown; cm++) {\n    const ry = zeroY + cm * pxPerCm\n    ctx.beginPath(); ctx.moveTo(rulerLeft, ry); ctx.lineTo(rulerLeft + 20, ry); ctx.stroke()\n\n    if (cm < maxCmDown) {\n      for (let mm = 1; mm < 10; mm++) {\n        const mmy = ry + mm * pxPerMmRuler\n        const mTickLen = mm === 5 ? 12 : 6\n        ctx.beginPath(); ctx.moveTo(rulerLeft, mmy); ctx.lineTo(rulerLeft + mTickLen, mmy); ctx.stroke()\n      }\n    }\n  }\n\n  const maxCmUp = Math.floor((zeroY - rulerTop) / pxPerCm)\n  for (let cm = 1; cm <= maxCmUp; cm++) {\n    const ry = zeroY - cm * pxPerCm\n    ctx.beginPath(); ctx.moveTo(rulerLeft, ry); ctx.lineTo(rulerLeft + 20, ry); ctx.stroke()\n\n    if (cm < maxCmUp || cm === maxCmUp) {\n      for (let mm = 1; mm < 10; mm++) {\n        const mmy = ry + mm * pxPerMmRuler\n        if (mmy > rulerBot) break\n        const mTickLen = mm === 5 ? 12 : 6\n        ctx.beginPath(); ctx.moveTo(rulerLeft, mmy); ctx.lineTo(rulerLeft + mTickLen, mmy); ctx.stroke()\n      }\n    }\n  }\n\n  // Readouts\n  const readoutX = rulerLeft + 28\n\n  if (hasMass) {\n    ctx.strokeStyle = '#dc2626'; ctx.lineWidth = 1.5\n    ctx.setLineDash([3, 3])\n    ctx.beginPath(); ctx.moveTo(readoutX, massY); ctx.lineTo(pivotX + massRadius + 5, massY); ctx.stroke()\n    ctx.setLineDash([])\n  }\n\n  ctx.fillStyle = 'rgba(59,130,246,0.12)'\n  ctx.fillRect(readoutX, natEqY - 10, 52, 20)\n  ctx.strokeStyle = 'rgba(59,130,246,0.4)'; ctx.lineWidth = 1\n  ctx.strokeRect(readoutX, natEqY - 10, 52, 20)\n  ctx.fillStyle = '#3b82f6'; ctx.font = 'bold 11px \"Courier New\",monospace'\n  ctx.textAlign = 'center'\n  ctx.fillText('0 cm', readoutX + 26, natEqY + 4)\n\n  if (hasMass) {\n    const dyStr = staticStretchCm.toFixed(2)\n    ctx.fillStyle = 'rgba(34,197,94,0.12)'\n    ctx.fillRect(readoutX, eqY - 10, 52, 20)\n    ctx.strokeStyle = 'rgba(34,197,94,0.4)'\n    ctx.strokeRect(readoutX, eqY - 10, 52, 20)\n    ctx.fillStyle = '#22c55e'; ctx.font = 'bold 11px \"Courier New\",monospace'\n    ctx.fillText(`${dyStr} cm`, readoutX + 26, eqY + 4)\n\n    const yCm = (staticStretchCm + simState.x * 100).toFixed(2)\n    ctx.fillStyle = 'rgba(220,38,38,0.12)'\n    ctx.fillRect(readoutX, massY - 10, 52, 20)\n    ctx.strokeStyle = 'rgba(220,38,38,0.4)'\n    ctx.strokeRect(readoutX, massY - 10, 52, 20)\n    ctx.fillStyle = '#dc2626'; ctx.font = 'bold 11px \"Courier New\",monospace'\n    ctx.fillText(`${yCm} cm`, readoutX + 26, massY + 4)\n  }\n\n  ctx.textAlign = 'start'\n\n  // Velocity arrow\n  if (Math.abs(simState.v) > 0.01) {\n    const vScale = 30\n    const vDir = simState.v > 0 ? 1 : -1\n    const vLen = Math.min(60, Math.abs(simState.v) * vScale)\n    const arrowY = massY + vDir * (massRadius + 10)\n    const tipY = arrowY + vDir * vLen\n    ctx.strokeStyle = '#2563eb'; ctx.lineWidth = 2\n    ctx.beginPath(); ctx.moveTo(pivotX + massRadius + 12, arrowY); ctx.lineTo(pivotX + massRadius + 12, tipY); ctx.stroke()\n    ctx.fillStyle = '#2563eb'\n    ctx.beginPath()\n    ctx.moveTo(pivotX + massRadius + 12, tipY)\n    ctx.lineTo(pivotX + massRadius + 12 - 4, tipY - vDir * 8)\n    ctx.lineTo(pivotX + massRadius + 12 + 4, tipY - vDir * 8)\n    ctx.fill()\n  }\n\n  return { massY, natEqY, staticStretchCm, pxPerCm }\n}\n",
      "start": 1783635315868,
      "end": 1783635315868,
      "order": "pre",
      "sourcemaps": null
    },
    {
      "name": "vite:css",
      "start": 1783635315868,
      "end": 1783635315868,
      "order": "normal"
    },
    {
      "name": "vite:esbuild",
      "result": "export function drawSpringScene(ctx, w, h, params, simState) {\n  const pivotX = w / 2;\n  const pivotY = 45;\n  const hasMass = params.mass > 1e-6;\n  const springTop = pivotY + 12;\n  const natLenPx = 50;\n  const natEqY = springTop + natLenPx;\n  const staticStretchCm = hasMass && params.k > 1e-9 ? params.mass * 9.81 / params.k * 100 : 0;\n  const basePxPerCm = 8;\n  const pxPerCm = natEqY + staticStretchCm * basePxPerCm > h * 0.75 && staticStretchCm > 1e-3 ? Math.max(4, (h * 0.75 - natEqY) / staticStretchCm) : basePxPerCm;\n  const maxTravel = h * 0.22;\n  const peakDisp = Math.max(params.amplitude, Math.abs(simState.x), 0.01);\n  const oscillationScale = Math.min(maxTravel / peakDisp, pxPerCm * 100);\n  const staticStretchPx = staticStretchCm * pxPerCm;\n  const eqY = natEqY + staticStretchPx;\n  const massY = eqY + simState.x * oscillationScale;\n  const massRadius = hasMass ? Math.max(14, Math.min(38, 12 + params.mass * 5.2)) : 0;\n  ctx.strokeStyle = \"rgba(148,163,184,0.2)\";\n  ctx.lineWidth = 1;\n  for (let i = 0; i < w; i += 40) {\n    ctx.beginPath();\n    ctx.moveTo(i, 0);\n    ctx.lineTo(i, h);\n    ctx.stroke();\n  }\n  for (let i = 0; i < h; i += 40) {\n    ctx.beginPath();\n    ctx.moveTo(0, i);\n    ctx.lineTo(w, i);\n    ctx.stroke();\n  }\n  ctx.fillStyle = \"#1e293b\";\n  ctx.fillRect(pivotX - 10, pivotY - 2, 20, 14);\n  ctx.strokeStyle = \"#64748b\";\n  ctx.lineWidth = 1;\n  ctx.strokeRect(pivotX - 10, pivotY - 2, 20, 14);\n  const springBot = hasMass ? massY - massRadius : massY;\n  const kNorm = Math.max(0, Math.min(1, (params.k - 5) / (200 - 5)));\n  const coils = Math.max(10, Math.min(28, Math.round(22 - kNorm * 8 + params.mass * 1.5)));\n  const coilWidth = Math.max(8, Math.min(24, 10 + (1 - kNorm) * 8 + params.mass * 1.5));\n  const segLen = Math.max(1.5, (springBot - springTop - 8) / coils);\n  const springGrad = ctx.createLinearGradient(pivotX - coilWidth, 0, pivotX + coilWidth, 0);\n  springGrad.addColorStop(0, `rgb(${50 + kNorm * 15},${55 + kNorm * 15},${70 + kNorm * 10})`);\n  springGrad.addColorStop(0.5, `rgb(${90 + kNorm * 25},${95 + kNorm * 25},${110 + kNorm * 15})`);\n  springGrad.addColorStop(1, `rgb(${50 + kNorm * 15},${55 + kNorm * 15},${70 + kNorm * 10})`);\n  ctx.beginPath();\n  ctx.moveTo(pivotX, springTop + 4);\n  for (let i = 1; i <= coils; i++) {\n    const cy = springTop + 4 + i * segLen;\n    const cx = pivotX + (i % 2 === 0 ? -coilWidth : coilWidth);\n    ctx.lineTo(cx, cy);\n  }\n  ctx.lineTo(pivotX, springBot);\n  ctx.lineWidth = 1.8 + kNorm * 2.2;\n  ctx.strokeStyle = springGrad;\n  ctx.lineCap = \"round\";\n  ctx.lineJoin = \"round\";\n  ctx.stroke();\n  ctx.lineCap = \"butt\";\n  ctx.lineJoin = \"miter\";\n  if (hasMass) {\n    ctx.fillStyle = \"rgba(15,23,42,0.1)\";\n    ctx.beginPath();\n    ctx.ellipse(pivotX + 6, massY + massRadius + 6, massRadius * 1.1, massRadius * 0.35, 0, 0, Math.PI * 2);\n    ctx.fill();\n    const massGrad = ctx.createRadialGradient(pivotX - massRadius * 0.3, massY - massRadius * 0.3, 3, pivotX, massY, massRadius * 1.1);\n    massGrad.addColorStop(0, \"#fca5a5\");\n    massGrad.addColorStop(0.5, \"#e11d48\");\n    massGrad.addColorStop(1, \"#7f1d1d\");\n    ctx.fillStyle = massGrad;\n    ctx.beginPath();\n    ctx.arc(pivotX, massY, massRadius, 0, 2 * Math.PI);\n    ctx.fill();\n    ctx.strokeStyle = \"#7f1d1d\";\n    ctx.lineWidth = 1.5;\n    ctx.stroke();\n    ctx.fillStyle = \"#fff\";\n    ctx.font = `bold ${Math.max(11, Math.min(16, Math.round(massRadius * 0.6)))}px Arial`;\n    ctx.textAlign = \"center\";\n    ctx.textBaseline = \"middle\";\n    ctx.fillText(params.mass.toFixed(2), pivotX, massY - 3);\n    ctx.font = \"9px Arial\";\n    ctx.fillText(\"kg\", pivotX, massY + 9);\n    ctx.textAlign = \"start\";\n    ctx.textBaseline = \"alphabetic\";\n    const trayW = massRadius * 2.5;\n    const trayH = 8;\n    ctx.fillStyle = \"#64748b\";\n    ctx.fillRect(pivotX - trayW / 2, massY + massRadius, trayW, trayH);\n    ctx.strokeStyle = \"#475569\";\n    ctx.lineWidth = 1;\n    ctx.strokeRect(pivotX - trayW / 2, massY + massRadius, trayW, trayH);\n    const numWeights = Math.min(10, Math.max(0, Math.round(params.mass * 2)));\n    for (let i = 0; i < numWeights; i++) {\n      const wy = massY + massRadius - 3 - i * 3;\n      ctx.fillStyle = i % 2 === 0 ? \"#3b82f6\" : \"#60a5fa\";\n      ctx.beginPath();\n      ctx.arc(pivotX, wy, 4, 0, 2 * Math.PI);\n      ctx.fill();\n    }\n  }\n  ctx.strokeStyle = \"rgba(59,130,246,0.4)\";\n  ctx.setLineDash([5, 4]);\n  ctx.beginPath();\n  ctx.moveTo(pivotX - 60, natEqY);\n  ctx.lineTo(pivotX + 60, natEqY);\n  ctx.stroke();\n  ctx.setLineDash([]);\n  if (hasMass) {\n    ctx.strokeStyle = \"rgba(34,197,94,0.5)\";\n    ctx.setLineDash([5, 4]);\n    ctx.beginPath();\n    ctx.moveTo(pivotX - 60, eqY);\n    ctx.lineTo(pivotX + 60, eqY);\n    ctx.stroke();\n    ctx.setLineDash([]);\n  }\n  const rulerLeft = pivotX + 50;\n  const rulerTop = pivotY + 10;\n  const rulerBot = h - 20;\n  const zeroY = natEqY;\n  ctx.fillStyle = \"rgba(241,245,249,0.3)\";\n  ctx.fillRect(rulerLeft - 2, rulerTop, 22, rulerBot - rulerTop);\n  ctx.strokeStyle = \"rgba(148,163,184,0.4)\";\n  ctx.lineWidth = 1;\n  ctx.strokeRect(rulerLeft - 2, rulerTop, 22, rulerBot - rulerTop);\n  const pxPerMmRuler = pxPerCm / 10;\n  const maxCmDown = Math.floor((rulerBot - zeroY) / pxPerCm);\n  for (let cm = 0; cm <= maxCmDown; cm++) {\n    const ry = zeroY + cm * pxPerCm;\n    ctx.beginPath();\n    ctx.moveTo(rulerLeft, ry);\n    ctx.lineTo(rulerLeft + 20, ry);\n    ctx.stroke();\n    if (cm < maxCmDown) {\n      for (let mm = 1; mm < 10; mm++) {\n        const mmy = ry + mm * pxPerMmRuler;\n        const mTickLen = mm === 5 ? 12 : 6;\n        ctx.beginPath();\n        ctx.moveTo(rulerLeft, mmy);\n        ctx.lineTo(rulerLeft + mTickLen, mmy);\n        ctx.stroke();\n      }\n    }\n  }\n  const maxCmUp = Math.floor((zeroY - rulerTop) / pxPerCm);\n  for (let cm = 1; cm <= maxCmUp; cm++) {\n    const ry = zeroY - cm * pxPerCm;\n    ctx.beginPath();\n    ctx.moveTo(rulerLeft, ry);\n    ctx.lineTo(rulerLeft + 20, ry);\n    ctx.stroke();\n    if (cm < maxCmUp || cm === maxCmUp) {\n      for (let mm = 1; mm < 10; mm++) {\n        const mmy = ry + mm * pxPerMmRuler;\n        if (mmy > rulerBot) break;\n        const mTickLen = mm === 5 ? 12 : 6;\n        ctx.beginPath();\n        ctx.moveTo(rulerLeft, mmy);\n        ctx.lineTo(rulerLeft + mTickLen, mmy);\n        ctx.stroke();\n      }\n    }\n  }\n  const readoutX = rulerLeft + 28;\n  if (hasMass) {\n    ctx.strokeStyle = \"#dc2626\";\n    ctx.lineWidth = 1.5;\n    ctx.setLineDash([3, 3]);\n    ctx.beginPath();\n    ctx.moveTo(readoutX, massY);\n    ctx.lineTo(pivotX + massRadius + 5, massY);\n    ctx.stroke();\n    ctx.setLineDash([]);\n  }\n  ctx.fillStyle = \"rgba(59,130,246,0.12)\";\n  ctx.fillRect(readoutX, natEqY - 10, 52, 20);\n  ctx.strokeStyle = \"rgba(59,130,246,0.4)\";\n  ctx.lineWidth = 1;\n  ctx.strokeRect(readoutX, natEqY - 10, 52, 20);\n  ctx.fillStyle = \"#3b82f6\";\n  ctx.font = 'bold 11px \"Courier New\",monospace';\n  ctx.textAlign = \"center\";\n  ctx.fillText(\"0 cm\", readoutX + 26, natEqY + 4);\n  if (hasMass) {\n    const dyStr = staticStretchCm.toFixed(2);\n    ctx.fillStyle = \"rgba(34,197,94,0.12)\";\n    ctx.fillRect(readoutX, eqY - 10, 52, 20);\n    ctx.strokeStyle = \"rgba(34,197,94,0.4)\";\n    ctx.strokeRect(readoutX, eqY - 10, 52, 20);\n    ctx.fillStyle = \"#22c55e\";\n    ctx.font = 'bold 11px \"Courier New\",monospace';\n    ctx.fillText(`${dyStr} cm`, readoutX + 26, eqY + 4);\n    const yCm = (staticStretchCm + simState.x * 100).toFixed(2);\n    ctx.fillStyle = \"rgba(220,38,38,0.12)\";\n    ctx.fillRect(readoutX, massY - 10, 52, 20);\n    ctx.strokeStyle = \"rgba(220,38,38,0.4)\";\n    ctx.strokeRect(readoutX, massY - 10, 52, 20);\n    ctx.fillStyle = \"#dc2626\";\n    ctx.font = 'bold 11px \"Courier New\",monospace';\n    ctx.fillText(`${yCm} cm`, readoutX + 26, massY + 4);\n  }\n  ctx.textAlign = \"start\";\n  if (Math.abs(simState.v) > 0.01) {\n    const vScale = 30;\n    const vDir = simState.v > 0 ? 1 : -1;\n    const vLen = Math.min(60, Math.abs(simState.v) * vScale);\n    const arrowY = massY + vDir * (massRadius + 10);\n    const tipY = arrowY + vDir * vLen;\n    ctx.strokeStyle = \"#2563eb\";\n    ctx.lineWidth = 2;\n    ctx.beginPath();\n    ctx.moveTo(pivotX + massRadius + 12, arrowY);\n    ctx.lineTo(pivotX + massRadius + 12, tipY);\n    ctx.stroke();\n    ctx.fillStyle = \"#2563eb\";\n    ctx.beginPath();\n    ctx.moveTo(pivotX + massRadius + 12, tipY);\n    ctx.lineTo(pivotX + massRadius + 12 - 4, tipY - vDir * 8);\n    ctx.lineTo(pivotX + massRadius + 12 + 4, tipY - vDir * 8);\n    ctx.fill();\n  }\n  return { massY, natEqY, staticStretchCm, pxPerCm };\n}\n",
      "start": 1783635315868,
      "end": 1783635316532,
      "order": "normal",
      "sourcemaps": {
        "version": 3,
        "sources": [
          "useSpringScene.ts"
        ],
        "sourcesContent": [
          "import type { SpringParams } from '../../modules/physics/experiments/spring/useSpringPhysics'\n\ninterface SimState {\n  x: number; v: number; t: number; running: boolean; paused: boolean\n}\n\nexport function drawSpringScene(\n  ctx: CanvasRenderingContext2D,\n  w: number,\n  h: number,\n  params: SpringParams,\n  simState: SimState,\n) {\n  const pivotX = w / 2\n  const pivotY = 45\n  const hasMass = params.mass > 1e-6\n\n  const springTop = pivotY + 12\n  const natLenPx = 50\n  const natEqY = springTop + natLenPx\n\n  const staticStretchCm = hasMass && params.k > 1e-9\n    ? (params.mass * 9.81 / params.k) * 100\n    : 0\n\n  const basePxPerCm = 8\n  const pxPerCm = (natEqY + staticStretchCm * basePxPerCm > h * 0.75 && staticStretchCm > 0.001)\n    ? Math.max(4, (h * 0.75 - natEqY) / staticStretchCm)\n    : basePxPerCm\n\n  const maxTravel = h * 0.22\n  const peakDisp = Math.max(params.amplitude, Math.abs(simState.x), 0.01)\n  const oscillationScale = Math.min(maxTravel / peakDisp, pxPerCm * 100)\n\n  const staticStretchPx = staticStretchCm * pxPerCm\n  const eqY = natEqY + staticStretchPx\n  const massY = eqY + simState.x * oscillationScale\n  const massRadius = hasMass\n    ? Math.max(14, Math.min(38, 12 + params.mass * 5.2))\n    : 0\n\n  // Background grid\n  ctx.strokeStyle = 'rgba(148,163,184,0.2)'\n  ctx.lineWidth = 1\n  for (let i = 0; i < w; i += 40) { ctx.beginPath(); ctx.moveTo(i, 0); ctx.lineTo(i, h); ctx.stroke() }\n  for (let i = 0; i < h; i += 40) { ctx.beginPath(); ctx.moveTo(0, i); ctx.lineTo(w, i); ctx.stroke() }\n\n  // Pivot\n  ctx.fillStyle = '#1e293b'\n  ctx.fillRect(pivotX - 10, pivotY - 2, 20, 14)\n  ctx.strokeStyle = '#64748b'; ctx.lineWidth = 1\n  ctx.strokeRect(pivotX - 10, pivotY - 2, 20, 14)\n\n  // Spring\n  const springBot = hasMass ? massY - massRadius : massY\n  const kNorm = Math.max(0, Math.min(1, (params.k - 5) / (200 - 5)))\n  const coils = Math.max(10, Math.min(28, Math.round(22 - kNorm * 8 + params.mass * 1.5)))\n  const coilWidth = Math.max(8, Math.min(24, 10 + (1 - kNorm) * 8 + params.mass * 1.5))\n  const segLen = Math.max(1.5, (springBot - springTop - 8) / coils)\n\n  const springGrad = ctx.createLinearGradient(pivotX - coilWidth, 0, pivotX + coilWidth, 0)\n  springGrad.addColorStop(0, `rgb(${50 + kNorm * 15},${55 + kNorm * 15},${70 + kNorm * 10})`)\n  springGrad.addColorStop(0.5, `rgb(${90 + kNorm * 25},${95 + kNorm * 25},${110 + kNorm * 15})`)\n  springGrad.addColorStop(1, `rgb(${50 + kNorm * 15},${55 + kNorm * 15},${70 + kNorm * 10})`)\n\n  ctx.beginPath()\n  ctx.moveTo(pivotX, springTop + 4)\n  for (let i = 1; i <= coils; i++) {\n    const cy = springTop + 4 + i * segLen\n    const cx = pivotX + (i % 2 === 0 ? -coilWidth : coilWidth)\n    ctx.lineTo(cx, cy)\n  }\n  ctx.lineTo(pivotX, springBot)\n  ctx.lineWidth = 1.8 + kNorm * 2.2\n  ctx.strokeStyle = springGrad\n  ctx.lineCap = 'round'\n  ctx.lineJoin = 'round'\n  ctx.stroke()\n  ctx.lineCap = 'butt'\n  ctx.lineJoin = 'miter'\n\n  // Mass\n  if (hasMass) {\n    ctx.fillStyle = 'rgba(15,23,42,0.1)'\n    ctx.beginPath()\n    ctx.ellipse(pivotX + 6, massY + massRadius + 6, massRadius * 1.1, massRadius * 0.35, 0, 0, Math.PI * 2)\n    ctx.fill()\n\n    const massGrad = ctx.createRadialGradient(pivotX - massRadius * 0.3, massY - massRadius * 0.3, 3, pivotX, massY, massRadius * 1.1)\n    massGrad.addColorStop(0, '#fca5a5')\n    massGrad.addColorStop(0.5, '#e11d48')\n    massGrad.addColorStop(1, '#7f1d1d')\n\n    ctx.fillStyle = massGrad\n    ctx.beginPath(); ctx.arc(pivotX, massY, massRadius, 0, 2 * Math.PI); ctx.fill()\n    ctx.strokeStyle = '#7f1d1d'; ctx.lineWidth = 1.5; ctx.stroke()\n\n    ctx.fillStyle = '#fff'\n    ctx.font = `bold ${Math.max(11, Math.min(16, Math.round(massRadius * 0.6)))}px Arial`\n    ctx.textAlign = 'center'\n    ctx.textBaseline = 'middle'\n    ctx.fillText(params.mass.toFixed(2), pivotX, massY - 3)\n    ctx.font = '9px Arial'\n    ctx.fillText('kg', pivotX, massY + 9)\n    ctx.textAlign = 'start'\n    ctx.textBaseline = 'alphabetic'\n\n    const trayW = massRadius * 2.5\n    const trayH = 8\n    ctx.fillStyle = '#64748b'\n    ctx.fillRect(pivotX - trayW / 2, massY + massRadius, trayW, trayH)\n    ctx.strokeStyle = '#475569'; ctx.lineWidth = 1\n    ctx.strokeRect(pivotX - trayW / 2, massY + massRadius, trayW, trayH)\n\n    const numWeights = Math.min(10, Math.max(0, Math.round(params.mass * 2)))\n    for (let i = 0; i < numWeights; i++) {\n      const wy = massY + massRadius - 3 - i * 3\n      ctx.fillStyle = i % 2 === 0 ? '#3b82f6' : '#60a5fa'\n      ctx.beginPath(); ctx.arc(pivotX, wy, 4, 0, 2 * Math.PI); ctx.fill()\n    }\n  }\n\n  // Reference lines\n  ctx.strokeStyle = 'rgba(59,130,246,0.4)'\n  ctx.setLineDash([5, 4])\n  ctx.beginPath(); ctx.moveTo(pivotX - 60, natEqY); ctx.lineTo(pivotX + 60, natEqY); ctx.stroke()\n  ctx.setLineDash([])\n\n  if (hasMass) {\n    ctx.strokeStyle = 'rgba(34,197,94,0.5)'\n    ctx.setLineDash([5, 4])\n    ctx.beginPath(); ctx.moveTo(pivotX - 60, eqY); ctx.lineTo(pivotX + 60, eqY); ctx.stroke()\n    ctx.setLineDash([])\n  }\n\n  // Ruler\n  const rulerLeft = pivotX + 50\n  const rulerTop = pivotY + 10\n  const rulerBot = h - 20\n  const zeroY = natEqY\n\n  ctx.fillStyle = 'rgba(241,245,249,0.3)'\n  ctx.fillRect(rulerLeft - 2, rulerTop, 22, rulerBot - rulerTop)\n  ctx.strokeStyle = 'rgba(148,163,184,0.4)'; ctx.lineWidth = 1\n  ctx.strokeRect(rulerLeft - 2, rulerTop, 22, rulerBot - rulerTop)\n\n  const pxPerMmRuler = pxPerCm / 10\n\n  const maxCmDown = Math.floor((rulerBot - zeroY) / pxPerCm)\n  for (let cm = 0; cm <= maxCmDown; cm++) {\n    const ry = zeroY + cm * pxPerCm\n    ctx.beginPath(); ctx.moveTo(rulerLeft, ry); ctx.lineTo(rulerLeft + 20, ry); ctx.stroke()\n\n    if (cm < maxCmDown) {\n      for (let mm = 1; mm < 10; mm++) {\n        const mmy = ry + mm * pxPerMmRuler\n        const mTickLen = mm === 5 ? 12 : 6\n        ctx.beginPath(); ctx.moveTo(rulerLeft, mmy); ctx.lineTo(rulerLeft + mTickLen, mmy); ctx.stroke()\n      }\n    }\n  }\n\n  const maxCmUp = Math.floor((zeroY - rulerTop) / pxPerCm)\n  for (let cm = 1; cm <= maxCmUp; cm++) {\n    const ry = zeroY - cm * pxPerCm\n    ctx.beginPath(); ctx.moveTo(rulerLeft, ry); ctx.lineTo(rulerLeft + 20, ry); ctx.stroke()\n\n    if (cm < maxCmUp || cm === maxCmUp) {\n      for (let mm = 1; mm < 10; mm++) {\n        const mmy = ry + mm * pxPerMmRuler\n        if (mmy > rulerBot) break\n        const mTickLen = mm === 5 ? 12 : 6\n        ctx.beginPath(); ctx.moveTo(rulerLeft, mmy); ctx.lineTo(rulerLeft + mTickLen, mmy); ctx.stroke()\n      }\n    }\n  }\n\n  // Readouts\n  const readoutX = rulerLeft + 28\n\n  if (hasMass) {\n    ctx.strokeStyle = '#dc2626'; ctx.lineWidth = 1.5\n    ctx.setLineDash([3, 3])\n    ctx.beginPath(); ctx.moveTo(readoutX, massY); ctx.lineTo(pivotX + massRadius + 5, massY); ctx.stroke()\n    ctx.setLineDash([])\n  }\n\n  ctx.fillStyle = 'rgba(59,130,246,0.12)'\n  ctx.fillRect(readoutX, natEqY - 10, 52, 20)\n  ctx.strokeStyle = 'rgba(59,130,246,0.4)'; ctx.lineWidth = 1\n  ctx.strokeRect(readoutX, natEqY - 10, 52, 20)\n  ctx.fillStyle = '#3b82f6'; ctx.font = 'bold 11px \"Courier New\",monospace'\n  ctx.textAlign = 'center'\n  ctx.fillText('0 cm', readoutX + 26, natEqY + 4)\n\n  if (hasMass) {\n    const dyStr = staticStretchCm.toFixed(2)\n    ctx.fillStyle = 'rgba(34,197,94,0.12)'\n    ctx.fillRect(readoutX, eqY - 10, 52, 20)\n    ctx.strokeStyle = 'rgba(34,197,94,0.4)'\n    ctx.strokeRect(readoutX, eqY - 10, 52, 20)\n    ctx.fillStyle = '#22c55e'; ctx.font = 'bold 11px \"Courier New\",monospace'\n    ctx.fillText(`${dyStr} cm`, readoutX + 26, eqY + 4)\n\n    const yCm = (staticStretchCm + simState.x * 100).toFixed(2)\n    ctx.fillStyle = 'rgba(220,38,38,0.12)'\n    ctx.fillRect(readoutX, massY - 10, 52, 20)\n    ctx.strokeStyle = 'rgba(220,38,38,0.4)'\n    ctx.strokeRect(readoutX, massY - 10, 52, 20)\n    ctx.fillStyle = '#dc2626'; ctx.font = 'bold 11px \"Courier New\",monospace'\n    ctx.fillText(`${yCm} cm`, readoutX + 26, massY + 4)\n  }\n\n  ctx.textAlign = 'start'\n\n  // Velocity arrow\n  if (Math.abs(simState.v) > 0.01) {\n    const vScale = 30\n    const vDir = simState.v > 0 ? 1 : -1\n    const vLen = Math.min(60, Math.abs(simState.v) * vScale)\n    const arrowY = massY + vDir * (massRadius + 10)\n    const tipY = arrowY + vDir * vLen\n    ctx.strokeStyle = '#2563eb'; ctx.lineWidth = 2\n    ctx.beginPath(); ctx.moveTo(pivotX + massRadius + 12, arrowY); ctx.lineTo(pivotX + massRadius + 12, tipY); ctx.stroke()\n    ctx.fillStyle = '#2563eb'\n    ctx.beginPath()\n    ctx.moveTo(pivotX + massRadius + 12, tipY)\n    ctx.lineTo(pivotX + massRadius + 12 - 4, tipY - vDir * 8)\n    ctx.lineTo(pivotX + massRadius + 12 + 4, tipY - vDir * 8)\n    ctx.fill()\n  }\n\n  return { massY, natEqY, staticStretchCm, pxPerCm }\n}\n"
        ],
        "mappings": "AAMO,gBAAS,gBACd,KACA,GACA,GACA,QACA,UACA;AACA,QAAM,SAAS,IAAI;AACnB,QAAM,SAAS;AACf,QAAM,UAAU,OAAO,OAAO;AAE9B,QAAM,YAAY,SAAS;AAC3B,QAAM,WAAW;AACjB,QAAM,SAAS,YAAY;AAE3B,QAAM,kBAAkB,WAAW,OAAO,IAAI,OACzC,OAAO,OAAO,OAAO,OAAO,IAAK,MAClC;AAEJ,QAAM,cAAc;AACpB,QAAM,UAAW,SAAS,kBAAkB,cAAc,IAAI,QAAQ,kBAAkB,OACpF,KAAK,IAAI,IAAI,IAAI,OAAO,UAAU,eAAe,IACjD;AAEJ,QAAM,YAAY,IAAI;AACtB,QAAM,WAAW,KAAK,IAAI,OAAO,WAAW,KAAK,IAAI,SAAS,CAAC,GAAG,IAAI;AACtE,QAAM,mBAAmB,KAAK,IAAI,YAAY,UAAU,UAAU,GAAG;AAErE,QAAM,kBAAkB,kBAAkB;AAC1C,QAAM,MAAM,SAAS;AACrB,QAAM,QAAQ,MAAM,SAAS,IAAI;AACjC,QAAM,aAAa,UACf,KAAK,IAAI,IAAI,KAAK,IAAI,IAAI,KAAK,OAAO,OAAO,GAAG,CAAC,IACjD;AAGJ,MAAI,cAAc;AAClB,MAAI,YAAY;AAChB,WAAS,IAAI,GAAG,IAAI,GAAG,KAAK,IAAI;AAAE,QAAI,UAAU;AAAG,QAAI,OAAO,GAAG,CAAC;AAAG,QAAI,OAAO,GAAG,CAAC;AAAG,QAAI,OAAO;AAAA,EAAE;AACpG,WAAS,IAAI,GAAG,IAAI,GAAG,KAAK,IAAI;AAAE,QAAI,UAAU;AAAG,QAAI,OAAO,GAAG,CAAC;AAAG,QAAI,OAAO,GAAG,CAAC;AAAG,QAAI,OAAO;AAAA,EAAE;AAGpG,MAAI,YAAY;AAChB,MAAI,SAAS,SAAS,IAAI,SAAS,GAAG,IAAI,EAAE;AAC5C,MAAI,cAAc;AAAW,MAAI,YAAY;AAC7C,MAAI,WAAW,SAAS,IAAI,SAAS,GAAG,IAAI,EAAE;AAG9C,QAAM,YAAY,UAAU,QAAQ,aAAa;AACjD,QAAM,QAAQ,KAAK,IAAI,GAAG,KAAK,IAAI,IAAI,OAAO,IAAI,MAAM,MAAM,EAAE,CAAC;AACjE,QAAM,QAAQ,KAAK,IAAI,IAAI,KAAK,IAAI,IAAI,KAAK,MAAM,KAAK,QAAQ,IAAI,OAAO,OAAO,GAAG,CAAC,CAAC;AACvF,QAAM,YAAY,KAAK,IAAI,GAAG,KAAK,IAAI,IAAI,MAAM,IAAI,SAAS,IAAI,OAAO,OAAO,GAAG,CAAC;AACpF,QAAM,SAAS,KAAK,IAAI,MAAM,YAAY,YAAY,KAAK,KAAK;AAEhE,QAAM,aAAa,IAAI,qBAAqB,SAAS,WAAW,GAAG,SAAS,WAAW,CAAC;AACxF,aAAW,aAAa,GAAG,OAAO,KAAK,QAAQ,EAAE,IAAI,KAAK,QAAQ,EAAE,IAAI,KAAK,QAAQ,EAAE,GAAG;AAC1F,aAAW,aAAa,KAAK,OAAO,KAAK,QAAQ,EAAE,IAAI,KAAK,QAAQ,EAAE,IAAI,MAAM,QAAQ,EAAE,GAAG;AAC7F,aAAW,aAAa,GAAG,OAAO,KAAK,QAAQ,EAAE,IAAI,KAAK,QAAQ,EAAE,IAAI,KAAK,QAAQ,EAAE,GAAG;AAE1F,MAAI,UAAU;AACd,MAAI,OAAO,QAAQ,YAAY,CAAC;AAChC,WAAS,IAAI,GAAG,KAAK,OAAO,KAAK;AAC/B,UAAM,KAAK,YAAY,IAAI,IAAI;AAC/B,UAAM,KAAK,UAAU,IAAI,MAAM,IAAI,CAAC,YAAY;AAChD,QAAI,OAAO,IAAI,EAAE;AAAA,EACnB;AACA,MAAI,OAAO,QAAQ,SAAS;AAC5B,MAAI,YAAY,MAAM,QAAQ;AAC9B,MAAI,cAAc;AAClB,MAAI,UAAU;AACd,MAAI,WAAW;AACf,MAAI,OAAO;AACX,MAAI,UAAU;AACd,MAAI,WAAW;AAGf,MAAI,SAAS;AACX,QAAI,YAAY;AAChB,QAAI,UAAU;AACd,QAAI,QAAQ,SAAS,GAAG,QAAQ,aAAa,GAAG,aAAa,KAAK,aAAa,MAAM,GAAG,GAAG,KAAK,KAAK,CAAC;AACtG,QAAI,KAAK;AAET,UAAM,WAAW,IAAI,qBAAqB,SAAS,aAAa,KAAK,QAAQ,aAAa,KAAK,GAAG,QAAQ,OAAO,aAAa,GAAG;AACjI,aAAS,aAAa,GAAG,SAAS;AAClC,aAAS,aAAa,KAAK,SAAS;AACpC,aAAS,aAAa,GAAG,SAAS;AAElC,QAAI,YAAY;AAChB,QAAI,UAAU;AAAG,QAAI,IAAI,QAAQ,OAAO,YAAY,GAAG,IAAI,KAAK,EAAE;AAAG,QAAI,KAAK;AAC9E,QAAI,cAAc;AAAW,QAAI,YAAY;AAAK,QAAI,OAAO;AAE7D,QAAI,YAAY;AAChB,QAAI,OAAO,QAAQ,KAAK,IAAI,IAAI,KAAK,IAAI,IAAI,KAAK,MAAM,aAAa,GAAG,CAAC,CAAC,CAAC;AAC3E,QAAI,YAAY;AAChB,QAAI,eAAe;AACnB,QAAI,SAAS,OAAO,KAAK,QAAQ,CAAC,GAAG,QAAQ,QAAQ,CAAC;AACtD,QAAI,OAAO;AACX,QAAI,SAAS,MAAM,QAAQ,QAAQ,CAAC;AACpC,QAAI,YAAY;AAChB,QAAI,eAAe;AAEnB,UAAM,QAAQ,aAAa;AAC3B,UAAM,QAAQ;AACd,QAAI,YAAY;AAChB,QAAI,SAAS,SAAS,QAAQ,GAAG,QAAQ,YAAY,OAAO,KAAK;AACjE,QAAI,cAAc;AAAW,QAAI,YAAY;AAC7C,QAAI,WAAW,SAAS,QAAQ,GAAG,QAAQ,YAAY,OAAO,KAAK;AAEnE,UAAM,aAAa,KAAK,IAAI,IAAI,KAAK,IAAI,GAAG,KAAK,MAAM,OAAO,OAAO,CAAC,CAAC,CAAC;AACxE,aAAS,IAAI,GAAG,IAAI,YAAY,KAAK;AACnC,YAAM,KAAK,QAAQ,aAAa,IAAI,IAAI;AACxC,UAAI,YAAY,IAAI,MAAM,IAAI,YAAY;AAC1C,UAAI,UAAU;AAAG,UAAI,IAAI,QAAQ,IAAI,GAAG,GAAG,IAAI,KAAK,EAAE;AAAG,UAAI,KAAK;AAAA,IACpE;AAAA,EACF;AAGA,MAAI,cAAc;AAClB,MAAI,YAAY,CAAC,GAAG,CAAC,CAAC;AACtB,MAAI,UAAU;AAAG,MAAI,OAAO,SAAS,IAAI,MAAM;AAAG,MAAI,OAAO,SAAS,IAAI,MAAM;AAAG,MAAI,OAAO;AAC9F,MAAI,YAAY,CAAC,CAAC;AAElB,MAAI,SAAS;AACX,QAAI,cAAc;AAClB,QAAI,YAAY,CAAC,GAAG,CAAC,CAAC;AACtB,QAAI,UAAU;AAAG,QAAI,OAAO,SAAS,IAAI,GAAG;AAAG,QAAI,OAAO,SAAS,IAAI,GAAG;AAAG,QAAI,OAAO;AACxF,QAAI,YAAY,CAAC,CAAC;AAAA,EACpB;AAGA,QAAM,YAAY,SAAS;AAC3B,QAAM,WAAW,SAAS;AAC1B,QAAM,WAAW,IAAI;AACrB,QAAM,QAAQ;AAEd,MAAI,YAAY;AAChB,MAAI,SAAS,YAAY,GAAG,UAAU,IAAI,WAAW,QAAQ;AAC7D,MAAI,cAAc;AAAyB,MAAI,YAAY;AAC3D,MAAI,WAAW,YAAY,GAAG,UAAU,IAAI,WAAW,QAAQ;AAE/D,QAAM,eAAe,UAAU;AAE/B,QAAM,YAAY,KAAK,OAAO,WAAW,SAAS,OAAO;AACzD,WAAS,KAAK,GAAG,MAAM,WAAW,MAAM;AACtC,UAAM,KAAK,QAAQ,KAAK;AACxB,QAAI,UAAU;AAAG,QAAI,OAAO,WAAW,EAAE;AAAG,QAAI,OAAO,YAAY,IAAI,EAAE;AAAG,QAAI,OAAO;AAEvF,QAAI,KAAK,WAAW;AAClB,eAAS,KAAK,GAAG,KAAK,IAAI,MAAM;AAC9B,cAAM,MAAM,KAAK,KAAK;AACtB,cAAM,WAAW,OAAO,IAAI,KAAK;AACjC,YAAI,UAAU;AAAG,YAAI,OAAO,WAAW,GAAG;AAAG,YAAI,OAAO,YAAY,UAAU,GAAG;AAAG,YAAI,OAAO;AAAA,MACjG;AAAA,IACF;AAAA,EACF;AAEA,QAAM,UAAU,KAAK,OAAO,QAAQ,YAAY,OAAO;AACvD,WAAS,KAAK,GAAG,MAAM,SAAS,MAAM;AACpC,UAAM,KAAK,QAAQ,KAAK;AACxB,QAAI,UAAU;AAAG,QAAI,OAAO,WAAW,EAAE;AAAG,QAAI,OAAO,YAAY,IAAI,EAAE;AAAG,QAAI,OAAO;AAEvF,QAAI,KAAK,WAAW,OAAO,SAAS;AAClC,eAAS,KAAK,GAAG,KAAK,IAAI,MAAM;AAC9B,cAAM,MAAM,KAAK,KAAK;AACtB,YAAI,MAAM,SAAU;AACpB,cAAM,WAAW,OAAO,IAAI,KAAK;AACjC,YAAI,UAAU;AAAG,YAAI,OAAO,WAAW,GAAG;AAAG,YAAI,OAAO,YAAY,UAAU,GAAG;AAAG,YAAI,OAAO;AAAA,MACjG;AAAA,IACF;AAAA,EACF;AAGA,QAAM,WAAW,YAAY;AAE7B,MAAI,SAAS;AACX,QAAI,cAAc;AAAW,QAAI,YAAY;AAC7C,QAAI,YAAY,CAAC,GAAG,CAAC,CAAC;AACtB,QAAI,UAAU;AAAG,QAAI,OAAO,UAAU,KAAK;AAAG,QAAI,OAAO,SAAS,aAAa,GAAG,KAAK;AAAG,QAAI,OAAO;AACrG,QAAI,YAAY,CAAC,CAAC;AAAA,EACpB;AAEA,MAAI,YAAY;AAChB,MAAI,SAAS,UAAU,SAAS,IAAI,IAAI,EAAE;AAC1C,MAAI,cAAc;AAAwB,MAAI,YAAY;AAC1D,MAAI,WAAW,UAAU,SAAS,IAAI,IAAI,EAAE;AAC5C,MAAI,YAAY;AAAW,MAAI,OAAO;AACtC,MAAI,YAAY;AAChB,MAAI,SAAS,QAAQ,WAAW,IAAI,SAAS,CAAC;AAE9C,MAAI,SAAS;AACX,UAAM,QAAQ,gBAAgB,QAAQ,CAAC;AACvC,QAAI,YAAY;AAChB,QAAI,SAAS,UAAU,MAAM,IAAI,IAAI,EAAE;AACvC,QAAI,cAAc;AAClB,QAAI,WAAW,UAAU,MAAM,IAAI,IAAI,EAAE;AACzC,QAAI,YAAY;AAAW,QAAI,OAAO;AACtC,QAAI,SAAS,GAAG,KAAK,OAAO,WAAW,IAAI,MAAM,CAAC;AAElD,UAAM,OAAO,kBAAkB,SAAS,IAAI,KAAK,QAAQ,CAAC;AAC1D,QAAI,YAAY;AAChB,QAAI,SAAS,UAAU,QAAQ,IAAI,IAAI,EAAE;AACzC,QAAI,cAAc;AAClB,QAAI,WAAW,UAAU,QAAQ,IAAI,IAAI,EAAE;AAC3C,QAAI,YAAY;AAAW,QAAI,OAAO;AACtC,QAAI,SAAS,GAAG,GAAG,OAAO,WAAW,IAAI,QAAQ,CAAC;AAAA,EACpD;AAEA,MAAI,YAAY;AAGhB,MAAI,KAAK,IAAI,SAAS,CAAC,IAAI,MAAM;AAC/B,UAAM,SAAS;AACf,UAAM,OAAO,SAAS,IAAI,IAAI,IAAI;AAClC,UAAM,OAAO,KAAK,IAAI,IAAI,KAAK,IAAI,SAAS,CAAC,IAAI,MAAM;AACvD,UAAM,SAAS,QAAQ,QAAQ,aAAa;AAC5C,UAAM,OAAO,SAAS,OAAO;AAC7B,QAAI,cAAc;AAAW,QAAI,YAAY;AAC7C,QAAI,UAAU;AAAG,QAAI,OAAO,SAAS,aAAa,IAAI,MAAM;AAAG,QAAI,OAAO,SAAS,aAAa,IAAI,IAAI;AAAG,QAAI,OAAO;AACtH,QAAI,YAAY;AAChB,QAAI,UAAU;AACd,QAAI,OAAO,SAAS,aAAa,IAAI,IAAI;AACzC,QAAI,OAAO,SAAS,aAAa,KAAK,GAAG,OAAO,OAAO,CAAC;AACxD,QAAI,OAAO,SAAS,aAAa,KAAK,GAAG,OAAO,OAAO,CAAC;AACxD,QAAI,KAAK;AAAA,EACX;AAEA,SAAO,EAAE,OAAO,QAAQ,iBAAiB,QAAQ;AACnD;",
        "names": []
      }
    },
    {
      "name": "vite:json",
      "start": 1783635316532,
      "end": 1783635316532,
      "order": "normal"
    },
    {
      "name": "vite:worker",
      "start": 1783635316532,
      "end": 1783635316532,
      "order": "normal"
    },
    {
      "name": "vite:vue",
      "start": 1783635316532,
      "end": 1783635316532,
      "order": "normal-normal"
    },
    {
      "name": "vite:define",
      "start": 1783635316532,
      "end": 1783635316532,
      "order": "normal"
    },
    {
      "name": "vite:css-post",
      "start": 1783635316532,
      "end": 1783635316532,
      "order": "normal"
    },
    {
      "name": "vite:worker-import-meta-url",
      "start": 1783635316532,
      "end": 1783635316532,
      "order": "normal"
    },
    {
      "name": "vite:asset-import-meta-url",
      "start": 1783635316532,
      "end": 1783635316532,
      "order": "normal"
    },
    {
      "name": "vite:dynamic-import-vars",
      "start": 1783635316532,
      "end": 1783635316532,
      "order": "normal"
    },
    {
      "name": "vite:import-glob",
      "start": 1783635316532,
      "end": 1783635316532,
      "order": "normal"
    },
    {
      "name": "vite-plugin-vue-inspector:post",
      "start": 1783635316532,
      "end": 1783635316532,
      "order": "post"
    },
    {
      "name": "vite:client-inject",
      "start": 1783635316532,
      "end": 1783635316532,
      "order": "normal"
    },
    {
      "name": "vite:css-analysis",
      "start": 1783635316532,
      "end": 1783635316532,
      "order": "normal"
    },
    {
      "name": "vite:import-analysis",
      "result": "export function drawSpringScene(ctx, w, h, params, simState) {\n  const pivotX = w / 2;\n  const pivotY = 45;\n  const hasMass = params.mass > 1e-6;\n  const springTop = pivotY + 12;\n  const natLenPx = 50;\n  const natEqY = springTop + natLenPx;\n  const staticStretchCm = hasMass && params.k > 1e-9 ? params.mass * 9.81 / params.k * 100 : 0;\n  const basePxPerCm = 8;\n  const pxPerCm = natEqY + staticStretchCm * basePxPerCm > h * 0.75 && staticStretchCm > 1e-3 ? Math.max(4, (h * 0.75 - natEqY) / staticStretchCm) : basePxPerCm;\n  const maxTravel = h * 0.22;\n  const peakDisp = Math.max(params.amplitude, Math.abs(simState.x), 0.01);\n  const oscillationScale = Math.min(maxTravel / peakDisp, pxPerCm * 100);\n  const staticStretchPx = staticStretchCm * pxPerCm;\n  const eqY = natEqY + staticStretchPx;\n  const massY = eqY + simState.x * oscillationScale;\n  const massRadius = hasMass ? Math.max(14, Math.min(38, 12 + params.mass * 5.2)) : 0;\n  ctx.strokeStyle = \"rgba(148,163,184,0.2)\";\n  ctx.lineWidth = 1;\n  for (let i = 0; i < w; i += 40) {\n    ctx.beginPath();\n    ctx.moveTo(i, 0);\n    ctx.lineTo(i, h);\n    ctx.stroke();\n  }\n  for (let i = 0; i < h; i += 40) {\n    ctx.beginPath();\n    ctx.moveTo(0, i);\n    ctx.lineTo(w, i);\n    ctx.stroke();\n  }\n  ctx.fillStyle = \"#1e293b\";\n  ctx.fillRect(pivotX - 10, pivotY - 2, 20, 14);\n  ctx.strokeStyle = \"#64748b\";\n  ctx.lineWidth = 1;\n  ctx.strokeRect(pivotX - 10, pivotY - 2, 20, 14);\n  const springBot = hasMass ? massY - massRadius : massY;\n  const kNorm = Math.max(0, Math.min(1, (params.k - 5) / (200 - 5)));\n  const coils = Math.max(10, Math.min(28, Math.round(22 - kNorm * 8 + params.mass * 1.5)));\n  const coilWidth = Math.max(8, Math.min(24, 10 + (1 - kNorm) * 8 + params.mass * 1.5));\n  const segLen = Math.max(1.5, (springBot - springTop - 8) / coils);\n  const springGrad = ctx.createLinearGradient(pivotX - coilWidth, 0, pivotX + coilWidth, 0);\n  springGrad.addColorStop(0, `rgb(${50 + kNorm * 15},${55 + kNorm * 15},${70 + kNorm * 10})`);\n  springGrad.addColorStop(0.5, `rgb(${90 + kNorm * 25},${95 + kNorm * 25},${110 + kNorm * 15})`);\n  springGrad.addColorStop(1, `rgb(${50 + kNorm * 15},${55 + kNorm * 15},${70 + kNorm * 10})`);\n  ctx.beginPath();\n  ctx.moveTo(pivotX, springTop + 4);\n  for (let i = 1; i <= coils; i++) {\n    const cy = springTop + 4 + i * segLen;\n    const cx = pivotX + (i % 2 === 0 ? -coilWidth : coilWidth);\n    ctx.lineTo(cx, cy);\n  }\n  ctx.lineTo(pivotX, springBot);\n  ctx.lineWidth = 1.8 + kNorm * 2.2;\n  ctx.strokeStyle = springGrad;\n  ctx.lineCap = \"round\";\n  ctx.lineJoin = \"round\";\n  ctx.stroke();\n  ctx.lineCap = \"butt\";\n  ctx.lineJoin = \"miter\";\n  if (hasMass) {\n    ctx.fillStyle = \"rgba(15,23,42,0.1)\";\n    ctx.beginPath();\n    ctx.ellipse(pivotX + 6, massY + massRadius + 6, massRadius * 1.1, massRadius * 0.35, 0, 0, Math.PI * 2);\n    ctx.fill();\n    const massGrad = ctx.createRadialGradient(pivotX - massRadius * 0.3, massY - massRadius * 0.3, 3, pivotX, massY, massRadius * 1.1);\n    massGrad.addColorStop(0, \"#fca5a5\");\n    massGrad.addColorStop(0.5, \"#e11d48\");\n    massGrad.addColorStop(1, \"#7f1d1d\");\n    ctx.fillStyle = massGrad;\n    ctx.beginPath();\n    ctx.arc(pivotX, massY, massRadius, 0, 2 * Math.PI);\n    ctx.fill();\n    ctx.strokeStyle = \"#7f1d1d\";\n    ctx.lineWidth = 1.5;\n    ctx.stroke();\n    ctx.fillStyle = \"#fff\";\n    ctx.font = `bold ${Math.max(11, Math.min(16, Math.round(massRadius * 0.6)))}px Arial`;\n    ctx.textAlign = \"center\";\n    ctx.textBaseline = \"middle\";\n    ctx.fillText(params.mass.toFixed(2), pivotX, massY - 3);\n    ctx.font = \"9px Arial\";\n    ctx.fillText(\"kg\", pivotX, massY + 9);\n    ctx.textAlign = \"start\";\n    ctx.textBaseline = \"alphabetic\";\n    const trayW = massRadius * 2.5;\n    const trayH = 8;\n    ctx.fillStyle = \"#64748b\";\n    ctx.fillRect(pivotX - trayW / 2, massY + massRadius, trayW, trayH);\n    ctx.strokeStyle = \"#475569\";\n    ctx.lineWidth = 1;\n    ctx.strokeRect(pivotX - trayW / 2, massY + massRadius, trayW, trayH);\n    const numWeights = Math.min(10, Math.max(0, Math.round(params.mass * 2)));\n    for (let i = 0; i < numWeights; i++) {\n      const wy = massY + massRadius - 3 - i * 3;\n      ctx.fillStyle = i % 2 === 0 ? \"#3b82f6\" : \"#60a5fa\";\n      ctx.beginPath();\n      ctx.arc(pivotX, wy, 4, 0, 2 * Math.PI);\n      ctx.fill();\n    }\n  }\n  ctx.strokeStyle = \"rgba(59,130,246,0.4)\";\n  ctx.setLineDash([5, 4]);\n  ctx.beginPath();\n  ctx.moveTo(pivotX - 60, natEqY);\n  ctx.lineTo(pivotX + 60, natEqY);\n  ctx.stroke();\n  ctx.setLineDash([]);\n  if (hasMass) {\n    ctx.strokeStyle = \"rgba(34,197,94,0.5)\";\n    ctx.setLineDash([5, 4]);\n    ctx.beginPath();\n    ctx.moveTo(pivotX - 60, eqY);\n    ctx.lineTo(pivotX + 60, eqY);\n    ctx.stroke();\n    ctx.setLineDash([]);\n  }\n  const rulerLeft = pivotX + 50;\n  const rulerTop = pivotY + 10;\n  const rulerBot = h - 20;\n  const zeroY = natEqY;\n  ctx.fillStyle = \"rgba(241,245,249,0.3)\";\n  ctx.fillRect(rulerLeft - 2, rulerTop, 22, rulerBot - rulerTop);\n  ctx.strokeStyle = \"rgba(148,163,184,0.4)\";\n  ctx.lineWidth = 1;\n  ctx.strokeRect(rulerLeft - 2, rulerTop, 22, rulerBot - rulerTop);\n  const pxPerMmRuler = pxPerCm / 10;\n  const maxCmDown = Math.floor((rulerBot - zeroY) / pxPerCm);\n  for (let cm = 0; cm <= maxCmDown; cm++) {\n    const ry = zeroY + cm * pxPerCm;\n    ctx.beginPath();\n    ctx.moveTo(rulerLeft, ry);\n    ctx.lineTo(rulerLeft + 20, ry);\n    ctx.stroke();\n    if (cm < maxCmDown) {\n      for (let mm = 1; mm < 10; mm++) {\n        const mmy = ry + mm * pxPerMmRuler;\n        const mTickLen = mm === 5 ? 12 : 6;\n        ctx.beginPath();\n        ctx.moveTo(rulerLeft, mmy);\n        ctx.lineTo(rulerLeft + mTickLen, mmy);\n        ctx.stroke();\n      }\n    }\n  }\n  const maxCmUp = Math.floor((zeroY - rulerTop) / pxPerCm);\n  for (let cm = 1; cm <= maxCmUp; cm++) {\n    const ry = zeroY - cm * pxPerCm;\n    ctx.beginPath();\n    ctx.moveTo(rulerLeft, ry);\n    ctx.lineTo(rulerLeft + 20, ry);\n    ctx.stroke();\n    if (cm < maxCmUp || cm === maxCmUp) {\n      for (let mm = 1; mm < 10; mm++) {\n        const mmy = ry + mm * pxPerMmRuler;\n        if (mmy > rulerBot) break;\n        const mTickLen = mm === 5 ? 12 : 6;\n        ctx.beginPath();\n        ctx.moveTo(rulerLeft, mmy);\n        ctx.lineTo(rulerLeft + mTickLen, mmy);\n        ctx.stroke();\n      }\n    }\n  }\n  const readoutX = rulerLeft + 28;\n  if (hasMass) {\n    ctx.strokeStyle = \"#dc2626\";\n    ctx.lineWidth = 1.5;\n    ctx.setLineDash([3, 3]);\n    ctx.beginPath();\n    ctx.moveTo(readoutX, massY);\n    ctx.lineTo(pivotX + massRadius + 5, massY);\n    ctx.stroke();\n    ctx.setLineDash([]);\n  }\n  ctx.fillStyle = \"rgba(59,130,246,0.12)\";\n  ctx.fillRect(readoutX, natEqY - 10, 52, 20);\n  ctx.strokeStyle = \"rgba(59,130,246,0.4)\";\n  ctx.lineWidth = 1;\n  ctx.strokeRect(readoutX, natEqY - 10, 52, 20);\n  ctx.fillStyle = \"#3b82f6\";\n  ctx.font = 'bold 11px \"Courier New\",monospace';\n  ctx.textAlign = \"center\";\n  ctx.fillText(\"0 cm\", readoutX + 26, natEqY + 4);\n  if (hasMass) {\n    const dyStr = staticStretchCm.toFixed(2);\n    ctx.fillStyle = \"rgba(34,197,94,0.12)\";\n    ctx.fillRect(readoutX, eqY - 10, 52, 20);\n    ctx.strokeStyle = \"rgba(34,197,94,0.4)\";\n    ctx.strokeRect(readoutX, eqY - 10, 52, 20);\n    ctx.fillStyle = \"#22c55e\";\n    ctx.font = 'bold 11px \"Courier New\",monospace';\n    ctx.fillText(`${dyStr} cm`, readoutX + 26, eqY + 4);\n    const yCm = (staticStretchCm + simState.x * 100).toFixed(2);\n    ctx.fillStyle = \"rgba(220,38,38,0.12)\";\n    ctx.fillRect(readoutX, massY - 10, 52, 20);\n    ctx.strokeStyle = \"rgba(220,38,38,0.4)\";\n    ctx.strokeRect(readoutX, massY - 10, 52, 20);\n    ctx.fillStyle = \"#dc2626\";\n    ctx.font = 'bold 11px \"Courier New\",monospace';\n    ctx.fillText(`${yCm} cm`, readoutX + 26, massY + 4);\n  }\n  ctx.textAlign = \"start\";\n  if (Math.abs(simState.v) > 0.01) {\n    const vScale = 30;\n    const vDir = simState.v > 0 ? 1 : -1;\n    const vLen = Math.min(60, Math.abs(simState.v) * vScale);\n    const arrowY = massY + vDir * (massRadius + 10);\n    const tipY = arrowY + vDir * vLen;\n    ctx.strokeStyle = \"#2563eb\";\n    ctx.lineWidth = 2;\n    ctx.beginPath();\n    ctx.moveTo(pivotX + massRadius + 12, arrowY);\n    ctx.lineTo(pivotX + massRadius + 12, tipY);\n    ctx.stroke();\n    ctx.fillStyle = \"#2563eb\";\n    ctx.beginPath();\n    ctx.moveTo(pivotX + massRadius + 12, tipY);\n    ctx.lineTo(pivotX + massRadius + 12 - 4, tipY - vDir * 8);\n    ctx.lineTo(pivotX + massRadius + 12 + 4, tipY - vDir * 8);\n    ctx.fill();\n  }\n  return { massY, natEqY, staticStretchCm, pxPerCm };\n}\n",
      "start": 1783635316532,
      "end": 1783635316532,
      "order": "normal",
      "sourcemaps": null
    }
  ]
}

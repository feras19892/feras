{
  "resolvedId": "C:/Users/feras/Desktop/feras/apps/web/src/composables/chemistry/useLabSimulation.ts",
  "transforms": [
    {
      "name": "vite:load-fallback",
      "result": "import { ref } from 'vue';\r\nimport type { LabItem } from './useChemistryTools';\r\nimport {\r\n  items, getLiquid, getBurette,\r\n  receivingMap, pourFlowMap, tiltAngleMap,\r\n  getBurnerState, simSpeed, phProbeTipMap, stopperMap,\r\n  buretteConsumedThisRefill,\r\n  isContainer, retortStandMap\r\n} from './useChemistryLab';\r\nimport { isBeaker, isTestTube } from './chemLabIds';\r\nimport { handleDropMixWithRecording } from './useBuretteMixRecorder';\r\nimport { getPhReading } from './usePhMeter';\r\nimport { pushMicroHistory } from './useChemistryHistory';\r\n\r\n// Re-export extracted functions for backward compatibility\r\nexport { phColor, getPhReading } from './usePhMeter';\r\nexport { computeBalanceWeight, getContainerWeight, getBalanceReading } from './useBalance';\r\nexport { stepUndo, stepRedo } from './useStepControl';\r\n\r\n// ================== BURETTE WARNING ==================\r\nexport type BuretteWarning = 'approaching' | 'equivalence' | 'exceeded' | null;\r\nexport const buretteWarning = ref<BuretteWarning>(null);\r\n\r\n// ================== BURETTE FIND ==================\r\nexport function findContainerBelow(burette: LabItem): LabItem | null {\r\n  const buretteTipX = burette.x + 30; // center of burette tip (width=60)\r\n  const buretteTipY = burette.y + 170; // tip Y position\r\n\r\n  // Check if burette is attached to a retort stand\r\n  let attachedStand: string | null = null;\r\n  for (const [standUid, st] of Object.entries(retortStandMap)) {\r\n    if (st.slotOccupants.includes(burette.uid)) {\r\n      attachedStand = standUid;\r\n      break;\r\n    }\r\n  }\r\n\r\n  // Find all containers whose opening horizontally covers the burette tip\r\n  const candidates = items.value.filter((i: LabItem) => {\r\n    if (i.uid === burette.uid || !isContainer(i.id)) return false;\r\n\r\n    // Detect if beaker is attached to bottom clamp (scale=0.5, width=70)\r\n    let isBottomSlot = false;\r\n    for (const st of Object.values(retortStandMap)) {\r\n      if (st.bottomSlotOccupant === i.uid) { isBottomSlot = true; break; }\r\n    }\r\n\r\n    // Container opening center and radius\r\n    const containerCenterX = i.x + (isBeaker(i.id) ? (isBottomSlot ? 35 : 70) : isTestTube(i.id) ? 20 : 40);\r\n    const dx = Math.abs(containerCenterX - buretteTipX);\r\n    const openingRadius = isBeaker(i.id) ? (isBottomSlot ? 24 : 35) : isTestTube(i.id) ? 20 : 40;\r\n\r\n    // Allow tip slightly above or inside the container\r\n    const containerTopY = i.y;\r\n    const containerHeight = isBeaker(i.id) ? (isBottomSlot ? 100 : 200) : isTestTube(i.id) ? 80 : 100;\r\n\r\n    return dx <= openingRadius && buretteTipY >= containerTopY - 30 && buretteTipY <= containerTopY + containerHeight;\r\n  });\r\n\r\n  if (candidates.length === 0) return null;\r\n\r\n  // If attached to a stand, prefer the beaker on that same stand\r\n  if (attachedStand) {\r\n    const st = retortStandMap[attachedStand];\r\n    const standBeaker = candidates.find(c => c.uid === st?.bottomSlotOccupant);\r\n    if (standBeaker) return standBeaker;\r\n  }\r\n\r\n  // Return the closest one to the burette tip vertically\r\n  return candidates.reduce((closest, current) => {\r\n    const closestDy = Math.abs(closest.y - buretteTipY);\r\n    const currentDy = Math.abs(current.y - buretteTipY);\r\n    return currentDy < closestDy ? current : closest;\r\n  });\r\n}\r\n\r\n// ================== HEATING ==================\r\nexport function isHeated(item: LabItem): boolean {\r\n  if (item.type !== 'container') return false;\r\n  return items.value.some((other: LabItem) =>\r\n    other.uid !== item.uid &&\r\n    (other.id === 'bunsen-burner' || other.id === 'heating-mantle') &&\r\n    getBurnerState(other.uid).on &&\r\n    Math.abs(other.x - item.x) < 80 && other.y > item.y && other.y < item.y + 250\r\n  );\r\n}\r\n\r\n// ================== SIMULATION LOOP ==================\r\nlet simTimer = 0;\r\n\r\nexport function startSimulation(_onSync: (item: LabItem | null) => void) {\r\n  function run() {\r\n    // Clear receiving state\r\n    Object.keys(receivingMap).forEach(k => delete receivingMap[k]);\r\n\r\n    // Burette dripping\r\n    for (const item of items.value) {\r\n      if (item.id !== 'burette') continue;\r\n      const bState = getBurette(item.uid);\r\n      if (!bState.valveOpen || bState.volume <= 0) continue;\r\n\r\n      const container = findContainerBelow(item);\r\n      if (!container) continue;\r\n      const bLiquid = getLiquid(container.uid);\r\n      if (bLiquid.volume >= bLiquid.maxVolume) continue;\r\n\r\n      // Record micro-history snapshot before each drop\r\n      pushMicroHistory();\r\n\r\n      const flowRate = 0.05;\r\n      const transfer = Math.min(flowRate, bState.volume, bLiquid.maxVolume - bLiquid.volume);\r\n\r\n      bState.volume = +(bState.volume - transfer).toFixed(2);\r\n      // Track burette consumption\r\n      buretteConsumedThisRefill[item.uid] = (buretteConsumedThisRefill[item.uid] || 0) + transfer;\r\n\r\n      // Always add volume to target container\r\n      bLiquid.volume = +(bLiquid.volume + transfer).toFixed(2);\r\n\r\n      // Trigger chemical reaction via reaction engine\r\n      if (bState.chemicalId) {\r\n        handleDropMixWithRecording({\r\n          sourceUid: item.uid,\r\n          targetUid: container.uid,\r\n          sourceChemicalId: bState.chemicalId,\r\n          targetChemicalId: bLiquid.chemicalId || '',\r\n          dropVolume: transfer,\r\n        });\r\n\r\n        // Update burette warning based on target pH\r\n        if (bLiquid.ph !== null && bLiquid.ph !== undefined) {\r\n          if (bLiquid.ph >= 9.0) {\r\n            buretteWarning.value = 'exceeded';\r\n          } else if (bLiquid.ph >= 8.0) {\r\n            buretteWarning.value = 'equivalence';\r\n          } else if (bLiquid.ph >= 7.5) {\r\n            buretteWarning.value = 'approaching';\r\n          }\r\n        }\r\n      } else {\r\n        // No chemical in burette, just copy color\r\n        bLiquid.color = bState.color;\r\n        bLiquid.opacity = bState.opacity;\r\n      }\r\n      receivingMap[container.uid] = true;\r\n    }\r\n\r\n    // Temperature + pH simulation\r\n    for (const item of items.value) {\r\n      if (!isContainer(item.id)) continue;\r\n      const liq = getLiquid(item.uid);\r\n      const burner = items.value.find((o: LabItem) =>\r\n        o.uid !== item.uid &&\r\n        (o.id === 'bunsen-burner' || o.id === 'heating-mantle') &&\r\n        getBurnerState(o.uid).on &&\r\n        Math.abs(o.x - item.x) < 80 && o.y > item.y && o.y < item.y + 250\r\n      );\r\n      const hasStopper = !!stopperMap[item.uid];\r\n      if (burner) {\r\n        liq.heated = true;\r\n        const intensity = getBurnerState(burner.uid).intensity;\r\n        // Realistic heating: proportional to intensity, ~0.6°C/sec max at 1x\r\n        const rate = 0.01 * intensity * simSpeed.value;\r\n        if (liq.temperature < 100) liq.temperature = Math.min(100, +(liq.temperature + rate).toFixed(2));\r\n        // Evaporation: lose ~1.2mL/min when heated, blocked by rubber stopper\r\n        if (!hasStopper && liq.volume > 0 && liq.temperature > 50) {\r\n          const evapRate = 0.02 * intensity * simSpeed.value;\r\n          liq.volume = Math.max(0, +(liq.volume - evapRate).toFixed(2));\r\n        }\r\n      } else {\r\n        liq.heated = false;\r\n        const coolRate = 0.02 * simSpeed.value;\r\n        if (liq.temperature > 25) liq.temperature = Math.max(25, +(liq.temperature - coolRate).toFixed(2));\r\n      }\r\n      const phItem = items.value.find((other: LabItem) =>\r\n        other.id === 'ph-meter' && phProbeTipMap[other.uid] &&\r\n        Math.abs((item.x + 40) - phProbeTipMap[other.uid].x) < 60 &&\r\n        Math.abs((item.y + 10) - phProbeTipMap[other.uid].y) < 50\r\n      );\r\n      if (phItem) {\r\n        liq.ph = getPhReading(phItem);\r\n      }\r\n    }\r\n\r\n    // Continuous pour flow between containers\r\n    for (const [srcUid, dstUid] of Object.entries(pourFlowMap)) {\r\n      const src = getLiquid(srcUid);\r\n      const dst = getLiquid(dstUid);\r\n      const flowRate = 0.25; // mL per frame (~15 mL/sec)\r\n      const amount = Math.min(flowRate, src.volume, dst.maxVolume - dst.volume);\r\n      if (amount > 0) {\r\n        src.volume = +(src.volume - amount).toFixed(2);\r\n        dst.volume = +(dst.volume + amount).toFixed(2);\r\n        dst.color = src.color; dst.opacity = src.opacity;\r\n        if (!dst.label.includes('+') && src.label !== dst.label) {\r\n          dst.label = dst.label + ' + ' + src.label;\r\n        }\r\n        receivingMap[dstUid] = true;\r\n      } else {\r\n        // Source empty or destination full → stop flow\r\n        delete pourFlowMap[srcUid];\r\n      }\r\n    }\r\n\r\n    // Auto-pour when container is tilted > 45° with liquid and container below\r\n    for (const item of items.value) {\r\n      if (!isContainer(item.id)) continue;\r\n      const angle = tiltAngleMap[item.uid] || 0;\r\n      if (Math.abs(angle) < 45) continue;\r\n      const liq = getLiquid(item.uid);\r\n      if (liq.volume <= 0) continue;\r\n      if (pourFlowMap[item.uid]) continue; // already pouring\r\n      // Find container below\r\n      const below = items.value.find((i: LabItem) =>\r\n        i.uid !== item.uid && isContainer(i.id) &&\r\n        Math.abs(i.x - item.x) < 80 && i.y > item.y + 50 && i.y < item.y + 250\r\n      );\r\n      if (below) {\r\n        pourFlowMap[item.uid] = below.uid;\r\n      }\r\n    }\r\n\r\n    simTimer = requestAnimationFrame(run);\r\n  }\r\n  simTimer = requestAnimationFrame(run);\r\n}\r\n\r\nexport function stopSimulation() {\r\n  cancelAnimationFrame(simTimer);\r\n}\r\n\r\n",
      "start": 1783634639326,
      "end": 1783634639596,
      "sourcemaps": null
    },
    {
      "name": "vite:css",
      "start": 1783634639596,
      "end": 1783634639596,
      "order": "normal"
    },
    {
      "name": "vite:esbuild",
      "result": "import { ref } from \"vue\";\nimport {\n  items,\n  getLiquid,\n  getBurette,\n  receivingMap,\n  pourFlowMap,\n  tiltAngleMap,\n  getBurnerState,\n  simSpeed,\n  phProbeTipMap,\n  stopperMap,\n  buretteConsumedThisRefill,\n  isContainer,\n  retortStandMap\n} from \"./useChemistryLab\";\nimport { isBeaker, isTestTube } from \"./chemLabIds\";\nimport { handleDropMixWithRecording } from \"./useBuretteMixRecorder\";\nimport { getPhReading } from \"./usePhMeter\";\nimport { pushMicroHistory } from \"./useChemistryHistory\";\nexport { phColor, getPhReading } from \"./usePhMeter\";\nexport { computeBalanceWeight, getContainerWeight, getBalanceReading } from \"./useBalance\";\nexport { stepUndo, stepRedo } from \"./useStepControl\";\nexport const buretteWarning = ref(null);\nexport function findContainerBelow(burette) {\n  const buretteTipX = burette.x + 30;\n  const buretteTipY = burette.y + 170;\n  let attachedStand = null;\n  for (const [standUid, st] of Object.entries(retortStandMap)) {\n    if (st.slotOccupants.includes(burette.uid)) {\n      attachedStand = standUid;\n      break;\n    }\n  }\n  const candidates = items.value.filter((i) => {\n    if (i.uid === burette.uid || !isContainer(i.id)) return false;\n    let isBottomSlot = false;\n    for (const st of Object.values(retortStandMap)) {\n      if (st.bottomSlotOccupant === i.uid) {\n        isBottomSlot = true;\n        break;\n      }\n    }\n    const containerCenterX = i.x + (isBeaker(i.id) ? isBottomSlot ? 35 : 70 : isTestTube(i.id) ? 20 : 40);\n    const dx = Math.abs(containerCenterX - buretteTipX);\n    const openingRadius = isBeaker(i.id) ? isBottomSlot ? 24 : 35 : isTestTube(i.id) ? 20 : 40;\n    const containerTopY = i.y;\n    const containerHeight = isBeaker(i.id) ? isBottomSlot ? 100 : 200 : isTestTube(i.id) ? 80 : 100;\n    return dx <= openingRadius && buretteTipY >= containerTopY - 30 && buretteTipY <= containerTopY + containerHeight;\n  });\n  if (candidates.length === 0) return null;\n  if (attachedStand) {\n    const st = retortStandMap[attachedStand];\n    const standBeaker = candidates.find((c) => c.uid === st?.bottomSlotOccupant);\n    if (standBeaker) return standBeaker;\n  }\n  return candidates.reduce((closest, current) => {\n    const closestDy = Math.abs(closest.y - buretteTipY);\n    const currentDy = Math.abs(current.y - buretteTipY);\n    return currentDy < closestDy ? current : closest;\n  });\n}\nexport function isHeated(item) {\n  if (item.type !== \"container\") return false;\n  return items.value.some(\n    (other) => other.uid !== item.uid && (other.id === \"bunsen-burner\" || other.id === \"heating-mantle\") && getBurnerState(other.uid).on && Math.abs(other.x - item.x) < 80 && other.y > item.y && other.y < item.y + 250\n  );\n}\nlet simTimer = 0;\nexport function startSimulation(_onSync) {\n  function run() {\n    Object.keys(receivingMap).forEach((k) => delete receivingMap[k]);\n    for (const item of items.value) {\n      if (item.id !== \"burette\") continue;\n      const bState = getBurette(item.uid);\n      if (!bState.valveOpen || bState.volume <= 0) continue;\n      const container = findContainerBelow(item);\n      if (!container) continue;\n      const bLiquid = getLiquid(container.uid);\n      if (bLiquid.volume >= bLiquid.maxVolume) continue;\n      pushMicroHistory();\n      const flowRate = 0.05;\n      const transfer = Math.min(flowRate, bState.volume, bLiquid.maxVolume - bLiquid.volume);\n      bState.volume = +(bState.volume - transfer).toFixed(2);\n      buretteConsumedThisRefill[item.uid] = (buretteConsumedThisRefill[item.uid] || 0) + transfer;\n      bLiquid.volume = +(bLiquid.volume + transfer).toFixed(2);\n      if (bState.chemicalId) {\n        handleDropMixWithRecording({\n          sourceUid: item.uid,\n          targetUid: container.uid,\n          sourceChemicalId: bState.chemicalId,\n          targetChemicalId: bLiquid.chemicalId || \"\",\n          dropVolume: transfer\n        });\n        if (bLiquid.ph !== null && bLiquid.ph !== void 0) {\n          if (bLiquid.ph >= 9) {\n            buretteWarning.value = \"exceeded\";\n          } else if (bLiquid.ph >= 8) {\n            buretteWarning.value = \"equivalence\";\n          } else if (bLiquid.ph >= 7.5) {\n            buretteWarning.value = \"approaching\";\n          }\n        }\n      } else {\n        bLiquid.color = bState.color;\n        bLiquid.opacity = bState.opacity;\n      }\n      receivingMap[container.uid] = true;\n    }\n    for (const item of items.value) {\n      if (!isContainer(item.id)) continue;\n      const liq = getLiquid(item.uid);\n      const burner = items.value.find(\n        (o) => o.uid !== item.uid && (o.id === \"bunsen-burner\" || o.id === \"heating-mantle\") && getBurnerState(o.uid).on && Math.abs(o.x - item.x) < 80 && o.y > item.y && o.y < item.y + 250\n      );\n      const hasStopper = !!stopperMap[item.uid];\n      if (burner) {\n        liq.heated = true;\n        const intensity = getBurnerState(burner.uid).intensity;\n        const rate = 0.01 * intensity * simSpeed.value;\n        if (liq.temperature < 100) liq.temperature = Math.min(100, +(liq.temperature + rate).toFixed(2));\n        if (!hasStopper && liq.volume > 0 && liq.temperature > 50) {\n          const evapRate = 0.02 * intensity * simSpeed.value;\n          liq.volume = Math.max(0, +(liq.volume - evapRate).toFixed(2));\n        }\n      } else {\n        liq.heated = false;\n        const coolRate = 0.02 * simSpeed.value;\n        if (liq.temperature > 25) liq.temperature = Math.max(25, +(liq.temperature - coolRate).toFixed(2));\n      }\n      const phItem = items.value.find(\n        (other) => other.id === \"ph-meter\" && phProbeTipMap[other.uid] && Math.abs(item.x + 40 - phProbeTipMap[other.uid].x) < 60 && Math.abs(item.y + 10 - phProbeTipMap[other.uid].y) < 50\n      );\n      if (phItem) {\n        liq.ph = getPhReading(phItem);\n      }\n    }\n    for (const [srcUid, dstUid] of Object.entries(pourFlowMap)) {\n      const src = getLiquid(srcUid);\n      const dst = getLiquid(dstUid);\n      const flowRate = 0.25;\n      const amount = Math.min(flowRate, src.volume, dst.maxVolume - dst.volume);\n      if (amount > 0) {\n        src.volume = +(src.volume - amount).toFixed(2);\n        dst.volume = +(dst.volume + amount).toFixed(2);\n        dst.color = src.color;\n        dst.opacity = src.opacity;\n        if (!dst.label.includes(\"+\") && src.label !== dst.label) {\n          dst.label = dst.label + \" + \" + src.label;\n        }\n        receivingMap[dstUid] = true;\n      } else {\n        delete pourFlowMap[srcUid];\n      }\n    }\n    for (const item of items.value) {\n      if (!isContainer(item.id)) continue;\n      const angle = tiltAngleMap[item.uid] || 0;\n      if (Math.abs(angle) < 45) continue;\n      const liq = getLiquid(item.uid);\n      if (liq.volume <= 0) continue;\n      if (pourFlowMap[item.uid]) continue;\n      const below = items.value.find(\n        (i) => i.uid !== item.uid && isContainer(i.id) && Math.abs(i.x - item.x) < 80 && i.y > item.y + 50 && i.y < item.y + 250\n      );\n      if (below) {\n        pourFlowMap[item.uid] = below.uid;\n      }\n    }\n    simTimer = requestAnimationFrame(run);\n  }\n  simTimer = requestAnimationFrame(run);\n}\nexport function stopSimulation() {\n  cancelAnimationFrame(simTimer);\n}\n",
      "start": 1783634639596,
      "end": 1783634639940,
      "order": "normal",
      "sourcemaps": {
        "version": 3,
        "sources": [
          "C:/Users/feras/Desktop/feras/apps/web/src/composables/chemistry/useLabSimulation.ts"
        ],
        "sourcesContent": [
          "import { ref } from 'vue';\r\nimport type { LabItem } from './useChemistryTools';\r\nimport {\r\n  items, getLiquid, getBurette,\r\n  receivingMap, pourFlowMap, tiltAngleMap,\r\n  getBurnerState, simSpeed, phProbeTipMap, stopperMap,\r\n  buretteConsumedThisRefill,\r\n  isContainer, retortStandMap\r\n} from './useChemistryLab';\r\nimport { isBeaker, isTestTube } from './chemLabIds';\r\nimport { handleDropMixWithRecording } from './useBuretteMixRecorder';\r\nimport { getPhReading } from './usePhMeter';\r\nimport { pushMicroHistory } from './useChemistryHistory';\r\n\r\n// Re-export extracted functions for backward compatibility\r\nexport { phColor, getPhReading } from './usePhMeter';\r\nexport { computeBalanceWeight, getContainerWeight, getBalanceReading } from './useBalance';\r\nexport { stepUndo, stepRedo } from './useStepControl';\r\n\r\n// ================== BURETTE WARNING ==================\r\nexport type BuretteWarning = 'approaching' | 'equivalence' | 'exceeded' | null;\r\nexport const buretteWarning = ref<BuretteWarning>(null);\r\n\r\n// ================== BURETTE FIND ==================\r\nexport function findContainerBelow(burette: LabItem): LabItem | null {\r\n  const buretteTipX = burette.x + 30; // center of burette tip (width=60)\r\n  const buretteTipY = burette.y + 170; // tip Y position\r\n\r\n  // Check if burette is attached to a retort stand\r\n  let attachedStand: string | null = null;\r\n  for (const [standUid, st] of Object.entries(retortStandMap)) {\r\n    if (st.slotOccupants.includes(burette.uid)) {\r\n      attachedStand = standUid;\r\n      break;\r\n    }\r\n  }\r\n\r\n  // Find all containers whose opening horizontally covers the burette tip\r\n  const candidates = items.value.filter((i: LabItem) => {\r\n    if (i.uid === burette.uid || !isContainer(i.id)) return false;\r\n\r\n    // Detect if beaker is attached to bottom clamp (scale=0.5, width=70)\r\n    let isBottomSlot = false;\r\n    for (const st of Object.values(retortStandMap)) {\r\n      if (st.bottomSlotOccupant === i.uid) { isBottomSlot = true; break; }\r\n    }\r\n\r\n    // Container opening center and radius\r\n    const containerCenterX = i.x + (isBeaker(i.id) ? (isBottomSlot ? 35 : 70) : isTestTube(i.id) ? 20 : 40);\r\n    const dx = Math.abs(containerCenterX - buretteTipX);\r\n    const openingRadius = isBeaker(i.id) ? (isBottomSlot ? 24 : 35) : isTestTube(i.id) ? 20 : 40;\r\n\r\n    // Allow tip slightly above or inside the container\r\n    const containerTopY = i.y;\r\n    const containerHeight = isBeaker(i.id) ? (isBottomSlot ? 100 : 200) : isTestTube(i.id) ? 80 : 100;\r\n\r\n    return dx <= openingRadius && buretteTipY >= containerTopY - 30 && buretteTipY <= containerTopY + containerHeight;\r\n  });\r\n\r\n  if (candidates.length === 0) return null;\r\n\r\n  // If attached to a stand, prefer the beaker on that same stand\r\n  if (attachedStand) {\r\n    const st = retortStandMap[attachedStand];\r\n    const standBeaker = candidates.find(c => c.uid === st?.bottomSlotOccupant);\r\n    if (standBeaker) return standBeaker;\r\n  }\r\n\r\n  // Return the closest one to the burette tip vertically\r\n  return candidates.reduce((closest, current) => {\r\n    const closestDy = Math.abs(closest.y - buretteTipY);\r\n    const currentDy = Math.abs(current.y - buretteTipY);\r\n    return currentDy < closestDy ? current : closest;\r\n  });\r\n}\r\n\r\n// ================== HEATING ==================\r\nexport function isHeated(item: LabItem): boolean {\r\n  if (item.type !== 'container') return false;\r\n  return items.value.some((other: LabItem) =>\r\n    other.uid !== item.uid &&\r\n    (other.id === 'bunsen-burner' || other.id === 'heating-mantle') &&\r\n    getBurnerState(other.uid).on &&\r\n    Math.abs(other.x - item.x) < 80 && other.y > item.y && other.y < item.y + 250\r\n  );\r\n}\r\n\r\n// ================== SIMULATION LOOP ==================\r\nlet simTimer = 0;\r\n\r\nexport function startSimulation(_onSync: (item: LabItem | null) => void) {\r\n  function run() {\r\n    // Clear receiving state\r\n    Object.keys(receivingMap).forEach(k => delete receivingMap[k]);\r\n\r\n    // Burette dripping\r\n    for (const item of items.value) {\r\n      if (item.id !== 'burette') continue;\r\n      const bState = getBurette(item.uid);\r\n      if (!bState.valveOpen || bState.volume <= 0) continue;\r\n\r\n      const container = findContainerBelow(item);\r\n      if (!container) continue;\r\n      const bLiquid = getLiquid(container.uid);\r\n      if (bLiquid.volume >= bLiquid.maxVolume) continue;\r\n\r\n      // Record micro-history snapshot before each drop\r\n      pushMicroHistory();\r\n\r\n      const flowRate = 0.05;\r\n      const transfer = Math.min(flowRate, bState.volume, bLiquid.maxVolume - bLiquid.volume);\r\n\r\n      bState.volume = +(bState.volume - transfer).toFixed(2);\r\n      // Track burette consumption\r\n      buretteConsumedThisRefill[item.uid] = (buretteConsumedThisRefill[item.uid] || 0) + transfer;\r\n\r\n      // Always add volume to target container\r\n      bLiquid.volume = +(bLiquid.volume + transfer).toFixed(2);\r\n\r\n      // Trigger chemical reaction via reaction engine\r\n      if (bState.chemicalId) {\r\n        handleDropMixWithRecording({\r\n          sourceUid: item.uid,\r\n          targetUid: container.uid,\r\n          sourceChemicalId: bState.chemicalId,\r\n          targetChemicalId: bLiquid.chemicalId || '',\r\n          dropVolume: transfer,\r\n        });\r\n\r\n        // Update burette warning based on target pH\r\n        if (bLiquid.ph !== null && bLiquid.ph !== undefined) {\r\n          if (bLiquid.ph >= 9.0) {\r\n            buretteWarning.value = 'exceeded';\r\n          } else if (bLiquid.ph >= 8.0) {\r\n            buretteWarning.value = 'equivalence';\r\n          } else if (bLiquid.ph >= 7.5) {\r\n            buretteWarning.value = 'approaching';\r\n          }\r\n        }\r\n      } else {\r\n        // No chemical in burette, just copy color\r\n        bLiquid.color = bState.color;\r\n        bLiquid.opacity = bState.opacity;\r\n      }\r\n      receivingMap[container.uid] = true;\r\n    }\r\n\r\n    // Temperature + pH simulation\r\n    for (const item of items.value) {\r\n      if (!isContainer(item.id)) continue;\r\n      const liq = getLiquid(item.uid);\r\n      const burner = items.value.find((o: LabItem) =>\r\n        o.uid !== item.uid &&\r\n        (o.id === 'bunsen-burner' || o.id === 'heating-mantle') &&\r\n        getBurnerState(o.uid).on &&\r\n        Math.abs(o.x - item.x) < 80 && o.y > item.y && o.y < item.y + 250\r\n      );\r\n      const hasStopper = !!stopperMap[item.uid];\r\n      if (burner) {\r\n        liq.heated = true;\r\n        const intensity = getBurnerState(burner.uid).intensity;\r\n        // Realistic heating: proportional to intensity, ~0.6°C/sec max at 1x\r\n        const rate = 0.01 * intensity * simSpeed.value;\r\n        if (liq.temperature < 100) liq.temperature = Math.min(100, +(liq.temperature + rate).toFixed(2));\r\n        // Evaporation: lose ~1.2mL/min when heated, blocked by rubber stopper\r\n        if (!hasStopper && liq.volume > 0 && liq.temperature > 50) {\r\n          const evapRate = 0.02 * intensity * simSpeed.value;\r\n          liq.volume = Math.max(0, +(liq.volume - evapRate).toFixed(2));\r\n        }\r\n      } else {\r\n        liq.heated = false;\r\n        const coolRate = 0.02 * simSpeed.value;\r\n        if (liq.temperature > 25) liq.temperature = Math.max(25, +(liq.temperature - coolRate).toFixed(2));\r\n      }\r\n      const phItem = items.value.find((other: LabItem) =>\r\n        other.id === 'ph-meter' && phProbeTipMap[other.uid] &&\r\n        Math.abs((item.x + 40) - phProbeTipMap[other.uid].x) < 60 &&\r\n        Math.abs((item.y + 10) - phProbeTipMap[other.uid].y) < 50\r\n      );\r\n      if (phItem) {\r\n        liq.ph = getPhReading(phItem);\r\n      }\r\n    }\r\n\r\n    // Continuous pour flow between containers\r\n    for (const [srcUid, dstUid] of Object.entries(pourFlowMap)) {\r\n      const src = getLiquid(srcUid);\r\n      const dst = getLiquid(dstUid);\r\n      const flowRate = 0.25; // mL per frame (~15 mL/sec)\r\n      const amount = Math.min(flowRate, src.volume, dst.maxVolume - dst.volume);\r\n      if (amount > 0) {\r\n        src.volume = +(src.volume - amount).toFixed(2);\r\n        dst.volume = +(dst.volume + amount).toFixed(2);\r\n        dst.color = src.color; dst.opacity = src.opacity;\r\n        if (!dst.label.includes('+') && src.label !== dst.label) {\r\n          dst.label = dst.label + ' + ' + src.label;\r\n        }\r\n        receivingMap[dstUid] = true;\r\n      } else {\r\n        // Source empty or destination full → stop flow\r\n        delete pourFlowMap[srcUid];\r\n      }\r\n    }\r\n\r\n    // Auto-pour when container is tilted > 45° with liquid and container below\r\n    for (const item of items.value) {\r\n      if (!isContainer(item.id)) continue;\r\n      const angle = tiltAngleMap[item.uid] || 0;\r\n      if (Math.abs(angle) < 45) continue;\r\n      const liq = getLiquid(item.uid);\r\n      if (liq.volume <= 0) continue;\r\n      if (pourFlowMap[item.uid]) continue; // already pouring\r\n      // Find container below\r\n      const below = items.value.find((i: LabItem) =>\r\n        i.uid !== item.uid && isContainer(i.id) &&\r\n        Math.abs(i.x - item.x) < 80 && i.y > item.y + 50 && i.y < item.y + 250\r\n      );\r\n      if (below) {\r\n        pourFlowMap[item.uid] = below.uid;\r\n      }\r\n    }\r\n\r\n    simTimer = requestAnimationFrame(run);\r\n  }\r\n  simTimer = requestAnimationFrame(run);\r\n}\r\n\r\nexport function stopSimulation() {\r\n  cancelAnimationFrame(simTimer);\r\n}\r\n\r\n"
        ],
        "mappings": "AAAA,SAAS,WAAW;AAEpB;AAAA,EACE;AAAA,EAAO;AAAA,EAAW;AAAA,EAClB;AAAA,EAAc;AAAA,EAAa;AAAA,EAC3B;AAAA,EAAgB;AAAA,EAAU;AAAA,EAAe;AAAA,EACzC;AAAA,EACA;AAAA,EAAa;AAAA,OACR;AACP,SAAS,UAAU,kBAAkB;AACrC,SAAS,kCAAkC;AAC3C,SAAS,oBAAoB;AAC7B,SAAS,wBAAwB;AAGjC,SAAS,SAAS,oBAAoB;AACtC,SAAS,sBAAsB,oBAAoB,yBAAyB;AAC5E,SAAS,UAAU,gBAAgB;AAI5B,aAAM,iBAAiB,IAAoB,IAAI;AAG/C,gBAAS,mBAAmB,SAAkC;AACnE,QAAM,cAAc,QAAQ,IAAI;AAChC,QAAM,cAAc,QAAQ,IAAI;AAGhC,MAAI,gBAA+B;AACnC,aAAW,CAAC,UAAU,EAAE,KAAK,OAAO,QAAQ,cAAc,GAAG;AAC3D,QAAI,GAAG,cAAc,SAAS,QAAQ,GAAG,GAAG;AAC1C,sBAAgB;AAChB;AAAA,IACF;AAAA,EACF;AAGA,QAAM,aAAa,MAAM,MAAM,OAAO,CAAC,MAAe;AACpD,QAAI,EAAE,QAAQ,QAAQ,OAAO,CAAC,YAAY,EAAE,EAAE,EAAG,QAAO;AAGxD,QAAI,eAAe;AACnB,eAAW,MAAM,OAAO,OAAO,cAAc,GAAG;AAC9C,UAAI,GAAG,uBAAuB,EAAE,KAAK;AAAE,uBAAe;AAAM;AAAA,MAAO;AAAA,IACrE;AAGA,UAAM,mBAAmB,EAAE,KAAK,SAAS,EAAE,EAAE,IAAK,eAAe,KAAK,KAAM,WAAW,EAAE,EAAE,IAAI,KAAK;AACpG,UAAM,KAAK,KAAK,IAAI,mBAAmB,WAAW;AAClD,UAAM,gBAAgB,SAAS,EAAE,EAAE,IAAK,eAAe,KAAK,KAAM,WAAW,EAAE,EAAE,IAAI,KAAK;AAG1F,UAAM,gBAAgB,EAAE;AACxB,UAAM,kBAAkB,SAAS,EAAE,EAAE,IAAK,eAAe,MAAM,MAAO,WAAW,EAAE,EAAE,IAAI,KAAK;AAE9F,WAAO,MAAM,iBAAiB,eAAe,gBAAgB,MAAM,eAAe,gBAAgB;AAAA,EACpG,CAAC;AAED,MAAI,WAAW,WAAW,EAAG,QAAO;AAGpC,MAAI,eAAe;AACjB,UAAM,KAAK,eAAe,aAAa;AACvC,UAAM,cAAc,WAAW,KAAK,OAAK,EAAE,QAAQ,IAAI,kBAAkB;AACzE,QAAI,YAAa,QAAO;AAAA,EAC1B;AAGA,SAAO,WAAW,OAAO,CAAC,SAAS,YAAY;AAC7C,UAAM,YAAY,KAAK,IAAI,QAAQ,IAAI,WAAW;AAClD,UAAM,YAAY,KAAK,IAAI,QAAQ,IAAI,WAAW;AAClD,WAAO,YAAY,YAAY,UAAU;AAAA,EAC3C,CAAC;AACH;AAGO,gBAAS,SAAS,MAAwB;AAC/C,MAAI,KAAK,SAAS,YAAa,QAAO;AACtC,SAAO,MAAM,MAAM;AAAA,IAAK,CAAC,UACvB,MAAM,QAAQ,KAAK,QAClB,MAAM,OAAO,mBAAmB,MAAM,OAAO,qBAC9C,eAAe,MAAM,GAAG,EAAE,MAC1B,KAAK,IAAI,MAAM,IAAI,KAAK,CAAC,IAAI,MAAM,MAAM,IAAI,KAAK,KAAK,MAAM,IAAI,KAAK,IAAI;AAAA,EAC5E;AACF;AAGA,IAAI,WAAW;AAER,gBAAS,gBAAgB,SAAyC;AACvE,WAAS,MAAM;AAEb,WAAO,KAAK,YAAY,EAAE,QAAQ,OAAK,OAAO,aAAa,CAAC,CAAC;AAG7D,eAAW,QAAQ,MAAM,OAAO;AAC9B,UAAI,KAAK,OAAO,UAAW;AAC3B,YAAM,SAAS,WAAW,KAAK,GAAG;AAClC,UAAI,CAAC,OAAO,aAAa,OAAO,UAAU,EAAG;AAE7C,YAAM,YAAY,mBAAmB,IAAI;AACzC,UAAI,CAAC,UAAW;AAChB,YAAM,UAAU,UAAU,UAAU,GAAG;AACvC,UAAI,QAAQ,UAAU,QAAQ,UAAW;AAGzC,uBAAiB;AAEjB,YAAM,WAAW;AACjB,YAAM,WAAW,KAAK,IAAI,UAAU,OAAO,QAAQ,QAAQ,YAAY,QAAQ,MAAM;AAErF,aAAO,SAAS,EAAE,OAAO,SAAS,UAAU,QAAQ,CAAC;AAErD,gCAA0B,KAAK,GAAG,KAAK,0BAA0B,KAAK,GAAG,KAAK,KAAK;AAGnF,cAAQ,SAAS,EAAE,QAAQ,SAAS,UAAU,QAAQ,CAAC;AAGvD,UAAI,OAAO,YAAY;AACrB,mCAA2B;AAAA,UACzB,WAAW,KAAK;AAAA,UAChB,WAAW,UAAU;AAAA,UACrB,kBAAkB,OAAO;AAAA,UACzB,kBAAkB,QAAQ,cAAc;AAAA,UACxC,YAAY;AAAA,QACd,CAAC;AAGD,YAAI,QAAQ,OAAO,QAAQ,QAAQ,OAAO,QAAW;AACnD,cAAI,QAAQ,MAAM,GAAK;AACrB,2BAAe,QAAQ;AAAA,UACzB,WAAW,QAAQ,MAAM,GAAK;AAC5B,2BAAe,QAAQ;AAAA,UACzB,WAAW,QAAQ,MAAM,KAAK;AAC5B,2BAAe,QAAQ;AAAA,UACzB;AAAA,QACF;AAAA,MACF,OAAO;AAEL,gBAAQ,QAAQ,OAAO;AACvB,gBAAQ,UAAU,OAAO;AAAA,MAC3B;AACA,mBAAa,UAAU,GAAG,IAAI;AAAA,IAChC;AAGA,eAAW,QAAQ,MAAM,OAAO;AAC9B,UAAI,CAAC,YAAY,KAAK,EAAE,EAAG;AAC3B,YAAM,MAAM,UAAU,KAAK,GAAG;AAC9B,YAAM,SAAS,MAAM,MAAM;AAAA,QAAK,CAAC,MAC/B,EAAE,QAAQ,KAAK,QACd,EAAE,OAAO,mBAAmB,EAAE,OAAO,qBACtC,eAAe,EAAE,GAAG,EAAE,MACtB,KAAK,IAAI,EAAE,IAAI,KAAK,CAAC,IAAI,MAAM,EAAE,IAAI,KAAK,KAAK,EAAE,IAAI,KAAK,IAAI;AAAA,MAChE;AACA,YAAM,aAAa,CAAC,CAAC,WAAW,KAAK,GAAG;AACxC,UAAI,QAAQ;AACV,YAAI,SAAS;AACb,cAAM,YAAY,eAAe,OAAO,GAAG,EAAE;AAE7C,cAAM,OAAO,OAAO,YAAY,SAAS;AACzC,YAAI,IAAI,cAAc,IAAK,KAAI,cAAc,KAAK,IAAI,KAAK,EAAE,IAAI,cAAc,MAAM,QAAQ,CAAC,CAAC;AAE/F,YAAI,CAAC,cAAc,IAAI,SAAS,KAAK,IAAI,cAAc,IAAI;AACzD,gBAAM,WAAW,OAAO,YAAY,SAAS;AAC7C,cAAI,SAAS,KAAK,IAAI,GAAG,EAAE,IAAI,SAAS,UAAU,QAAQ,CAAC,CAAC;AAAA,QAC9D;AAAA,MACF,OAAO;AACL,YAAI,SAAS;AACb,cAAM,WAAW,OAAO,SAAS;AACjC,YAAI,IAAI,cAAc,GAAI,KAAI,cAAc,KAAK,IAAI,IAAI,EAAE,IAAI,cAAc,UAAU,QAAQ,CAAC,CAAC;AAAA,MACnG;AACA,YAAM,SAAS,MAAM,MAAM;AAAA,QAAK,CAAC,UAC/B,MAAM,OAAO,cAAc,cAAc,MAAM,GAAG,KAClD,KAAK,IAAK,KAAK,IAAI,KAAM,cAAc,MAAM,GAAG,EAAE,CAAC,IAAI,MACvD,KAAK,IAAK,KAAK,IAAI,KAAM,cAAc,MAAM,GAAG,EAAE,CAAC,IAAI;AAAA,MACzD;AACA,UAAI,QAAQ;AACV,YAAI,KAAK,aAAa,MAAM;AAAA,MAC9B;AAAA,IACF;AAGA,eAAW,CAAC,QAAQ,MAAM,KAAK,OAAO,QAAQ,WAAW,GAAG;AAC1D,YAAM,MAAM,UAAU,MAAM;AAC5B,YAAM,MAAM,UAAU,MAAM;AAC5B,YAAM,WAAW;AACjB,YAAM,SAAS,KAAK,IAAI,UAAU,IAAI,QAAQ,IAAI,YAAY,IAAI,MAAM;AACxE,UAAI,SAAS,GAAG;AACd,YAAI,SAAS,EAAE,IAAI,SAAS,QAAQ,QAAQ,CAAC;AAC7C,YAAI,SAAS,EAAE,IAAI,SAAS,QAAQ,QAAQ,CAAC;AAC7C,YAAI,QAAQ,IAAI;AAAO,YAAI,UAAU,IAAI;AACzC,YAAI,CAAC,IAAI,MAAM,SAAS,GAAG,KAAK,IAAI,UAAU,IAAI,OAAO;AACvD,cAAI,QAAQ,IAAI,QAAQ,QAAQ,IAAI;AAAA,QACtC;AACA,qBAAa,MAAM,IAAI;AAAA,MACzB,OAAO;AAEL,eAAO,YAAY,MAAM;AAAA,MAC3B;AAAA,IACF;AAGA,eAAW,QAAQ,MAAM,OAAO;AAC9B,UAAI,CAAC,YAAY,KAAK,EAAE,EAAG;AAC3B,YAAM,QAAQ,aAAa,KAAK,GAAG,KAAK;AACxC,UAAI,KAAK,IAAI,KAAK,IAAI,GAAI;AAC1B,YAAM,MAAM,UAAU,KAAK,GAAG;AAC9B,UAAI,IAAI,UAAU,EAAG;AACrB,UAAI,YAAY,KAAK,GAAG,EAAG;AAE3B,YAAM,QAAQ,MAAM,MAAM;AAAA,QAAK,CAAC,MAC9B,EAAE,QAAQ,KAAK,OAAO,YAAY,EAAE,EAAE,KACtC,KAAK,IAAI,EAAE,IAAI,KAAK,CAAC,IAAI,MAAM,EAAE,IAAI,KAAK,IAAI,MAAM,EAAE,IAAI,KAAK,IAAI;AAAA,MACrE;AACA,UAAI,OAAO;AACT,oBAAY,KAAK,GAAG,IAAI,MAAM;AAAA,MAChC;AAAA,IACF;AAEA,eAAW,sBAAsB,GAAG;AAAA,EACtC;AACA,aAAW,sBAAsB,GAAG;AACtC;AAEO,gBAAS,iBAAiB;AAC/B,uBAAqB,QAAQ;AAC/B;",
        "names": []
      }
    },
    {
      "name": "vite:json",
      "start": 1783634639940,
      "end": 1783634639940,
      "order": "normal"
    },
    {
      "name": "vite:worker",
      "start": 1783634639940,
      "end": 1783634639940,
      "order": "normal"
    },
    {
      "name": "vite:define",
      "start": 1783634639940,
      "end": 1783634639940,
      "order": "normal"
    },
    {
      "name": "vite:css-post",
      "start": 1783634639940,
      "end": 1783634639940,
      "order": "normal"
    },
    {
      "name": "vite:build-html",
      "start": 1783634639940,
      "end": 1783634639940,
      "order": "normal"
    },
    {
      "name": "vite:worker-import-meta-url",
      "start": 1783634639940,
      "end": 1783634639940,
      "order": "normal"
    },
    {
      "name": "vite:asset-import-meta-url",
      "start": 1783634639940,
      "end": 1783634639940,
      "order": "normal"
    },
    {
      "name": "commonjs",
      "start": 1783634639940,
      "end": 1783634639940,
      "order": "normal"
    },
    {
      "name": "vite:dynamic-import-vars",
      "start": 1783634639941,
      "end": 1783634639941,
      "order": "normal"
    },
    {
      "name": "vite:import-glob",
      "start": 1783634639941,
      "end": 1783634639941,
      "order": "normal"
    },
    {
      "name": "vite:build-import-analysis",
      "start": 1783634639941,
      "end": 1783634639941,
      "order": "normal"
    },
    {
      "name": "vite:reporter",
      "start": 1783634639941,
      "end": 1783634639941,
      "order": "normal"
    }
  ]
}

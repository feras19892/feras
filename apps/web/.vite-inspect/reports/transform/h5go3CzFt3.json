{
  "resolvedId": "C:/Users/feras/Desktop/feras/apps/web/src/composables/chemistry/useWorkspaceDrag.ts",
  "transforms": [
    {
      "name": "__load__",
      "result": "import { ref } from 'vue';\nimport type { LabItem } from './useChemistryTools';\nimport {\n  items, pourFlowMap, phProbeTipMap, stopperMap,\n  createLabItem, isContainer,\n  getPipette, retortStandMap,\n} from './useChemistryLab';\nimport { isPipette, isRetortStandAssembly } from './chemLabIds';\nimport {\n  getMagnetOffset, isClampAttachable,\n  retortStandSnapUid, bottomClampSnapUid,\n  findNearestSlot, findNearestBottomClamp,\n  finalizeTopSlotSnap, finalizeBottomClampSnap,\n  detachFromTopSlots, detachFromBottomClamp,\n} from './workspaceDragSnap';\nimport { pipetteDraw, pipetteDispense } from './usePipetteActions';\nimport type { ToolDef } from './useChemistryTools';\nimport type { ToolState } from './chemLabTypes';\n\n\n\nexport function useWorkspaceDrag(\n  workspaceRef: { value: HTMLDivElement | null },\n  selectedItemRef: { value: LabItem | null },\n  emit: (name: 'select', item: LabItem | null, state: ToolState | null) => void,\n  buildToolState: (item: LabItem | null) => ToolState | null\n) {\n  const draggingItem = ref<LabItem | null>(null);\n  const dragOffsetX = ref(0);\n  const dragOffsetY = ref(0);\n  let workspaceRectLeft = 0;\n  let workspaceRectTop = 0;\n  let dragStartClientX = 0;\n  let dragStartClientY = 0;\n  let dragStartItemX = 0;\n  let dragStartItemY = 0;\n  let dragJustEnded = false;\n\n  function sceneX(clientX: number): number {\n    if (!workspaceRef.value) return clientX;\n    return clientX - workspaceRef.value.getBoundingClientRect().left;\n  }\n  function sceneY(clientY: number): number {\n    if (!workspaceRef.value) return clientY;\n    return clientY - workspaceRef.value.getBoundingClientRect().top;\n  }\n\n  function onDragOver(e: DragEvent) {\n    e.preventDefault();\n    if (e.dataTransfer) e.dataTransfer.dropEffect = 'copy';\n  }\n\n  function onDrop(e: DragEvent) {\n    e.preventDefault();\n    const data = e.dataTransfer?.getData('application/json');\n    if (!data) return;\n    const def: ToolDef = JSON.parse(data);\n    const item = createLabItem(def, sceneX(e.clientX) - 40, sceneY(e.clientY) - 40);\n    items.value.push(item);\n    // --- Clamp-attached tool snap on drop to nearest stand slot ---\n    if (isClampAttachable(item.id)) {\n      const result = findNearestSlot(item.uid, item.x, item.y, item.id);\n      if (result) {\n        const st = retortStandMap[result.standUid];\n        const stand = items.value.find(i => i.uid === result.standUid);\n        if (!stand || !st) return;\n        const slotWorldX = stand.x + 54 + st.slotOffsets[result.slotIdx];\n        const slotWorldY = stand.y + st.topClampY + 14;\n        const off = getMagnetOffset(item.id);\n        item.x = slotWorldX - off.x;\n        item.y = slotWorldY - off.y;\n        st.slotOccupants[result.slotIdx] = item.uid;\n      }\n    }\n  }\n\n  function onItemMouseDown(e: MouseEvent, item: LabItem) {\n    if (pourFlowMap[item.uid]) {\n      delete pourFlowMap[item.uid];\n      return;\n    }\n    draggingItem.value = item;\n    if (workspaceRef.value) {\n      const rect = workspaceRef.value.getBoundingClientRect();\n      workspaceRectLeft = rect.left;\n      workspaceRectTop = rect.top;\n    }\n    dragStartClientX = e.clientX;\n    dragStartClientY = e.clientY;\n    dragStartItemX = item.x;\n    dragStartItemY = item.y;\n    dragOffsetX.value = (e.clientX - workspaceRectLeft) - item.x;\n    dragOffsetY.value = (e.clientY - workspaceRectTop) - item.y;\n    selectedItemRef.value = item;\n    emit('select', item, buildToolState(item));\n  }\n\n  let rafId: number | null = null;\n  let pendingClientX = 0;\n  let pendingClientY = 0;\n  let lastSnapTime = 0;\n\n  function applyDrag(newClientX: number, newClientY: number) {\n    const item = draggingItem.value;\n    if (!item) return;\n    const prevX = item.x;\n    const prevY = item.y;\n    const newX = dragStartItemX + (newClientX - dragStartClientX);\n    const newY = dragStartItemY + (newClientY - dragStartClientY);\n    item.x = newX;\n    item.y = newY;\n    const dx = newX - prevX;\n    const dy = newY - prevY;\n\n    if (item.id === 'ph-meter' && phProbeTipMap[item.uid]) {\n      phProbeTipMap[item.uid].x += dx;\n      phProbeTipMap[item.uid].y += dy;\n    }\n\n    // --- Stand drag syncs attached burette and bottom clamp beaker positions ---\n    if (isRetortStandAssembly(item.id)) {\n      const st = retortStandMap[item.uid];\n      if (st) {\n        for (const uid of st.slotOccupants.filter(Boolean) as string[]) {\n          const b = items.value.find(i => i.uid === uid);\n          if (b) { b.x += dx; b.y += dy; }\n        }\n        if (st.bottomSlotOccupant) {\n          const beaker = items.value.find(i => i.uid === st.bottomSlotOccupant);\n          if (beaker) { beaker.x += dx; beaker.y += dy; }\n        }\n      }\n    }\n\n    // --- Clamp-attached tool snap detection (throttled to every 50ms) ---\n    if (isClampAttachable(item.id)) {\n      const now = performance.now();\n      if (now - lastSnapTime > 50) {\n        lastSnapTime = now;\n        detachFromTopSlots(item.uid);\n        const result = findNearestSlot(item.uid, item.x, item.y, item.id);\n        retortStandSnapUid.value = result ? result.standUid + '|' + result.slotIdx : null;\n      }\n    } else {\n      retortStandSnapUid.value = null;\n    }\n\n    // --- Bottom clamp beaker snap detection (throttled) ---\n    if (item.id === 'beaker-100' || item.id === 'beaker-250' || item.id === 'beaker-500') {\n      const now = performance.now();\n      if (now - lastSnapTime > 50) {\n        lastSnapTime = now;\n        detachFromBottomClamp(item.uid);\n        bottomClampSnapUid.value = findNearestBottomClamp(item.uid, item.x, item.y);\n      }\n    } else {\n      bottomClampSnapUid.value = null;\n    }\n  }\n\n  function onDragMove(e: MouseEvent) {\n    if (!draggingItem.value) return;\n    pendingClientX = e.clientX;\n    pendingClientY = e.clientY;\n    if (rafId) return;\n    rafId = requestAnimationFrame(() => {\n      rafId = null;\n      applyDrag(pendingClientX, pendingClientY);\n    });\n  }\n\n  function onDragUp() {\n    if (rafId) { cancelAnimationFrame(rafId); rafId = null; }\n    if (!draggingItem.value) { draggingItem.value = null; return; }\n    const item = draggingItem.value;\n\n    // --- Clamp-attached tool snap to retort stand slot ---\n    if (isClampAttachable(item.id)) {\n      finalizeTopSlotSnap(item.uid);\n    }\n\n    // --- Bottom clamp beaker snap ---\n    if (item.id === 'beaker-100' || item.id === 'beaker-250' || item.id === 'beaker-500') {\n      finalizeBottomClampSnap(item.uid);\n    }\n\n    // Rubber stopper snap\n    if (item.id === 'rubber-stopper') {\n      const nearest = items.value.find(i =>\n        i.uid !== item.uid && isContainer(i.id) &&\n        Math.abs(i.x - item.x) < 50 && Math.abs(i.y - item.y) < 80\n      );\n      if (nearest) {\n        stopperMap[nearest.uid] = item.uid;\n        item.x = nearest.x + 20;\n        item.y = nearest.y - 5;\n      }\n    }\n\n    // Pipette drag-to-draw / drag-to-dispense\n    if (isPipette(item.id)) {\n      const pip = getPipette(item.uid);\n      if (pip) {\n        const nearest = items.value.find(i =>\n          i.uid !== item.uid && isContainer(i.id) &&\n          Math.abs(i.x - item.x) < 60 && Math.abs(i.y - item.y) < 120\n        );\n        if (nearest) {\n          if (pip.volume <= 0) {\n            pipetteDraw(item, selectedItemRef, emit);\n          } else {\n            pipetteDispense(item, selectedItemRef, emit);\n          }\n        }\n      }\n    }\n\n    dragJustEnded = true;\n    draggingItem.value = null;\n  }\n\n  function onWorkspaceClick(e: MouseEvent) {\n    if (dragJustEnded) { dragJustEnded = false; return; }\n    if ((e.target as HTMLElement).closest('.lab-item')) return;\n    if ((e.target as HTMLElement).closest('.floating-inspector')) return;\n    selectedItemRef.value = null;\n    emit('select', null, null);\n    if (Object.keys(pourFlowMap).length > 0) {\n      Object.keys(pourFlowMap).forEach(k => delete pourFlowMap[k]);\n    }\n  }\n\n  return {\n    draggingItem,\n    dragOffsetX,\n    dragOffsetY,\n    onDragOver,\n    onDrop,\n    onItemMouseDown,\n    onDragMove,\n    onDragUp,\n    onWorkspaceClick,\n  };\n}\n",
      "start": 1783626308066,
      "end": 1783626308066
    },
    {
      "name": "vite-plugin-vue-inspector",
      "start": 1783626308066,
      "end": 1783626308066,
      "order": "pre"
    },
    {
      "name": "vite-plugin-vue-devtools",
      "result": "import { ref } from 'vue';\nimport type { LabItem } from './useChemistryTools';\nimport {\n  items, pourFlowMap, phProbeTipMap, stopperMap,\n  createLabItem, isContainer,\n  getPipette, retortStandMap,\n} from './useChemistryLab';\nimport { isPipette, isRetortStandAssembly } from './chemLabIds';\nimport {\n  getMagnetOffset, isClampAttachable,\n  retortStandSnapUid, bottomClampSnapUid,\n  findNearestSlot, findNearestBottomClamp,\n  finalizeTopSlotSnap, finalizeBottomClampSnap,\n  detachFromTopSlots, detachFromBottomClamp,\n} from './workspaceDragSnap';\nimport { pipetteDraw, pipetteDispense } from './usePipetteActions';\nimport type { ToolDef } from './useChemistryTools';\nimport type { ToolState } from './chemLabTypes';\n\n\n\nexport function useWorkspaceDrag(\n  workspaceRef: { value: HTMLDivElement | null },\n  selectedItemRef: { value: LabItem | null },\n  emit: (name: 'select', item: LabItem | null, state: ToolState | null) => void,\n  buildToolState: (item: LabItem | null) => ToolState | null\n) {\n  const draggingItem = ref<LabItem | null>(null);\n  const dragOffsetX = ref(0);\n  const dragOffsetY = ref(0);\n  let workspaceRectLeft = 0;\n  let workspaceRectTop = 0;\n  let dragStartClientX = 0;\n  let dragStartClientY = 0;\n  let dragStartItemX = 0;\n  let dragStartItemY = 0;\n  let dragJustEnded = false;\n\n  function sceneX(clientX: number): number {\n    if (!workspaceRef.value) return clientX;\n    return clientX - workspaceRef.value.getBoundingClientRect().left;\n  }\n  function sceneY(clientY: number): number {\n    if (!workspaceRef.value) return clientY;\n    return clientY - workspaceRef.value.getBoundingClientRect().top;\n  }\n\n  function onDragOver(e: DragEvent) {\n    e.preventDefault();\n    if (e.dataTransfer) e.dataTransfer.dropEffect = 'copy';\n  }\n\n  function onDrop(e: DragEvent) {\n    e.preventDefault();\n    const data = e.dataTransfer?.getData('application/json');\n    if (!data) return;\n    const def: ToolDef = JSON.parse(data);\n    const item = createLabItem(def, sceneX(e.clientX) - 40, sceneY(e.clientY) - 40);\n    items.value.push(item);\n    // --- Clamp-attached tool snap on drop to nearest stand slot ---\n    if (isClampAttachable(item.id)) {\n      const result = findNearestSlot(item.uid, item.x, item.y, item.id);\n      if (result) {\n        const st = retortStandMap[result.standUid];\n        const stand = items.value.find(i => i.uid === result.standUid);\n        if (!stand || !st) return;\n        const slotWorldX = stand.x + 54 + st.slotOffsets[result.slotIdx];\n        const slotWorldY = stand.y + st.topClampY + 14;\n        const off = getMagnetOffset(item.id);\n        item.x = slotWorldX - off.x;\n        item.y = slotWorldY - off.y;\n        st.slotOccupants[result.slotIdx] = item.uid;\n      }\n    }\n  }\n\n  function onItemMouseDown(e: MouseEvent, item: LabItem) {\n    if (pourFlowMap[item.uid]) {\n      delete pourFlowMap[item.uid];\n      return;\n    }\n    draggingItem.value = item;\n    if (workspaceRef.value) {\n      const rect = workspaceRef.value.getBoundingClientRect();\n      workspaceRectLeft = rect.left;\n      workspaceRectTop = rect.top;\n    }\n    dragStartClientX = e.clientX;\n    dragStartClientY = e.clientY;\n    dragStartItemX = item.x;\n    dragStartItemY = item.y;\n    dragOffsetX.value = (e.clientX - workspaceRectLeft) - item.x;\n    dragOffsetY.value = (e.clientY - workspaceRectTop) - item.y;\n    selectedItemRef.value = item;\n    emit('select', item, buildToolState(item));\n  }\n\n  let rafId: number | null = null;\n  let pendingClientX = 0;\n  let pendingClientY = 0;\n  let lastSnapTime = 0;\n\n  function applyDrag(newClientX: number, newClientY: number) {\n    const item = draggingItem.value;\n    if (!item) return;\n    const prevX = item.x;\n    const prevY = item.y;\n    const newX = dragStartItemX + (newClientX - dragStartClientX);\n    const newY = dragStartItemY + (newClientY - dragStartClientY);\n    item.x = newX;\n    item.y = newY;\n    const dx = newX - prevX;\n    const dy = newY - prevY;\n\n    if (item.id === 'ph-meter' && phProbeTipMap[item.uid]) {\n      phProbeTipMap[item.uid].x += dx;\n      phProbeTipMap[item.uid].y += dy;\n    }\n\n    // --- Stand drag syncs attached burette and bottom clamp beaker positions ---\n    if (isRetortStandAssembly(item.id)) {\n      const st = retortStandMap[item.uid];\n      if (st) {\n        for (const uid of st.slotOccupants.filter(Boolean) as string[]) {\n          const b = items.value.find(i => i.uid === uid);\n          if (b) { b.x += dx; b.y += dy; }\n        }\n        if (st.bottomSlotOccupant) {\n          const beaker = items.value.find(i => i.uid === st.bottomSlotOccupant);\n          if (beaker) { beaker.x += dx; beaker.y += dy; }\n        }\n      }\n    }\n\n    // --- Clamp-attached tool snap detection (throttled to every 50ms) ---\n    if (isClampAttachable(item.id)) {\n      const now = performance.now();\n      if (now - lastSnapTime > 50) {\n        lastSnapTime = now;\n        detachFromTopSlots(item.uid);\n        const result = findNearestSlot(item.uid, item.x, item.y, item.id);\n        retortStandSnapUid.value = result ? result.standUid + '|' + result.slotIdx : null;\n      }\n    } else {\n      retortStandSnapUid.value = null;\n    }\n\n    // --- Bottom clamp beaker snap detection (throttled) ---\n    if (item.id === 'beaker-100' || item.id === 'beaker-250' || item.id === 'beaker-500') {\n      const now = performance.now();\n      if (now - lastSnapTime > 50) {\n        lastSnapTime = now;\n        detachFromBottomClamp(item.uid);\n        bottomClampSnapUid.value = findNearestBottomClamp(item.uid, item.x, item.y);\n      }\n    } else {\n      bottomClampSnapUid.value = null;\n    }\n  }\n\n  function onDragMove(e: MouseEvent) {\n    if (!draggingItem.value) return;\n    pendingClientX = e.clientX;\n    pendingClientY = e.clientY;\n    if (rafId) return;\n    rafId = requestAnimationFrame(() => {\n      rafId = null;\n      applyDrag(pendingClientX, pendingClientY);\n    });\n  }\n\n  function onDragUp() {\n    if (rafId) { cancelAnimationFrame(rafId); rafId = null; }\n    if (!draggingItem.value) { draggingItem.value = null; return; }\n    const item = draggingItem.value;\n\n    // --- Clamp-attached tool snap to retort stand slot ---\n    if (isClampAttachable(item.id)) {\n      finalizeTopSlotSnap(item.uid);\n    }\n\n    // --- Bottom clamp beaker snap ---\n    if (item.id === 'beaker-100' || item.id === 'beaker-250' || item.id === 'beaker-500') {\n      finalizeBottomClampSnap(item.uid);\n    }\n\n    // Rubber stopper snap\n    if (item.id === 'rubber-stopper') {\n      const nearest = items.value.find(i =>\n        i.uid !== item.uid && isContainer(i.id) &&\n        Math.abs(i.x - item.x) < 50 && Math.abs(i.y - item.y) < 80\n      );\n      if (nearest) {\n        stopperMap[nearest.uid] = item.uid;\n        item.x = nearest.x + 20;\n        item.y = nearest.y - 5;\n      }\n    }\n\n    // Pipette drag-to-draw / drag-to-dispense\n    if (isPipette(item.id)) {\n      const pip = getPipette(item.uid);\n      if (pip) {\n        const nearest = items.value.find(i =>\n          i.uid !== item.uid && isContainer(i.id) &&\n          Math.abs(i.x - item.x) < 60 && Math.abs(i.y - item.y) < 120\n        );\n        if (nearest) {\n          if (pip.volume <= 0) {\n            pipetteDraw(item, selectedItemRef, emit);\n          } else {\n            pipetteDispense(item, selectedItemRef, emit);\n          }\n        }\n      }\n    }\n\n    dragJustEnded = true;\n    draggingItem.value = null;\n  }\n\n  function onWorkspaceClick(e: MouseEvent) {\n    if (dragJustEnded) { dragJustEnded = false; return; }\n    if ((e.target as HTMLElement).closest('.lab-item')) return;\n    if ((e.target as HTMLElement).closest('.floating-inspector')) return;\n    selectedItemRef.value = null;\n    emit('select', null, null);\n    if (Object.keys(pourFlowMap).length > 0) {\n      Object.keys(pourFlowMap).forEach(k => delete pourFlowMap[k]);\n    }\n  }\n\n  return {\n    draggingItem,\n    dragOffsetX,\n    dragOffsetY,\n    onDragOver,\n    onDrop,\n    onItemMouseDown,\n    onDragMove,\n    onDragUp,\n    onWorkspaceClick,\n  };\n}\n",
      "start": 1783626308066,
      "end": 1783626308066,
      "order": "pre",
      "sourcemaps": null
    },
    {
      "name": "vite:css",
      "start": 1783626308066,
      "end": 1783626308066,
      "order": "normal"
    },
    {
      "name": "vite:esbuild",
      "result": "import { ref } from \"vue\";\nimport {\n  items,\n  pourFlowMap,\n  phProbeTipMap,\n  stopperMap,\n  createLabItem,\n  isContainer,\n  getPipette,\n  retortStandMap\n} from \"./useChemistryLab\";\nimport { isPipette, isRetortStandAssembly } from \"./chemLabIds\";\nimport {\n  getMagnetOffset,\n  isClampAttachable,\n  retortStandSnapUid,\n  bottomClampSnapUid,\n  findNearestSlot,\n  findNearestBottomClamp,\n  finalizeTopSlotSnap,\n  finalizeBottomClampSnap,\n  detachFromTopSlots,\n  detachFromBottomClamp\n} from \"./workspaceDragSnap\";\nimport { pipetteDraw, pipetteDispense } from \"./usePipetteActions\";\nexport function useWorkspaceDrag(workspaceRef, selectedItemRef, emit, buildToolState) {\n  const draggingItem = ref(null);\n  const dragOffsetX = ref(0);\n  const dragOffsetY = ref(0);\n  let workspaceRectLeft = 0;\n  let workspaceRectTop = 0;\n  let dragStartClientX = 0;\n  let dragStartClientY = 0;\n  let dragStartItemX = 0;\n  let dragStartItemY = 0;\n  let dragJustEnded = false;\n  function sceneX(clientX) {\n    if (!workspaceRef.value) return clientX;\n    return clientX - workspaceRef.value.getBoundingClientRect().left;\n  }\n  function sceneY(clientY) {\n    if (!workspaceRef.value) return clientY;\n    return clientY - workspaceRef.value.getBoundingClientRect().top;\n  }\n  function onDragOver(e) {\n    e.preventDefault();\n    if (e.dataTransfer) e.dataTransfer.dropEffect = \"copy\";\n  }\n  function onDrop(e) {\n    e.preventDefault();\n    const data = e.dataTransfer?.getData(\"application/json\");\n    if (!data) return;\n    const def = JSON.parse(data);\n    const item = createLabItem(def, sceneX(e.clientX) - 40, sceneY(e.clientY) - 40);\n    items.value.push(item);\n    if (isClampAttachable(item.id)) {\n      const result = findNearestSlot(item.uid, item.x, item.y, item.id);\n      if (result) {\n        const st = retortStandMap[result.standUid];\n        const stand = items.value.find((i) => i.uid === result.standUid);\n        if (!stand || !st) return;\n        const slotWorldX = stand.x + 54 + st.slotOffsets[result.slotIdx];\n        const slotWorldY = stand.y + st.topClampY + 14;\n        const off = getMagnetOffset(item.id);\n        item.x = slotWorldX - off.x;\n        item.y = slotWorldY - off.y;\n        st.slotOccupants[result.slotIdx] = item.uid;\n      }\n    }\n  }\n  function onItemMouseDown(e, item) {\n    if (pourFlowMap[item.uid]) {\n      delete pourFlowMap[item.uid];\n      return;\n    }\n    draggingItem.value = item;\n    if (workspaceRef.value) {\n      const rect = workspaceRef.value.getBoundingClientRect();\n      workspaceRectLeft = rect.left;\n      workspaceRectTop = rect.top;\n    }\n    dragStartClientX = e.clientX;\n    dragStartClientY = e.clientY;\n    dragStartItemX = item.x;\n    dragStartItemY = item.y;\n    dragOffsetX.value = e.clientX - workspaceRectLeft - item.x;\n    dragOffsetY.value = e.clientY - workspaceRectTop - item.y;\n    selectedItemRef.value = item;\n    emit(\"select\", item, buildToolState(item));\n  }\n  let rafId = null;\n  let pendingClientX = 0;\n  let pendingClientY = 0;\n  let lastSnapTime = 0;\n  function applyDrag(newClientX, newClientY) {\n    const item = draggingItem.value;\n    if (!item) return;\n    const prevX = item.x;\n    const prevY = item.y;\n    const newX = dragStartItemX + (newClientX - dragStartClientX);\n    const newY = dragStartItemY + (newClientY - dragStartClientY);\n    item.x = newX;\n    item.y = newY;\n    const dx = newX - prevX;\n    const dy = newY - prevY;\n    if (item.id === \"ph-meter\" && phProbeTipMap[item.uid]) {\n      phProbeTipMap[item.uid].x += dx;\n      phProbeTipMap[item.uid].y += dy;\n    }\n    if (isRetortStandAssembly(item.id)) {\n      const st = retortStandMap[item.uid];\n      if (st) {\n        for (const uid of st.slotOccupants.filter(Boolean)) {\n          const b = items.value.find((i) => i.uid === uid);\n          if (b) {\n            b.x += dx;\n            b.y += dy;\n          }\n        }\n        if (st.bottomSlotOccupant) {\n          const beaker = items.value.find((i) => i.uid === st.bottomSlotOccupant);\n          if (beaker) {\n            beaker.x += dx;\n            beaker.y += dy;\n          }\n        }\n      }\n    }\n    if (isClampAttachable(item.id)) {\n      const now = performance.now();\n      if (now - lastSnapTime > 50) {\n        lastSnapTime = now;\n        detachFromTopSlots(item.uid);\n        const result = findNearestSlot(item.uid, item.x, item.y, item.id);\n        retortStandSnapUid.value = result ? result.standUid + \"|\" + result.slotIdx : null;\n      }\n    } else {\n      retortStandSnapUid.value = null;\n    }\n    if (item.id === \"beaker-100\" || item.id === \"beaker-250\" || item.id === \"beaker-500\") {\n      const now = performance.now();\n      if (now - lastSnapTime > 50) {\n        lastSnapTime = now;\n        detachFromBottomClamp(item.uid);\n        bottomClampSnapUid.value = findNearestBottomClamp(item.uid, item.x, item.y);\n      }\n    } else {\n      bottomClampSnapUid.value = null;\n    }\n  }\n  function onDragMove(e) {\n    if (!draggingItem.value) return;\n    pendingClientX = e.clientX;\n    pendingClientY = e.clientY;\n    if (rafId) return;\n    rafId = requestAnimationFrame(() => {\n      rafId = null;\n      applyDrag(pendingClientX, pendingClientY);\n    });\n  }\n  function onDragUp() {\n    if (rafId) {\n      cancelAnimationFrame(rafId);\n      rafId = null;\n    }\n    if (!draggingItem.value) {\n      draggingItem.value = null;\n      return;\n    }\n    const item = draggingItem.value;\n    if (isClampAttachable(item.id)) {\n      finalizeTopSlotSnap(item.uid);\n    }\n    if (item.id === \"beaker-100\" || item.id === \"beaker-250\" || item.id === \"beaker-500\") {\n      finalizeBottomClampSnap(item.uid);\n    }\n    if (item.id === \"rubber-stopper\") {\n      const nearest = items.value.find(\n        (i) => i.uid !== item.uid && isContainer(i.id) && Math.abs(i.x - item.x) < 50 && Math.abs(i.y - item.y) < 80\n      );\n      if (nearest) {\n        stopperMap[nearest.uid] = item.uid;\n        item.x = nearest.x + 20;\n        item.y = nearest.y - 5;\n      }\n    }\n    if (isPipette(item.id)) {\n      const pip = getPipette(item.uid);\n      if (pip) {\n        const nearest = items.value.find(\n          (i) => i.uid !== item.uid && isContainer(i.id) && Math.abs(i.x - item.x) < 60 && Math.abs(i.y - item.y) < 120\n        );\n        if (nearest) {\n          if (pip.volume <= 0) {\n            pipetteDraw(item, selectedItemRef, emit);\n          } else {\n            pipetteDispense(item, selectedItemRef, emit);\n          }\n        }\n      }\n    }\n    dragJustEnded = true;\n    draggingItem.value = null;\n  }\n  function onWorkspaceClick(e) {\n    if (dragJustEnded) {\n      dragJustEnded = false;\n      return;\n    }\n    if (e.target.closest(\".lab-item\")) return;\n    if (e.target.closest(\".floating-inspector\")) return;\n    selectedItemRef.value = null;\n    emit(\"select\", null, null);\n    if (Object.keys(pourFlowMap).length > 0) {\n      Object.keys(pourFlowMap).forEach((k) => delete pourFlowMap[k]);\n    }\n  }\n  return {\n    draggingItem,\n    dragOffsetX,\n    dragOffsetY,\n    onDragOver,\n    onDrop,\n    onItemMouseDown,\n    onDragMove,\n    onDragUp,\n    onWorkspaceClick\n  };\n}\n",
      "start": 1783626308066,
      "end": 1783626308636,
      "order": "normal",
      "sourcemaps": {
        "version": 3,
        "sources": [
          "useWorkspaceDrag.ts"
        ],
        "sourcesContent": [
          "import { ref } from 'vue';\nimport type { LabItem } from './useChemistryTools';\nimport {\n  items, pourFlowMap, phProbeTipMap, stopperMap,\n  createLabItem, isContainer,\n  getPipette, retortStandMap,\n} from './useChemistryLab';\nimport { isPipette, isRetortStandAssembly } from './chemLabIds';\nimport {\n  getMagnetOffset, isClampAttachable,\n  retortStandSnapUid, bottomClampSnapUid,\n  findNearestSlot, findNearestBottomClamp,\n  finalizeTopSlotSnap, finalizeBottomClampSnap,\n  detachFromTopSlots, detachFromBottomClamp,\n} from './workspaceDragSnap';\nimport { pipetteDraw, pipetteDispense } from './usePipetteActions';\nimport type { ToolDef } from './useChemistryTools';\nimport type { ToolState } from './chemLabTypes';\n\n\n\nexport function useWorkspaceDrag(\n  workspaceRef: { value: HTMLDivElement | null },\n  selectedItemRef: { value: LabItem | null },\n  emit: (name: 'select', item: LabItem | null, state: ToolState | null) => void,\n  buildToolState: (item: LabItem | null) => ToolState | null\n) {\n  const draggingItem = ref<LabItem | null>(null);\n  const dragOffsetX = ref(0);\n  const dragOffsetY = ref(0);\n  let workspaceRectLeft = 0;\n  let workspaceRectTop = 0;\n  let dragStartClientX = 0;\n  let dragStartClientY = 0;\n  let dragStartItemX = 0;\n  let dragStartItemY = 0;\n  let dragJustEnded = false;\n\n  function sceneX(clientX: number): number {\n    if (!workspaceRef.value) return clientX;\n    return clientX - workspaceRef.value.getBoundingClientRect().left;\n  }\n  function sceneY(clientY: number): number {\n    if (!workspaceRef.value) return clientY;\n    return clientY - workspaceRef.value.getBoundingClientRect().top;\n  }\n\n  function onDragOver(e: DragEvent) {\n    e.preventDefault();\n    if (e.dataTransfer) e.dataTransfer.dropEffect = 'copy';\n  }\n\n  function onDrop(e: DragEvent) {\n    e.preventDefault();\n    const data = e.dataTransfer?.getData('application/json');\n    if (!data) return;\n    const def: ToolDef = JSON.parse(data);\n    const item = createLabItem(def, sceneX(e.clientX) - 40, sceneY(e.clientY) - 40);\n    items.value.push(item);\n    // --- Clamp-attached tool snap on drop to nearest stand slot ---\n    if (isClampAttachable(item.id)) {\n      const result = findNearestSlot(item.uid, item.x, item.y, item.id);\n      if (result) {\n        const st = retortStandMap[result.standUid];\n        const stand = items.value.find(i => i.uid === result.standUid);\n        if (!stand || !st) return;\n        const slotWorldX = stand.x + 54 + st.slotOffsets[result.slotIdx];\n        const slotWorldY = stand.y + st.topClampY + 14;\n        const off = getMagnetOffset(item.id);\n        item.x = slotWorldX - off.x;\n        item.y = slotWorldY - off.y;\n        st.slotOccupants[result.slotIdx] = item.uid;\n      }\n    }\n  }\n\n  function onItemMouseDown(e: MouseEvent, item: LabItem) {\n    if (pourFlowMap[item.uid]) {\n      delete pourFlowMap[item.uid];\n      return;\n    }\n    draggingItem.value = item;\n    if (workspaceRef.value) {\n      const rect = workspaceRef.value.getBoundingClientRect();\n      workspaceRectLeft = rect.left;\n      workspaceRectTop = rect.top;\n    }\n    dragStartClientX = e.clientX;\n    dragStartClientY = e.clientY;\n    dragStartItemX = item.x;\n    dragStartItemY = item.y;\n    dragOffsetX.value = (e.clientX - workspaceRectLeft) - item.x;\n    dragOffsetY.value = (e.clientY - workspaceRectTop) - item.y;\n    selectedItemRef.value = item;\n    emit('select', item, buildToolState(item));\n  }\n\n  let rafId: number | null = null;\n  let pendingClientX = 0;\n  let pendingClientY = 0;\n  let lastSnapTime = 0;\n\n  function applyDrag(newClientX: number, newClientY: number) {\n    const item = draggingItem.value;\n    if (!item) return;\n    const prevX = item.x;\n    const prevY = item.y;\n    const newX = dragStartItemX + (newClientX - dragStartClientX);\n    const newY = dragStartItemY + (newClientY - dragStartClientY);\n    item.x = newX;\n    item.y = newY;\n    const dx = newX - prevX;\n    const dy = newY - prevY;\n\n    if (item.id === 'ph-meter' && phProbeTipMap[item.uid]) {\n      phProbeTipMap[item.uid].x += dx;\n      phProbeTipMap[item.uid].y += dy;\n    }\n\n    // --- Stand drag syncs attached burette and bottom clamp beaker positions ---\n    if (isRetortStandAssembly(item.id)) {\n      const st = retortStandMap[item.uid];\n      if (st) {\n        for (const uid of st.slotOccupants.filter(Boolean) as string[]) {\n          const b = items.value.find(i => i.uid === uid);\n          if (b) { b.x += dx; b.y += dy; }\n        }\n        if (st.bottomSlotOccupant) {\n          const beaker = items.value.find(i => i.uid === st.bottomSlotOccupant);\n          if (beaker) { beaker.x += dx; beaker.y += dy; }\n        }\n      }\n    }\n\n    // --- Clamp-attached tool snap detection (throttled to every 50ms) ---\n    if (isClampAttachable(item.id)) {\n      const now = performance.now();\n      if (now - lastSnapTime > 50) {\n        lastSnapTime = now;\n        detachFromTopSlots(item.uid);\n        const result = findNearestSlot(item.uid, item.x, item.y, item.id);\n        retortStandSnapUid.value = result ? result.standUid + '|' + result.slotIdx : null;\n      }\n    } else {\n      retortStandSnapUid.value = null;\n    }\n\n    // --- Bottom clamp beaker snap detection (throttled) ---\n    if (item.id === 'beaker-100' || item.id === 'beaker-250' || item.id === 'beaker-500') {\n      const now = performance.now();\n      if (now - lastSnapTime > 50) {\n        lastSnapTime = now;\n        detachFromBottomClamp(item.uid);\n        bottomClampSnapUid.value = findNearestBottomClamp(item.uid, item.x, item.y);\n      }\n    } else {\n      bottomClampSnapUid.value = null;\n    }\n  }\n\n  function onDragMove(e: MouseEvent) {\n    if (!draggingItem.value) return;\n    pendingClientX = e.clientX;\n    pendingClientY = e.clientY;\n    if (rafId) return;\n    rafId = requestAnimationFrame(() => {\n      rafId = null;\n      applyDrag(pendingClientX, pendingClientY);\n    });\n  }\n\n  function onDragUp() {\n    if (rafId) { cancelAnimationFrame(rafId); rafId = null; }\n    if (!draggingItem.value) { draggingItem.value = null; return; }\n    const item = draggingItem.value;\n\n    // --- Clamp-attached tool snap to retort stand slot ---\n    if (isClampAttachable(item.id)) {\n      finalizeTopSlotSnap(item.uid);\n    }\n\n    // --- Bottom clamp beaker snap ---\n    if (item.id === 'beaker-100' || item.id === 'beaker-250' || item.id === 'beaker-500') {\n      finalizeBottomClampSnap(item.uid);\n    }\n\n    // Rubber stopper snap\n    if (item.id === 'rubber-stopper') {\n      const nearest = items.value.find(i =>\n        i.uid !== item.uid && isContainer(i.id) &&\n        Math.abs(i.x - item.x) < 50 && Math.abs(i.y - item.y) < 80\n      );\n      if (nearest) {\n        stopperMap[nearest.uid] = item.uid;\n        item.x = nearest.x + 20;\n        item.y = nearest.y - 5;\n      }\n    }\n\n    // Pipette drag-to-draw / drag-to-dispense\n    if (isPipette(item.id)) {\n      const pip = getPipette(item.uid);\n      if (pip) {\n        const nearest = items.value.find(i =>\n          i.uid !== item.uid && isContainer(i.id) &&\n          Math.abs(i.x - item.x) < 60 && Math.abs(i.y - item.y) < 120\n        );\n        if (nearest) {\n          if (pip.volume <= 0) {\n            pipetteDraw(item, selectedItemRef, emit);\n          } else {\n            pipetteDispense(item, selectedItemRef, emit);\n          }\n        }\n      }\n    }\n\n    dragJustEnded = true;\n    draggingItem.value = null;\n  }\n\n  function onWorkspaceClick(e: MouseEvent) {\n    if (dragJustEnded) { dragJustEnded = false; return; }\n    if ((e.target as HTMLElement).closest('.lab-item')) return;\n    if ((e.target as HTMLElement).closest('.floating-inspector')) return;\n    selectedItemRef.value = null;\n    emit('select', null, null);\n    if (Object.keys(pourFlowMap).length > 0) {\n      Object.keys(pourFlowMap).forEach(k => delete pourFlowMap[k]);\n    }\n  }\n\n  return {\n    draggingItem,\n    dragOffsetX,\n    dragOffsetY,\n    onDragOver,\n    onDrop,\n    onItemMouseDown,\n    onDragMove,\n    onDragUp,\n    onWorkspaceClick,\n  };\n}\n"
        ],
        "mappings": "AAAA,SAAS,WAAW;AAEpB;AAAA,EACE;AAAA,EAAO;AAAA,EAAa;AAAA,EAAe;AAAA,EACnC;AAAA,EAAe;AAAA,EACf;AAAA,EAAY;AAAA,OACP;AACP,SAAS,WAAW,6BAA6B;AACjD;AAAA,EACE;AAAA,EAAiB;AAAA,EACjB;AAAA,EAAoB;AAAA,EACpB;AAAA,EAAiB;AAAA,EACjB;AAAA,EAAqB;AAAA,EACrB;AAAA,EAAoB;AAAA,OACf;AACP,SAAS,aAAa,uBAAuB;AAMtC,gBAAS,iBACd,cACA,iBACA,MACA,gBACA;AACA,QAAM,eAAe,IAAoB,IAAI;AAC7C,QAAM,cAAc,IAAI,CAAC;AACzB,QAAM,cAAc,IAAI,CAAC;AACzB,MAAI,oBAAoB;AACxB,MAAI,mBAAmB;AACvB,MAAI,mBAAmB;AACvB,MAAI,mBAAmB;AACvB,MAAI,iBAAiB;AACrB,MAAI,iBAAiB;AACrB,MAAI,gBAAgB;AAEpB,WAAS,OAAO,SAAyB;AACvC,QAAI,CAAC,aAAa,MAAO,QAAO;AAChC,WAAO,UAAU,aAAa,MAAM,sBAAsB,EAAE;AAAA,EAC9D;AACA,WAAS,OAAO,SAAyB;AACvC,QAAI,CAAC,aAAa,MAAO,QAAO;AAChC,WAAO,UAAU,aAAa,MAAM,sBAAsB,EAAE;AAAA,EAC9D;AAEA,WAAS,WAAW,GAAc;AAChC,MAAE,eAAe;AACjB,QAAI,EAAE,aAAc,GAAE,aAAa,aAAa;AAAA,EAClD;AAEA,WAAS,OAAO,GAAc;AAC5B,MAAE,eAAe;AACjB,UAAM,OAAO,EAAE,cAAc,QAAQ,kBAAkB;AACvD,QAAI,CAAC,KAAM;AACX,UAAM,MAAe,KAAK,MAAM,IAAI;AACpC,UAAM,OAAO,cAAc,KAAK,OAAO,EAAE,OAAO,IAAI,IAAI,OAAO,EAAE,OAAO,IAAI,EAAE;AAC9E,UAAM,MAAM,KAAK,IAAI;AAErB,QAAI,kBAAkB,KAAK,EAAE,GAAG;AAC9B,YAAM,SAAS,gBAAgB,KAAK,KAAK,KAAK,GAAG,KAAK,GAAG,KAAK,EAAE;AAChE,UAAI,QAAQ;AACV,cAAM,KAAK,eAAe,OAAO,QAAQ;AACzC,cAAM,QAAQ,MAAM,MAAM,KAAK,OAAK,EAAE,QAAQ,OAAO,QAAQ;AAC7D,YAAI,CAAC,SAAS,CAAC,GAAI;AACnB,cAAM,aAAa,MAAM,IAAI,KAAK,GAAG,YAAY,OAAO,OAAO;AAC/D,cAAM,aAAa,MAAM,IAAI,GAAG,YAAY;AAC5C,cAAM,MAAM,gBAAgB,KAAK,EAAE;AACnC,aAAK,IAAI,aAAa,IAAI;AAC1B,aAAK,IAAI,aAAa,IAAI;AAC1B,WAAG,cAAc,OAAO,OAAO,IAAI,KAAK;AAAA,MAC1C;AAAA,IACF;AAAA,EACF;AAEA,WAAS,gBAAgB,GAAe,MAAe;AACrD,QAAI,YAAY,KAAK,GAAG,GAAG;AACzB,aAAO,YAAY,KAAK,GAAG;AAC3B;AAAA,IACF;AACA,iBAAa,QAAQ;AACrB,QAAI,aAAa,OAAO;AACtB,YAAM,OAAO,aAAa,MAAM,sBAAsB;AACtD,0BAAoB,KAAK;AACzB,yBAAmB,KAAK;AAAA,IAC1B;AACA,uBAAmB,EAAE;AACrB,uBAAmB,EAAE;AACrB,qBAAiB,KAAK;AACtB,qBAAiB,KAAK;AACtB,gBAAY,QAAS,EAAE,UAAU,oBAAqB,KAAK;AAC3D,gBAAY,QAAS,EAAE,UAAU,mBAAoB,KAAK;AAC1D,oBAAgB,QAAQ;AACxB,SAAK,UAAU,MAAM,eAAe,IAAI,CAAC;AAAA,EAC3C;AAEA,MAAI,QAAuB;AAC3B,MAAI,iBAAiB;AACrB,MAAI,iBAAiB;AACrB,MAAI,eAAe;AAEnB,WAAS,UAAU,YAAoB,YAAoB;AACzD,UAAM,OAAO,aAAa;AAC1B,QAAI,CAAC,KAAM;AACX,UAAM,QAAQ,KAAK;AACnB,UAAM,QAAQ,KAAK;AACnB,UAAM,OAAO,kBAAkB,aAAa;AAC5C,UAAM,OAAO,kBAAkB,aAAa;AAC5C,SAAK,IAAI;AACT,SAAK,IAAI;AACT,UAAM,KAAK,OAAO;AAClB,UAAM,KAAK,OAAO;AAElB,QAAI,KAAK,OAAO,cAAc,cAAc,KAAK,GAAG,GAAG;AACrD,oBAAc,KAAK,GAAG,EAAE,KAAK;AAC7B,oBAAc,KAAK,GAAG,EAAE,KAAK;AAAA,IAC/B;AAGA,QAAI,sBAAsB,KAAK,EAAE,GAAG;AAClC,YAAM,KAAK,eAAe,KAAK,GAAG;AAClC,UAAI,IAAI;AACN,mBAAW,OAAO,GAAG,cAAc,OAAO,OAAO,GAAe;AAC9D,gBAAM,IAAI,MAAM,MAAM,KAAK,OAAK,EAAE,QAAQ,GAAG;AAC7C,cAAI,GAAG;AAAE,cAAE,KAAK;AAAI,cAAE,KAAK;AAAA,UAAI;AAAA,QACjC;AACA,YAAI,GAAG,oBAAoB;AACzB,gBAAM,SAAS,MAAM,MAAM,KAAK,OAAK,EAAE,QAAQ,GAAG,kBAAkB;AACpE,cAAI,QAAQ;AAAE,mBAAO,KAAK;AAAI,mBAAO,KAAK;AAAA,UAAI;AAAA,QAChD;AAAA,MACF;AAAA,IACF;AAGA,QAAI,kBAAkB,KAAK,EAAE,GAAG;AAC9B,YAAM,MAAM,YAAY,IAAI;AAC5B,UAAI,MAAM,eAAe,IAAI;AAC3B,uBAAe;AACf,2BAAmB,KAAK,GAAG;AAC3B,cAAM,SAAS,gBAAgB,KAAK,KAAK,KAAK,GAAG,KAAK,GAAG,KAAK,EAAE;AAChE,2BAAmB,QAAQ,SAAS,OAAO,WAAW,MAAM,OAAO,UAAU;AAAA,MAC/E;AAAA,IACF,OAAO;AACL,yBAAmB,QAAQ;AAAA,IAC7B;AAGA,QAAI,KAAK,OAAO,gBAAgB,KAAK,OAAO,gBAAgB,KAAK,OAAO,cAAc;AACpF,YAAM,MAAM,YAAY,IAAI;AAC5B,UAAI,MAAM,eAAe,IAAI;AAC3B,uBAAe;AACf,8BAAsB,KAAK,GAAG;AAC9B,2BAAmB,QAAQ,uBAAuB,KAAK,KAAK,KAAK,GAAG,KAAK,CAAC;AAAA,MAC5E;AAAA,IACF,OAAO;AACL,yBAAmB,QAAQ;AAAA,IAC7B;AAAA,EACF;AAEA,WAAS,WAAW,GAAe;AACjC,QAAI,CAAC,aAAa,MAAO;AACzB,qBAAiB,EAAE;AACnB,qBAAiB,EAAE;AACnB,QAAI,MAAO;AACX,YAAQ,sBAAsB,MAAM;AAClC,cAAQ;AACR,gBAAU,gBAAgB,cAAc;AAAA,IAC1C,CAAC;AAAA,EACH;AAEA,WAAS,WAAW;AAClB,QAAI,OAAO;AAAE,2BAAqB,KAAK;AAAG,cAAQ;AAAA,IAAM;AACxD,QAAI,CAAC,aAAa,OAAO;AAAE,mBAAa,QAAQ;AAAM;AAAA,IAAQ;AAC9D,UAAM,OAAO,aAAa;AAG1B,QAAI,kBAAkB,KAAK,EAAE,GAAG;AAC9B,0BAAoB,KAAK,GAAG;AAAA,IAC9B;AAGA,QAAI,KAAK,OAAO,gBAAgB,KAAK,OAAO,gBAAgB,KAAK,OAAO,cAAc;AACpF,8BAAwB,KAAK,GAAG;AAAA,IAClC;AAGA,QAAI,KAAK,OAAO,kBAAkB;AAChC,YAAM,UAAU,MAAM,MAAM;AAAA,QAAK,OAC/B,EAAE,QAAQ,KAAK,OAAO,YAAY,EAAE,EAAE,KACtC,KAAK,IAAI,EAAE,IAAI,KAAK,CAAC,IAAI,MAAM,KAAK,IAAI,EAAE,IAAI,KAAK,CAAC,IAAI;AAAA,MAC1D;AACA,UAAI,SAAS;AACX,mBAAW,QAAQ,GAAG,IAAI,KAAK;AAC/B,aAAK,IAAI,QAAQ,IAAI;AACrB,aAAK,IAAI,QAAQ,IAAI;AAAA,MACvB;AAAA,IACF;AAGA,QAAI,UAAU,KAAK,EAAE,GAAG;AACtB,YAAM,MAAM,WAAW,KAAK,GAAG;AAC/B,UAAI,KAAK;AACP,cAAM,UAAU,MAAM,MAAM;AAAA,UAAK,OAC/B,EAAE,QAAQ,KAAK,OAAO,YAAY,EAAE,EAAE,KACtC,KAAK,IAAI,EAAE,IAAI,KAAK,CAAC,IAAI,MAAM,KAAK,IAAI,EAAE,IAAI,KAAK,CAAC,IAAI;AAAA,QAC1D;AACA,YAAI,SAAS;AACX,cAAI,IAAI,UAAU,GAAG;AACnB,wBAAY,MAAM,iBAAiB,IAAI;AAAA,UACzC,OAAO;AACL,4BAAgB,MAAM,iBAAiB,IAAI;AAAA,UAC7C;AAAA,QACF;AAAA,MACF;AAAA,IACF;AAEA,oBAAgB;AAChB,iBAAa,QAAQ;AAAA,EACvB;AAEA,WAAS,iBAAiB,GAAe;AACvC,QAAI,eAAe;AAAE,sBAAgB;AAAO;AAAA,IAAQ;AACpD,QAAK,EAAE,OAAuB,QAAQ,WAAW,EAAG;AACpD,QAAK,EAAE,OAAuB,QAAQ,qBAAqB,EAAG;AAC9D,oBAAgB,QAAQ;AACxB,SAAK,UAAU,MAAM,IAAI;AACzB,QAAI,OAAO,KAAK,WAAW,EAAE,SAAS,GAAG;AACvC,aAAO,KAAK,WAAW,EAAE,QAAQ,OAAK,OAAO,YAAY,CAAC,CAAC;AAAA,IAC7D;AAAA,EACF;AAEA,SAAO;AAAA,IACL;AAAA,IACA;AAAA,IACA;AAAA,IACA;AAAA,IACA;AAAA,IACA;AAAA,IACA;AAAA,IACA;AAAA,IACA;AAAA,EACF;AACF;",
        "names": []
      }
    },
    {
      "name": "vite:json",
      "start": 1783626308636,
      "end": 1783626308636,
      "order": "normal"
    },
    {
      "name": "vite:worker",
      "start": 1783626308636,
      "end": 1783626308636,
      "order": "normal"
    },
    {
      "name": "vite:vue",
      "start": 1783626308636,
      "end": 1783626308636,
      "order": "normal-normal"
    },
    {
      "name": "vite:define",
      "start": 1783626308636,
      "end": 1783626308636,
      "order": "normal"
    },
    {
      "name": "vite:css-post",
      "start": 1783626308636,
      "end": 1783626308636,
      "order": "normal"
    },
    {
      "name": "vite:worker-import-meta-url",
      "start": 1783626308636,
      "end": 1783626308636,
      "order": "normal"
    },
    {
      "name": "vite:asset-import-meta-url",
      "start": 1783626308636,
      "end": 1783626308636,
      "order": "normal"
    },
    {
      "name": "vite:dynamic-import-vars",
      "start": 1783626308636,
      "end": 1783626308636,
      "order": "normal"
    },
    {
      "name": "vite:import-glob",
      "start": 1783626308636,
      "end": 1783626308636,
      "order": "normal"
    },
    {
      "name": "vite-plugin-vue-inspector:post",
      "start": 1783626308636,
      "end": 1783626308639,
      "order": "post"
    },
    {
      "name": "vite:client-inject",
      "start": 1783626308639,
      "end": 1783626308639,
      "order": "normal"
    },
    {
      "name": "vite:css-analysis",
      "start": 1783626308639,
      "end": 1783626308639,
      "order": "normal"
    },
    {
      "name": "vite:import-analysis",
      "result": "import { ref } from \"/node_modules/.vite/deps/vue.js?v=09a3ad4c\";\nimport {\n  items,\n  pourFlowMap,\n  phProbeTipMap,\n  stopperMap,\n  createLabItem,\n  isContainer,\n  getPipette,\n  retortStandMap\n} from \"/src/composables/chemistry/useChemistryLab.ts\";\nimport { isPipette, isRetortStandAssembly } from \"/src/composables/chemistry/chemLabIds.ts\";\nimport {\n  getMagnetOffset,\n  isClampAttachable,\n  retortStandSnapUid,\n  bottomClampSnapUid,\n  findNearestSlot,\n  findNearestBottomClamp,\n  finalizeTopSlotSnap,\n  finalizeBottomClampSnap,\n  detachFromTopSlots,\n  detachFromBottomClamp\n} from \"/src/composables/chemistry/workspaceDragSnap.ts\";\nimport { pipetteDraw, pipetteDispense } from \"/src/composables/chemistry/usePipetteActions.ts\";\nexport function useWorkspaceDrag(workspaceRef, selectedItemRef, emit, buildToolState) {\n  const draggingItem = ref(null);\n  const dragOffsetX = ref(0);\n  const dragOffsetY = ref(0);\n  let workspaceRectLeft = 0;\n  let workspaceRectTop = 0;\n  let dragStartClientX = 0;\n  let dragStartClientY = 0;\n  let dragStartItemX = 0;\n  let dragStartItemY = 0;\n  let dragJustEnded = false;\n  function sceneX(clientX) {\n    if (!workspaceRef.value) return clientX;\n    return clientX - workspaceRef.value.getBoundingClientRect().left;\n  }\n  function sceneY(clientY) {\n    if (!workspaceRef.value) return clientY;\n    return clientY - workspaceRef.value.getBoundingClientRect().top;\n  }\n  function onDragOver(e) {\n    e.preventDefault();\n    if (e.dataTransfer) e.dataTransfer.dropEffect = \"copy\";\n  }\n  function onDrop(e) {\n    e.preventDefault();\n    const data = e.dataTransfer?.getData(\"application/json\");\n    if (!data) return;\n    const def = JSON.parse(data);\n    const item = createLabItem(def, sceneX(e.clientX) - 40, sceneY(e.clientY) - 40);\n    items.value.push(item);\n    if (isClampAttachable(item.id)) {\n      const result = findNearestSlot(item.uid, item.x, item.y, item.id);\n      if (result) {\n        const st = retortStandMap[result.standUid];\n        const stand = items.value.find((i) => i.uid === result.standUid);\n        if (!stand || !st) return;\n        const slotWorldX = stand.x + 54 + st.slotOffsets[result.slotIdx];\n        const slotWorldY = stand.y + st.topClampY + 14;\n        const off = getMagnetOffset(item.id);\n        item.x = slotWorldX - off.x;\n        item.y = slotWorldY - off.y;\n        st.slotOccupants[result.slotIdx] = item.uid;\n      }\n    }\n  }\n  function onItemMouseDown(e, item) {\n    if (pourFlowMap[item.uid]) {\n      delete pourFlowMap[item.uid];\n      return;\n    }\n    draggingItem.value = item;\n    if (workspaceRef.value) {\n      const rect = workspaceRef.value.getBoundingClientRect();\n      workspaceRectLeft = rect.left;\n      workspaceRectTop = rect.top;\n    }\n    dragStartClientX = e.clientX;\n    dragStartClientY = e.clientY;\n    dragStartItemX = item.x;\n    dragStartItemY = item.y;\n    dragOffsetX.value = e.clientX - workspaceRectLeft - item.x;\n    dragOffsetY.value = e.clientY - workspaceRectTop - item.y;\n    selectedItemRef.value = item;\n    emit(\"select\", item, buildToolState(item));\n  }\n  let rafId = null;\n  let pendingClientX = 0;\n  let pendingClientY = 0;\n  let lastSnapTime = 0;\n  function applyDrag(newClientX, newClientY) {\n    const item = draggingItem.value;\n    if (!item) return;\n    const prevX = item.x;\n    const prevY = item.y;\n    const newX = dragStartItemX + (newClientX - dragStartClientX);\n    const newY = dragStartItemY + (newClientY - dragStartClientY);\n    item.x = newX;\n    item.y = newY;\n    const dx = newX - prevX;\n    const dy = newY - prevY;\n    if (item.id === \"ph-meter\" && phProbeTipMap[item.uid]) {\n      phProbeTipMap[item.uid].x += dx;\n      phProbeTipMap[item.uid].y += dy;\n    }\n    if (isRetortStandAssembly(item.id)) {\n      const st = retortStandMap[item.uid];\n      if (st) {\n        for (const uid of st.slotOccupants.filter(Boolean)) {\n          const b = items.value.find((i) => i.uid === uid);\n          if (b) {\n            b.x += dx;\n            b.y += dy;\n          }\n        }\n        if (st.bottomSlotOccupant) {\n          const beaker = items.value.find((i) => i.uid === st.bottomSlotOccupant);\n          if (beaker) {\n            beaker.x += dx;\n            beaker.y += dy;\n          }\n        }\n      }\n    }\n    if (isClampAttachable(item.id)) {\n      const now = performance.now();\n      if (now - lastSnapTime > 50) {\n        lastSnapTime = now;\n        detachFromTopSlots(item.uid);\n        const result = findNearestSlot(item.uid, item.x, item.y, item.id);\n        retortStandSnapUid.value = result ? result.standUid + \"|\" + result.slotIdx : null;\n      }\n    } else {\n      retortStandSnapUid.value = null;\n    }\n    if (item.id === \"beaker-100\" || item.id === \"beaker-250\" || item.id === \"beaker-500\") {\n      const now = performance.now();\n      if (now - lastSnapTime > 50) {\n        lastSnapTime = now;\n        detachFromBottomClamp(item.uid);\n        bottomClampSnapUid.value = findNearestBottomClamp(item.uid, item.x, item.y);\n      }\n    } else {\n      bottomClampSnapUid.value = null;\n    }\n  }\n  function onDragMove(e) {\n    if (!draggingItem.value) return;\n    pendingClientX = e.clientX;\n    pendingClientY = e.clientY;\n    if (rafId) return;\n    rafId = requestAnimationFrame(() => {\n      rafId = null;\n      applyDrag(pendingClientX, pendingClientY);\n    });\n  }\n  function onDragUp() {\n    if (rafId) {\n      cancelAnimationFrame(rafId);\n      rafId = null;\n    }\n    if (!draggingItem.value) {\n      draggingItem.value = null;\n      return;\n    }\n    const item = draggingItem.value;\n    if (isClampAttachable(item.id)) {\n      finalizeTopSlotSnap(item.uid);\n    }\n    if (item.id === \"beaker-100\" || item.id === \"beaker-250\" || item.id === \"beaker-500\") {\n      finalizeBottomClampSnap(item.uid);\n    }\n    if (item.id === \"rubber-stopper\") {\n      const nearest = items.value.find(\n        (i) => i.uid !== item.uid && isContainer(i.id) && Math.abs(i.x - item.x) < 50 && Math.abs(i.y - item.y) < 80\n      );\n      if (nearest) {\n        stopperMap[nearest.uid] = item.uid;\n        item.x = nearest.x + 20;\n        item.y = nearest.y - 5;\n      }\n    }\n    if (isPipette(item.id)) {\n      const pip = getPipette(item.uid);\n      if (pip) {\n        const nearest = items.value.find(\n          (i) => i.uid !== item.uid && isContainer(i.id) && Math.abs(i.x - item.x) < 60 && Math.abs(i.y - item.y) < 120\n        );\n        if (nearest) {\n          if (pip.volume <= 0) {\n            pipetteDraw(item, selectedItemRef, emit);\n          } else {\n            pipetteDispense(item, selectedItemRef, emit);\n          }\n        }\n      }\n    }\n    dragJustEnded = true;\n    draggingItem.value = null;\n  }\n  function onWorkspaceClick(e) {\n    if (dragJustEnded) {\n      dragJustEnded = false;\n      return;\n    }\n    if (e.target.closest(\".lab-item\")) return;\n    if (e.target.closest(\".floating-inspector\")) return;\n    selectedItemRef.value = null;\n    emit(\"select\", null, null);\n    if (Object.keys(pourFlowMap).length > 0) {\n      Object.keys(pourFlowMap).forEach((k) => delete pourFlowMap[k]);\n    }\n  }\n  return {\n    draggingItem,\n    dragOffsetX,\n    dragOffsetY,\n    onDragOver,\n    onDrop,\n    onItemMouseDown,\n    onDragMove,\n    onDragUp,\n    onWorkspaceClick\n  };\n}\n",
      "start": 1783626308639,
      "end": 1783626308656,
      "order": "normal",
      "sourcemaps": null
    }
  ]
}

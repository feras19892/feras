{
  "resolvedId": "C:/Users/feras/Desktop/feras/apps/web/src/composables/prism/usePrismRenderer.ts",
  "transforms": [
    {
      "name": "vite:load-fallback",
      "result": "import { prismVertices, toRad, toDeg, lineIntersection } from './prism-geometry'\nimport { traceRay } from './prism-raytracer'\nimport {\n  clearCanvas, drawPrismShape, drawLaserRay, drawLaserSource,\n  drawRefractedRay, drawTIRRay, drawNormal, drawLabel,\n  drawAngleArc, drawVirtualScreen, drawTIRFlash, drawApexHandle,\n} from './prism-drawing'\nimport type { Point } from './prism-geometry'\nimport type { ViewOptions, DrawResult } from './usePrismInteraction'\n\ninterface PrismProps {\n  prismAngle: number\n  angleIncidence: number\n  wavelength: number\n  material: string\n  hitRatio: number\n  angleRefraction1: number | null\n  deviation: number | null\n  n: number\n  totalInternalReflection: boolean\n  criticalAngle: number | null\n  running: boolean\n}\n\nconst SPECTRUM = [\n  { wavelength: 400, color: '#9400D3' },\n  { wavelength: 430, color: '#4B0082' },\n  { wavelength: 470, color: '#2244DD' },\n  { wavelength: 530, color: '#00BB00' },\n  { wavelength: 580, color: '#CCCC00' },\n  { wavelength: 600, color: '#FF7700' },\n  { wavelength: 650, color: '#EE1111' },\n]\n\nconst SRC_DIST = 150\n\nexport function drawPrism(\n  canvas: HTMLCanvasElement,\n  props: PrismProps,\n  offset: { x: number; y: number },\n  options: ViewOptions,\n  hoverTarget: string | null,\n  dragTarget: string | null\n): DrawResult | null {\n  const ctx = canvas.getContext('2d')\n  if (!ctx) return null\n\n  const W = canvas.width, H = canvas.height\n\n  clearCanvas(ctx, W, H, options.showGrid)\n\n  const screenX = W - 70\n  const prismSize = Math.min(W, H) * 0.48\n  const centerX = W * 0.50 + offset.x\n  const centerY = H * 0.53 + offset.y\n\n  const { pA, pB, pC, edgeAB_angle } = prismVertices(props.prismAngle, prismSize, centerX, centerY)\n  const normal1Angle = edgeAB_angle + Math.PI / 2\n  const hr = Math.max(0.05, Math.min(0.95, props.hitRatio))\n  const p1: Point = { x: pA.x + hr * (pB.x - pA.x), y: pA.y + hr * (pB.y - pA.y) }\n\n  const incidentRayAngle = normal1Angle + Math.PI + toRad(props.angleIncidence)\n  const srcDist = Math.min(SRC_DIST, p1.x - 15)\n  const srcX = p1.x - Math.cos(incidentRayAngle) * srcDist\n  const srcY = p1.y - Math.sin(incidentRayAngle) * srcDist\n\n  const isDraggingPrism = dragTarget === 'prism'\n\n  if (props.totalInternalReflection) drawTIRFlash(ctx, pA, pB, pC)\n  drawPrismShape(ctx, pA, pB, pC, isDraggingPrism)\n\n  if (options.showNormals) drawNormal(ctx, p1, normal1Angle, 60)\n  drawLabel(ctx, 'A', pA, -16, -10)\n\n  // ── Draw all spectrum rays ──────────────────────────────────────────────\n  const screenHits: { y: number; color: string }[] = []\n\n  SPECTRUM.forEach((ray) => {\n    const result = traceRay(p1, pA, pB, pC, props.angleIncidence, ray.wavelength, props.material, edgeAB_angle, normal1Angle)\n    if (!result) return\n\n    const isSelected = Math.abs(ray.wavelength - props.wavelength) <= 15\n    const alpha = options.showAllWavelengths ? (isSelected ? 1 : 0.22) : (isSelected ? 1 : 0)\n\n    ctx.save(); ctx.globalAlpha = alpha\n\n    drawRefractedRay(ctx, p1, result.p2, ray.color, isSelected ? 2.8 : 1.2, false)\n\n    if (result.tir && result.reflectionAngle !== null) {\n      const pBase = lineIntersection(\n        result.p2,\n        { x: result.p2.x + Math.cos(result.reflectionAngle) * 1e6, y: result.p2.y + Math.sin(result.reflectionAngle) * 1e6 },\n        pB, pC, true\n      )\n      drawTIRRay(ctx, result.p2, pBase, ray.color, result.reflectionAngle)\n    } else if (result.emergentAngle !== null) {\n      const limitX = options.showScreen ? screenX : W * 1.5\n      const pEnd: Point = {\n        x: result.p2.x + Math.cos(result.emergentAngle) * limitX * 2,\n        y: result.p2.y + Math.sin(result.emergentAngle) * limitX * 2,\n      }\n      drawRefractedRay(ctx, result.p2, pEnd, ray.color, isSelected ? 3 : 1.4, isSelected)\n      if (options.showScreen) {\n        const hitY = result.p2.y + (screenX - result.p2.x) * Math.tan(result.emergentAngle)\n        screenHits.push({ y: hitY, color: ray.color })\n      }\n    }\n\n    ctx.restore()\n  })\n\n  // ── Incident ray ───────────────────────────────────────────────────────\n  if (!props.totalInternalReflection) {\n    drawLaserRay(ctx, srcX, srcY, p1)\n  }\n\n  // ── Virtual screen ─────────────────────────────────────────────────────\n  if (options.showScreen && screenHits.length) {\n    drawVirtualScreen(ctx, screenX, H, screenHits)\n  }\n\n  // ── Angle arcs ─────────────────────────────────────────────────────────\n  if (options.showAngleArcs) {\n    if (props.angleIncidence > 0.5) {\n      const incDir = normal1Angle + toRad(props.angleIncidence)\n      drawAngleArc(ctx, p1, normal1Angle, incDir, 34, '#67e8f9', `θ₁=${props.angleIncidence.toFixed(0)}°`)\n    }\n    if (props.angleRefraction1 !== null && props.angleRefraction1 > 0.3) {\n      const selectedResult = traceRay(p1, pA, pB, pC, props.angleIncidence, props.wavelength, props.material, edgeAB_angle, normal1Angle)\n      if (selectedResult) {\n        drawAngleArc(ctx, p1, normal1Angle + Math.PI, selectedResult.refractedRayAngle, 28, '#4ade80', `r₁=${props.angleRefraction1.toFixed(1)}°`)\n        if (!selectedResult.tir && selectedResult.emergentAngle !== null && props.deviation !== null) {\n          const { edgeP, edgeQ } = selectedResult\n          const faceDir = { x: edgeQ.x - edgeP.x, y: edgeQ.y - edgeP.y }\n          const faceLen = Math.hypot(faceDir.x, faceDir.y)\n          const fT = faceLen > 1e-9 ? { x: faceDir.x / faceLen, y: faceDir.y / faceLen } : { x: 1, y: 0 }\n          const nCand = { x: -fT.y, y: fT.x }\n          const midEdge = { x: (edgeP.x + edgeQ.x) / 2, y: (edgeP.y + edgeQ.y) / 2 }\n          const thirdVertex = Math.hypot(edgeP.x - pA.x, edgeP.y - pA.y) < 1 ? pB : pA\n          const toThird = { x: thirdVertex.x - midEdge.x, y: thirdVertex.y - midEdge.y }\n          const outN2 = (nCand.x * toThird.x + nCand.y * toThird.y < 0) ? nCand : { x: fT.y, y: -fT.x }\n          const outNorm2Angle = Math.atan2(outN2.y, outN2.x)\n          drawAngleArc(ctx, selectedResult.p2, outNorm2Angle, selectedResult.refractedRayAngle, 26, '#fbbf24', `r₂=${selectedResult.internalIncidentAngle ? toDeg(selectedResult.internalIncidentAngle).toFixed(1) : '?'}°`)\n          drawAngleArc(ctx, selectedResult.p2, outNorm2Angle, selectedResult.emergentAngle, 36, '#f87171', `θ₂`)\n        }\n      }\n    }\n  }\n\n  // ── Interactive handles ────────────────────────────────────────────────\n  drawApexHandle(ctx, pA, hoverTarget === 'apex', dragTarget === 'apex')\n  drawLaserSource(ctx, srcX, srcY, dragTarget === 'source', hoverTarget === 'source')\n\n  return { pA, pB, pC, p1, normal1Angle, srcX, srcY }\n}\n",
      "start": 1783196195047,
      "end": 1783196195326,
      "sourcemaps": null
    },
    {
      "name": "vite:css",
      "start": 1783196195326,
      "end": 1783196195326,
      "order": "normal"
    },
    {
      "name": "vite:esbuild",
      "result": "import { prismVertices, toRad, toDeg, lineIntersection } from \"./prism-geometry\";\nimport { traceRay } from \"./prism-raytracer\";\nimport {\n  clearCanvas,\n  drawPrismShape,\n  drawLaserRay,\n  drawLaserSource,\n  drawRefractedRay,\n  drawTIRRay,\n  drawNormal,\n  drawLabel,\n  drawAngleArc,\n  drawVirtualScreen,\n  drawTIRFlash,\n  drawApexHandle\n} from \"./prism-drawing\";\nconst SPECTRUM = [\n  { wavelength: 400, color: \"#9400D3\" },\n  { wavelength: 430, color: \"#4B0082\" },\n  { wavelength: 470, color: \"#2244DD\" },\n  { wavelength: 530, color: \"#00BB00\" },\n  { wavelength: 580, color: \"#CCCC00\" },\n  { wavelength: 600, color: \"#FF7700\" },\n  { wavelength: 650, color: \"#EE1111\" }\n];\nconst SRC_DIST = 150;\nexport function drawPrism(canvas, props, offset, options, hoverTarget, dragTarget) {\n  const ctx = canvas.getContext(\"2d\");\n  if (!ctx) return null;\n  const W = canvas.width, H = canvas.height;\n  clearCanvas(ctx, W, H, options.showGrid);\n  const screenX = W - 70;\n  const prismSize = Math.min(W, H) * 0.48;\n  const centerX = W * 0.5 + offset.x;\n  const centerY = H * 0.53 + offset.y;\n  const { pA, pB, pC, edgeAB_angle } = prismVertices(props.prismAngle, prismSize, centerX, centerY);\n  const normal1Angle = edgeAB_angle + Math.PI / 2;\n  const hr = Math.max(0.05, Math.min(0.95, props.hitRatio));\n  const p1 = { x: pA.x + hr * (pB.x - pA.x), y: pA.y + hr * (pB.y - pA.y) };\n  const incidentRayAngle = normal1Angle + Math.PI + toRad(props.angleIncidence);\n  const srcDist = Math.min(SRC_DIST, p1.x - 15);\n  const srcX = p1.x - Math.cos(incidentRayAngle) * srcDist;\n  const srcY = p1.y - Math.sin(incidentRayAngle) * srcDist;\n  const isDraggingPrism = dragTarget === \"prism\";\n  if (props.totalInternalReflection) drawTIRFlash(ctx, pA, pB, pC);\n  drawPrismShape(ctx, pA, pB, pC, isDraggingPrism);\n  if (options.showNormals) drawNormal(ctx, p1, normal1Angle, 60);\n  drawLabel(ctx, \"A\", pA, -16, -10);\n  const screenHits = [];\n  SPECTRUM.forEach((ray) => {\n    const result = traceRay(p1, pA, pB, pC, props.angleIncidence, ray.wavelength, props.material, edgeAB_angle, normal1Angle);\n    if (!result) return;\n    const isSelected = Math.abs(ray.wavelength - props.wavelength) <= 15;\n    const alpha = options.showAllWavelengths ? isSelected ? 1 : 0.22 : isSelected ? 1 : 0;\n    ctx.save();\n    ctx.globalAlpha = alpha;\n    drawRefractedRay(ctx, p1, result.p2, ray.color, isSelected ? 2.8 : 1.2, false);\n    if (result.tir && result.reflectionAngle !== null) {\n      const pBase = lineIntersection(\n        result.p2,\n        { x: result.p2.x + Math.cos(result.reflectionAngle) * 1e6, y: result.p2.y + Math.sin(result.reflectionAngle) * 1e6 },\n        pB,\n        pC,\n        true\n      );\n      drawTIRRay(ctx, result.p2, pBase, ray.color, result.reflectionAngle);\n    } else if (result.emergentAngle !== null) {\n      const limitX = options.showScreen ? screenX : W * 1.5;\n      const pEnd = {\n        x: result.p2.x + Math.cos(result.emergentAngle) * limitX * 2,\n        y: result.p2.y + Math.sin(result.emergentAngle) * limitX * 2\n      };\n      drawRefractedRay(ctx, result.p2, pEnd, ray.color, isSelected ? 3 : 1.4, isSelected);\n      if (options.showScreen) {\n        const hitY = result.p2.y + (screenX - result.p2.x) * Math.tan(result.emergentAngle);\n        screenHits.push({ y: hitY, color: ray.color });\n      }\n    }\n    ctx.restore();\n  });\n  if (!props.totalInternalReflection) {\n    drawLaserRay(ctx, srcX, srcY, p1);\n  }\n  if (options.showScreen && screenHits.length) {\n    drawVirtualScreen(ctx, screenX, H, screenHits);\n  }\n  if (options.showAngleArcs) {\n    if (props.angleIncidence > 0.5) {\n      const incDir = normal1Angle + toRad(props.angleIncidence);\n      drawAngleArc(ctx, p1, normal1Angle, incDir, 34, \"#67e8f9\", `θ₁=${props.angleIncidence.toFixed(0)}°`);\n    }\n    if (props.angleRefraction1 !== null && props.angleRefraction1 > 0.3) {\n      const selectedResult = traceRay(p1, pA, pB, pC, props.angleIncidence, props.wavelength, props.material, edgeAB_angle, normal1Angle);\n      if (selectedResult) {\n        drawAngleArc(ctx, p1, normal1Angle + Math.PI, selectedResult.refractedRayAngle, 28, \"#4ade80\", `r₁=${props.angleRefraction1.toFixed(1)}°`);\n        if (!selectedResult.tir && selectedResult.emergentAngle !== null && props.deviation !== null) {\n          const { edgeP, edgeQ } = selectedResult;\n          const faceDir = { x: edgeQ.x - edgeP.x, y: edgeQ.y - edgeP.y };\n          const faceLen = Math.hypot(faceDir.x, faceDir.y);\n          const fT = faceLen > 1e-9 ? { x: faceDir.x / faceLen, y: faceDir.y / faceLen } : { x: 1, y: 0 };\n          const nCand = { x: -fT.y, y: fT.x };\n          const midEdge = { x: (edgeP.x + edgeQ.x) / 2, y: (edgeP.y + edgeQ.y) / 2 };\n          const thirdVertex = Math.hypot(edgeP.x - pA.x, edgeP.y - pA.y) < 1 ? pB : pA;\n          const toThird = { x: thirdVertex.x - midEdge.x, y: thirdVertex.y - midEdge.y };\n          const outN2 = nCand.x * toThird.x + nCand.y * toThird.y < 0 ? nCand : { x: fT.y, y: -fT.x };\n          const outNorm2Angle = Math.atan2(outN2.y, outN2.x);\n          drawAngleArc(ctx, selectedResult.p2, outNorm2Angle, selectedResult.refractedRayAngle, 26, \"#fbbf24\", `r₂=${selectedResult.internalIncidentAngle ? toDeg(selectedResult.internalIncidentAngle).toFixed(1) : \"?\"}°`);\n          drawAngleArc(ctx, selectedResult.p2, outNorm2Angle, selectedResult.emergentAngle, 36, \"#f87171\", `θ₂`);\n        }\n      }\n    }\n  }\n  drawApexHandle(ctx, pA, hoverTarget === \"apex\", dragTarget === \"apex\");\n  drawLaserSource(ctx, srcX, srcY, dragTarget === \"source\", hoverTarget === \"source\");\n  return { pA, pB, pC, p1, normal1Angle, srcX, srcY };\n}\n",
      "start": 1783196195326,
      "end": 1783196195566,
      "order": "normal",
      "sourcemaps": {
        "version": 3,
        "sources": [
          "C:/Users/feras/Desktop/feras/apps/web/src/composables/prism/usePrismRenderer.ts"
        ],
        "sourcesContent": [
          "import { prismVertices, toRad, toDeg, lineIntersection } from './prism-geometry'\nimport { traceRay } from './prism-raytracer'\nimport {\n  clearCanvas, drawPrismShape, drawLaserRay, drawLaserSource,\n  drawRefractedRay, drawTIRRay, drawNormal, drawLabel,\n  drawAngleArc, drawVirtualScreen, drawTIRFlash, drawApexHandle,\n} from './prism-drawing'\nimport type { Point } from './prism-geometry'\nimport type { ViewOptions, DrawResult } from './usePrismInteraction'\n\ninterface PrismProps {\n  prismAngle: number\n  angleIncidence: number\n  wavelength: number\n  material: string\n  hitRatio: number\n  angleRefraction1: number | null\n  deviation: number | null\n  n: number\n  totalInternalReflection: boolean\n  criticalAngle: number | null\n  running: boolean\n}\n\nconst SPECTRUM = [\n  { wavelength: 400, color: '#9400D3' },\n  { wavelength: 430, color: '#4B0082' },\n  { wavelength: 470, color: '#2244DD' },\n  { wavelength: 530, color: '#00BB00' },\n  { wavelength: 580, color: '#CCCC00' },\n  { wavelength: 600, color: '#FF7700' },\n  { wavelength: 650, color: '#EE1111' },\n]\n\nconst SRC_DIST = 150\n\nexport function drawPrism(\n  canvas: HTMLCanvasElement,\n  props: PrismProps,\n  offset: { x: number; y: number },\n  options: ViewOptions,\n  hoverTarget: string | null,\n  dragTarget: string | null\n): DrawResult | null {\n  const ctx = canvas.getContext('2d')\n  if (!ctx) return null\n\n  const W = canvas.width, H = canvas.height\n\n  clearCanvas(ctx, W, H, options.showGrid)\n\n  const screenX = W - 70\n  const prismSize = Math.min(W, H) * 0.48\n  const centerX = W * 0.50 + offset.x\n  const centerY = H * 0.53 + offset.y\n\n  const { pA, pB, pC, edgeAB_angle } = prismVertices(props.prismAngle, prismSize, centerX, centerY)\n  const normal1Angle = edgeAB_angle + Math.PI / 2\n  const hr = Math.max(0.05, Math.min(0.95, props.hitRatio))\n  const p1: Point = { x: pA.x + hr * (pB.x - pA.x), y: pA.y + hr * (pB.y - pA.y) }\n\n  const incidentRayAngle = normal1Angle + Math.PI + toRad(props.angleIncidence)\n  const srcDist = Math.min(SRC_DIST, p1.x - 15)\n  const srcX = p1.x - Math.cos(incidentRayAngle) * srcDist\n  const srcY = p1.y - Math.sin(incidentRayAngle) * srcDist\n\n  const isDraggingPrism = dragTarget === 'prism'\n\n  if (props.totalInternalReflection) drawTIRFlash(ctx, pA, pB, pC)\n  drawPrismShape(ctx, pA, pB, pC, isDraggingPrism)\n\n  if (options.showNormals) drawNormal(ctx, p1, normal1Angle, 60)\n  drawLabel(ctx, 'A', pA, -16, -10)\n\n  // ── Draw all spectrum rays ──────────────────────────────────────────────\n  const screenHits: { y: number; color: string }[] = []\n\n  SPECTRUM.forEach((ray) => {\n    const result = traceRay(p1, pA, pB, pC, props.angleIncidence, ray.wavelength, props.material, edgeAB_angle, normal1Angle)\n    if (!result) return\n\n    const isSelected = Math.abs(ray.wavelength - props.wavelength) <= 15\n    const alpha = options.showAllWavelengths ? (isSelected ? 1 : 0.22) : (isSelected ? 1 : 0)\n\n    ctx.save(); ctx.globalAlpha = alpha\n\n    drawRefractedRay(ctx, p1, result.p2, ray.color, isSelected ? 2.8 : 1.2, false)\n\n    if (result.tir && result.reflectionAngle !== null) {\n      const pBase = lineIntersection(\n        result.p2,\n        { x: result.p2.x + Math.cos(result.reflectionAngle) * 1e6, y: result.p2.y + Math.sin(result.reflectionAngle) * 1e6 },\n        pB, pC, true\n      )\n      drawTIRRay(ctx, result.p2, pBase, ray.color, result.reflectionAngle)\n    } else if (result.emergentAngle !== null) {\n      const limitX = options.showScreen ? screenX : W * 1.5\n      const pEnd: Point = {\n        x: result.p2.x + Math.cos(result.emergentAngle) * limitX * 2,\n        y: result.p2.y + Math.sin(result.emergentAngle) * limitX * 2,\n      }\n      drawRefractedRay(ctx, result.p2, pEnd, ray.color, isSelected ? 3 : 1.4, isSelected)\n      if (options.showScreen) {\n        const hitY = result.p2.y + (screenX - result.p2.x) * Math.tan(result.emergentAngle)\n        screenHits.push({ y: hitY, color: ray.color })\n      }\n    }\n\n    ctx.restore()\n  })\n\n  // ── Incident ray ───────────────────────────────────────────────────────\n  if (!props.totalInternalReflection) {\n    drawLaserRay(ctx, srcX, srcY, p1)\n  }\n\n  // ── Virtual screen ─────────────────────────────────────────────────────\n  if (options.showScreen && screenHits.length) {\n    drawVirtualScreen(ctx, screenX, H, screenHits)\n  }\n\n  // ── Angle arcs ─────────────────────────────────────────────────────────\n  if (options.showAngleArcs) {\n    if (props.angleIncidence > 0.5) {\n      const incDir = normal1Angle + toRad(props.angleIncidence)\n      drawAngleArc(ctx, p1, normal1Angle, incDir, 34, '#67e8f9', `θ₁=${props.angleIncidence.toFixed(0)}°`)\n    }\n    if (props.angleRefraction1 !== null && props.angleRefraction1 > 0.3) {\n      const selectedResult = traceRay(p1, pA, pB, pC, props.angleIncidence, props.wavelength, props.material, edgeAB_angle, normal1Angle)\n      if (selectedResult) {\n        drawAngleArc(ctx, p1, normal1Angle + Math.PI, selectedResult.refractedRayAngle, 28, '#4ade80', `r₁=${props.angleRefraction1.toFixed(1)}°`)\n        if (!selectedResult.tir && selectedResult.emergentAngle !== null && props.deviation !== null) {\n          const { edgeP, edgeQ } = selectedResult\n          const faceDir = { x: edgeQ.x - edgeP.x, y: edgeQ.y - edgeP.y }\n          const faceLen = Math.hypot(faceDir.x, faceDir.y)\n          const fT = faceLen > 1e-9 ? { x: faceDir.x / faceLen, y: faceDir.y / faceLen } : { x: 1, y: 0 }\n          const nCand = { x: -fT.y, y: fT.x }\n          const midEdge = { x: (edgeP.x + edgeQ.x) / 2, y: (edgeP.y + edgeQ.y) / 2 }\n          const thirdVertex = Math.hypot(edgeP.x - pA.x, edgeP.y - pA.y) < 1 ? pB : pA\n          const toThird = { x: thirdVertex.x - midEdge.x, y: thirdVertex.y - midEdge.y }\n          const outN2 = (nCand.x * toThird.x + nCand.y * toThird.y < 0) ? nCand : { x: fT.y, y: -fT.x }\n          const outNorm2Angle = Math.atan2(outN2.y, outN2.x)\n          drawAngleArc(ctx, selectedResult.p2, outNorm2Angle, selectedResult.refractedRayAngle, 26, '#fbbf24', `r₂=${selectedResult.internalIncidentAngle ? toDeg(selectedResult.internalIncidentAngle).toFixed(1) : '?'}°`)\n          drawAngleArc(ctx, selectedResult.p2, outNorm2Angle, selectedResult.emergentAngle, 36, '#f87171', `θ₂`)\n        }\n      }\n    }\n  }\n\n  // ── Interactive handles ────────────────────────────────────────────────\n  drawApexHandle(ctx, pA, hoverTarget === 'apex', dragTarget === 'apex')\n  drawLaserSource(ctx, srcX, srcY, dragTarget === 'source', hoverTarget === 'source')\n\n  return { pA, pB, pC, p1, normal1Angle, srcX, srcY }\n}\n"
        ],
        "mappings": "AAAA,SAAS,eAAe,OAAO,OAAO,wBAAwB;AAC9D,SAAS,gBAAgB;AACzB;AAAA,EACE;AAAA,EAAa;AAAA,EAAgB;AAAA,EAAc;AAAA,EAC3C;AAAA,EAAkB;AAAA,EAAY;AAAA,EAAY;AAAA,EAC1C;AAAA,EAAc;AAAA,EAAmB;AAAA,EAAc;AAAA,OAC1C;AAkBP,MAAM,WAAW;AAAA,EACf,EAAE,YAAY,KAAK,OAAO,UAAU;AAAA,EACpC,EAAE,YAAY,KAAK,OAAO,UAAU;AAAA,EACpC,EAAE,YAAY,KAAK,OAAO,UAAU;AAAA,EACpC,EAAE,YAAY,KAAK,OAAO,UAAU;AAAA,EACpC,EAAE,YAAY,KAAK,OAAO,UAAU;AAAA,EACpC,EAAE,YAAY,KAAK,OAAO,UAAU;AAAA,EACpC,EAAE,YAAY,KAAK,OAAO,UAAU;AACtC;AAEA,MAAM,WAAW;AAEV,gBAAS,UACd,QACA,OACA,QACA,SACA,aACA,YACmB;AACnB,QAAM,MAAM,OAAO,WAAW,IAAI;AAClC,MAAI,CAAC,IAAK,QAAO;AAEjB,QAAM,IAAI,OAAO,OAAO,IAAI,OAAO;AAEnC,cAAY,KAAK,GAAG,GAAG,QAAQ,QAAQ;AAEvC,QAAM,UAAU,IAAI;AACpB,QAAM,YAAY,KAAK,IAAI,GAAG,CAAC,IAAI;AACnC,QAAM,UAAU,IAAI,MAAO,OAAO;AAClC,QAAM,UAAU,IAAI,OAAO,OAAO;AAElC,QAAM,EAAE,IAAI,IAAI,IAAI,aAAa,IAAI,cAAc,MAAM,YAAY,WAAW,SAAS,OAAO;AAChG,QAAM,eAAe,eAAe,KAAK,KAAK;AAC9C,QAAM,KAAK,KAAK,IAAI,MAAM,KAAK,IAAI,MAAM,MAAM,QAAQ,CAAC;AACxD,QAAM,KAAY,EAAE,GAAG,GAAG,IAAI,MAAM,GAAG,IAAI,GAAG,IAAI,GAAG,GAAG,IAAI,MAAM,GAAG,IAAI,GAAG,GAAG;AAE/E,QAAM,mBAAmB,eAAe,KAAK,KAAK,MAAM,MAAM,cAAc;AAC5E,QAAM,UAAU,KAAK,IAAI,UAAU,GAAG,IAAI,EAAE;AAC5C,QAAM,OAAO,GAAG,IAAI,KAAK,IAAI,gBAAgB,IAAI;AACjD,QAAM,OAAO,GAAG,IAAI,KAAK,IAAI,gBAAgB,IAAI;AAEjD,QAAM,kBAAkB,eAAe;AAEvC,MAAI,MAAM,wBAAyB,cAAa,KAAK,IAAI,IAAI,EAAE;AAC/D,iBAAe,KAAK,IAAI,IAAI,IAAI,eAAe;AAE/C,MAAI,QAAQ,YAAa,YAAW,KAAK,IAAI,cAAc,EAAE;AAC7D,YAAU,KAAK,KAAK,IAAI,KAAK,GAAG;AAGhC,QAAM,aAA6C,CAAC;AAEpD,WAAS,QAAQ,CAAC,QAAQ;AACxB,UAAM,SAAS,SAAS,IAAI,IAAI,IAAI,IAAI,MAAM,gBAAgB,IAAI,YAAY,MAAM,UAAU,cAAc,YAAY;AACxH,QAAI,CAAC,OAAQ;AAEb,UAAM,aAAa,KAAK,IAAI,IAAI,aAAa,MAAM,UAAU,KAAK;AAClE,UAAM,QAAQ,QAAQ,qBAAsB,aAAa,IAAI,OAAS,aAAa,IAAI;AAEvF,QAAI,KAAK;AAAG,QAAI,cAAc;AAE9B,qBAAiB,KAAK,IAAI,OAAO,IAAI,IAAI,OAAO,aAAa,MAAM,KAAK,KAAK;AAE7E,QAAI,OAAO,OAAO,OAAO,oBAAoB,MAAM;AACjD,YAAM,QAAQ;AAAA,QACZ,OAAO;AAAA,QACP,EAAE,GAAG,OAAO,GAAG,IAAI,KAAK,IAAI,OAAO,eAAe,IAAI,KAAK,GAAG,OAAO,GAAG,IAAI,KAAK,IAAI,OAAO,eAAe,IAAI,IAAI;AAAA,QACnH;AAAA,QAAI;AAAA,QAAI;AAAA,MACV;AACA,iBAAW,KAAK,OAAO,IAAI,OAAO,IAAI,OAAO,OAAO,eAAe;AAAA,IACrE,WAAW,OAAO,kBAAkB,MAAM;AACxC,YAAM,SAAS,QAAQ,aAAa,UAAU,IAAI;AAClD,YAAM,OAAc;AAAA,QAClB,GAAG,OAAO,GAAG,IAAI,KAAK,IAAI,OAAO,aAAa,IAAI,SAAS;AAAA,QAC3D,GAAG,OAAO,GAAG,IAAI,KAAK,IAAI,OAAO,aAAa,IAAI,SAAS;AAAA,MAC7D;AACA,uBAAiB,KAAK,OAAO,IAAI,MAAM,IAAI,OAAO,aAAa,IAAI,KAAK,UAAU;AAClF,UAAI,QAAQ,YAAY;AACtB,cAAM,OAAO,OAAO,GAAG,KAAK,UAAU,OAAO,GAAG,KAAK,KAAK,IAAI,OAAO,aAAa;AAClF,mBAAW,KAAK,EAAE,GAAG,MAAM,OAAO,IAAI,MAAM,CAAC;AAAA,MAC/C;AAAA,IACF;AAEA,QAAI,QAAQ;AAAA,EACd,CAAC;AAGD,MAAI,CAAC,MAAM,yBAAyB;AAClC,iBAAa,KAAK,MAAM,MAAM,EAAE;AAAA,EAClC;AAGA,MAAI,QAAQ,cAAc,WAAW,QAAQ;AAC3C,sBAAkB,KAAK,SAAS,GAAG,UAAU;AAAA,EAC/C;AAGA,MAAI,QAAQ,eAAe;AACzB,QAAI,MAAM,iBAAiB,KAAK;AAC9B,YAAM,SAAS,eAAe,MAAM,MAAM,cAAc;AACxD,mBAAa,KAAK,IAAI,cAAc,QAAQ,IAAI,WAAW,MAAM,MAAM,eAAe,QAAQ,CAAC,CAAC,GAAG;AAAA,IACrG;AACA,QAAI,MAAM,qBAAqB,QAAQ,MAAM,mBAAmB,KAAK;AACnE,YAAM,iBAAiB,SAAS,IAAI,IAAI,IAAI,IAAI,MAAM,gBAAgB,MAAM,YAAY,MAAM,UAAU,cAAc,YAAY;AAClI,UAAI,gBAAgB;AAClB,qBAAa,KAAK,IAAI,eAAe,KAAK,IAAI,eAAe,mBAAmB,IAAI,WAAW,MAAM,MAAM,iBAAiB,QAAQ,CAAC,CAAC,GAAG;AACzI,YAAI,CAAC,eAAe,OAAO,eAAe,kBAAkB,QAAQ,MAAM,cAAc,MAAM;AAC5F,gBAAM,EAAE,OAAO,MAAM,IAAI;AACzB,gBAAM,UAAU,EAAE,GAAG,MAAM,IAAI,MAAM,GAAG,GAAG,MAAM,IAAI,MAAM,EAAE;AAC7D,gBAAM,UAAU,KAAK,MAAM,QAAQ,GAAG,QAAQ,CAAC;AAC/C,gBAAM,KAAK,UAAU,OAAO,EAAE,GAAG,QAAQ,IAAI,SAAS,GAAG,QAAQ,IAAI,QAAQ,IAAI,EAAE,GAAG,GAAG,GAAG,EAAE;AAC9F,gBAAM,QAAQ,EAAE,GAAG,CAAC,GAAG,GAAG,GAAG,GAAG,EAAE;AAClC,gBAAM,UAAU,EAAE,IAAI,MAAM,IAAI,MAAM,KAAK,GAAG,IAAI,MAAM,IAAI,MAAM,KAAK,EAAE;AACzE,gBAAM,cAAc,KAAK,MAAM,MAAM,IAAI,GAAG,GAAG,MAAM,IAAI,GAAG,CAAC,IAAI,IAAI,KAAK;AAC1E,gBAAM,UAAU,EAAE,GAAG,YAAY,IAAI,QAAQ,GAAG,GAAG,YAAY,IAAI,QAAQ,EAAE;AAC7E,gBAAM,QAAS,MAAM,IAAI,QAAQ,IAAI,MAAM,IAAI,QAAQ,IAAI,IAAK,QAAQ,EAAE,GAAG,GAAG,GAAG,GAAG,CAAC,GAAG,EAAE;AAC5F,gBAAM,gBAAgB,KAAK,MAAM,MAAM,GAAG,MAAM,CAAC;AACjD,uBAAa,KAAK,eAAe,IAAI,eAAe,eAAe,mBAAmB,IAAI,WAAW,MAAM,eAAe,wBAAwB,MAAM,eAAe,qBAAqB,EAAE,QAAQ,CAAC,IAAI,GAAG,GAAG;AACjN,uBAAa,KAAK,eAAe,IAAI,eAAe,eAAe,eAAe,IAAI,WAAW,IAAI;AAAA,QACvG;AAAA,MACF;AAAA,IACF;AAAA,EACF;AAGA,iBAAe,KAAK,IAAI,gBAAgB,QAAQ,eAAe,MAAM;AACrE,kBAAgB,KAAK,MAAM,MAAM,eAAe,UAAU,gBAAgB,QAAQ;AAElF,SAAO,EAAE,IAAI,IAAI,IAAI,IAAI,cAAc,MAAM,KAAK;AACpD;",
        "names": []
      }
    },
    {
      "name": "vite:json",
      "start": 1783196195566,
      "end": 1783196195566,
      "order": "normal"
    },
    {
      "name": "vite:worker",
      "start": 1783196195566,
      "end": 1783196195566,
      "order": "normal"
    },
    {
      "name": "vite:define",
      "start": 1783196195566,
      "end": 1783196195566,
      "order": "normal"
    },
    {
      "name": "vite:css-post",
      "start": 1783196195566,
      "end": 1783196195566,
      "order": "normal"
    },
    {
      "name": "vite:build-html",
      "start": 1783196195566,
      "end": 1783196195566,
      "order": "normal"
    },
    {
      "name": "vite:worker-import-meta-url",
      "start": 1783196195566,
      "end": 1783196195566,
      "order": "normal"
    },
    {
      "name": "vite:asset-import-meta-url",
      "start": 1783196195566,
      "end": 1783196195566,
      "order": "normal"
    },
    {
      "name": "commonjs",
      "start": 1783196195566,
      "end": 1783196195566,
      "order": "normal"
    },
    {
      "name": "vite:dynamic-import-vars",
      "start": 1783196195566,
      "end": 1783196195566,
      "order": "normal"
    },
    {
      "name": "vite:import-glob",
      "start": 1783196195566,
      "end": 1783196195566,
      "order": "normal"
    },
    {
      "name": "vite:build-import-analysis",
      "start": 1783196195567,
      "end": 1783196195567,
      "order": "normal"
    },
    {
      "name": "vite:reporter",
      "start": 1783196195567,
      "end": 1783196195567,
      "order": "normal"
    }
  ]
}

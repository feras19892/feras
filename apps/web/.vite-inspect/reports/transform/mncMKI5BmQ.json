{
  "resolvedId": "C:/Users/feras/Desktop/feras/apps/web/src/composables/specific-heat/useSpecificHeatExperiment.ts",
  "transforms": [
    {
      "name": "vite:load-fallback",
      "result": "import { ref, reactive, computed, watch } from 'vue'\nimport { useRouter } from 'vue-router'\nimport type { AnalysisPayload } from '../../types/physics'\nimport { sendToAnalysis } from '../analysis/sendToAnalysis'\nimport { useSpecificHeatLayout } from './useSpecificHeatLayout'\nimport { useSpecificHeatTrials } from './useSpecificHeatTrials'\nimport { equilibriumTemp, findMetalSpecificHeat, METAL_CATALOG, linearRegression, C_WATER } from './useSpecificHeatCalculations'\n\nexport function useSpecificHeatExperiment() {\n  // Metal sample (unknown) + cold water in calorimeter\n  const params = reactive({\n    metalType: 'metalA' as string,\n    metalMass: 0.100,   // kg (100 g)\n    waterMass: 0.200,   // kg (200 g)\n    waterTemp: 20,      // °C (cold water initial)\n  })\n\n  watch(() => params.metalMass, (v) => { params.metalMass = Math.max(0.05, Math.min(0.2, Math.round(v * 1000) / 1000)) })\n  watch(() => params.waterMass, (v) => { params.waterMass = Math.max(0.1, Math.min(0.3, Math.round(v * 1000) / 1000)) })\n  watch(() => params.waterTemp, (v) => { params.waterTemp = Math.max(5, Math.min(35, Math.round(v))) })\n\n  // True c of selected metal (hidden from user)\n  const cTrue = computed(() => METAL_CATALOG[params.metalType]?.c ?? 900)\n\n  // Metal is heated to 100°C in water bath\n  const metalTemp = ref(params.waterTemp) // starts at room temp\n  const finalTemp = computed(() => equilibriumTemp(params.metalMass, cTrue.value, 100, params.waterMass, params.waterTemp))\n\n  // Extracted specific heat from the measured (simulated) equilibrium\n  const cExtracted = computed(() => findMetalSpecificHeat(params.metalMass, 100, params.waterMass, params.waterTemp, finalTemp.value))\n\n  const running = ref(false)\n  const paused = ref(false)\n  const simTime = ref(0)\n  const phase = ref<'ready' | 'heating' | 'transfer' | 'mixing' | 'done'>('ready')\n  const unknownMode = ref(false)\n\n  // Randomize metal when unknown mode is active\n  const metalKeys = Object.keys(METAL_CATALOG)\n  function randomizeMetal() {\n    const idx = Math.floor(Math.random() * metalKeys.length)\n    params.metalType = metalKeys[idx]\n  }\n  function guessMetal(cGuess: number) {\n    let bestKey = ''\n    let bestDiff = Infinity\n    for (const [key, info] of Object.entries(METAL_CATALOG)) {\n      const diff = Math.abs(info.c - cGuess)\n      if (diff < bestDiff) { bestDiff = diff; bestKey = key }\n    }\n    return { key: bestKey, info: METAL_CATALOG[bestKey], diff: bestDiff }\n  }\n  watch(unknownMode, (v) => { if (v) randomizeMetal() })\n\n  // During heating animation, metalTemp rises toward 100\n  // During mixing animation, displayed temp rises toward finalTemp\n  const displayT = ref(params.waterTemp)\n\n  function updateSim(dt_: number) {\n    if (!running.value || paused.value) return\n    simTime.value += dt_\n    const HEAT_TIME = 2.5\n    const MIX_TIME = 3.0\n\n    if (phase.value === 'heating') {\n      const ratio = Math.min(1, simTime.value / HEAT_TIME)\n      metalTemp.value = params.waterTemp + (100 - params.waterTemp) * ratio\n      displayT.value = metalTemp.value\n      if (ratio >= 1) { metalTemp.value = 100; phase.value = 'transfer'; simTime.value = 0 }\n    } else if (phase.value === 'mixing') {\n      const ratio = Math.min(1, simTime.value / MIX_TIME)\n      // Metal cools from 100°C down to finalTemp\n      metalTemp.value = 100 - (100 - finalTemp.value) * ratio\n      // Water/mixture warms from T_w up to finalTemp\n      displayT.value = params.waterTemp + (finalTemp.value - params.waterTemp) * ratio\n      if (ratio >= 1) {\n        metalTemp.value = finalTemp.value\n        displayT.value = finalTemp.value\n        phase.value = 'done'\n      }\n    }\n  }\n\n  function transferMetal() {\n    if (phase.value === 'transfer') {\n      phase.value = 'mixing'\n      simTime.value = 0\n    }\n  }\n\n  function togglePause() {\n    if (!running.value) {\n      if (unknownMode.value) randomizeMetal()\n      running.value = true; paused.value = false; simTime.value = 0\n      phase.value = 'heating'; metalTemp.value = params.waterTemp; displayT.value = params.waterTemp\n    } else { paused.value = !paused.value }\n  }\n  function resetSim() {\n    running.value = false; paused.value = false; simTime.value = 0; phase.value = 'ready'\n    metalTemp.value = params.waterTemp; displayT.value = params.waterTemp\n  }\n\n  const layout = useSpecificHeatLayout()\n  const trials = useSpecificHeatTrials(\n    { get value() {\n      return {\n        metalType: params.metalType, metalMass: params.metalMass, metalTemp: 100,\n        waterMass: params.waterMass, waterTemp: params.waterTemp, finalTemp: finalTemp.value,\n        cExtracted: cExtracted.value, cTrue: cTrue.value,\n      }\n    } }\n  )\n\n  // Regression per report: Y = (Tm - Tf) vs X = m_w (kg)\n  // Slope = Δ(Tm-Tf) / Δm_w\n  // c_m = c_w * avg(Tf-Tw) / (m_m * Slope)\n  const regression = computed(() => {\n    const valid = trials.trials.value.filter((t) => t.cExtracted > 0 && t.finalTemp > t.waterTemp)\n    if (valid.length < 2) return { m: 0, b: 0, r2: 0, avgDeltaT: 0 }\n    const pts = valid.map((t) => ({\n      x: t.waterMass * 1000, // convert to grams for display\n      y: t.metalTemp - t.finalTemp,\n    }))\n    const avgDeltaT = valid.reduce((s, t) => s + (t.finalTemp - t.waterTemp), 0) / valid.length\n    const reg = linearRegression(pts)\n    return { ...reg, avgDeltaT }\n  })\n\n  // c_m extracted from regression slope per report formula\n  const cFromSlope = computed(() => {\n    const m = regression.value.m\n    const avg = regression.value.avgDeltaT\n    if (!isFinite(m) || m <= 0 || avg <= 0 || params.metalMass <= 0) return 0\n    return (C_WATER * avg) / (params.metalMass * m)\n  })\n\n  const router = useRouter()\n  function exportToAnalysis() {\n    if (trials.trials.value.length < 2) return\n    const payload: AnalysisPayload = {\n      sourceExperiment: 'specific-heat', sourceNameAr: 'السعة الحرارية النوعية (طريقة الخلط)',\n      hasCalcTab: true,\n      readings: trials.trials.value.map(t => ({\n        metalType: t.metalType, metalMass: t.metalMass, waterMass: t.waterMass,\n        waterTemp: t.waterTemp, finalTemp: t.finalTemp, cExtracted: t.cExtracted, cTrue: t.cTrue,\n      })),\n      columns: [\n        { key: 'metalType', label: 'Metal', unit: '' },\n        { key: 'metalMass', label: 'm_m (kg)', unit: 'kg' },\n        { key: 'waterMass', label: 'm_w (kg)', unit: 'kg' },\n        { key: 'waterTemp', label: 'T_w (°C)', unit: '°C' },\n        { key: 'finalTemp', label: 'T_f (°C)', unit: '°C' },\n        { key: 'cExtracted', label: 'c_m extracted', unit: 'J/(kg·°C)' },\n        { key: 'cTrue', label: 'c_m true', unit: 'J/(kg·°C)' },\n      ],\n      equations: [\n        { name: 'Heat Balance', formula: 'm_m * c_m * (T_m - T_f) = m_w * c_w * (T_f - T_w)', variables: [\n          { symbol: 'm_m', label: 'Metal mass' }, { symbol: 'c_m', label: 'Metal specific heat' },\n          { symbol: 'T_m', label: 'Metal initial temp' }, { symbol: 'T_f', label: 'Final temp' },\n          { symbol: 'm_w', label: 'Water mass' }, { symbol: 'c_w', label: 'Water specific heat' }, { symbol: 'T_w', label: 'Water initial temp' },\n        ], solveFor: ['c_m'] },\n      ],\n      suggestedPlots: [{ xKey: 'metalMass', yKey: 'cExtracted', xLabel: 'm_m (kg)', yLabel: 'c_m', type: 'scatter' }],\n    }\n    sendToAnalysis(router, payload)\n  }\n  function handleDrop(fromId: string, x?: number, y?: number) {\n    if (x === undefined || y === undefined) return\n    const el = document.elementFromPoint(x, y)\n    const toPanel = el?.closest('.draggable-panel')\n    const toId = toPanel?.getAttribute('data-id')\n    if (!toId || fromId === toId) return\n    for (const col of Object.keys(layout.columnMap)) {\n      const arr = layout.columnMap[col]\n      const fi = arr.indexOf(fromId), ti = arr.indexOf(toId)\n      if (fi >= 0 && ti >= 0) { const t = arr[fi]; arr[fi] = arr[ti]; arr[ti] = t }\n    }\n  }\n  function onResizeStart(col: string, e: MouseEvent) {\n    if (!(col in layout.widths)) return\n    const startX = e.clientX, startW = (layout.widths as Record<string, number>)[col] as number\n    function move(ev: MouseEvent) { (layout.widths as Record<string, number>)[col] = Math.max(220, startW + (ev.clientX - startX)) }\n    function up() { window.removeEventListener('mousemove', move); window.removeEventListener('mouseup', up) }\n    window.addEventListener('mousemove', move); window.addEventListener('mouseup', up)\n  }\n\n  const lab = {\n    running, paused, simTime, phase, metalTemp, displayT, finalTemp, cExtracted, cTrue, unknownMode,\n    waterTemp: computed(() => params.waterTemp),\n    togglePause, transferMetal, updateSim, randomizeMetal, guessMetal, METAL_CATALOG, C_WATER,\n  }\n  return { params, lab, layout, trials, regression, cFromSlope, resetSim, exportToAnalysis, onResizeStart, handleDrop }\n}\n",
      "start": 1783634640960,
      "end": 1783634641337,
      "sourcemaps": null
    },
    {
      "name": "vite:css",
      "start": 1783634641337,
      "end": 1783634641337,
      "order": "normal"
    },
    {
      "name": "vite:esbuild",
      "result": "import { ref, reactive, computed, watch } from \"vue\";\nimport { useRouter } from \"vue-router\";\nimport { sendToAnalysis } from \"../analysis/sendToAnalysis\";\nimport { useSpecificHeatLayout } from \"./useSpecificHeatLayout\";\nimport { useSpecificHeatTrials } from \"./useSpecificHeatTrials\";\nimport { equilibriumTemp, findMetalSpecificHeat, METAL_CATALOG, linearRegression, C_WATER } from \"./useSpecificHeatCalculations\";\nexport function useSpecificHeatExperiment() {\n  const params = reactive({\n    metalType: \"metalA\",\n    metalMass: 0.1,\n    // kg (100 g)\n    waterMass: 0.2,\n    // kg (200 g)\n    waterTemp: 20\n    // °C (cold water initial)\n  });\n  watch(() => params.metalMass, (v) => {\n    params.metalMass = Math.max(0.05, Math.min(0.2, Math.round(v * 1e3) / 1e3));\n  });\n  watch(() => params.waterMass, (v) => {\n    params.waterMass = Math.max(0.1, Math.min(0.3, Math.round(v * 1e3) / 1e3));\n  });\n  watch(() => params.waterTemp, (v) => {\n    params.waterTemp = Math.max(5, Math.min(35, Math.round(v)));\n  });\n  const cTrue = computed(() => METAL_CATALOG[params.metalType]?.c ?? 900);\n  const metalTemp = ref(params.waterTemp);\n  const finalTemp = computed(() => equilibriumTemp(params.metalMass, cTrue.value, 100, params.waterMass, params.waterTemp));\n  const cExtracted = computed(() => findMetalSpecificHeat(params.metalMass, 100, params.waterMass, params.waterTemp, finalTemp.value));\n  const running = ref(false);\n  const paused = ref(false);\n  const simTime = ref(0);\n  const phase = ref(\"ready\");\n  const unknownMode = ref(false);\n  const metalKeys = Object.keys(METAL_CATALOG);\n  function randomizeMetal() {\n    const idx = Math.floor(Math.random() * metalKeys.length);\n    params.metalType = metalKeys[idx];\n  }\n  function guessMetal(cGuess) {\n    let bestKey = \"\";\n    let bestDiff = Infinity;\n    for (const [key, info] of Object.entries(METAL_CATALOG)) {\n      const diff = Math.abs(info.c - cGuess);\n      if (diff < bestDiff) {\n        bestDiff = diff;\n        bestKey = key;\n      }\n    }\n    return { key: bestKey, info: METAL_CATALOG[bestKey], diff: bestDiff };\n  }\n  watch(unknownMode, (v) => {\n    if (v) randomizeMetal();\n  });\n  const displayT = ref(params.waterTemp);\n  function updateSim(dt_) {\n    if (!running.value || paused.value) return;\n    simTime.value += dt_;\n    const HEAT_TIME = 2.5;\n    const MIX_TIME = 3;\n    if (phase.value === \"heating\") {\n      const ratio = Math.min(1, simTime.value / HEAT_TIME);\n      metalTemp.value = params.waterTemp + (100 - params.waterTemp) * ratio;\n      displayT.value = metalTemp.value;\n      if (ratio >= 1) {\n        metalTemp.value = 100;\n        phase.value = \"transfer\";\n        simTime.value = 0;\n      }\n    } else if (phase.value === \"mixing\") {\n      const ratio = Math.min(1, simTime.value / MIX_TIME);\n      metalTemp.value = 100 - (100 - finalTemp.value) * ratio;\n      displayT.value = params.waterTemp + (finalTemp.value - params.waterTemp) * ratio;\n      if (ratio >= 1) {\n        metalTemp.value = finalTemp.value;\n        displayT.value = finalTemp.value;\n        phase.value = \"done\";\n      }\n    }\n  }\n  function transferMetal() {\n    if (phase.value === \"transfer\") {\n      phase.value = \"mixing\";\n      simTime.value = 0;\n    }\n  }\n  function togglePause() {\n    if (!running.value) {\n      if (unknownMode.value) randomizeMetal();\n      running.value = true;\n      paused.value = false;\n      simTime.value = 0;\n      phase.value = \"heating\";\n      metalTemp.value = params.waterTemp;\n      displayT.value = params.waterTemp;\n    } else {\n      paused.value = !paused.value;\n    }\n  }\n  function resetSim() {\n    running.value = false;\n    paused.value = false;\n    simTime.value = 0;\n    phase.value = \"ready\";\n    metalTemp.value = params.waterTemp;\n    displayT.value = params.waterTemp;\n  }\n  const layout = useSpecificHeatLayout();\n  const trials = useSpecificHeatTrials(\n    { get value() {\n      return {\n        metalType: params.metalType,\n        metalMass: params.metalMass,\n        metalTemp: 100,\n        waterMass: params.waterMass,\n        waterTemp: params.waterTemp,\n        finalTemp: finalTemp.value,\n        cExtracted: cExtracted.value,\n        cTrue: cTrue.value\n      };\n    } }\n  );\n  const regression = computed(() => {\n    const valid = trials.trials.value.filter((t) => t.cExtracted > 0 && t.finalTemp > t.waterTemp);\n    if (valid.length < 2) return { m: 0, b: 0, r2: 0, avgDeltaT: 0 };\n    const pts = valid.map((t) => ({\n      x: t.waterMass * 1e3,\n      // convert to grams for display\n      y: t.metalTemp - t.finalTemp\n    }));\n    const avgDeltaT = valid.reduce((s, t) => s + (t.finalTemp - t.waterTemp), 0) / valid.length;\n    const reg = linearRegression(pts);\n    return { ...reg, avgDeltaT };\n  });\n  const cFromSlope = computed(() => {\n    const m = regression.value.m;\n    const avg = regression.value.avgDeltaT;\n    if (!isFinite(m) || m <= 0 || avg <= 0 || params.metalMass <= 0) return 0;\n    return C_WATER * avg / (params.metalMass * m);\n  });\n  const router = useRouter();\n  function exportToAnalysis() {\n    if (trials.trials.value.length < 2) return;\n    const payload = {\n      sourceExperiment: \"specific-heat\",\n      sourceNameAr: \"السعة الحرارية النوعية (طريقة الخلط)\",\n      hasCalcTab: true,\n      readings: trials.trials.value.map((t) => ({\n        metalType: t.metalType,\n        metalMass: t.metalMass,\n        waterMass: t.waterMass,\n        waterTemp: t.waterTemp,\n        finalTemp: t.finalTemp,\n        cExtracted: t.cExtracted,\n        cTrue: t.cTrue\n      })),\n      columns: [\n        { key: \"metalType\", label: \"Metal\", unit: \"\" },\n        { key: \"metalMass\", label: \"m_m (kg)\", unit: \"kg\" },\n        { key: \"waterMass\", label: \"m_w (kg)\", unit: \"kg\" },\n        { key: \"waterTemp\", label: \"T_w (°C)\", unit: \"°C\" },\n        { key: \"finalTemp\", label: \"T_f (°C)\", unit: \"°C\" },\n        { key: \"cExtracted\", label: \"c_m extracted\", unit: \"J/(kg·°C)\" },\n        { key: \"cTrue\", label: \"c_m true\", unit: \"J/(kg·°C)\" }\n      ],\n      equations: [\n        { name: \"Heat Balance\", formula: \"m_m * c_m * (T_m - T_f) = m_w * c_w * (T_f - T_w)\", variables: [\n          { symbol: \"m_m\", label: \"Metal mass\" },\n          { symbol: \"c_m\", label: \"Metal specific heat\" },\n          { symbol: \"T_m\", label: \"Metal initial temp\" },\n          { symbol: \"T_f\", label: \"Final temp\" },\n          { symbol: \"m_w\", label: \"Water mass\" },\n          { symbol: \"c_w\", label: \"Water specific heat\" },\n          { symbol: \"T_w\", label: \"Water initial temp\" }\n        ], solveFor: [\"c_m\"] }\n      ],\n      suggestedPlots: [{ xKey: \"metalMass\", yKey: \"cExtracted\", xLabel: \"m_m (kg)\", yLabel: \"c_m\", type: \"scatter\" }]\n    };\n    sendToAnalysis(router, payload);\n  }\n  function handleDrop(fromId, x, y) {\n    if (x === void 0 || y === void 0) return;\n    const el = document.elementFromPoint(x, y);\n    const toPanel = el?.closest(\".draggable-panel\");\n    const toId = toPanel?.getAttribute(\"data-id\");\n    if (!toId || fromId === toId) return;\n    for (const col of Object.keys(layout.columnMap)) {\n      const arr = layout.columnMap[col];\n      const fi = arr.indexOf(fromId), ti = arr.indexOf(toId);\n      if (fi >= 0 && ti >= 0) {\n        const t = arr[fi];\n        arr[fi] = arr[ti];\n        arr[ti] = t;\n      }\n    }\n  }\n  function onResizeStart(col, e) {\n    if (!(col in layout.widths)) return;\n    const startX = e.clientX, startW = layout.widths[col];\n    function move(ev) {\n      layout.widths[col] = Math.max(220, startW + (ev.clientX - startX));\n    }\n    function up() {\n      window.removeEventListener(\"mousemove\", move);\n      window.removeEventListener(\"mouseup\", up);\n    }\n    window.addEventListener(\"mousemove\", move);\n    window.addEventListener(\"mouseup\", up);\n  }\n  const lab = {\n    running,\n    paused,\n    simTime,\n    phase,\n    metalTemp,\n    displayT,\n    finalTemp,\n    cExtracted,\n    cTrue,\n    unknownMode,\n    waterTemp: computed(() => params.waterTemp),\n    togglePause,\n    transferMetal,\n    updateSim,\n    randomizeMetal,\n    guessMetal,\n    METAL_CATALOG,\n    C_WATER\n  };\n  return { params, lab, layout, trials, regression, cFromSlope, resetSim, exportToAnalysis, onResizeStart, handleDrop };\n}\n",
      "start": 1783634641337,
      "end": 1783634641637,
      "order": "normal",
      "sourcemaps": {
        "version": 3,
        "sources": [
          "C:/Users/feras/Desktop/feras/apps/web/src/composables/specific-heat/useSpecificHeatExperiment.ts"
        ],
        "sourcesContent": [
          "import { ref, reactive, computed, watch } from 'vue'\nimport { useRouter } from 'vue-router'\nimport type { AnalysisPayload } from '../../types/physics'\nimport { sendToAnalysis } from '../analysis/sendToAnalysis'\nimport { useSpecificHeatLayout } from './useSpecificHeatLayout'\nimport { useSpecificHeatTrials } from './useSpecificHeatTrials'\nimport { equilibriumTemp, findMetalSpecificHeat, METAL_CATALOG, linearRegression, C_WATER } from './useSpecificHeatCalculations'\n\nexport function useSpecificHeatExperiment() {\n  // Metal sample (unknown) + cold water in calorimeter\n  const params = reactive({\n    metalType: 'metalA' as string,\n    metalMass: 0.100,   // kg (100 g)\n    waterMass: 0.200,   // kg (200 g)\n    waterTemp: 20,      // °C (cold water initial)\n  })\n\n  watch(() => params.metalMass, (v) => { params.metalMass = Math.max(0.05, Math.min(0.2, Math.round(v * 1000) / 1000)) })\n  watch(() => params.waterMass, (v) => { params.waterMass = Math.max(0.1, Math.min(0.3, Math.round(v * 1000) / 1000)) })\n  watch(() => params.waterTemp, (v) => { params.waterTemp = Math.max(5, Math.min(35, Math.round(v))) })\n\n  // True c of selected metal (hidden from user)\n  const cTrue = computed(() => METAL_CATALOG[params.metalType]?.c ?? 900)\n\n  // Metal is heated to 100°C in water bath\n  const metalTemp = ref(params.waterTemp) // starts at room temp\n  const finalTemp = computed(() => equilibriumTemp(params.metalMass, cTrue.value, 100, params.waterMass, params.waterTemp))\n\n  // Extracted specific heat from the measured (simulated) equilibrium\n  const cExtracted = computed(() => findMetalSpecificHeat(params.metalMass, 100, params.waterMass, params.waterTemp, finalTemp.value))\n\n  const running = ref(false)\n  const paused = ref(false)\n  const simTime = ref(0)\n  const phase = ref<'ready' | 'heating' | 'transfer' | 'mixing' | 'done'>('ready')\n  const unknownMode = ref(false)\n\n  // Randomize metal when unknown mode is active\n  const metalKeys = Object.keys(METAL_CATALOG)\n  function randomizeMetal() {\n    const idx = Math.floor(Math.random() * metalKeys.length)\n    params.metalType = metalKeys[idx]\n  }\n  function guessMetal(cGuess: number) {\n    let bestKey = ''\n    let bestDiff = Infinity\n    for (const [key, info] of Object.entries(METAL_CATALOG)) {\n      const diff = Math.abs(info.c - cGuess)\n      if (diff < bestDiff) { bestDiff = diff; bestKey = key }\n    }\n    return { key: bestKey, info: METAL_CATALOG[bestKey], diff: bestDiff }\n  }\n  watch(unknownMode, (v) => { if (v) randomizeMetal() })\n\n  // During heating animation, metalTemp rises toward 100\n  // During mixing animation, displayed temp rises toward finalTemp\n  const displayT = ref(params.waterTemp)\n\n  function updateSim(dt_: number) {\n    if (!running.value || paused.value) return\n    simTime.value += dt_\n    const HEAT_TIME = 2.5\n    const MIX_TIME = 3.0\n\n    if (phase.value === 'heating') {\n      const ratio = Math.min(1, simTime.value / HEAT_TIME)\n      metalTemp.value = params.waterTemp + (100 - params.waterTemp) * ratio\n      displayT.value = metalTemp.value\n      if (ratio >= 1) { metalTemp.value = 100; phase.value = 'transfer'; simTime.value = 0 }\n    } else if (phase.value === 'mixing') {\n      const ratio = Math.min(1, simTime.value / MIX_TIME)\n      // Metal cools from 100°C down to finalTemp\n      metalTemp.value = 100 - (100 - finalTemp.value) * ratio\n      // Water/mixture warms from T_w up to finalTemp\n      displayT.value = params.waterTemp + (finalTemp.value - params.waterTemp) * ratio\n      if (ratio >= 1) {\n        metalTemp.value = finalTemp.value\n        displayT.value = finalTemp.value\n        phase.value = 'done'\n      }\n    }\n  }\n\n  function transferMetal() {\n    if (phase.value === 'transfer') {\n      phase.value = 'mixing'\n      simTime.value = 0\n    }\n  }\n\n  function togglePause() {\n    if (!running.value) {\n      if (unknownMode.value) randomizeMetal()\n      running.value = true; paused.value = false; simTime.value = 0\n      phase.value = 'heating'; metalTemp.value = params.waterTemp; displayT.value = params.waterTemp\n    } else { paused.value = !paused.value }\n  }\n  function resetSim() {\n    running.value = false; paused.value = false; simTime.value = 0; phase.value = 'ready'\n    metalTemp.value = params.waterTemp; displayT.value = params.waterTemp\n  }\n\n  const layout = useSpecificHeatLayout()\n  const trials = useSpecificHeatTrials(\n    { get value() {\n      return {\n        metalType: params.metalType, metalMass: params.metalMass, metalTemp: 100,\n        waterMass: params.waterMass, waterTemp: params.waterTemp, finalTemp: finalTemp.value,\n        cExtracted: cExtracted.value, cTrue: cTrue.value,\n      }\n    } }\n  )\n\n  // Regression per report: Y = (Tm - Tf) vs X = m_w (kg)\n  // Slope = Δ(Tm-Tf) / Δm_w\n  // c_m = c_w * avg(Tf-Tw) / (m_m * Slope)\n  const regression = computed(() => {\n    const valid = trials.trials.value.filter((t) => t.cExtracted > 0 && t.finalTemp > t.waterTemp)\n    if (valid.length < 2) return { m: 0, b: 0, r2: 0, avgDeltaT: 0 }\n    const pts = valid.map((t) => ({\n      x: t.waterMass * 1000, // convert to grams for display\n      y: t.metalTemp - t.finalTemp,\n    }))\n    const avgDeltaT = valid.reduce((s, t) => s + (t.finalTemp - t.waterTemp), 0) / valid.length\n    const reg = linearRegression(pts)\n    return { ...reg, avgDeltaT }\n  })\n\n  // c_m extracted from regression slope per report formula\n  const cFromSlope = computed(() => {\n    const m = regression.value.m\n    const avg = regression.value.avgDeltaT\n    if (!isFinite(m) || m <= 0 || avg <= 0 || params.metalMass <= 0) return 0\n    return (C_WATER * avg) / (params.metalMass * m)\n  })\n\n  const router = useRouter()\n  function exportToAnalysis() {\n    if (trials.trials.value.length < 2) return\n    const payload: AnalysisPayload = {\n      sourceExperiment: 'specific-heat', sourceNameAr: 'السعة الحرارية النوعية (طريقة الخلط)',\n      hasCalcTab: true,\n      readings: trials.trials.value.map(t => ({\n        metalType: t.metalType, metalMass: t.metalMass, waterMass: t.waterMass,\n        waterTemp: t.waterTemp, finalTemp: t.finalTemp, cExtracted: t.cExtracted, cTrue: t.cTrue,\n      })),\n      columns: [\n        { key: 'metalType', label: 'Metal', unit: '' },\n        { key: 'metalMass', label: 'm_m (kg)', unit: 'kg' },\n        { key: 'waterMass', label: 'm_w (kg)', unit: 'kg' },\n        { key: 'waterTemp', label: 'T_w (°C)', unit: '°C' },\n        { key: 'finalTemp', label: 'T_f (°C)', unit: '°C' },\n        { key: 'cExtracted', label: 'c_m extracted', unit: 'J/(kg·°C)' },\n        { key: 'cTrue', label: 'c_m true', unit: 'J/(kg·°C)' },\n      ],\n      equations: [\n        { name: 'Heat Balance', formula: 'm_m * c_m * (T_m - T_f) = m_w * c_w * (T_f - T_w)', variables: [\n          { symbol: 'm_m', label: 'Metal mass' }, { symbol: 'c_m', label: 'Metal specific heat' },\n          { symbol: 'T_m', label: 'Metal initial temp' }, { symbol: 'T_f', label: 'Final temp' },\n          { symbol: 'm_w', label: 'Water mass' }, { symbol: 'c_w', label: 'Water specific heat' }, { symbol: 'T_w', label: 'Water initial temp' },\n        ], solveFor: ['c_m'] },\n      ],\n      suggestedPlots: [{ xKey: 'metalMass', yKey: 'cExtracted', xLabel: 'm_m (kg)', yLabel: 'c_m', type: 'scatter' }],\n    }\n    sendToAnalysis(router, payload)\n  }\n  function handleDrop(fromId: string, x?: number, y?: number) {\n    if (x === undefined || y === undefined) return\n    const el = document.elementFromPoint(x, y)\n    const toPanel = el?.closest('.draggable-panel')\n    const toId = toPanel?.getAttribute('data-id')\n    if (!toId || fromId === toId) return\n    for (const col of Object.keys(layout.columnMap)) {\n      const arr = layout.columnMap[col]\n      const fi = arr.indexOf(fromId), ti = arr.indexOf(toId)\n      if (fi >= 0 && ti >= 0) { const t = arr[fi]; arr[fi] = arr[ti]; arr[ti] = t }\n    }\n  }\n  function onResizeStart(col: string, e: MouseEvent) {\n    if (!(col in layout.widths)) return\n    const startX = e.clientX, startW = (layout.widths as Record<string, number>)[col] as number\n    function move(ev: MouseEvent) { (layout.widths as Record<string, number>)[col] = Math.max(220, startW + (ev.clientX - startX)) }\n    function up() { window.removeEventListener('mousemove', move); window.removeEventListener('mouseup', up) }\n    window.addEventListener('mousemove', move); window.addEventListener('mouseup', up)\n  }\n\n  const lab = {\n    running, paused, simTime, phase, metalTemp, displayT, finalTemp, cExtracted, cTrue, unknownMode,\n    waterTemp: computed(() => params.waterTemp),\n    togglePause, transferMetal, updateSim, randomizeMetal, guessMetal, METAL_CATALOG, C_WATER,\n  }\n  return { params, lab, layout, trials, regression, cFromSlope, resetSim, exportToAnalysis, onResizeStart, handleDrop }\n}\n"
        ],
        "mappings": "AAAA,SAAS,KAAK,UAAU,UAAU,aAAa;AAC/C,SAAS,iBAAiB;AAE1B,SAAS,sBAAsB;AAC/B,SAAS,6BAA6B;AACtC,SAAS,6BAA6B;AACtC,SAAS,iBAAiB,uBAAuB,eAAe,kBAAkB,eAAe;AAE1F,gBAAS,4BAA4B;AAE1C,QAAM,SAAS,SAAS;AAAA,IACtB,WAAW;AAAA,IACX,WAAW;AAAA;AAAA,IACX,WAAW;AAAA;AAAA,IACX,WAAW;AAAA;AAAA,EACb,CAAC;AAED,QAAM,MAAM,OAAO,WAAW,CAAC,MAAM;AAAE,WAAO,YAAY,KAAK,IAAI,MAAM,KAAK,IAAI,KAAK,KAAK,MAAM,IAAI,GAAI,IAAI,GAAI,CAAC;AAAA,EAAE,CAAC;AACtH,QAAM,MAAM,OAAO,WAAW,CAAC,MAAM;AAAE,WAAO,YAAY,KAAK,IAAI,KAAK,KAAK,IAAI,KAAK,KAAK,MAAM,IAAI,GAAI,IAAI,GAAI,CAAC;AAAA,EAAE,CAAC;AACrH,QAAM,MAAM,OAAO,WAAW,CAAC,MAAM;AAAE,WAAO,YAAY,KAAK,IAAI,GAAG,KAAK,IAAI,IAAI,KAAK,MAAM,CAAC,CAAC,CAAC;AAAA,EAAE,CAAC;AAGpG,QAAM,QAAQ,SAAS,MAAM,cAAc,OAAO,SAAS,GAAG,KAAK,GAAG;AAGtE,QAAM,YAAY,IAAI,OAAO,SAAS;AACtC,QAAM,YAAY,SAAS,MAAM,gBAAgB,OAAO,WAAW,MAAM,OAAO,KAAK,OAAO,WAAW,OAAO,SAAS,CAAC;AAGxH,QAAM,aAAa,SAAS,MAAM,sBAAsB,OAAO,WAAW,KAAK,OAAO,WAAW,OAAO,WAAW,UAAU,KAAK,CAAC;AAEnI,QAAM,UAAU,IAAI,KAAK;AACzB,QAAM,SAAS,IAAI,KAAK;AACxB,QAAM,UAAU,IAAI,CAAC;AACrB,QAAM,QAAQ,IAA0D,OAAO;AAC/E,QAAM,cAAc,IAAI,KAAK;AAG7B,QAAM,YAAY,OAAO,KAAK,aAAa;AAC3C,WAAS,iBAAiB;AACxB,UAAM,MAAM,KAAK,MAAM,KAAK,OAAO,IAAI,UAAU,MAAM;AACvD,WAAO,YAAY,UAAU,GAAG;AAAA,EAClC;AACA,WAAS,WAAW,QAAgB;AAClC,QAAI,UAAU;AACd,QAAI,WAAW;AACf,eAAW,CAAC,KAAK,IAAI,KAAK,OAAO,QAAQ,aAAa,GAAG;AACvD,YAAM,OAAO,KAAK,IAAI,KAAK,IAAI,MAAM;AACrC,UAAI,OAAO,UAAU;AAAE,mBAAW;AAAM,kBAAU;AAAA,MAAI;AAAA,IACxD;AACA,WAAO,EAAE,KAAK,SAAS,MAAM,cAAc,OAAO,GAAG,MAAM,SAAS;AAAA,EACtE;AACA,QAAM,aAAa,CAAC,MAAM;AAAE,QAAI,EAAG,gBAAe;AAAA,EAAE,CAAC;AAIrD,QAAM,WAAW,IAAI,OAAO,SAAS;AAErC,WAAS,UAAU,KAAa;AAC9B,QAAI,CAAC,QAAQ,SAAS,OAAO,MAAO;AACpC,YAAQ,SAAS;AACjB,UAAM,YAAY;AAClB,UAAM,WAAW;AAEjB,QAAI,MAAM,UAAU,WAAW;AAC7B,YAAM,QAAQ,KAAK,IAAI,GAAG,QAAQ,QAAQ,SAAS;AACnD,gBAAU,QAAQ,OAAO,aAAa,MAAM,OAAO,aAAa;AAChE,eAAS,QAAQ,UAAU;AAC3B,UAAI,SAAS,GAAG;AAAE,kBAAU,QAAQ;AAAK,cAAM,QAAQ;AAAY,gBAAQ,QAAQ;AAAA,MAAE;AAAA,IACvF,WAAW,MAAM,UAAU,UAAU;AACnC,YAAM,QAAQ,KAAK,IAAI,GAAG,QAAQ,QAAQ,QAAQ;AAElD,gBAAU,QAAQ,OAAO,MAAM,UAAU,SAAS;AAElD,eAAS,QAAQ,OAAO,aAAa,UAAU,QAAQ,OAAO,aAAa;AAC3E,UAAI,SAAS,GAAG;AACd,kBAAU,QAAQ,UAAU;AAC5B,iBAAS,QAAQ,UAAU;AAC3B,cAAM,QAAQ;AAAA,MAChB;AAAA,IACF;AAAA,EACF;AAEA,WAAS,gBAAgB;AACvB,QAAI,MAAM,UAAU,YAAY;AAC9B,YAAM,QAAQ;AACd,cAAQ,QAAQ;AAAA,IAClB;AAAA,EACF;AAEA,WAAS,cAAc;AACrB,QAAI,CAAC,QAAQ,OAAO;AAClB,UAAI,YAAY,MAAO,gBAAe;AACtC,cAAQ,QAAQ;AAAM,aAAO,QAAQ;AAAO,cAAQ,QAAQ;AAC5D,YAAM,QAAQ;AAAW,gBAAU,QAAQ,OAAO;AAAW,eAAS,QAAQ,OAAO;AAAA,IACvF,OAAO;AAAE,aAAO,QAAQ,CAAC,OAAO;AAAA,IAAM;AAAA,EACxC;AACA,WAAS,WAAW;AAClB,YAAQ,QAAQ;AAAO,WAAO,QAAQ;AAAO,YAAQ,QAAQ;AAAG,UAAM,QAAQ;AAC9E,cAAU,QAAQ,OAAO;AAAW,aAAS,QAAQ,OAAO;AAAA,EAC9D;AAEA,QAAM,SAAS,sBAAsB;AACrC,QAAM,SAAS;AAAA,IACb,EAAE,IAAI,QAAQ;AACZ,aAAO;AAAA,QACL,WAAW,OAAO;AAAA,QAAW,WAAW,OAAO;AAAA,QAAW,WAAW;AAAA,QACrE,WAAW,OAAO;AAAA,QAAW,WAAW,OAAO;AAAA,QAAW,WAAW,UAAU;AAAA,QAC/E,YAAY,WAAW;AAAA,QAAO,OAAO,MAAM;AAAA,MAC7C;AAAA,IACF,EAAE;AAAA,EACJ;AAKA,QAAM,aAAa,SAAS,MAAM;AAChC,UAAM,QAAQ,OAAO,OAAO,MAAM,OAAO,CAAC,MAAM,EAAE,aAAa,KAAK,EAAE,YAAY,EAAE,SAAS;AAC7F,QAAI,MAAM,SAAS,EAAG,QAAO,EAAE,GAAG,GAAG,GAAG,GAAG,IAAI,GAAG,WAAW,EAAE;AAC/D,UAAM,MAAM,MAAM,IAAI,CAAC,OAAO;AAAA,MAC5B,GAAG,EAAE,YAAY;AAAA;AAAA,MACjB,GAAG,EAAE,YAAY,EAAE;AAAA,IACrB,EAAE;AACF,UAAM,YAAY,MAAM,OAAO,CAAC,GAAG,MAAM,KAAK,EAAE,YAAY,EAAE,YAAY,CAAC,IAAI,MAAM;AACrF,UAAM,MAAM,iBAAiB,GAAG;AAChC,WAAO,EAAE,GAAG,KAAK,UAAU;AAAA,EAC7B,CAAC;AAGD,QAAM,aAAa,SAAS,MAAM;AAChC,UAAM,IAAI,WAAW,MAAM;AAC3B,UAAM,MAAM,WAAW,MAAM;AAC7B,QAAI,CAAC,SAAS,CAAC,KAAK,KAAK,KAAK,OAAO,KAAK,OAAO,aAAa,EAAG,QAAO;AACxE,WAAQ,UAAU,OAAQ,OAAO,YAAY;AAAA,EAC/C,CAAC;AAED,QAAM,SAAS,UAAU;AACzB,WAAS,mBAAmB;AAC1B,QAAI,OAAO,OAAO,MAAM,SAAS,EAAG;AACpC,UAAM,UAA2B;AAAA,MAC/B,kBAAkB;AAAA,MAAiB,cAAc;AAAA,MACjD,YAAY;AAAA,MACZ,UAAU,OAAO,OAAO,MAAM,IAAI,QAAM;AAAA,QACtC,WAAW,EAAE;AAAA,QAAW,WAAW,EAAE;AAAA,QAAW,WAAW,EAAE;AAAA,QAC7D,WAAW,EAAE;AAAA,QAAW,WAAW,EAAE;AAAA,QAAW,YAAY,EAAE;AAAA,QAAY,OAAO,EAAE;AAAA,MACrF,EAAE;AAAA,MACF,SAAS;AAAA,QACP,EAAE,KAAK,aAAa,OAAO,SAAS,MAAM,GAAG;AAAA,QAC7C,EAAE,KAAK,aAAa,OAAO,YAAY,MAAM,KAAK;AAAA,QAClD,EAAE,KAAK,aAAa,OAAO,YAAY,MAAM,KAAK;AAAA,QAClD,EAAE,KAAK,aAAa,OAAO,YAAY,MAAM,KAAK;AAAA,QAClD,EAAE,KAAK,aAAa,OAAO,YAAY,MAAM,KAAK;AAAA,QAClD,EAAE,KAAK,cAAc,OAAO,iBAAiB,MAAM,YAAY;AAAA,QAC/D,EAAE,KAAK,SAAS,OAAO,YAAY,MAAM,YAAY;AAAA,MACvD;AAAA,MACA,WAAW;AAAA,QACT,EAAE,MAAM,gBAAgB,SAAS,qDAAqD,WAAW;AAAA,UAC/F,EAAE,QAAQ,OAAO,OAAO,aAAa;AAAA,UAAG,EAAE,QAAQ,OAAO,OAAO,sBAAsB;AAAA,UACtF,EAAE,QAAQ,OAAO,OAAO,qBAAqB;AAAA,UAAG,EAAE,QAAQ,OAAO,OAAO,aAAa;AAAA,UACrF,EAAE,QAAQ,OAAO,OAAO,aAAa;AAAA,UAAG,EAAE,QAAQ,OAAO,OAAO,sBAAsB;AAAA,UAAG,EAAE,QAAQ,OAAO,OAAO,qBAAqB;AAAA,QACxI,GAAG,UAAU,CAAC,KAAK,EAAE;AAAA,MACvB;AAAA,MACA,gBAAgB,CAAC,EAAE,MAAM,aAAa,MAAM,cAAc,QAAQ,YAAY,QAAQ,OAAO,MAAM,UAAU,CAAC;AAAA,IAChH;AACA,mBAAe,QAAQ,OAAO;AAAA,EAChC;AACA,WAAS,WAAW,QAAgB,GAAY,GAAY;AAC1D,QAAI,MAAM,UAAa,MAAM,OAAW;AACxC,UAAM,KAAK,SAAS,iBAAiB,GAAG,CAAC;AACzC,UAAM,UAAU,IAAI,QAAQ,kBAAkB;AAC9C,UAAM,OAAO,SAAS,aAAa,SAAS;AAC5C,QAAI,CAAC,QAAQ,WAAW,KAAM;AAC9B,eAAW,OAAO,OAAO,KAAK,OAAO,SAAS,GAAG;AAC/C,YAAM,MAAM,OAAO,UAAU,GAAG;AAChC,YAAM,KAAK,IAAI,QAAQ,MAAM,GAAG,KAAK,IAAI,QAAQ,IAAI;AACrD,UAAI,MAAM,KAAK,MAAM,GAAG;AAAE,cAAM,IAAI,IAAI,EAAE;AAAG,YAAI,EAAE,IAAI,IAAI,EAAE;AAAG,YAAI,EAAE,IAAI;AAAA,MAAE;AAAA,IAC9E;AAAA,EACF;AACA,WAAS,cAAc,KAAa,GAAe;AACjD,QAAI,EAAE,OAAO,OAAO,QAAS;AAC7B,UAAM,SAAS,EAAE,SAAS,SAAU,OAAO,OAAkC,GAAG;AAChF,aAAS,KAAK,IAAgB;AAAE,MAAC,OAAO,OAAkC,GAAG,IAAI,KAAK,IAAI,KAAK,UAAU,GAAG,UAAU,OAAO;AAAA,IAAE;AAC/H,aAAS,KAAK;AAAE,aAAO,oBAAoB,aAAa,IAAI;AAAG,aAAO,oBAAoB,WAAW,EAAE;AAAA,IAAE;AACzG,WAAO,iBAAiB,aAAa,IAAI;AAAG,WAAO,iBAAiB,WAAW,EAAE;AAAA,EACnF;AAEA,QAAM,MAAM;AAAA,IACV;AAAA,IAAS;AAAA,IAAQ;AAAA,IAAS;AAAA,IAAO;AAAA,IAAW;AAAA,IAAU;AAAA,IAAW;AAAA,IAAY;AAAA,IAAO;AAAA,IACpF,WAAW,SAAS,MAAM,OAAO,SAAS;AAAA,IAC1C;AAAA,IAAa;AAAA,IAAe;AAAA,IAAW;AAAA,IAAgB;AAAA,IAAY;AAAA,IAAe;AAAA,EACpF;AACA,SAAO,EAAE,QAAQ,KAAK,QAAQ,QAAQ,YAAY,YAAY,UAAU,kBAAkB,eAAe,WAAW;AACtH;",
        "names": []
      }
    },
    {
      "name": "vite:json",
      "start": 1783634641637,
      "end": 1783634641637,
      "order": "normal"
    },
    {
      "name": "vite:worker",
      "start": 1783634641637,
      "end": 1783634641637,
      "order": "normal"
    },
    {
      "name": "vite:define",
      "start": 1783634641637,
      "end": 1783634641637,
      "order": "normal"
    },
    {
      "name": "vite:css-post",
      "start": 1783634641637,
      "end": 1783634641637,
      "order": "normal"
    },
    {
      "name": "vite:build-html",
      "start": 1783634641637,
      "end": 1783634641637,
      "order": "normal"
    },
    {
      "name": "vite:worker-import-meta-url",
      "start": 1783634641637,
      "end": 1783634641637,
      "order": "normal"
    },
    {
      "name": "vite:asset-import-meta-url",
      "start": 1783634641637,
      "end": 1783634641637,
      "order": "normal"
    },
    {
      "name": "commonjs",
      "start": 1783634641637,
      "end": 1783634641637,
      "order": "normal"
    },
    {
      "name": "vite:dynamic-import-vars",
      "start": 1783634641637,
      "end": 1783634641637,
      "order": "normal"
    },
    {
      "name": "vite:import-glob",
      "start": 1783634641637,
      "end": 1783634641637,
      "order": "normal"
    },
    {
      "name": "vite:build-import-analysis",
      "start": 1783634641638,
      "end": 1783634641638,
      "order": "normal"
    },
    {
      "name": "vite:reporter",
      "start": 1783634641638,
      "end": 1783634641638,
      "order": "normal"
    }
  ]
}

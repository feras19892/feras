{
  "resolvedId": "C:/Users/feras/Desktop/feras/apps/web/src/composables/experiment/analysis/useEquationSolver.ts",
  "transforms": [
    {
      "name": "vite:load-fallback",
      "result": "import { ref, computed, watch } from 'vue'\nimport { useI18n } from '../../../composables/useI18n'\nimport type { AnalysisEquation } from '../../../types/physics'\n\nexport function useEquationSolver(\n  equations: () => AnalysisEquation[],\n  readings: () => Record<string, number>[],\n) {\n  const { t } = useI18n()\n  const selectedIndex = ref(0)\n  const varValues = ref<Record<string, number>>({})\n  const targetVar = ref('')\n  const result = ref<string | null>(null)\n  const solvedEquations = ref<{ equationName: string; formula: string; targetVar: string; varValues: Record<string, number>; result: string; timestamp: number }[]>([])\n\n  function round3(n: number) { return Math.round(n * 1000) / 1000 }\n\n  function fillFromReadings() {\n    const eqs = equations()\n    if (!eqs.length) return\n    const eq = eqs[selectedIndex.value]\n    if (!eq) return\n    for (const row of readings()) {\n      let hasValue = false\n      for (const v of eq.variables) {\n        if (row[v.symbol] !== undefined && row[v.symbol] !== 0) {\n          varValues.value[v.symbol] = round3(row[v.symbol])\n          hasValue = true\n        }\n      }\n      if (hasValue) break\n    }\n  }\n\n  watch(equations, (eqs) => {\n    if (eqs.length) {\n      selectedIndex.value = 0\n      const vars: Record<string, number> = {}\n      for (const v of eqs[0].variables) { vars[v.symbol] = v.value ?? 0 }\n      varValues.value = vars\n      targetVar.value = eqs[0].solveFor[0] ?? ''\n      fillFromReadings()\n    }\n  }, { immediate: true })\n\n  watch(readings, () => { fillFromReadings() }, { deep: true })\n\n  const activeEquation = computed(() => equations()[selectedIndex.value])\n\n  function steps(name: string, formula: string, value: number) {\n    return `${name} = ${formula} = ${value.toFixed(4)}`\n  }\n\n  function solve() {\n    const eq = activeEquation.value\n    if (!eq || !targetVar.value) { result.value = null; return }\n\n    const vals = { ...varValues.value }\n    const missing = targetVar.value\n\n    // === Spring ===\n    if (eq.formula.includes('T = 2π√(m/k)')) {\n      const m = vals['m'] ?? 0; const T = vals['T'] ?? 0; const k = vals['k'] ?? 0\n      if (missing === 'k' && m > 0 && T > 0) {\n        result.value = steps('k', `4π²·m/T² = 4π²·${m.toFixed(4)}/${(T * T).toFixed(4)}`, (4 * Math.PI * Math.PI * m) / (T * T))\n      } else if (missing === 'T' && m > 0 && k > 0) {\n        result.value = steps('T', `2π√(m/k) = 2π√(${m.toFixed(4)}/${k.toFixed(4)})`, 2 * Math.PI * Math.sqrt(m / k))\n      } else if (missing === 'm' && T > 0 && k > 0) {\n        result.value = steps('m', `k·T²/(4π²) = ${k.toFixed(4)}·${(T * T).toFixed(4)}/(4π²)`, (k * T * T) / (4 * Math.PI * Math.PI))\n      } else result.value = t('analysis.enterValidKnownValues')\n    }\n    // === Pendulum ===\n    else if (eq.formula.includes('T = 2π√(L/g)')) {\n      const L = vals['L'] ?? 0; const T = vals['T'] ?? 0; const g = vals['g'] ?? 0\n      if (missing === 'g' && L > 0 && T > 0) {\n        result.value = steps('g', `4π²·L/T² = 4π²·${L.toFixed(4)}/${(T * T).toFixed(4)}`, (4 * Math.PI * Math.PI * L) / (T * T))\n      } else if (missing === 'T' && L > 0 && g > 0) {\n        result.value = steps('T', `2π√(L/g) = 2π√(${L.toFixed(4)}/${g.toFixed(4)})`, 2 * Math.PI * Math.sqrt(L / g))\n      } else if (missing === 'L' && T > 0 && g > 0) {\n        result.value = steps('L', `g·T²/(4π²) = ${g.toFixed(4)}·${(T * T).toFixed(4)}/(4π²)`, (g * T * T) / (4 * Math.PI * Math.PI))\n      } else result.value = t('analysis.enterValidKnownValues')\n    }\n    // === Pendulum T² form ===\n    else if (eq.formula.includes('T² = (4π²/g)·L')) {\n      const L = vals['L'] ?? 0; const T = vals['T'] ?? 0\n      if (missing === 'g' && L > 0 && T > 0) {\n        result.value = steps('g', `4π²·L/T² = 4π²·${L.toFixed(4)}/${(T * T).toFixed(4)}`, (4 * Math.PI * Math.PI * L) / (T * T))\n      } else result.value = t('analysis.enterValidValues')\n    }\n    // === Free Fall ===\n    else if (eq.formula.includes('h = ½gt²')) {\n      const h = vals['h'] ?? 0; const tVal = vals['t'] ?? 0; const g = vals['g'] ?? 0\n      if (missing === 'g' && h > 0 && tVal > 0) {\n        result.value = steps('g', `2h/t² = 2·${h.toFixed(4)}/${(tVal * tVal).toFixed(4)}`, (2 * h) / (tVal * tVal))\n      } else if (missing === 't' && h > 0 && g > 0) {\n        result.value = steps('t', `√(2h/g) = √(2·${h.toFixed(4)}/${g.toFixed(4)})`, Math.sqrt((2 * h) / g))\n      } else if (missing === 'h' && g > 0 && tVal > 0) {\n        result.value = steps('h', `½gt² = ½·${g.toFixed(4)}·${(tVal * tVal).toFixed(4)}`, 0.5 * g * tVal * tVal)\n      } else result.value = t('analysis.enterValidKnownValues')\n    }\n    // === Inclined Plane ===\n    else if (eq.formula.includes('a = g·sinθ − μ·g·cosθ')) {\n      const a = vals['a'] ?? 0; const g = vals['g'] ?? 0; const theta = (vals['θ'] ?? 0) * Math.PI / 180; const mu = vals['μ'] ?? 0\n      if (missing === 'a' && g > 0) {\n        result.value = steps('a', `g·sinθ−μ·g·cosθ = ${g.toFixed(4)}·${Math.sin(theta).toFixed(4)}−${mu.toFixed(4)}·${g.toFixed(4)}·${Math.cos(theta).toFixed(4)}`, g * Math.sin(theta) - mu * g * Math.cos(theta))\n      } else if (missing === 'μ' && g > 0 && Math.cos(theta) > 1e-6) {\n        result.value = steps('μ', `(g·sinθ−a)/(g·cosθ)`, (g * Math.sin(theta) - a) / (g * Math.cos(theta)))\n      } else result.value = t('analysis.enterValidValues')\n    }\n    else if (eq.formula.includes('s = ½at²')) {\n      const s = vals['s'] ?? 0; const a = vals['a'] ?? 0; const tVal = vals['t'] ?? 0\n      if (missing === 'a' && s > 0 && tVal > 0) {\n        result.value = steps('a', `2s/t² = 2·${s.toFixed(4)}/${(tVal * tVal).toFixed(4)}`, (2 * s) / (tVal * tVal))\n      } else if (missing === 't' && s > 0 && a > 0) {\n        result.value = steps('t', `√(2s/a) = √(2·${s.toFixed(4)}/${a.toFixed(4)})`, Math.sqrt((2 * s) / a))\n      } else if (missing === 's' && a > 0 && tVal > 0) {\n        result.value = steps('s', `½at² = ½·${a.toFixed(4)}·${(tVal * tVal).toFixed(4)}`, 0.5 * a * tVal * tVal)\n      } else result.value = t('analysis.enterValidValues')\n    }\n    // === Collision ===\n    else if (eq.formula.includes('m₁v₁i + m₂v₂i = m₁v₁f + m₂v₂f')) {\n      const m1 = vals['m1'] ?? 0; const m2 = vals['m2'] ?? 0\n      const v1i = vals['v1i'] ?? 0; const v2i = vals['v2i'] ?? 0\n      const v1f = vals['v1f'] ?? 0; const v2f = vals['v2f'] ?? 0\n      if (missing === 'v1f') {\n        result.value = steps('v₁f', `(m₁v₁i+m₂v₂i−m₂v₂f)/m₁`, (m1 * v1i + m2 * v2i - m2 * v2f) / m1)\n      } else if (missing === 'v2f') {\n        result.value = steps('v₂f', `(m₁v₁i+m₂v₂i−m₁v₁f)/m₂`, (m1 * v1i + m2 * v2i - m1 * v1f) / m2)\n      } else result.value = t('analysis.enterValidValues')\n    }\n    else if (eq.formula.includes('e = (v₂f − v₁f)/(v₁i − v₂i)')) {\n      const v1i = vals['v1i'] ?? 0; const v2i = vals['v2i'] ?? 0; const v1f = vals['v1f'] ?? 0; const v2f = vals['v2f'] ?? 0\n      if (missing === 'e' && Math.abs(v1i - v2i) > 1e-6) {\n        result.value = steps('e', `(v₂f−v₁f)/(v₁i−v₂i)`, (v2f - v1f) / (v1i - v2i))\n      } else result.value = t('analysis.enterValidValues')\n    }\n    // === Projectile ===\n    else if (eq.formula.includes('R = v₀²·sin(2θ)/g')) {\n      const v0 = vals['v0'] ?? 0; const theta = (vals['θ'] ?? 0) * Math.PI / 180; const g = vals['g'] ?? 0; const R = vals['R'] ?? 0\n      if (missing === 'R' && v0 > 0 && g > 0) {\n        result.value = steps('R', `v₀²·sin(2θ)/g`, (v0 * v0 * Math.sin(2 * theta)) / g)\n      } else if (missing === 'v0' && R > 0 && g > 0 && Math.sin(2 * theta) > 1e-6) {\n        result.value = steps('v₀', `√(R·g/sin(2θ))`, Math.sqrt((R * g) / Math.sin(2 * theta)))\n      } else if (missing === 'θ' && R > 0 && v0 > 0 && g > 0) {\n        result.value = steps('θ', `½·arcsin(R·g/v₀²)`, Math.asin((R * g) / (v0 * v0)) / 2 * 180 / Math.PI)\n      } else result.value = t('analysis.enterValidValues')\n    }\n    else if (eq.formula.includes('H = v₀²·sin²(θ)/(2g)')) {\n      const v0 = vals['v0'] ?? 0; const theta = (vals['θ'] ?? 0) * Math.PI / 180; const g = vals['g'] ?? 0\n      if (missing === 'H' && v0 > 0 && g > 0) {\n        result.value = steps('H', `v₀²·sin²(θ)/(2g)`, (v0 * v0 * Math.sin(theta) * Math.sin(theta)) / (2 * g))\n      } else result.value = t('analysis.enterValidValues')\n    }\n    // === Straight Wire (Biot-Savart) ===\n    else if (eq.formula.includes('B = μ₀I / (2πr)')) {\n      const B_val = vals['B'] ?? 0; const I_val = vals['I'] ?? 0; const r_val = vals['r'] ?? 0; const mu0 = vals['μ₀'] ?? 0\n      if (missing === 'B' && I_val > 0 && r_val > 0) {\n        const mu = mu0 > 0 ? mu0 : (4 * Math.PI * 1e-7)\n        result.value = steps('B', `μ₀I/(2πr) = ${mu.toExponential(4)}·${I_val.toFixed(4)}/(2π·${r_val.toFixed(4)})`, (mu * I_val) / (2 * Math.PI * r_val))\n      } else if (missing === 'I' && B_val > 0 && r_val > 0) {\n        const mu = mu0 > 0 ? mu0 : (4 * Math.PI * 1e-7)\n        result.value = steps('I', `B·2πr/μ₀ = ${B_val.toExponential(4)}·2π·${r_val.toFixed(4)}/${mu.toExponential(4)}`, (B_val * 2 * Math.PI * r_val) / mu)\n      } else if (missing === 'r' && B_val > 0 && I_val > 0) {\n        const mu = mu0 > 0 ? mu0 : (4 * Math.PI * 1e-7)\n        result.value = steps('r', `μ₀I/(2πB) = ${mu.toExponential(4)}·${I_val.toFixed(4)}/(2π·${B_val.toExponential(4)})`, (mu * I_val) / (2 * Math.PI * B_val))\n      } else if (missing === 'μ₀' && B_val > 0 && I_val > 0 && r_val > 0) {\n        result.value = steps('μ₀', `B·2πr/I = ${B_val.toExponential(4)}·2π·${r_val.toFixed(4)}/${I_val.toFixed(4)}`, (B_val * 2 * Math.PI * r_val) / I_val)\n      } else result.value = t('analysis.enterValidKnownValues')\n    }\n    // === Circular Coil (Biot-Savart) ===\n    else if (eq.formula.includes('B = μ₀NI / (2R)')) {\n      const B_val = vals['B'] ?? 0; const I_val = vals['I'] ?? 0; const N_val = vals['N'] ?? 0; const R_val = vals['R'] ?? 0; const mu0 = vals['μ₀'] ?? 0\n      if (missing === 'B' && I_val > 0 && N_val > 0 && R_val > 0) {\n        const mu = mu0 > 0 ? mu0 : (4 * Math.PI * 1e-7)\n        result.value = steps('B', `μ₀NI/(2R) = ${mu.toExponential(4)}·${N_val}·${I_val.toFixed(4)}/(2·${R_val.toFixed(4)})`, (mu * N_val * I_val) / (2 * R_val))\n      } else if (missing === 'I' && B_val > 0 && N_val > 0 && R_val > 0) {\n        const mu = mu0 > 0 ? mu0 : (4 * Math.PI * 1e-7)\n        result.value = steps('I', `B·2R/(μ₀N) = ${B_val.toExponential(4)}·2·${R_val.toFixed(4)}/(${mu.toExponential(4)}·${N_val})`, (B_val * 2 * R_val) / (mu * N_val))\n      } else if (missing === 'N' && B_val > 0 && I_val > 0 && R_val > 0) {\n        const mu = mu0 > 0 ? mu0 : (4 * Math.PI * 1e-7)\n        result.value = steps('N', `B·2R/(μ₀I) = ${B_val.toExponential(4)}·2·${R_val.toFixed(4)}/(${mu.toExponential(4)}·${I_val.toFixed(4)})`, Math.round((B_val * 2 * R_val) / (mu * I_val)))\n      } else if (missing === 'R' && B_val > 0 && I_val > 0 && N_val > 0) {\n        const mu = mu0 > 0 ? mu0 : (4 * Math.PI * 1e-7)\n        result.value = steps('R', `μ₀NI/(2B) = ${mu.toExponential(4)}·${N_val}·${I_val.toFixed(4)}/(2·${B_val.toExponential(4)})`, (mu * N_val * I_val) / (2 * B_val))\n      } else if (missing === 'μ₀' && B_val > 0 && I_val > 0 && N_val > 0 && R_val > 0) {\n        result.value = steps('μ₀', `B·2R/(NI) = ${B_val.toExponential(4)}·2·${R_val.toFixed(4)}/(${N_val}·${I_val.toFixed(4)})`, (B_val * 2 * R_val) / (N_val * I_val))\n      } else result.value = t('analysis.enterValidKnownValues')\n    }\n    else {\n      result.value = t('analysis.calculateFromEquation', { missing, formula: eq.formula })\n    }\n  }\n\n  // Auto-solve when enough inputs are filled\n  watch([varValues, targetVar], () => {\n    const eq = activeEquation.value\n    if (!eq || !targetVar.value) return\n    const known = eq.variables.filter(v => varValues.value[v.symbol] !== undefined && varValues.value[v.symbol] !== 0).length\n    const total = eq.variables.length\n    if (known >= total - 1) solve()\n  }, { deep: true })\n\n  // Capture solved results for report\n  watch(result, (r) => {\n    const eq = activeEquation.value\n    if (!r || !eq) return\n    const entry = {\n      equationName: eq.name,\n      formula: eq.formula,\n      targetVar: targetVar.value,\n      varValues: { ...varValues.value },\n      result: r,\n      timestamp: Date.now(),\n    }\n    // Avoid duplicates (same equation + same target)\n    const idx = solvedEquations.value.findIndex(\n      s => s.equationName === entry.equationName && s.targetVar === entry.targetVar\n    )\n    if (idx >= 0) solvedEquations.value[idx] = entry\n    else solvedEquations.value.push(entry)\n  })\n\n  return {\n    selectedIndex,\n    varValues,\n    targetVar,\n    result,\n    activeEquation,\n    solve,\n    solvedEquations,\n  }\n}\n",
      "start": 1783634641030,
      "end": 1783634641546,
      "sourcemaps": null
    },
    {
      "name": "vite:css",
      "start": 1783634641546,
      "end": 1783634641546,
      "order": "normal"
    },
    {
      "name": "vite:esbuild",
      "result": "import { ref, computed, watch } from \"vue\";\nimport { useI18n } from \"../../../composables/useI18n\";\nexport function useEquationSolver(equations, readings) {\n  const { t } = useI18n();\n  const selectedIndex = ref(0);\n  const varValues = ref({});\n  const targetVar = ref(\"\");\n  const result = ref(null);\n  const solvedEquations = ref([]);\n  function round3(n) {\n    return Math.round(n * 1e3) / 1e3;\n  }\n  function fillFromReadings() {\n    const eqs = equations();\n    if (!eqs.length) return;\n    const eq = eqs[selectedIndex.value];\n    if (!eq) return;\n    for (const row of readings()) {\n      let hasValue = false;\n      for (const v of eq.variables) {\n        if (row[v.symbol] !== void 0 && row[v.symbol] !== 0) {\n          varValues.value[v.symbol] = round3(row[v.symbol]);\n          hasValue = true;\n        }\n      }\n      if (hasValue) break;\n    }\n  }\n  watch(equations, (eqs) => {\n    if (eqs.length) {\n      selectedIndex.value = 0;\n      const vars = {};\n      for (const v of eqs[0].variables) {\n        vars[v.symbol] = v.value ?? 0;\n      }\n      varValues.value = vars;\n      targetVar.value = eqs[0].solveFor[0] ?? \"\";\n      fillFromReadings();\n    }\n  }, { immediate: true });\n  watch(readings, () => {\n    fillFromReadings();\n  }, { deep: true });\n  const activeEquation = computed(() => equations()[selectedIndex.value]);\n  function steps(name, formula, value) {\n    return `${name} = ${formula} = ${value.toFixed(4)}`;\n  }\n  function solve() {\n    const eq = activeEquation.value;\n    if (!eq || !targetVar.value) {\n      result.value = null;\n      return;\n    }\n    const vals = { ...varValues.value };\n    const missing = targetVar.value;\n    if (eq.formula.includes(\"T = 2π√(m/k)\")) {\n      const m = vals[\"m\"] ?? 0;\n      const T = vals[\"T\"] ?? 0;\n      const k = vals[\"k\"] ?? 0;\n      if (missing === \"k\" && m > 0 && T > 0) {\n        result.value = steps(\"k\", `4π²·m/T² = 4π²·${m.toFixed(4)}/${(T * T).toFixed(4)}`, 4 * Math.PI * Math.PI * m / (T * T));\n      } else if (missing === \"T\" && m > 0 && k > 0) {\n        result.value = steps(\"T\", `2π√(m/k) = 2π√(${m.toFixed(4)}/${k.toFixed(4)})`, 2 * Math.PI * Math.sqrt(m / k));\n      } else if (missing === \"m\" && T > 0 && k > 0) {\n        result.value = steps(\"m\", `k·T²/(4π²) = ${k.toFixed(4)}·${(T * T).toFixed(4)}/(4π²)`, k * T * T / (4 * Math.PI * Math.PI));\n      } else result.value = t(\"analysis.enterValidKnownValues\");\n    } else if (eq.formula.includes(\"T = 2π√(L/g)\")) {\n      const L = vals[\"L\"] ?? 0;\n      const T = vals[\"T\"] ?? 0;\n      const g = vals[\"g\"] ?? 0;\n      if (missing === \"g\" && L > 0 && T > 0) {\n        result.value = steps(\"g\", `4π²·L/T² = 4π²·${L.toFixed(4)}/${(T * T).toFixed(4)}`, 4 * Math.PI * Math.PI * L / (T * T));\n      } else if (missing === \"T\" && L > 0 && g > 0) {\n        result.value = steps(\"T\", `2π√(L/g) = 2π√(${L.toFixed(4)}/${g.toFixed(4)})`, 2 * Math.PI * Math.sqrt(L / g));\n      } else if (missing === \"L\" && T > 0 && g > 0) {\n        result.value = steps(\"L\", `g·T²/(4π²) = ${g.toFixed(4)}·${(T * T).toFixed(4)}/(4π²)`, g * T * T / (4 * Math.PI * Math.PI));\n      } else result.value = t(\"analysis.enterValidKnownValues\");\n    } else if (eq.formula.includes(\"T² = (4π²/g)·L\")) {\n      const L = vals[\"L\"] ?? 0;\n      const T = vals[\"T\"] ?? 0;\n      if (missing === \"g\" && L > 0 && T > 0) {\n        result.value = steps(\"g\", `4π²·L/T² = 4π²·${L.toFixed(4)}/${(T * T).toFixed(4)}`, 4 * Math.PI * Math.PI * L / (T * T));\n      } else result.value = t(\"analysis.enterValidValues\");\n    } else if (eq.formula.includes(\"h = ½gt²\")) {\n      const h = vals[\"h\"] ?? 0;\n      const tVal = vals[\"t\"] ?? 0;\n      const g = vals[\"g\"] ?? 0;\n      if (missing === \"g\" && h > 0 && tVal > 0) {\n        result.value = steps(\"g\", `2h/t² = 2·${h.toFixed(4)}/${(tVal * tVal).toFixed(4)}`, 2 * h / (tVal * tVal));\n      } else if (missing === \"t\" && h > 0 && g > 0) {\n        result.value = steps(\"t\", `√(2h/g) = √(2·${h.toFixed(4)}/${g.toFixed(4)})`, Math.sqrt(2 * h / g));\n      } else if (missing === \"h\" && g > 0 && tVal > 0) {\n        result.value = steps(\"h\", `½gt² = ½·${g.toFixed(4)}·${(tVal * tVal).toFixed(4)}`, 0.5 * g * tVal * tVal);\n      } else result.value = t(\"analysis.enterValidKnownValues\");\n    } else if (eq.formula.includes(\"a = g·sinθ − μ·g·cosθ\")) {\n      const a = vals[\"a\"] ?? 0;\n      const g = vals[\"g\"] ?? 0;\n      const theta = (vals[\"θ\"] ?? 0) * Math.PI / 180;\n      const mu = vals[\"μ\"] ?? 0;\n      if (missing === \"a\" && g > 0) {\n        result.value = steps(\"a\", `g·sinθ−μ·g·cosθ = ${g.toFixed(4)}·${Math.sin(theta).toFixed(4)}−${mu.toFixed(4)}·${g.toFixed(4)}·${Math.cos(theta).toFixed(4)}`, g * Math.sin(theta) - mu * g * Math.cos(theta));\n      } else if (missing === \"μ\" && g > 0 && Math.cos(theta) > 1e-6) {\n        result.value = steps(\"μ\", `(g·sinθ−a)/(g·cosθ)`, (g * Math.sin(theta) - a) / (g * Math.cos(theta)));\n      } else result.value = t(\"analysis.enterValidValues\");\n    } else if (eq.formula.includes(\"s = ½at²\")) {\n      const s = vals[\"s\"] ?? 0;\n      const a = vals[\"a\"] ?? 0;\n      const tVal = vals[\"t\"] ?? 0;\n      if (missing === \"a\" && s > 0 && tVal > 0) {\n        result.value = steps(\"a\", `2s/t² = 2·${s.toFixed(4)}/${(tVal * tVal).toFixed(4)}`, 2 * s / (tVal * tVal));\n      } else if (missing === \"t\" && s > 0 && a > 0) {\n        result.value = steps(\"t\", `√(2s/a) = √(2·${s.toFixed(4)}/${a.toFixed(4)})`, Math.sqrt(2 * s / a));\n      } else if (missing === \"s\" && a > 0 && tVal > 0) {\n        result.value = steps(\"s\", `½at² = ½·${a.toFixed(4)}·${(tVal * tVal).toFixed(4)}`, 0.5 * a * tVal * tVal);\n      } else result.value = t(\"analysis.enterValidValues\");\n    } else if (eq.formula.includes(\"m₁v₁i + m₂v₂i = m₁v₁f + m₂v₂f\")) {\n      const m1 = vals[\"m1\"] ?? 0;\n      const m2 = vals[\"m2\"] ?? 0;\n      const v1i = vals[\"v1i\"] ?? 0;\n      const v2i = vals[\"v2i\"] ?? 0;\n      const v1f = vals[\"v1f\"] ?? 0;\n      const v2f = vals[\"v2f\"] ?? 0;\n      if (missing === \"v1f\") {\n        result.value = steps(\"v₁f\", `(m₁v₁i+m₂v₂i−m₂v₂f)/m₁`, (m1 * v1i + m2 * v2i - m2 * v2f) / m1);\n      } else if (missing === \"v2f\") {\n        result.value = steps(\"v₂f\", `(m₁v₁i+m₂v₂i−m₁v₁f)/m₂`, (m1 * v1i + m2 * v2i - m1 * v1f) / m2);\n      } else result.value = t(\"analysis.enterValidValues\");\n    } else if (eq.formula.includes(\"e = (v₂f − v₁f)/(v₁i − v₂i)\")) {\n      const v1i = vals[\"v1i\"] ?? 0;\n      const v2i = vals[\"v2i\"] ?? 0;\n      const v1f = vals[\"v1f\"] ?? 0;\n      const v2f = vals[\"v2f\"] ?? 0;\n      if (missing === \"e\" && Math.abs(v1i - v2i) > 1e-6) {\n        result.value = steps(\"e\", `(v₂f−v₁f)/(v₁i−v₂i)`, (v2f - v1f) / (v1i - v2i));\n      } else result.value = t(\"analysis.enterValidValues\");\n    } else if (eq.formula.includes(\"R = v₀²·sin(2θ)/g\")) {\n      const v0 = vals[\"v0\"] ?? 0;\n      const theta = (vals[\"θ\"] ?? 0) * Math.PI / 180;\n      const g = vals[\"g\"] ?? 0;\n      const R = vals[\"R\"] ?? 0;\n      if (missing === \"R\" && v0 > 0 && g > 0) {\n        result.value = steps(\"R\", `v₀²·sin(2θ)/g`, v0 * v0 * Math.sin(2 * theta) / g);\n      } else if (missing === \"v0\" && R > 0 && g > 0 && Math.sin(2 * theta) > 1e-6) {\n        result.value = steps(\"v₀\", `√(R·g/sin(2θ))`, Math.sqrt(R * g / Math.sin(2 * theta)));\n      } else if (missing === \"θ\" && R > 0 && v0 > 0 && g > 0) {\n        result.value = steps(\"θ\", `½·arcsin(R·g/v₀²)`, Math.asin(R * g / (v0 * v0)) / 2 * 180 / Math.PI);\n      } else result.value = t(\"analysis.enterValidValues\");\n    } else if (eq.formula.includes(\"H = v₀²·sin²(θ)/(2g)\")) {\n      const v0 = vals[\"v0\"] ?? 0;\n      const theta = (vals[\"θ\"] ?? 0) * Math.PI / 180;\n      const g = vals[\"g\"] ?? 0;\n      if (missing === \"H\" && v0 > 0 && g > 0) {\n        result.value = steps(\"H\", `v₀²·sin²(θ)/(2g)`, v0 * v0 * Math.sin(theta) * Math.sin(theta) / (2 * g));\n      } else result.value = t(\"analysis.enterValidValues\");\n    } else if (eq.formula.includes(\"B = μ₀I / (2πr)\")) {\n      const B_val = vals[\"B\"] ?? 0;\n      const I_val = vals[\"I\"] ?? 0;\n      const r_val = vals[\"r\"] ?? 0;\n      const mu0 = vals[\"μ₀\"] ?? 0;\n      if (missing === \"B\" && I_val > 0 && r_val > 0) {\n        const mu = mu0 > 0 ? mu0 : 4 * Math.PI * 1e-7;\n        result.value = steps(\"B\", `μ₀I/(2πr) = ${mu.toExponential(4)}·${I_val.toFixed(4)}/(2π·${r_val.toFixed(4)})`, mu * I_val / (2 * Math.PI * r_val));\n      } else if (missing === \"I\" && B_val > 0 && r_val > 0) {\n        const mu = mu0 > 0 ? mu0 : 4 * Math.PI * 1e-7;\n        result.value = steps(\"I\", `B·2πr/μ₀ = ${B_val.toExponential(4)}·2π·${r_val.toFixed(4)}/${mu.toExponential(4)}`, B_val * 2 * Math.PI * r_val / mu);\n      } else if (missing === \"r\" && B_val > 0 && I_val > 0) {\n        const mu = mu0 > 0 ? mu0 : 4 * Math.PI * 1e-7;\n        result.value = steps(\"r\", `μ₀I/(2πB) = ${mu.toExponential(4)}·${I_val.toFixed(4)}/(2π·${B_val.toExponential(4)})`, mu * I_val / (2 * Math.PI * B_val));\n      } else if (missing === \"μ₀\" && B_val > 0 && I_val > 0 && r_val > 0) {\n        result.value = steps(\"μ₀\", `B·2πr/I = ${B_val.toExponential(4)}·2π·${r_val.toFixed(4)}/${I_val.toFixed(4)}`, B_val * 2 * Math.PI * r_val / I_val);\n      } else result.value = t(\"analysis.enterValidKnownValues\");\n    } else if (eq.formula.includes(\"B = μ₀NI / (2R)\")) {\n      const B_val = vals[\"B\"] ?? 0;\n      const I_val = vals[\"I\"] ?? 0;\n      const N_val = vals[\"N\"] ?? 0;\n      const R_val = vals[\"R\"] ?? 0;\n      const mu0 = vals[\"μ₀\"] ?? 0;\n      if (missing === \"B\" && I_val > 0 && N_val > 0 && R_val > 0) {\n        const mu = mu0 > 0 ? mu0 : 4 * Math.PI * 1e-7;\n        result.value = steps(\"B\", `μ₀NI/(2R) = ${mu.toExponential(4)}·${N_val}·${I_val.toFixed(4)}/(2·${R_val.toFixed(4)})`, mu * N_val * I_val / (2 * R_val));\n      } else if (missing === \"I\" && B_val > 0 && N_val > 0 && R_val > 0) {\n        const mu = mu0 > 0 ? mu0 : 4 * Math.PI * 1e-7;\n        result.value = steps(\"I\", `B·2R/(μ₀N) = ${B_val.toExponential(4)}·2·${R_val.toFixed(4)}/(${mu.toExponential(4)}·${N_val})`, B_val * 2 * R_val / (mu * N_val));\n      } else if (missing === \"N\" && B_val > 0 && I_val > 0 && R_val > 0) {\n        const mu = mu0 > 0 ? mu0 : 4 * Math.PI * 1e-7;\n        result.value = steps(\"N\", `B·2R/(μ₀I) = ${B_val.toExponential(4)}·2·${R_val.toFixed(4)}/(${mu.toExponential(4)}·${I_val.toFixed(4)})`, Math.round(B_val * 2 * R_val / (mu * I_val)));\n      } else if (missing === \"R\" && B_val > 0 && I_val > 0 && N_val > 0) {\n        const mu = mu0 > 0 ? mu0 : 4 * Math.PI * 1e-7;\n        result.value = steps(\"R\", `μ₀NI/(2B) = ${mu.toExponential(4)}·${N_val}·${I_val.toFixed(4)}/(2·${B_val.toExponential(4)})`, mu * N_val * I_val / (2 * B_val));\n      } else if (missing === \"μ₀\" && B_val > 0 && I_val > 0 && N_val > 0 && R_val > 0) {\n        result.value = steps(\"μ₀\", `B·2R/(NI) = ${B_val.toExponential(4)}·2·${R_val.toFixed(4)}/(${N_val}·${I_val.toFixed(4)})`, B_val * 2 * R_val / (N_val * I_val));\n      } else result.value = t(\"analysis.enterValidKnownValues\");\n    } else {\n      result.value = t(\"analysis.calculateFromEquation\", { missing, formula: eq.formula });\n    }\n  }\n  watch([varValues, targetVar], () => {\n    const eq = activeEquation.value;\n    if (!eq || !targetVar.value) return;\n    const known = eq.variables.filter((v) => varValues.value[v.symbol] !== void 0 && varValues.value[v.symbol] !== 0).length;\n    const total = eq.variables.length;\n    if (known >= total - 1) solve();\n  }, { deep: true });\n  watch(result, (r) => {\n    const eq = activeEquation.value;\n    if (!r || !eq) return;\n    const entry = {\n      equationName: eq.name,\n      formula: eq.formula,\n      targetVar: targetVar.value,\n      varValues: { ...varValues.value },\n      result: r,\n      timestamp: Date.now()\n    };\n    const idx = solvedEquations.value.findIndex(\n      (s) => s.equationName === entry.equationName && s.targetVar === entry.targetVar\n    );\n    if (idx >= 0) solvedEquations.value[idx] = entry;\n    else solvedEquations.value.push(entry);\n  });\n  return {\n    selectedIndex,\n    varValues,\n    targetVar,\n    result,\n    activeEquation,\n    solve,\n    solvedEquations\n  };\n}\n",
      "start": 1783634641546,
      "end": 1783634641667,
      "order": "normal",
      "sourcemaps": {
        "version": 3,
        "sources": [
          "C:/Users/feras/Desktop/feras/apps/web/src/composables/experiment/analysis/useEquationSolver.ts"
        ],
        "sourcesContent": [
          "import { ref, computed, watch } from 'vue'\nimport { useI18n } from '../../../composables/useI18n'\nimport type { AnalysisEquation } from '../../../types/physics'\n\nexport function useEquationSolver(\n  equations: () => AnalysisEquation[],\n  readings: () => Record<string, number>[],\n) {\n  const { t } = useI18n()\n  const selectedIndex = ref(0)\n  const varValues = ref<Record<string, number>>({})\n  const targetVar = ref('')\n  const result = ref<string | null>(null)\n  const solvedEquations = ref<{ equationName: string; formula: string; targetVar: string; varValues: Record<string, number>; result: string; timestamp: number }[]>([])\n\n  function round3(n: number) { return Math.round(n * 1000) / 1000 }\n\n  function fillFromReadings() {\n    const eqs = equations()\n    if (!eqs.length) return\n    const eq = eqs[selectedIndex.value]\n    if (!eq) return\n    for (const row of readings()) {\n      let hasValue = false\n      for (const v of eq.variables) {\n        if (row[v.symbol] !== undefined && row[v.symbol] !== 0) {\n          varValues.value[v.symbol] = round3(row[v.symbol])\n          hasValue = true\n        }\n      }\n      if (hasValue) break\n    }\n  }\n\n  watch(equations, (eqs) => {\n    if (eqs.length) {\n      selectedIndex.value = 0\n      const vars: Record<string, number> = {}\n      for (const v of eqs[0].variables) { vars[v.symbol] = v.value ?? 0 }\n      varValues.value = vars\n      targetVar.value = eqs[0].solveFor[0] ?? ''\n      fillFromReadings()\n    }\n  }, { immediate: true })\n\n  watch(readings, () => { fillFromReadings() }, { deep: true })\n\n  const activeEquation = computed(() => equations()[selectedIndex.value])\n\n  function steps(name: string, formula: string, value: number) {\n    return `${name} = ${formula} = ${value.toFixed(4)}`\n  }\n\n  function solve() {\n    const eq = activeEquation.value\n    if (!eq || !targetVar.value) { result.value = null; return }\n\n    const vals = { ...varValues.value }\n    const missing = targetVar.value\n\n    // === Spring ===\n    if (eq.formula.includes('T = 2π√(m/k)')) {\n      const m = vals['m'] ?? 0; const T = vals['T'] ?? 0; const k = vals['k'] ?? 0\n      if (missing === 'k' && m > 0 && T > 0) {\n        result.value = steps('k', `4π²·m/T² = 4π²·${m.toFixed(4)}/${(T * T).toFixed(4)}`, (4 * Math.PI * Math.PI * m) / (T * T))\n      } else if (missing === 'T' && m > 0 && k > 0) {\n        result.value = steps('T', `2π√(m/k) = 2π√(${m.toFixed(4)}/${k.toFixed(4)})`, 2 * Math.PI * Math.sqrt(m / k))\n      } else if (missing === 'm' && T > 0 && k > 0) {\n        result.value = steps('m', `k·T²/(4π²) = ${k.toFixed(4)}·${(T * T).toFixed(4)}/(4π²)`, (k * T * T) / (4 * Math.PI * Math.PI))\n      } else result.value = t('analysis.enterValidKnownValues')\n    }\n    // === Pendulum ===\n    else if (eq.formula.includes('T = 2π√(L/g)')) {\n      const L = vals['L'] ?? 0; const T = vals['T'] ?? 0; const g = vals['g'] ?? 0\n      if (missing === 'g' && L > 0 && T > 0) {\n        result.value = steps('g', `4π²·L/T² = 4π²·${L.toFixed(4)}/${(T * T).toFixed(4)}`, (4 * Math.PI * Math.PI * L) / (T * T))\n      } else if (missing === 'T' && L > 0 && g > 0) {\n        result.value = steps('T', `2π√(L/g) = 2π√(${L.toFixed(4)}/${g.toFixed(4)})`, 2 * Math.PI * Math.sqrt(L / g))\n      } else if (missing === 'L' && T > 0 && g > 0) {\n        result.value = steps('L', `g·T²/(4π²) = ${g.toFixed(4)}·${(T * T).toFixed(4)}/(4π²)`, (g * T * T) / (4 * Math.PI * Math.PI))\n      } else result.value = t('analysis.enterValidKnownValues')\n    }\n    // === Pendulum T² form ===\n    else if (eq.formula.includes('T² = (4π²/g)·L')) {\n      const L = vals['L'] ?? 0; const T = vals['T'] ?? 0\n      if (missing === 'g' && L > 0 && T > 0) {\n        result.value = steps('g', `4π²·L/T² = 4π²·${L.toFixed(4)}/${(T * T).toFixed(4)}`, (4 * Math.PI * Math.PI * L) / (T * T))\n      } else result.value = t('analysis.enterValidValues')\n    }\n    // === Free Fall ===\n    else if (eq.formula.includes('h = ½gt²')) {\n      const h = vals['h'] ?? 0; const tVal = vals['t'] ?? 0; const g = vals['g'] ?? 0\n      if (missing === 'g' && h > 0 && tVal > 0) {\n        result.value = steps('g', `2h/t² = 2·${h.toFixed(4)}/${(tVal * tVal).toFixed(4)}`, (2 * h) / (tVal * tVal))\n      } else if (missing === 't' && h > 0 && g > 0) {\n        result.value = steps('t', `√(2h/g) = √(2·${h.toFixed(4)}/${g.toFixed(4)})`, Math.sqrt((2 * h) / g))\n      } else if (missing === 'h' && g > 0 && tVal > 0) {\n        result.value = steps('h', `½gt² = ½·${g.toFixed(4)}·${(tVal * tVal).toFixed(4)}`, 0.5 * g * tVal * tVal)\n      } else result.value = t('analysis.enterValidKnownValues')\n    }\n    // === Inclined Plane ===\n    else if (eq.formula.includes('a = g·sinθ − μ·g·cosθ')) {\n      const a = vals['a'] ?? 0; const g = vals['g'] ?? 0; const theta = (vals['θ'] ?? 0) * Math.PI / 180; const mu = vals['μ'] ?? 0\n      if (missing === 'a' && g > 0) {\n        result.value = steps('a', `g·sinθ−μ·g·cosθ = ${g.toFixed(4)}·${Math.sin(theta).toFixed(4)}−${mu.toFixed(4)}·${g.toFixed(4)}·${Math.cos(theta).toFixed(4)}`, g * Math.sin(theta) - mu * g * Math.cos(theta))\n      } else if (missing === 'μ' && g > 0 && Math.cos(theta) > 1e-6) {\n        result.value = steps('μ', `(g·sinθ−a)/(g·cosθ)`, (g * Math.sin(theta) - a) / (g * Math.cos(theta)))\n      } else result.value = t('analysis.enterValidValues')\n    }\n    else if (eq.formula.includes('s = ½at²')) {\n      const s = vals['s'] ?? 0; const a = vals['a'] ?? 0; const tVal = vals['t'] ?? 0\n      if (missing === 'a' && s > 0 && tVal > 0) {\n        result.value = steps('a', `2s/t² = 2·${s.toFixed(4)}/${(tVal * tVal).toFixed(4)}`, (2 * s) / (tVal * tVal))\n      } else if (missing === 't' && s > 0 && a > 0) {\n        result.value = steps('t', `√(2s/a) = √(2·${s.toFixed(4)}/${a.toFixed(4)})`, Math.sqrt((2 * s) / a))\n      } else if (missing === 's' && a > 0 && tVal > 0) {\n        result.value = steps('s', `½at² = ½·${a.toFixed(4)}·${(tVal * tVal).toFixed(4)}`, 0.5 * a * tVal * tVal)\n      } else result.value = t('analysis.enterValidValues')\n    }\n    // === Collision ===\n    else if (eq.formula.includes('m₁v₁i + m₂v₂i = m₁v₁f + m₂v₂f')) {\n      const m1 = vals['m1'] ?? 0; const m2 = vals['m2'] ?? 0\n      const v1i = vals['v1i'] ?? 0; const v2i = vals['v2i'] ?? 0\n      const v1f = vals['v1f'] ?? 0; const v2f = vals['v2f'] ?? 0\n      if (missing === 'v1f') {\n        result.value = steps('v₁f', `(m₁v₁i+m₂v₂i−m₂v₂f)/m₁`, (m1 * v1i + m2 * v2i - m2 * v2f) / m1)\n      } else if (missing === 'v2f') {\n        result.value = steps('v₂f', `(m₁v₁i+m₂v₂i−m₁v₁f)/m₂`, (m1 * v1i + m2 * v2i - m1 * v1f) / m2)\n      } else result.value = t('analysis.enterValidValues')\n    }\n    else if (eq.formula.includes('e = (v₂f − v₁f)/(v₁i − v₂i)')) {\n      const v1i = vals['v1i'] ?? 0; const v2i = vals['v2i'] ?? 0; const v1f = vals['v1f'] ?? 0; const v2f = vals['v2f'] ?? 0\n      if (missing === 'e' && Math.abs(v1i - v2i) > 1e-6) {\n        result.value = steps('e', `(v₂f−v₁f)/(v₁i−v₂i)`, (v2f - v1f) / (v1i - v2i))\n      } else result.value = t('analysis.enterValidValues')\n    }\n    // === Projectile ===\n    else if (eq.formula.includes('R = v₀²·sin(2θ)/g')) {\n      const v0 = vals['v0'] ?? 0; const theta = (vals['θ'] ?? 0) * Math.PI / 180; const g = vals['g'] ?? 0; const R = vals['R'] ?? 0\n      if (missing === 'R' && v0 > 0 && g > 0) {\n        result.value = steps('R', `v₀²·sin(2θ)/g`, (v0 * v0 * Math.sin(2 * theta)) / g)\n      } else if (missing === 'v0' && R > 0 && g > 0 && Math.sin(2 * theta) > 1e-6) {\n        result.value = steps('v₀', `√(R·g/sin(2θ))`, Math.sqrt((R * g) / Math.sin(2 * theta)))\n      } else if (missing === 'θ' && R > 0 && v0 > 0 && g > 0) {\n        result.value = steps('θ', `½·arcsin(R·g/v₀²)`, Math.asin((R * g) / (v0 * v0)) / 2 * 180 / Math.PI)\n      } else result.value = t('analysis.enterValidValues')\n    }\n    else if (eq.formula.includes('H = v₀²·sin²(θ)/(2g)')) {\n      const v0 = vals['v0'] ?? 0; const theta = (vals['θ'] ?? 0) * Math.PI / 180; const g = vals['g'] ?? 0\n      if (missing === 'H' && v0 > 0 && g > 0) {\n        result.value = steps('H', `v₀²·sin²(θ)/(2g)`, (v0 * v0 * Math.sin(theta) * Math.sin(theta)) / (2 * g))\n      } else result.value = t('analysis.enterValidValues')\n    }\n    // === Straight Wire (Biot-Savart) ===\n    else if (eq.formula.includes('B = μ₀I / (2πr)')) {\n      const B_val = vals['B'] ?? 0; const I_val = vals['I'] ?? 0; const r_val = vals['r'] ?? 0; const mu0 = vals['μ₀'] ?? 0\n      if (missing === 'B' && I_val > 0 && r_val > 0) {\n        const mu = mu0 > 0 ? mu0 : (4 * Math.PI * 1e-7)\n        result.value = steps('B', `μ₀I/(2πr) = ${mu.toExponential(4)}·${I_val.toFixed(4)}/(2π·${r_val.toFixed(4)})`, (mu * I_val) / (2 * Math.PI * r_val))\n      } else if (missing === 'I' && B_val > 0 && r_val > 0) {\n        const mu = mu0 > 0 ? mu0 : (4 * Math.PI * 1e-7)\n        result.value = steps('I', `B·2πr/μ₀ = ${B_val.toExponential(4)}·2π·${r_val.toFixed(4)}/${mu.toExponential(4)}`, (B_val * 2 * Math.PI * r_val) / mu)\n      } else if (missing === 'r' && B_val > 0 && I_val > 0) {\n        const mu = mu0 > 0 ? mu0 : (4 * Math.PI * 1e-7)\n        result.value = steps('r', `μ₀I/(2πB) = ${mu.toExponential(4)}·${I_val.toFixed(4)}/(2π·${B_val.toExponential(4)})`, (mu * I_val) / (2 * Math.PI * B_val))\n      } else if (missing === 'μ₀' && B_val > 0 && I_val > 0 && r_val > 0) {\n        result.value = steps('μ₀', `B·2πr/I = ${B_val.toExponential(4)}·2π·${r_val.toFixed(4)}/${I_val.toFixed(4)}`, (B_val * 2 * Math.PI * r_val) / I_val)\n      } else result.value = t('analysis.enterValidKnownValues')\n    }\n    // === Circular Coil (Biot-Savart) ===\n    else if (eq.formula.includes('B = μ₀NI / (2R)')) {\n      const B_val = vals['B'] ?? 0; const I_val = vals['I'] ?? 0; const N_val = vals['N'] ?? 0; const R_val = vals['R'] ?? 0; const mu0 = vals['μ₀'] ?? 0\n      if (missing === 'B' && I_val > 0 && N_val > 0 && R_val > 0) {\n        const mu = mu0 > 0 ? mu0 : (4 * Math.PI * 1e-7)\n        result.value = steps('B', `μ₀NI/(2R) = ${mu.toExponential(4)}·${N_val}·${I_val.toFixed(4)}/(2·${R_val.toFixed(4)})`, (mu * N_val * I_val) / (2 * R_val))\n      } else if (missing === 'I' && B_val > 0 && N_val > 0 && R_val > 0) {\n        const mu = mu0 > 0 ? mu0 : (4 * Math.PI * 1e-7)\n        result.value = steps('I', `B·2R/(μ₀N) = ${B_val.toExponential(4)}·2·${R_val.toFixed(4)}/(${mu.toExponential(4)}·${N_val})`, (B_val * 2 * R_val) / (mu * N_val))\n      } else if (missing === 'N' && B_val > 0 && I_val > 0 && R_val > 0) {\n        const mu = mu0 > 0 ? mu0 : (4 * Math.PI * 1e-7)\n        result.value = steps('N', `B·2R/(μ₀I) = ${B_val.toExponential(4)}·2·${R_val.toFixed(4)}/(${mu.toExponential(4)}·${I_val.toFixed(4)})`, Math.round((B_val * 2 * R_val) / (mu * I_val)))\n      } else if (missing === 'R' && B_val > 0 && I_val > 0 && N_val > 0) {\n        const mu = mu0 > 0 ? mu0 : (4 * Math.PI * 1e-7)\n        result.value = steps('R', `μ₀NI/(2B) = ${mu.toExponential(4)}·${N_val}·${I_val.toFixed(4)}/(2·${B_val.toExponential(4)})`, (mu * N_val * I_val) / (2 * B_val))\n      } else if (missing === 'μ₀' && B_val > 0 && I_val > 0 && N_val > 0 && R_val > 0) {\n        result.value = steps('μ₀', `B·2R/(NI) = ${B_val.toExponential(4)}·2·${R_val.toFixed(4)}/(${N_val}·${I_val.toFixed(4)})`, (B_val * 2 * R_val) / (N_val * I_val))\n      } else result.value = t('analysis.enterValidKnownValues')\n    }\n    else {\n      result.value = t('analysis.calculateFromEquation', { missing, formula: eq.formula })\n    }\n  }\n\n  // Auto-solve when enough inputs are filled\n  watch([varValues, targetVar], () => {\n    const eq = activeEquation.value\n    if (!eq || !targetVar.value) return\n    const known = eq.variables.filter(v => varValues.value[v.symbol] !== undefined && varValues.value[v.symbol] !== 0).length\n    const total = eq.variables.length\n    if (known >= total - 1) solve()\n  }, { deep: true })\n\n  // Capture solved results for report\n  watch(result, (r) => {\n    const eq = activeEquation.value\n    if (!r || !eq) return\n    const entry = {\n      equationName: eq.name,\n      formula: eq.formula,\n      targetVar: targetVar.value,\n      varValues: { ...varValues.value },\n      result: r,\n      timestamp: Date.now(),\n    }\n    // Avoid duplicates (same equation + same target)\n    const idx = solvedEquations.value.findIndex(\n      s => s.equationName === entry.equationName && s.targetVar === entry.targetVar\n    )\n    if (idx >= 0) solvedEquations.value[idx] = entry\n    else solvedEquations.value.push(entry)\n  })\n\n  return {\n    selectedIndex,\n    varValues,\n    targetVar,\n    result,\n    activeEquation,\n    solve,\n    solvedEquations,\n  }\n}\n"
        ],
        "mappings": "AAAA,SAAS,KAAK,UAAU,aAAa;AACrC,SAAS,eAAe;AAGjB,gBAAS,kBACd,WACA,UACA;AACA,QAAM,EAAE,EAAE,IAAI,QAAQ;AACtB,QAAM,gBAAgB,IAAI,CAAC;AAC3B,QAAM,YAAY,IAA4B,CAAC,CAAC;AAChD,QAAM,YAAY,IAAI,EAAE;AACxB,QAAM,SAAS,IAAmB,IAAI;AACtC,QAAM,kBAAkB,IAA0I,CAAC,CAAC;AAEpK,WAAS,OAAO,GAAW;AAAE,WAAO,KAAK,MAAM,IAAI,GAAI,IAAI;AAAA,EAAK;AAEhE,WAAS,mBAAmB;AAC1B,UAAM,MAAM,UAAU;AACtB,QAAI,CAAC,IAAI,OAAQ;AACjB,UAAM,KAAK,IAAI,cAAc,KAAK;AAClC,QAAI,CAAC,GAAI;AACT,eAAW,OAAO,SAAS,GAAG;AAC5B,UAAI,WAAW;AACf,iBAAW,KAAK,GAAG,WAAW;AAC5B,YAAI,IAAI,EAAE,MAAM,MAAM,UAAa,IAAI,EAAE,MAAM,MAAM,GAAG;AACtD,oBAAU,MAAM,EAAE,MAAM,IAAI,OAAO,IAAI,EAAE,MAAM,CAAC;AAChD,qBAAW;AAAA,QACb;AAAA,MACF;AACA,UAAI,SAAU;AAAA,IAChB;AAAA,EACF;AAEA,QAAM,WAAW,CAAC,QAAQ;AACxB,QAAI,IAAI,QAAQ;AACd,oBAAc,QAAQ;AACtB,YAAM,OAA+B,CAAC;AACtC,iBAAW,KAAK,IAAI,CAAC,EAAE,WAAW;AAAE,aAAK,EAAE,MAAM,IAAI,EAAE,SAAS;AAAA,MAAE;AAClE,gBAAU,QAAQ;AAClB,gBAAU,QAAQ,IAAI,CAAC,EAAE,SAAS,CAAC,KAAK;AACxC,uBAAiB;AAAA,IACnB;AAAA,EACF,GAAG,EAAE,WAAW,KAAK,CAAC;AAEtB,QAAM,UAAU,MAAM;AAAE,qBAAiB;AAAA,EAAE,GAAG,EAAE,MAAM,KAAK,CAAC;AAE5D,QAAM,iBAAiB,SAAS,MAAM,UAAU,EAAE,cAAc,KAAK,CAAC;AAEtE,WAAS,MAAM,MAAc,SAAiB,OAAe;AAC3D,WAAO,GAAG,IAAI,MAAM,OAAO,MAAM,MAAM,QAAQ,CAAC,CAAC;AAAA,EACnD;AAEA,WAAS,QAAQ;AACf,UAAM,KAAK,eAAe;AAC1B,QAAI,CAAC,MAAM,CAAC,UAAU,OAAO;AAAE,aAAO,QAAQ;AAAM;AAAA,IAAO;AAE3D,UAAM,OAAO,EAAE,GAAG,UAAU,MAAM;AAClC,UAAM,UAAU,UAAU;AAG1B,QAAI,GAAG,QAAQ,SAAS,cAAc,GAAG;AACvC,YAAM,IAAI,KAAK,GAAG,KAAK;AAAG,YAAM,IAAI,KAAK,GAAG,KAAK;AAAG,YAAM,IAAI,KAAK,GAAG,KAAK;AAC3E,UAAI,YAAY,OAAO,IAAI,KAAK,IAAI,GAAG;AACrC,eAAO,QAAQ,MAAM,KAAK,kBAAkB,EAAE,QAAQ,CAAC,CAAC,KAAK,IAAI,GAAG,QAAQ,CAAC,CAAC,IAAK,IAAI,KAAK,KAAK,KAAK,KAAK,KAAM,IAAI,EAAE;AAAA,MACzH,WAAW,YAAY,OAAO,IAAI,KAAK,IAAI,GAAG;AAC5C,eAAO,QAAQ,MAAM,KAAK,kBAAkB,EAAE,QAAQ,CAAC,CAAC,IAAI,EAAE,QAAQ,CAAC,CAAC,KAAK,IAAI,KAAK,KAAK,KAAK,KAAK,IAAI,CAAC,CAAC;AAAA,MAC7G,WAAW,YAAY,OAAO,IAAI,KAAK,IAAI,GAAG;AAC5C,eAAO,QAAQ,MAAM,KAAK,gBAAgB,EAAE,QAAQ,CAAC,CAAC,KAAK,IAAI,GAAG,QAAQ,CAAC,CAAC,UAAW,IAAI,IAAI,KAAM,IAAI,KAAK,KAAK,KAAK,GAAG;AAAA,MAC7H,MAAO,QAAO,QAAQ,EAAE,gCAAgC;AAAA,IAC1D,WAES,GAAG,QAAQ,SAAS,cAAc,GAAG;AAC5C,YAAM,IAAI,KAAK,GAAG,KAAK;AAAG,YAAM,IAAI,KAAK,GAAG,KAAK;AAAG,YAAM,IAAI,KAAK,GAAG,KAAK;AAC3E,UAAI,YAAY,OAAO,IAAI,KAAK,IAAI,GAAG;AACrC,eAAO,QAAQ,MAAM,KAAK,kBAAkB,EAAE,QAAQ,CAAC,CAAC,KAAK,IAAI,GAAG,QAAQ,CAAC,CAAC,IAAK,IAAI,KAAK,KAAK,KAAK,KAAK,KAAM,IAAI,EAAE;AAAA,MACzH,WAAW,YAAY,OAAO,IAAI,KAAK,IAAI,GAAG;AAC5C,eAAO,QAAQ,MAAM,KAAK,kBAAkB,EAAE,QAAQ,CAAC,CAAC,IAAI,EAAE,QAAQ,CAAC,CAAC,KAAK,IAAI,KAAK,KAAK,KAAK,KAAK,IAAI,CAAC,CAAC;AAAA,MAC7G,WAAW,YAAY,OAAO,IAAI,KAAK,IAAI,GAAG;AAC5C,eAAO,QAAQ,MAAM,KAAK,gBAAgB,EAAE,QAAQ,CAAC,CAAC,KAAK,IAAI,GAAG,QAAQ,CAAC,CAAC,UAAW,IAAI,IAAI,KAAM,IAAI,KAAK,KAAK,KAAK,GAAG;AAAA,MAC7H,MAAO,QAAO,QAAQ,EAAE,gCAAgC;AAAA,IAC1D,WAES,GAAG,QAAQ,SAAS,gBAAgB,GAAG;AAC9C,YAAM,IAAI,KAAK,GAAG,KAAK;AAAG,YAAM,IAAI,KAAK,GAAG,KAAK;AACjD,UAAI,YAAY,OAAO,IAAI,KAAK,IAAI,GAAG;AACrC,eAAO,QAAQ,MAAM,KAAK,kBAAkB,EAAE,QAAQ,CAAC,CAAC,KAAK,IAAI,GAAG,QAAQ,CAAC,CAAC,IAAK,IAAI,KAAK,KAAK,KAAK,KAAK,KAAM,IAAI,EAAE;AAAA,MACzH,MAAO,QAAO,QAAQ,EAAE,2BAA2B;AAAA,IACrD,WAES,GAAG,QAAQ,SAAS,UAAU,GAAG;AACxC,YAAM,IAAI,KAAK,GAAG,KAAK;AAAG,YAAM,OAAO,KAAK,GAAG,KAAK;AAAG,YAAM,IAAI,KAAK,GAAG,KAAK;AAC9E,UAAI,YAAY,OAAO,IAAI,KAAK,OAAO,GAAG;AACxC,eAAO,QAAQ,MAAM,KAAK,aAAa,EAAE,QAAQ,CAAC,CAAC,KAAK,OAAO,MAAM,QAAQ,CAAC,CAAC,IAAK,IAAI,KAAM,OAAO,KAAK;AAAA,MAC5G,WAAW,YAAY,OAAO,IAAI,KAAK,IAAI,GAAG;AAC5C,eAAO,QAAQ,MAAM,KAAK,iBAAiB,EAAE,QAAQ,CAAC,CAAC,IAAI,EAAE,QAAQ,CAAC,CAAC,KAAK,KAAK,KAAM,IAAI,IAAK,CAAC,CAAC;AAAA,MACpG,WAAW,YAAY,OAAO,IAAI,KAAK,OAAO,GAAG;AAC/C,eAAO,QAAQ,MAAM,KAAK,YAAY,EAAE,QAAQ,CAAC,CAAC,KAAK,OAAO,MAAM,QAAQ,CAAC,CAAC,IAAI,MAAM,IAAI,OAAO,IAAI;AAAA,MACzG,MAAO,QAAO,QAAQ,EAAE,gCAAgC;AAAA,IAC1D,WAES,GAAG,QAAQ,SAAS,uBAAuB,GAAG;AACrD,YAAM,IAAI,KAAK,GAAG,KAAK;AAAG,YAAM,IAAI,KAAK,GAAG,KAAK;AAAG,YAAM,SAAS,KAAK,GAAG,KAAK,KAAK,KAAK,KAAK;AAAK,YAAM,KAAK,KAAK,GAAG,KAAK;AAC5H,UAAI,YAAY,OAAO,IAAI,GAAG;AAC5B,eAAO,QAAQ,MAAM,KAAK,qBAAqB,EAAE,QAAQ,CAAC,CAAC,IAAI,KAAK,IAAI,KAAK,EAAE,QAAQ,CAAC,CAAC,IAAI,GAAG,QAAQ,CAAC,CAAC,IAAI,EAAE,QAAQ,CAAC,CAAC,IAAI,KAAK,IAAI,KAAK,EAAE,QAAQ,CAAC,CAAC,IAAI,IAAI,KAAK,IAAI,KAAK,IAAI,KAAK,IAAI,KAAK,IAAI,KAAK,CAAC;AAAA,MAC5M,WAAW,YAAY,OAAO,IAAI,KAAK,KAAK,IAAI,KAAK,IAAI,MAAM;AAC7D,eAAO,QAAQ,MAAM,KAAK,wBAAwB,IAAI,KAAK,IAAI,KAAK,IAAI,MAAM,IAAI,KAAK,IAAI,KAAK,EAAE;AAAA,MACpG,MAAO,QAAO,QAAQ,EAAE,2BAA2B;AAAA,IACrD,WACS,GAAG,QAAQ,SAAS,UAAU,GAAG;AACxC,YAAM,IAAI,KAAK,GAAG,KAAK;AAAG,YAAM,IAAI,KAAK,GAAG,KAAK;AAAG,YAAM,OAAO,KAAK,GAAG,KAAK;AAC9E,UAAI,YAAY,OAAO,IAAI,KAAK,OAAO,GAAG;AACxC,eAAO,QAAQ,MAAM,KAAK,aAAa,EAAE,QAAQ,CAAC,CAAC,KAAK,OAAO,MAAM,QAAQ,CAAC,CAAC,IAAK,IAAI,KAAM,OAAO,KAAK;AAAA,MAC5G,WAAW,YAAY,OAAO,IAAI,KAAK,IAAI,GAAG;AAC5C,eAAO,QAAQ,MAAM,KAAK,iBAAiB,EAAE,QAAQ,CAAC,CAAC,IAAI,EAAE,QAAQ,CAAC,CAAC,KAAK,KAAK,KAAM,IAAI,IAAK,CAAC,CAAC;AAAA,MACpG,WAAW,YAAY,OAAO,IAAI,KAAK,OAAO,GAAG;AAC/C,eAAO,QAAQ,MAAM,KAAK,YAAY,EAAE,QAAQ,CAAC,CAAC,KAAK,OAAO,MAAM,QAAQ,CAAC,CAAC,IAAI,MAAM,IAAI,OAAO,IAAI;AAAA,MACzG,MAAO,QAAO,QAAQ,EAAE,2BAA2B;AAAA,IACrD,WAES,GAAG,QAAQ,SAAS,+BAA+B,GAAG;AAC7D,YAAM,KAAK,KAAK,IAAI,KAAK;AAAG,YAAM,KAAK,KAAK,IAAI,KAAK;AACrD,YAAM,MAAM,KAAK,KAAK,KAAK;AAAG,YAAM,MAAM,KAAK,KAAK,KAAK;AACzD,YAAM,MAAM,KAAK,KAAK,KAAK;AAAG,YAAM,MAAM,KAAK,KAAK,KAAK;AACzD,UAAI,YAAY,OAAO;AACrB,eAAO,QAAQ,MAAM,OAAO,2BAA2B,KAAK,MAAM,KAAK,MAAM,KAAK,OAAO,EAAE;AAAA,MAC7F,WAAW,YAAY,OAAO;AAC5B,eAAO,QAAQ,MAAM,OAAO,2BAA2B,KAAK,MAAM,KAAK,MAAM,KAAK,OAAO,EAAE;AAAA,MAC7F,MAAO,QAAO,QAAQ,EAAE,2BAA2B;AAAA,IACrD,WACS,GAAG,QAAQ,SAAS,6BAA6B,GAAG;AAC3D,YAAM,MAAM,KAAK,KAAK,KAAK;AAAG,YAAM,MAAM,KAAK,KAAK,KAAK;AAAG,YAAM,MAAM,KAAK,KAAK,KAAK;AAAG,YAAM,MAAM,KAAK,KAAK,KAAK;AACrH,UAAI,YAAY,OAAO,KAAK,IAAI,MAAM,GAAG,IAAI,MAAM;AACjD,eAAO,QAAQ,MAAM,KAAK,wBAAwB,MAAM,QAAQ,MAAM,IAAI;AAAA,MAC5E,MAAO,QAAO,QAAQ,EAAE,2BAA2B;AAAA,IACrD,WAES,GAAG,QAAQ,SAAS,mBAAmB,GAAG;AACjD,YAAM,KAAK,KAAK,IAAI,KAAK;AAAG,YAAM,SAAS,KAAK,GAAG,KAAK,KAAK,KAAK,KAAK;AAAK,YAAM,IAAI,KAAK,GAAG,KAAK;AAAG,YAAM,IAAI,KAAK,GAAG,KAAK;AAC7H,UAAI,YAAY,OAAO,KAAK,KAAK,IAAI,GAAG;AACtC,eAAO,QAAQ,MAAM,KAAK,iBAAkB,KAAK,KAAK,KAAK,IAAI,IAAI,KAAK,IAAK,CAAC;AAAA,MAChF,WAAW,YAAY,QAAQ,IAAI,KAAK,IAAI,KAAK,KAAK,IAAI,IAAI,KAAK,IAAI,MAAM;AAC3E,eAAO,QAAQ,MAAM,MAAM,kBAAkB,KAAK,KAAM,IAAI,IAAK,KAAK,IAAI,IAAI,KAAK,CAAC,CAAC;AAAA,MACvF,WAAW,YAAY,OAAO,IAAI,KAAK,KAAK,KAAK,IAAI,GAAG;AACtD,eAAO,QAAQ,MAAM,KAAK,qBAAqB,KAAK,KAAM,IAAI,KAAM,KAAK,GAAG,IAAI,IAAI,MAAM,KAAK,EAAE;AAAA,MACnG,MAAO,QAAO,QAAQ,EAAE,2BAA2B;AAAA,IACrD,WACS,GAAG,QAAQ,SAAS,sBAAsB,GAAG;AACpD,YAAM,KAAK,KAAK,IAAI,KAAK;AAAG,YAAM,SAAS,KAAK,GAAG,KAAK,KAAK,KAAK,KAAK;AAAK,YAAM,IAAI,KAAK,GAAG,KAAK;AACnG,UAAI,YAAY,OAAO,KAAK,KAAK,IAAI,GAAG;AACtC,eAAO,QAAQ,MAAM,KAAK,oBAAqB,KAAK,KAAK,KAAK,IAAI,KAAK,IAAI,KAAK,IAAI,KAAK,KAAM,IAAI,EAAE;AAAA,MACvG,MAAO,QAAO,QAAQ,EAAE,2BAA2B;AAAA,IACrD,WAES,GAAG,QAAQ,SAAS,iBAAiB,GAAG;AAC/C,YAAM,QAAQ,KAAK,GAAG,KAAK;AAAG,YAAM,QAAQ,KAAK,GAAG,KAAK;AAAG,YAAM,QAAQ,KAAK,GAAG,KAAK;AAAG,YAAM,MAAM,KAAK,IAAI,KAAK;AACpH,UAAI,YAAY,OAAO,QAAQ,KAAK,QAAQ,GAAG;AAC7C,cAAM,KAAK,MAAM,IAAI,MAAO,IAAI,KAAK,KAAK;AAC1C,eAAO,QAAQ,MAAM,KAAK,eAAe,GAAG,cAAc,CAAC,CAAC,IAAI,MAAM,QAAQ,CAAC,CAAC,QAAQ,MAAM,QAAQ,CAAC,CAAC,KAAM,KAAK,SAAU,IAAI,KAAK,KAAK,MAAM;AAAA,MACnJ,WAAW,YAAY,OAAO,QAAQ,KAAK,QAAQ,GAAG;AACpD,cAAM,KAAK,MAAM,IAAI,MAAO,IAAI,KAAK,KAAK;AAC1C,eAAO,QAAQ,MAAM,KAAK,cAAc,MAAM,cAAc,CAAC,CAAC,OAAO,MAAM,QAAQ,CAAC,CAAC,IAAI,GAAG,cAAc,CAAC,CAAC,IAAK,QAAQ,IAAI,KAAK,KAAK,QAAS,EAAE;AAAA,MACpJ,WAAW,YAAY,OAAO,QAAQ,KAAK,QAAQ,GAAG;AACpD,cAAM,KAAK,MAAM,IAAI,MAAO,IAAI,KAAK,KAAK;AAC1C,eAAO,QAAQ,MAAM,KAAK,eAAe,GAAG,cAAc,CAAC,CAAC,IAAI,MAAM,QAAQ,CAAC,CAAC,QAAQ,MAAM,cAAc,CAAC,CAAC,KAAM,KAAK,SAAU,IAAI,KAAK,KAAK,MAAM;AAAA,MACzJ,WAAW,YAAY,QAAQ,QAAQ,KAAK,QAAQ,KAAK,QAAQ,GAAG;AAClE,eAAO,QAAQ,MAAM,MAAM,aAAa,MAAM,cAAc,CAAC,CAAC,OAAO,MAAM,QAAQ,CAAC,CAAC,IAAI,MAAM,QAAQ,CAAC,CAAC,IAAK,QAAQ,IAAI,KAAK,KAAK,QAAS,KAAK;AAAA,MACpJ,MAAO,QAAO,QAAQ,EAAE,gCAAgC;AAAA,IAC1D,WAES,GAAG,QAAQ,SAAS,iBAAiB,GAAG;AAC/C,YAAM,QAAQ,KAAK,GAAG,KAAK;AAAG,YAAM,QAAQ,KAAK,GAAG,KAAK;AAAG,YAAM,QAAQ,KAAK,GAAG,KAAK;AAAG,YAAM,QAAQ,KAAK,GAAG,KAAK;AAAG,YAAM,MAAM,KAAK,IAAI,KAAK;AAClJ,UAAI,YAAY,OAAO,QAAQ,KAAK,QAAQ,KAAK,QAAQ,GAAG;AAC1D,cAAM,KAAK,MAAM,IAAI,MAAO,IAAI,KAAK,KAAK;AAC1C,eAAO,QAAQ,MAAM,KAAK,eAAe,GAAG,cAAc,CAAC,CAAC,IAAI,KAAK,IAAI,MAAM,QAAQ,CAAC,CAAC,OAAO,MAAM,QAAQ,CAAC,CAAC,KAAM,KAAK,QAAQ,SAAU,IAAI,MAAM;AAAA,MACzJ,WAAW,YAAY,OAAO,QAAQ,KAAK,QAAQ,KAAK,QAAQ,GAAG;AACjE,cAAM,KAAK,MAAM,IAAI,MAAO,IAAI,KAAK,KAAK;AAC1C,eAAO,QAAQ,MAAM,KAAK,gBAAgB,MAAM,cAAc,CAAC,CAAC,MAAM,MAAM,QAAQ,CAAC,CAAC,KAAK,GAAG,cAAc,CAAC,CAAC,IAAI,KAAK,KAAM,QAAQ,IAAI,SAAU,KAAK,MAAM;AAAA,MAChK,WAAW,YAAY,OAAO,QAAQ,KAAK,QAAQ,KAAK,QAAQ,GAAG;AACjE,cAAM,KAAK,MAAM,IAAI,MAAO,IAAI,KAAK,KAAK;AAC1C,eAAO,QAAQ,MAAM,KAAK,gBAAgB,MAAM,cAAc,CAAC,CAAC,MAAM,MAAM,QAAQ,CAAC,CAAC,KAAK,GAAG,cAAc,CAAC,CAAC,IAAI,MAAM,QAAQ,CAAC,CAAC,KAAK,KAAK,MAAO,QAAQ,IAAI,SAAU,KAAK,MAAM,CAAC;AAAA,MACvL,WAAW,YAAY,OAAO,QAAQ,KAAK,QAAQ,KAAK,QAAQ,GAAG;AACjE,cAAM,KAAK,MAAM,IAAI,MAAO,IAAI,KAAK,KAAK;AAC1C,eAAO,QAAQ,MAAM,KAAK,eAAe,GAAG,cAAc,CAAC,CAAC,IAAI,KAAK,IAAI,MAAM,QAAQ,CAAC,CAAC,OAAO,MAAM,cAAc,CAAC,CAAC,KAAM,KAAK,QAAQ,SAAU,IAAI,MAAM;AAAA,MAC/J,WAAW,YAAY,QAAQ,QAAQ,KAAK,QAAQ,KAAK,QAAQ,KAAK,QAAQ,GAAG;AAC/E,eAAO,QAAQ,MAAM,MAAM,eAAe,MAAM,cAAc,CAAC,CAAC,MAAM,MAAM,QAAQ,CAAC,CAAC,KAAK,KAAK,IAAI,MAAM,QAAQ,CAAC,CAAC,KAAM,QAAQ,IAAI,SAAU,QAAQ,MAAM;AAAA,MAChK,MAAO,QAAO,QAAQ,EAAE,gCAAgC;AAAA,IAC1D,OACK;AACH,aAAO,QAAQ,EAAE,kCAAkC,EAAE,SAAS,SAAS,GAAG,QAAQ,CAAC;AAAA,IACrF;AAAA,EACF;AAGA,QAAM,CAAC,WAAW,SAAS,GAAG,MAAM;AAClC,UAAM,KAAK,eAAe;AAC1B,QAAI,CAAC,MAAM,CAAC,UAAU,MAAO;AAC7B,UAAM,QAAQ,GAAG,UAAU,OAAO,OAAK,UAAU,MAAM,EAAE,MAAM,MAAM,UAAa,UAAU,MAAM,EAAE,MAAM,MAAM,CAAC,EAAE;AACnH,UAAM,QAAQ,GAAG,UAAU;AAC3B,QAAI,SAAS,QAAQ,EAAG,OAAM;AAAA,EAChC,GAAG,EAAE,MAAM,KAAK,CAAC;AAGjB,QAAM,QAAQ,CAAC,MAAM;AACnB,UAAM,KAAK,eAAe;AAC1B,QAAI,CAAC,KAAK,CAAC,GAAI;AACf,UAAM,QAAQ;AAAA,MACZ,cAAc,GAAG;AAAA,MACjB,SAAS,GAAG;AAAA,MACZ,WAAW,UAAU;AAAA,MACrB,WAAW,EAAE,GAAG,UAAU,MAAM;AAAA,MAChC,QAAQ;AAAA,MACR,WAAW,KAAK,IAAI;AAAA,IACtB;AAEA,UAAM,MAAM,gBAAgB,MAAM;AAAA,MAChC,OAAK,EAAE,iBAAiB,MAAM,gBAAgB,EAAE,cAAc,MAAM;AAAA,IACtE;AACA,QAAI,OAAO,EAAG,iBAAgB,MAAM,GAAG,IAAI;AAAA,QACtC,iBAAgB,MAAM,KAAK,KAAK;AAAA,EACvC,CAAC;AAED,SAAO;AAAA,IACL;AAAA,IACA;AAAA,IACA;AAAA,IACA;AAAA,IACA;AAAA,IACA;AAAA,IACA;AAAA,EACF;AACF;",
        "names": []
      }
    },
    {
      "name": "vite:json",
      "start": 1783634641667,
      "end": 1783634641667,
      "order": "normal"
    },
    {
      "name": "vite:worker",
      "start": 1783634641667,
      "end": 1783634641667,
      "order": "normal"
    },
    {
      "name": "vite:define",
      "start": 1783634641668,
      "end": 1783634641668,
      "order": "normal"
    },
    {
      "name": "vite:css-post",
      "start": 1783634641668,
      "end": 1783634641668,
      "order": "normal"
    },
    {
      "name": "vite:build-html",
      "start": 1783634641668,
      "end": 1783634641668,
      "order": "normal"
    },
    {
      "name": "vite:worker-import-meta-url",
      "start": 1783634641668,
      "end": 1783634641668,
      "order": "normal"
    },
    {
      "name": "vite:asset-import-meta-url",
      "start": 1783634641668,
      "end": 1783634641668,
      "order": "normal"
    },
    {
      "name": "commonjs",
      "start": 1783634641668,
      "end": 1783634641668,
      "order": "normal"
    },
    {
      "name": "vite:dynamic-import-vars",
      "start": 1783634641668,
      "end": 1783634641668,
      "order": "normal"
    },
    {
      "name": "vite:import-glob",
      "start": 1783634641668,
      "end": 1783634641668,
      "order": "normal"
    },
    {
      "name": "vite:build-import-analysis",
      "start": 1783634641668,
      "end": 1783634641668,
      "order": "normal"
    },
    {
      "name": "vite:reporter",
      "start": 1783634641668,
      "end": 1783634641668,
      "order": "normal"
    }
  ]
}

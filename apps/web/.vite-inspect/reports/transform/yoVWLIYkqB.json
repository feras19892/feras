{
  "resolvedId": "C:/Users/feras/Desktop/feras/apps/web/src/composables/calorimetry/useCalorimetryTrials.ts",
  "transforms": [
    {
      "name": "__load__",
      "result": "import { ref } from 'vue'\n\nexport interface CalorimetryTrial {\n  id: number\n  mWater: number\n  tWater: number\n  mMetal: number\n  tMetal: number\n  tf: number\n  cMetal: number\n  cTrue?: number\n}\n\nexport interface TrialState {\n  trials: CalorimetryTrial[]\n  nextId: number\n}\n\nconst SAVE_KEY = 'calorimetry:trials:v1'\n\nexport function useCalorimetryTrials(\n  params: { value: { mWater: number; tWater: number; mMetal: number; tMetal: number; tf: number; cMetal: number; cTrue?: number } }\n) {\n  const trials = ref<CalorimetryTrial[]>([])\n  const nextId = ref(1)\n  const undoStack = ref<TrialState[]>([])\n  const redoStack = ref<TrialState[]>([])\n\n  function autoSave() {\n    try { localStorage.setItem(SAVE_KEY, JSON.stringify({ trials: trials.value, nextId: nextId.value, undoStack: undoStack.value.slice(-10), redoStack: redoStack.value.slice(-10) })) } catch { /* ignore */ }\n  }\n  function autoLoad() {\n    try {\n      const raw = localStorage.getItem(SAVE_KEY)\n      if (!raw) return\n      const parsed = JSON.parse(raw)\n      if (Array.isArray(parsed.trials)) {\n        trials.value = parsed.trials\n        nextId.value = typeof parsed.nextId === 'number' ? parsed.nextId : (trials.value.length > 0 ? Math.max(...trials.value.map(t => t.id)) + 1 : 1)\n        if (Array.isArray(parsed.undoStack)) undoStack.value = parsed.undoStack\n        if (Array.isArray(parsed.redoStack)) redoStack.value = parsed.redoStack\n      }\n    } catch { /* ignore */ }\n  }\n  function pushState() {\n    undoStack.value.push({ trials: [...trials.value], nextId: nextId.value })\n    redoStack.value = []\n    if (undoStack.value.length > 50) undoStack.value.shift()\n    autoSave()\n  }\n  function recordTrial() {\n    pushState()\n    const { mWater, tWater, mMetal, tMetal, tf, cMetal, cTrue } = params.value\n    trials.value.push({ id: nextId.value++, mWater, tWater, mMetal, tMetal, tf, cMetal, cTrue })\n  }\n  function undo() {\n    if (undoStack.value.length === 0) return\n    const current = { trials: [...trials.value], nextId: nextId.value }\n    redoStack.value.push(current)\n    const prev = undoStack.value.pop()!\n    trials.value = prev.trials; nextId.value = prev.nextId\n  }\n  function redo() {\n    if (redoStack.value.length === 0) return\n    const current = { trials: [...trials.value], nextId: nextId.value }\n    undoStack.value.push(current)\n    const next = redoStack.value.pop()!\n    trials.value = next.trials; nextId.value = next.nextId\n  }\n  function canUndo() { return undoStack.value.length > 0 }\n  function canRedo() { return redoStack.value.length > 0 }\n  function removeTrial(id: number) { pushState(); trials.value = trials.value.filter(t => t.id !== id); autoSave() }\n  function clearTrials() { pushState(); trials.value = []; nextId.value = 1; autoSave() }\n  function exportCsv(): string {\n    const headers = ['Trial','mWater(kg)','tWater(C)','mMetal(kg)','tMetal(C)','tf(C)','cMetal(J/kgK)']\n    const rows = trials.value.map(t => `${t.id},${t.mWater.toFixed(3)},${t.tWater.toFixed(1)},${t.mMetal.toFixed(3)},${t.tMetal.toFixed(1)},${t.tf.toFixed(1)},${t.cMetal.toFixed(0)}`)\n    return [headers.join(','), ...rows].join('\\n')\n  }\n  return { trials, recordTrial, undo, redo, canUndo, canRedo, removeTrial, clearTrials, exportCsv, autoLoad }\n}\n",
      "start": 1783635324296,
      "end": 1783635324296
    },
    {
      "name": "vite-plugin-vue-inspector",
      "start": 1783635324296,
      "end": 1783635324296,
      "order": "pre"
    },
    {
      "name": "vite-plugin-vue-devtools",
      "result": "import { ref } from 'vue'\n\nexport interface CalorimetryTrial {\n  id: number\n  mWater: number\n  tWater: number\n  mMetal: number\n  tMetal: number\n  tf: number\n  cMetal: number\n  cTrue?: number\n}\n\nexport interface TrialState {\n  trials: CalorimetryTrial[]\n  nextId: number\n}\n\nconst SAVE_KEY = 'calorimetry:trials:v1'\n\nexport function useCalorimetryTrials(\n  params: { value: { mWater: number; tWater: number; mMetal: number; tMetal: number; tf: number; cMetal: number; cTrue?: number } }\n) {\n  const trials = ref<CalorimetryTrial[]>([])\n  const nextId = ref(1)\n  const undoStack = ref<TrialState[]>([])\n  const redoStack = ref<TrialState[]>([])\n\n  function autoSave() {\n    try { localStorage.setItem(SAVE_KEY, JSON.stringify({ trials: trials.value, nextId: nextId.value, undoStack: undoStack.value.slice(-10), redoStack: redoStack.value.slice(-10) })) } catch { /* ignore */ }\n  }\n  function autoLoad() {\n    try {\n      const raw = localStorage.getItem(SAVE_KEY)\n      if (!raw) return\n      const parsed = JSON.parse(raw)\n      if (Array.isArray(parsed.trials)) {\n        trials.value = parsed.trials\n        nextId.value = typeof parsed.nextId === 'number' ? parsed.nextId : (trials.value.length > 0 ? Math.max(...trials.value.map(t => t.id)) + 1 : 1)\n        if (Array.isArray(parsed.undoStack)) undoStack.value = parsed.undoStack\n        if (Array.isArray(parsed.redoStack)) redoStack.value = parsed.redoStack\n      }\n    } catch { /* ignore */ }\n  }\n  function pushState() {\n    undoStack.value.push({ trials: [...trials.value], nextId: nextId.value })\n    redoStack.value = []\n    if (undoStack.value.length > 50) undoStack.value.shift()\n    autoSave()\n  }\n  function recordTrial() {\n    pushState()\n    const { mWater, tWater, mMetal, tMetal, tf, cMetal, cTrue } = params.value\n    trials.value.push({ id: nextId.value++, mWater, tWater, mMetal, tMetal, tf, cMetal, cTrue })\n  }\n  function undo() {\n    if (undoStack.value.length === 0) return\n    const current = { trials: [...trials.value], nextId: nextId.value }\n    redoStack.value.push(current)\n    const prev = undoStack.value.pop()!\n    trials.value = prev.trials; nextId.value = prev.nextId\n  }\n  function redo() {\n    if (redoStack.value.length === 0) return\n    const current = { trials: [...trials.value], nextId: nextId.value }\n    undoStack.value.push(current)\n    const next = redoStack.value.pop()!\n    trials.value = next.trials; nextId.value = next.nextId\n  }\n  function canUndo() { return undoStack.value.length > 0 }\n  function canRedo() { return redoStack.value.length > 0 }\n  function removeTrial(id: number) { pushState(); trials.value = trials.value.filter(t => t.id !== id); autoSave() }\n  function clearTrials() { pushState(); trials.value = []; nextId.value = 1; autoSave() }\n  function exportCsv(): string {\n    const headers = ['Trial','mWater(kg)','tWater(C)','mMetal(kg)','tMetal(C)','tf(C)','cMetal(J/kgK)']\n    const rows = trials.value.map(t => `${t.id},${t.mWater.toFixed(3)},${t.tWater.toFixed(1)},${t.mMetal.toFixed(3)},${t.tMetal.toFixed(1)},${t.tf.toFixed(1)},${t.cMetal.toFixed(0)}`)\n    return [headers.join(','), ...rows].join('\\n')\n  }\n  return { trials, recordTrial, undo, redo, canUndo, canRedo, removeTrial, clearTrials, exportCsv, autoLoad }\n}\n",
      "start": 1783635324296,
      "end": 1783635324296,
      "order": "pre",
      "sourcemaps": null
    },
    {
      "name": "vite:css",
      "start": 1783635324296,
      "end": 1783635324296,
      "order": "normal"
    },
    {
      "name": "vite:esbuild",
      "result": "import { ref } from \"vue\";\nconst SAVE_KEY = \"calorimetry:trials:v1\";\nexport function useCalorimetryTrials(params) {\n  const trials = ref([]);\n  const nextId = ref(1);\n  const undoStack = ref([]);\n  const redoStack = ref([]);\n  function autoSave() {\n    try {\n      localStorage.setItem(SAVE_KEY, JSON.stringify({ trials: trials.value, nextId: nextId.value, undoStack: undoStack.value.slice(-10), redoStack: redoStack.value.slice(-10) }));\n    } catch {\n    }\n  }\n  function autoLoad() {\n    try {\n      const raw = localStorage.getItem(SAVE_KEY);\n      if (!raw) return;\n      const parsed = JSON.parse(raw);\n      if (Array.isArray(parsed.trials)) {\n        trials.value = parsed.trials;\n        nextId.value = typeof parsed.nextId === \"number\" ? parsed.nextId : trials.value.length > 0 ? Math.max(...trials.value.map((t) => t.id)) + 1 : 1;\n        if (Array.isArray(parsed.undoStack)) undoStack.value = parsed.undoStack;\n        if (Array.isArray(parsed.redoStack)) redoStack.value = parsed.redoStack;\n      }\n    } catch {\n    }\n  }\n  function pushState() {\n    undoStack.value.push({ trials: [...trials.value], nextId: nextId.value });\n    redoStack.value = [];\n    if (undoStack.value.length > 50) undoStack.value.shift();\n    autoSave();\n  }\n  function recordTrial() {\n    pushState();\n    const { mWater, tWater, mMetal, tMetal, tf, cMetal, cTrue } = params.value;\n    trials.value.push({ id: nextId.value++, mWater, tWater, mMetal, tMetal, tf, cMetal, cTrue });\n  }\n  function undo() {\n    if (undoStack.value.length === 0) return;\n    const current = { trials: [...trials.value], nextId: nextId.value };\n    redoStack.value.push(current);\n    const prev = undoStack.value.pop();\n    trials.value = prev.trials;\n    nextId.value = prev.nextId;\n  }\n  function redo() {\n    if (redoStack.value.length === 0) return;\n    const current = { trials: [...trials.value], nextId: nextId.value };\n    undoStack.value.push(current);\n    const next = redoStack.value.pop();\n    trials.value = next.trials;\n    nextId.value = next.nextId;\n  }\n  function canUndo() {\n    return undoStack.value.length > 0;\n  }\n  function canRedo() {\n    return redoStack.value.length > 0;\n  }\n  function removeTrial(id) {\n    pushState();\n    trials.value = trials.value.filter((t) => t.id !== id);\n    autoSave();\n  }\n  function clearTrials() {\n    pushState();\n    trials.value = [];\n    nextId.value = 1;\n    autoSave();\n  }\n  function exportCsv() {\n    const headers = [\"Trial\", \"mWater(kg)\", \"tWater(C)\", \"mMetal(kg)\", \"tMetal(C)\", \"tf(C)\", \"cMetal(J/kgK)\"];\n    const rows = trials.value.map((t) => `${t.id},${t.mWater.toFixed(3)},${t.tWater.toFixed(1)},${t.mMetal.toFixed(3)},${t.tMetal.toFixed(1)},${t.tf.toFixed(1)},${t.cMetal.toFixed(0)}`);\n    return [headers.join(\",\"), ...rows].join(\"\\n\");\n  }\n  return { trials, recordTrial, undo, redo, canUndo, canRedo, removeTrial, clearTrials, exportCsv, autoLoad };\n}\n",
      "start": 1783635324296,
      "end": 1783635324978,
      "order": "normal",
      "sourcemaps": {
        "version": 3,
        "sources": [
          "useCalorimetryTrials.ts"
        ],
        "sourcesContent": [
          "import { ref } from 'vue'\n\nexport interface CalorimetryTrial {\n  id: number\n  mWater: number\n  tWater: number\n  mMetal: number\n  tMetal: number\n  tf: number\n  cMetal: number\n  cTrue?: number\n}\n\nexport interface TrialState {\n  trials: CalorimetryTrial[]\n  nextId: number\n}\n\nconst SAVE_KEY = 'calorimetry:trials:v1'\n\nexport function useCalorimetryTrials(\n  params: { value: { mWater: number; tWater: number; mMetal: number; tMetal: number; tf: number; cMetal: number; cTrue?: number } }\n) {\n  const trials = ref<CalorimetryTrial[]>([])\n  const nextId = ref(1)\n  const undoStack = ref<TrialState[]>([])\n  const redoStack = ref<TrialState[]>([])\n\n  function autoSave() {\n    try { localStorage.setItem(SAVE_KEY, JSON.stringify({ trials: trials.value, nextId: nextId.value, undoStack: undoStack.value.slice(-10), redoStack: redoStack.value.slice(-10) })) } catch { /* ignore */ }\n  }\n  function autoLoad() {\n    try {\n      const raw = localStorage.getItem(SAVE_KEY)\n      if (!raw) return\n      const parsed = JSON.parse(raw)\n      if (Array.isArray(parsed.trials)) {\n        trials.value = parsed.trials\n        nextId.value = typeof parsed.nextId === 'number' ? parsed.nextId : (trials.value.length > 0 ? Math.max(...trials.value.map(t => t.id)) + 1 : 1)\n        if (Array.isArray(parsed.undoStack)) undoStack.value = parsed.undoStack\n        if (Array.isArray(parsed.redoStack)) redoStack.value = parsed.redoStack\n      }\n    } catch { /* ignore */ }\n  }\n  function pushState() {\n    undoStack.value.push({ trials: [...trials.value], nextId: nextId.value })\n    redoStack.value = []\n    if (undoStack.value.length > 50) undoStack.value.shift()\n    autoSave()\n  }\n  function recordTrial() {\n    pushState()\n    const { mWater, tWater, mMetal, tMetal, tf, cMetal, cTrue } = params.value\n    trials.value.push({ id: nextId.value++, mWater, tWater, mMetal, tMetal, tf, cMetal, cTrue })\n  }\n  function undo() {\n    if (undoStack.value.length === 0) return\n    const current = { trials: [...trials.value], nextId: nextId.value }\n    redoStack.value.push(current)\n    const prev = undoStack.value.pop()!\n    trials.value = prev.trials; nextId.value = prev.nextId\n  }\n  function redo() {\n    if (redoStack.value.length === 0) return\n    const current = { trials: [...trials.value], nextId: nextId.value }\n    undoStack.value.push(current)\n    const next = redoStack.value.pop()!\n    trials.value = next.trials; nextId.value = next.nextId\n  }\n  function canUndo() { return undoStack.value.length > 0 }\n  function canRedo() { return redoStack.value.length > 0 }\n  function removeTrial(id: number) { pushState(); trials.value = trials.value.filter(t => t.id !== id); autoSave() }\n  function clearTrials() { pushState(); trials.value = []; nextId.value = 1; autoSave() }\n  function exportCsv(): string {\n    const headers = ['Trial','mWater(kg)','tWater(C)','mMetal(kg)','tMetal(C)','tf(C)','cMetal(J/kgK)']\n    const rows = trials.value.map(t => `${t.id},${t.mWater.toFixed(3)},${t.tWater.toFixed(1)},${t.mMetal.toFixed(3)},${t.tMetal.toFixed(1)},${t.tf.toFixed(1)},${t.cMetal.toFixed(0)}`)\n    return [headers.join(','), ...rows].join('\\n')\n  }\n  return { trials, recordTrial, undo, redo, canUndo, canRedo, removeTrial, clearTrials, exportCsv, autoLoad }\n}\n"
        ],
        "mappings": "AAAA,SAAS,WAAW;AAkBpB,MAAM,WAAW;AAEV,gBAAS,qBACd,QACA;AACA,QAAM,SAAS,IAAwB,CAAC,CAAC;AACzC,QAAM,SAAS,IAAI,CAAC;AACpB,QAAM,YAAY,IAAkB,CAAC,CAAC;AACtC,QAAM,YAAY,IAAkB,CAAC,CAAC;AAEtC,WAAS,WAAW;AAClB,QAAI;AAAE,mBAAa,QAAQ,UAAU,KAAK,UAAU,EAAE,QAAQ,OAAO,OAAO,QAAQ,OAAO,OAAO,WAAW,UAAU,MAAM,MAAM,GAAG,GAAG,WAAW,UAAU,MAAM,MAAM,GAAG,EAAE,CAAC,CAAC;AAAA,IAAE,QAAQ;AAAA,IAAe;AAAA,EAC5M;AACA,WAAS,WAAW;AAClB,QAAI;AACF,YAAM,MAAM,aAAa,QAAQ,QAAQ;AACzC,UAAI,CAAC,IAAK;AACV,YAAM,SAAS,KAAK,MAAM,GAAG;AAC7B,UAAI,MAAM,QAAQ,OAAO,MAAM,GAAG;AAChC,eAAO,QAAQ,OAAO;AACtB,eAAO,QAAQ,OAAO,OAAO,WAAW,WAAW,OAAO,SAAU,OAAO,MAAM,SAAS,IAAI,KAAK,IAAI,GAAG,OAAO,MAAM,IAAI,OAAK,EAAE,EAAE,CAAC,IAAI,IAAI;AAC7I,YAAI,MAAM,QAAQ,OAAO,SAAS,EAAG,WAAU,QAAQ,OAAO;AAC9D,YAAI,MAAM,QAAQ,OAAO,SAAS,EAAG,WAAU,QAAQ,OAAO;AAAA,MAChE;AAAA,IACF,QAAQ;AAAA,IAAe;AAAA,EACzB;AACA,WAAS,YAAY;AACnB,cAAU,MAAM,KAAK,EAAE,QAAQ,CAAC,GAAG,OAAO,KAAK,GAAG,QAAQ,OAAO,MAAM,CAAC;AACxE,cAAU,QAAQ,CAAC;AACnB,QAAI,UAAU,MAAM,SAAS,GAAI,WAAU,MAAM,MAAM;AACvD,aAAS;AAAA,EACX;AACA,WAAS,cAAc;AACrB,cAAU;AACV,UAAM,EAAE,QAAQ,QAAQ,QAAQ,QAAQ,IAAI,QAAQ,MAAM,IAAI,OAAO;AACrE,WAAO,MAAM,KAAK,EAAE,IAAI,OAAO,SAAS,QAAQ,QAAQ,QAAQ,QAAQ,IAAI,QAAQ,MAAM,CAAC;AAAA,EAC7F;AACA,WAAS,OAAO;AACd,QAAI,UAAU,MAAM,WAAW,EAAG;AAClC,UAAM,UAAU,EAAE,QAAQ,CAAC,GAAG,OAAO,KAAK,GAAG,QAAQ,OAAO,MAAM;AAClE,cAAU,MAAM,KAAK,OAAO;AAC5B,UAAM,OAAO,UAAU,MAAM,IAAI;AACjC,WAAO,QAAQ,KAAK;AAAQ,WAAO,QAAQ,KAAK;AAAA,EAClD;AACA,WAAS,OAAO;AACd,QAAI,UAAU,MAAM,WAAW,EAAG;AAClC,UAAM,UAAU,EAAE,QAAQ,CAAC,GAAG,OAAO,KAAK,GAAG,QAAQ,OAAO,MAAM;AAClE,cAAU,MAAM,KAAK,OAAO;AAC5B,UAAM,OAAO,UAAU,MAAM,IAAI;AACjC,WAAO,QAAQ,KAAK;AAAQ,WAAO,QAAQ,KAAK;AAAA,EAClD;AACA,WAAS,UAAU;AAAE,WAAO,UAAU,MAAM,SAAS;AAAA,EAAE;AACvD,WAAS,UAAU;AAAE,WAAO,UAAU,MAAM,SAAS;AAAA,EAAE;AACvD,WAAS,YAAY,IAAY;AAAE,cAAU;AAAG,WAAO,QAAQ,OAAO,MAAM,OAAO,OAAK,EAAE,OAAO,EAAE;AAAG,aAAS;AAAA,EAAE;AACjH,WAAS,cAAc;AAAE,cAAU;AAAG,WAAO,QAAQ,CAAC;AAAG,WAAO,QAAQ;AAAG,aAAS;AAAA,EAAE;AACtF,WAAS,YAAoB;AAC3B,UAAM,UAAU,CAAC,SAAQ,cAAa,aAAY,cAAa,aAAY,SAAQ,eAAe;AAClG,UAAM,OAAO,OAAO,MAAM,IAAI,OAAK,GAAG,EAAE,EAAE,IAAI,EAAE,OAAO,QAAQ,CAAC,CAAC,IAAI,EAAE,OAAO,QAAQ,CAAC,CAAC,IAAI,EAAE,OAAO,QAAQ,CAAC,CAAC,IAAI,EAAE,OAAO,QAAQ,CAAC,CAAC,IAAI,EAAE,GAAG,QAAQ,CAAC,CAAC,IAAI,EAAE,OAAO,QAAQ,CAAC,CAAC,EAAE;AAClL,WAAO,CAAC,QAAQ,KAAK,GAAG,GAAG,GAAG,IAAI,EAAE,KAAK,IAAI;AAAA,EAC/C;AACA,SAAO,EAAE,QAAQ,aAAa,MAAM,MAAM,SAAS,SAAS,aAAa,aAAa,WAAW,SAAS;AAC5G;",
        "names": []
      }
    },
    {
      "name": "vite:json",
      "start": 1783635324979,
      "end": 1783635324979,
      "order": "normal"
    },
    {
      "name": "vite:worker",
      "start": 1783635324979,
      "end": 1783635324979,
      "order": "normal"
    },
    {
      "name": "vite:vue",
      "start": 1783635324979,
      "end": 1783635324979,
      "order": "normal-normal"
    },
    {
      "name": "vite:define",
      "start": 1783635324979,
      "end": 1783635324979,
      "order": "normal"
    },
    {
      "name": "vite:css-post",
      "start": 1783635324979,
      "end": 1783635324979,
      "order": "normal"
    },
    {
      "name": "vite:worker-import-meta-url",
      "start": 1783635324979,
      "end": 1783635324979,
      "order": "normal"
    },
    {
      "name": "vite:asset-import-meta-url",
      "start": 1783635324979,
      "end": 1783635324980,
      "order": "normal"
    },
    {
      "name": "vite:dynamic-import-vars",
      "start": 1783635324980,
      "end": 1783635324980,
      "order": "normal"
    },
    {
      "name": "vite:import-glob",
      "start": 1783635324980,
      "end": 1783635324980,
      "order": "normal"
    },
    {
      "name": "vite-plugin-vue-inspector:post",
      "start": 1783635324980,
      "end": 1783635324980,
      "order": "post"
    },
    {
      "name": "vite:client-inject",
      "start": 1783635324980,
      "end": 1783635324980,
      "order": "normal"
    },
    {
      "name": "vite:css-analysis",
      "start": 1783635324980,
      "end": 1783635324980,
      "order": "normal"
    },
    {
      "name": "vite:import-analysis",
      "result": "import { ref } from \"/node_modules/.vite/deps/vue.js?v=09a3ad4c\";\nconst SAVE_KEY = \"calorimetry:trials:v1\";\nexport function useCalorimetryTrials(params) {\n  const trials = ref([]);\n  const nextId = ref(1);\n  const undoStack = ref([]);\n  const redoStack = ref([]);\n  function autoSave() {\n    try {\n      localStorage.setItem(SAVE_KEY, JSON.stringify({ trials: trials.value, nextId: nextId.value, undoStack: undoStack.value.slice(-10), redoStack: redoStack.value.slice(-10) }));\n    } catch {\n    }\n  }\n  function autoLoad() {\n    try {\n      const raw = localStorage.getItem(SAVE_KEY);\n      if (!raw) return;\n      const parsed = JSON.parse(raw);\n      if (Array.isArray(parsed.trials)) {\n        trials.value = parsed.trials;\n        nextId.value = typeof parsed.nextId === \"number\" ? parsed.nextId : trials.value.length > 0 ? Math.max(...trials.value.map((t) => t.id)) + 1 : 1;\n        if (Array.isArray(parsed.undoStack)) undoStack.value = parsed.undoStack;\n        if (Array.isArray(parsed.redoStack)) redoStack.value = parsed.redoStack;\n      }\n    } catch {\n    }\n  }\n  function pushState() {\n    undoStack.value.push({ trials: [...trials.value], nextId: nextId.value });\n    redoStack.value = [];\n    if (undoStack.value.length > 50) undoStack.value.shift();\n    autoSave();\n  }\n  function recordTrial() {\n    pushState();\n    const { mWater, tWater, mMetal, tMetal, tf, cMetal, cTrue } = params.value;\n    trials.value.push({ id: nextId.value++, mWater, tWater, mMetal, tMetal, tf, cMetal, cTrue });\n  }\n  function undo() {\n    if (undoStack.value.length === 0) return;\n    const current = { trials: [...trials.value], nextId: nextId.value };\n    redoStack.value.push(current);\n    const prev = undoStack.value.pop();\n    trials.value = prev.trials;\n    nextId.value = prev.nextId;\n  }\n  function redo() {\n    if (redoStack.value.length === 0) return;\n    const current = { trials: [...trials.value], nextId: nextId.value };\n    undoStack.value.push(current);\n    const next = redoStack.value.pop();\n    trials.value = next.trials;\n    nextId.value = next.nextId;\n  }\n  function canUndo() {\n    return undoStack.value.length > 0;\n  }\n  function canRedo() {\n    return redoStack.value.length > 0;\n  }\n  function removeTrial(id) {\n    pushState();\n    trials.value = trials.value.filter((t) => t.id !== id);\n    autoSave();\n  }\n  function clearTrials() {\n    pushState();\n    trials.value = [];\n    nextId.value = 1;\n    autoSave();\n  }\n  function exportCsv() {\n    const headers = [\"Trial\", \"mWater(kg)\", \"tWater(C)\", \"mMetal(kg)\", \"tMetal(C)\", \"tf(C)\", \"cMetal(J/kgK)\"];\n    const rows = trials.value.map((t) => `${t.id},${t.mWater.toFixed(3)},${t.tWater.toFixed(1)},${t.mMetal.toFixed(3)},${t.tMetal.toFixed(1)},${t.tf.toFixed(1)},${t.cMetal.toFixed(0)}`);\n    return [headers.join(\",\"), ...rows].join(\"\\n\");\n  }\n  return { trials, recordTrial, undo, redo, canUndo, canRedo, removeTrial, clearTrials, exportCsv, autoLoad };\n}\n",
      "start": 1783635324980,
      "end": 1783635324987,
      "order": "normal",
      "sourcemaps": null
    }
  ]
}

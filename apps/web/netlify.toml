[build]
  command = "cd ../.. && pnpm install --frozen-lockfile && cd apps/web && npx vite build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false

[build.environment]
  VITE_API_BASE_URL = ""

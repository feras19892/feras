{
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && cd apps/web && npx vite build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
